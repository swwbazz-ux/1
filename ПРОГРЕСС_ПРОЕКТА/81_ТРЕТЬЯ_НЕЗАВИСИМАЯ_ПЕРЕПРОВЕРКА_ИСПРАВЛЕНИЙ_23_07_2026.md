# Третья независимая перепроверка исправлений

Дата: 23.07.2026  
Основание: документ 80  
Рабочий контур: локальный Django MVP  
Ветка: `codex/new-public-site-ux-prototype`  
HEAD: `e75922ce7d26c1b9cf4335d8c1af67064a40d440`  
Итоговый вердикт: **NO-GO**  
Оценка готовности к полному двухсменному прогону: **7 из 10**

## 1. Краткий итог

Независимая перепроверка подтвердила исправление четырех из пяти блокирующих P1:

- `QA-P1-001` — `FIXED`;
- `QA-P1-002-R1` — `FIXED`;
- `QA-P1-003` — `FIXED`;
- `QA-P1-004` — `FIXED`;
- `QA-P1-005` — `NOT FIXED`.

`QA-P1-005` исправлен в основной части временного контура, но остается воспроизводимым в трех конкретных потребителях:

1. суточная сводка простоев группирует событие по календарной дате;
2. fallback плана и пустой snapshot Диспетчера используют календарную дату открытия ночной смены;
3. динамика руководства не применяет фильтр `День / Ночь` при гранулярности `День`, `Смена` и `Месяц`.

По регламенту 74 открытый P1 запрещает создавать новый комплект тестовых сотрудников и начинать полный двухсменный прогон. Поэтому новый комплект из 14 сотрудников не создавался.

## 2. Сверка переданной версии

Документ 80 указывает точную локальную версию:

```text
branch = codex/new-public-site-ux-prototype
HEAD   = e75922ce7d26c1b9cf4335d8c1af67064a40d440
```

Независимо проверено:

- ветка совпадает;
- HEAD совпадает;
- в документе 80 перечислены 30 файлов backend;
- все 30 файлов существуют;
- все 30 SHA-256 совпадают;
- несовпадений — `0`;
- `core/production_time.py` и `core/tests_production_time.py` действительно остаются неотслеживаемыми.

Код во время этой перепроверки не изменялся.

Важно для будущей публикации:

- `git add -u` потеряет два новых файла `core`;
- `git add .` захватит посторонние документы и материалы `SITE_PROTOTYPE`;
- допустимо только явное добавление согласованного списка файлов;
- текущая ветка не имеет upstream и содержит историю, не относящуюся только к исправлениям сквозного теста, поэтому публиковать ее целиком без отдельного аудита нельзя.

## 3. Резервная копия локальной базы

До браузерных действий создана копия:

```text
C:\Users\swwba\AppData\Local\Temp\accounting-mvp-qa-repeat-20260723-r03\db.before-repeat-qa.sqlite3
```

Размер: `2 584 576` байт.  
SHA-256:

```text
BC1CF6C3CDA7ED5A9E73F4CBE65FA9B8A29F3F624AB4879E1E99F6743671F1E2
```

Копия не восстанавливалась, потому что это могло бы затереть параллельные локальные изменения данных. Все созданные браузерным тестом смены закрыты штатными пользовательскими действиями.

## 4. Технические проверки

Успешно выполнено:

- `manage.py check` — `PASS`;
- `manage.py makemigrations --check --dry-run` — `No changes detected`;
- 11 целевых серверных тестов — `PASS`;
- полный набор при явной UTF-8-среде — **552/552 PASS**;
- `git diff --check` — `PASS`;
- повторная сверка 30 SHA-256 после Browser QA — `0` несовпадений.

Первый полный запуск из обычной Windows-среды получил одно падение не из-за бизнес-логики, а из-за декодирования UTF-8 дочернего процесса как `cp1251` в:

```text
users.test_role_apps.ProductionSecuritySettingsTests.test_invalid_https_security_flag_fails_fast
```

При:

```text
PYTHONUTF8=1
PYTHONIOENCODING=utf-8
```

тот же тест и полный набор прошли. Это наблюдение тестовой инфраструктуры, а не блокирующий продуктовый дефект. Рекомендуемый отдельный статус — `QA-ENV-001`, приоритет `P3`.

## 5. `QA-P1-001`

Статус: **FIXED**.

Целевые регрессии переходящего рейса прошли. Завершение рейса в новой смене не добавляет его объем в KPI этой новой смены ни до, ни после разгрузки.

## 6. `QA-P1-002-R1`

Статус: **FIXED**.

Независимый транзакционный сценарий:

- legacy-рейс без `unloading_shift`;
- `completed_at = 24.07.2026 01:30 Asia/Vladivostok`;
- производственная дата `23.07.2026`;
- после проверки все временные объекты откатаны.

Результат:

| Проверка | Фактически | Ожидалось |
|---|---:|---:|
| Фильтр `Все` | 1 | 1 |
| Фильтр `День` | 0 | 0 |
| Фильтр `Ночь` | 1 | 1 |
| Дата `24.07.2026` | 0 | 0 |

Экран и Excel используют одинаковую выборку.

## 7. `QA-P1-003`

Статус: **FIXED**.

Статус сохранен по предыдущей независимой мобильной проверке документа 79. Файлы исправления не изменились, их SHA-256 совпадают с переданной версией, полный регрессионный набор успешен. Новых признаков регрессии нулевой ширины полей начала смены не обнаружено.

Физическое Android-устройство в этом прогоне не использовалось.

## 8. `QA-P1-004`

Статус: **FIXED**.

Полный пользовательский путь повторен через Browser QA:

1. в локальной базе подготовлена открытая смена предыдущего Диспетчера;
2. Диспетчер Б открыл экран через штатную авторизацию;
3. интерфейс показал ФИО и время зависшей смены;
4. поле причины было обязательным;
5. кнопка `Закрыть предыдущую смену служебно` была доступна;
6. подтверждение выполнено через штатное модальное окно;
7. предыдущая смена №63 закрылась служебно;
8. в `DispatcherActionLog` создана запись с указанной причиной;
9. Диспетчер Б через интерфейс начал новую смену №64;
10. новая смена №64 затем завершена через штатное подтверждение и обязательный десятисекундный отсчет.

Проверка базы после сценария:

```text
shift 63: closed = true, is_service_closed = true, closed_by = employee 5
shift 64: closed = true, is_service_closed = false, closed_by = employee 5
open dispatcher shifts = 0
service action log with QA marker = 1
```

Ошибок консоли браузера не обнаружено. Данные авторизации в отчет не включены.

## 9. `QA-P1-005`

Статус: **NOT FIXED**.

Центральная функция правильно определяет контрольный момент:

```text
24.07.2026 01:30 Asia/Vladivostok
shift_type = night
production_date = 23.07.2026
```

Основные значения по умолчанию горных объемов, простоев, журнала смены и аналитики также выбирают `23.07.2026`. Однако три потребителя обходят единый контракт.

### 9.1. `QA-P1-005-R1`: суточная сводка простоев

Файл:

```text
СИСТЕМА_MVP/backend/reports/views.py:1903
```

Фактическая логика:

```python
day = timezone.localtime(row['started_at']).date()
```

Для события `24.07.2026 01:30 Asia/Vladivostok`:

```text
фактическая группа = 24.07.2026
ожидаемая группа   = 23.07.2026
```

Из-за этого основной фильтр отчета может выбрать правильный производственный интервал, но внутренняя суточная сводка страницы и Excel подписывает строку другой датой.

Требование:

- группировать производственные простои через центральную функцию производственной даты;
- не переводить несменные календарные отчеты на производственную дату без отдельного требования;
- добавить регрессию страницы и Excel для `01:30 D+1`.

### 9.2. `QA-P1-005-R2`: fallback плана и пустой snapshot

Файлы:

```text
СИСТЕМА_MVP/backend/shifts/services.py:614
СИСТЕМА_MVP/backend/shifts/services.py:634
СИСТЕМА_MVP/backend/trips/views.py:181
```

Остаточная календарная логика:

```python
timezone.localtime(reference_shift.opened_at).date()
timezone.localtime(open_shift.opened_at).date()
timezone.localtime(shift.opened_at).date()
```

Для ночной смены, открытой `24.07.2026 01:30 Asia/Vladivostok`:

| Потребитель | Фактически | Ожидалось |
|---|---|---|
| `dispatcher_empty_snapshot_progress` | `24.07.2026` | `23.07.2026` |
| `calculate_open_shift_progress` fallback | `24.07.2026` | `23.07.2026` |

Требование:

- использовать центральную производственную дату времени открытия смены;
- сохранить авторитет snapshot, если он уже назначен;
- отдельно проверить отсутствие плана, первое назначение плана и открытый snapshot в ночи после полуночи.

### 9.3. `QA-P1-005-R3`: фильтр смены в динамике руководства

Файл:

```text
СИСТЕМА_MVP/backend/reports/shift_analytics.py:603
```

Сейчас `shift_type` отсекает рейсы только при:

```python
granularity == 'hour'
```

При `day`, `shift` и `month` в расчет попадают рейсы обеих смен.

Независимый сценарий:

- один рейс дня в `10:00`;
- один рейс ночи в `20:00`;
- одинаковый диапазон дат;
- гранулярность `day`.

Результат:

| Фильтр | Фактически | Ожидалось |
|---|---:|---:|
| `День` | 2 рейса | 1 рейс |
| `Ночь` | 2 рейса | 1 рейс |

Требование:

- применять производственную сменную атрибуцию при всех поддерживаемых гранулярностях;
- добавить минимум матрицу `day/night × hour/day/shift/month`;
- проверить количество рейсов, объем, точки графика и итоговые KPI;
- если динамика экспортируется, повторить те же проверки в экспорте.

## 10. Дополнительные риски, не меняющие текущий вердикт

Остаются без закрытия:

- `QA-P2-002` — физический короткий touch-tap на настоящем Android, включая проверку ровно одного POST;
- `QA-RISK-001` — настоящие параллельные транзакции на штатной тестовой PostgreSQL;
- hold-обработчики простоев Водителя и Машиниста после `Enter` и после сетевой ошибки long-press;
- граничное покрытие всех часовых отметок динамики;
- отдельная проверка legacy-fallback в общем отчете объемов.

Эти пункты нельзя объявлять закрытыми по результатам SQLite или эмуляции мыши.

## 11. Почему полный прогон двух смен не начат

Документ 74 требует сначала независимо закрыть все пять P1. Поскольку `QA-P1-005` воспроизведен, создание 14 новых сотрудников, смен, расстановок и рейсов не принесло бы нового решения о готовности и только увеличило бы тестовые данные.

Маркер, подготовленный для возможного полного прогона:

```text
ТЕСТПОВТОР20260724R03
```

Сотрудников с этим маркером не создано. Сотрудников с техническим маркером браузерной проверки `ТЕСТПОВТОР20260723R03` также нет.

После закрытия `QA-P1-005-R1…R3` полный прогон необходимо выполнять в реальных дневной и ночной сменах по Владивостоку либо через отдельно согласованный QA-источник времени. В текущей системе подтвержденного тестового clock override нет; изменять производственное время скрытым способом запрещено.

## 12. Точное задание чату исправлений

Исправить только остатки `QA-P1-005-R1…R3`:

1. Перевести `downtime_daily_summary` с календарной даты на центральную производственную дату.
2. Перевести fallback плана в `calculate_truck_shift_progress` и `calculate_open_shift_progress` на производственную дату открытия смены.
3. Перевести дату `dispatcher_empty_snapshot_progress` на производственную дату открытия смены.
4. Применять `shift_type` в `build_excavator_dynamics` для всех гранулярностей, а не только для `hour`.
5. Добавить точечные регрессии для контрольного момента `24.07.2026 01:30 Asia/Vladivostok`.
6. Для динамики добавить матрицу `day/night × hour/day/shift/month` с одним дневным и одним ночным рейсом и ожидаемым результатом `1/1`.
7. Проверить страницу и Excel суточной сводки простоев.
8. Не менять несменные календарные отчеты без доказанного производственного требования.
9. Не объявлять `QA-P2-002` и `QA-RISK-001` закрытыми без требуемых сред.
10. По желанию отдельной P3-правкой сделать subprocess-тест независимым от системной кодировки Windows.

После исправления создать новый документ 82 и указать:

- точную ветку и HEAD;
- полный список измененных файлов;
- SHA-256 каждого файла;
- обязательное включение обоих неотслеживаемых файлов `core`, если они еще не зафиксированы в Git;
- результаты новых отрицательных тестов до исправления и положительных после;
- полный набор тестов;
- `manage.py check`;
- `makemigrations --check --dry-run`;
- `git diff --check`;
- точные шаги новой независимой перепроверки;
- отсутствие commit, push, deploy и production, если отдельного разрешения на них не было.

Документы 73–81 переписывать нельзя.

## 13. Данные после перепроверки

В локальной базе остались только закрытые и аудируемые записи браузерной проверки:

- смена №63 — закрыта служебно;
- смена №64 — закрыта штатно;
- одна запись `DispatcherActionLog` с причиной и QA-маркером;
- открытых диспетчерских смен — `0`;
- новых тестовых сотрудников — `0`.

Эти записи не удалялись, потому что подтверждают выполненный пользовательский сценарий. Восстановление всей базы из резервной копии без отдельного разрешения не выполнялось.

Локальный сервер остановлен штатным `STOP_SERVER_MVP.bat`.  
Порт 8000 свободен.  
HTTP на `127.0.0.1:8000` недоступен, как и ожидается после остановки.

## 14. Локальность результата

- Исправления документа 80 остаются только локальными.
- Этот тестовый чат изменил только проектную документацию и локальные QA-данные.
- Commit не выполнялся.
- Push не выполнялся.
- Deploy не выполнялся.
- Production не затрагивался.
- Миграции не создавались и не применялись.
- Проверять следующий результат нужно в локальном Django MVP после штатного запуска через `START_SERVER_MVP.bat`.
