# 107. Независимая перепроверка документа 106

Дата: 26.07.2026  
Основание: `106_ОТЧЕТ_ОБ_ИСПРАВЛЕНИЯХ_ПО_ДОКУМЕНТУ_105_26_07_2026.md`  
Итоговый статус: **ПРИНЯТО — 3/3 КАРТОЧКИ PASS**

## 1. Итог

Независимо приняты:

1. `QA-CHAOS-P1-P09-012-R1`;
2. `QA-REG-P2-002`;
3. `QA-REG-P2-PRIVATE-MEDIA-001`.

Новых дефектов в границе документа 106 не найдено.

Пакет исправлений документов `103–106` принят в доступной локальной среде.
Это не является окончательным `GO` всей системы: отдельно остаются активные
карточки документов `100–102`, настоящая PostgreSQL-конкуренция и специальные
сетевые Android-сценарии.

## 2. Точная версия

Git:

```text
ветка: codex/new-public-site-ux-prototype
HEAD:  e75922ce7d26c1b9cf4335d8c1af67064a40d440
```

SHA-256 документа 106:

```text
689D677D8158A3F1B2DFA85BB334E583360878A4DC8727DEF8F51508DECCB415
```

SHA-256 документа 105 сохранился:

```text
CBF75B59BEAE5292F018FC8CEC00DD2654D05AEE5E4E5902F251399ECAB9BE44
```

Все десять code/test-файлов таблицы документа 106 независимо сверены:

```text
размер: 10/10 MATCH
SHA-256: 10/10 MATCH
```

Новых или измененных файлов миграций в этапе 106 нет.

## 3. QA-CHAOS-P1-P09-012-R1

Статус: **PASS**.

Адресный исполняемый набор:

```text
node --test static/js/tests/deputy-contract-runtime.test.js
4/4 PASS
```

Подтверждено:

- при `locked=true` карточка сотрудника получает `draggable=false`;
- искусственный locked `drop` завершается до изменяющего пути;
- `fetch count = 0`;
- локальный план, version и autosave-текст не меняются;
- `saveSlot()` повторно проверяет contract до состояния `Сохраняю`;
- `postJson()` проверяет contract непосредственно перед `fetch`;
- после снятия lock один drag-and-drop выполняет ровно один POST;
- изменение contract/активной роли завершает drag и закрывает изменяющие
  диалоги;
- поиск и live/read-only-просмотр остаются доступными;
- cache-key Заместителя содержит `pathname + search`;
- точный `cache.match(cacheKey)` не возвращает старый ресурс для нового
  query-string;
- shell Заместителя согласован как
  `deputy-mining-manager-desktop-shell-v14`.

Исполняемый harness использует настоящий production-файл
`static/js/deputy-mining-manager-v3.js` и service worker из
`assignments/deputy_views.py`.

Живой изменяющий DnD против рабочей базы намеренно не выполнялся. Для него
нужна отдельная разрешенная тестовая база; рабочую БД изменять запрещено.

## 4. QA-REG-P2-002

Статус: **PASS в разрешенной Browser QA**.

Автоматические регрессии:

```text
login-mobile-overflow-runtime.test.js: 4/4 PASS
users.test_login_mobile_overflow:      2/2 OK
```

Подтверждено в коде и исполняемом тесте:

- `input.scrollIntoView` отсутствует;
- горизонтальная позиция dialog и document сбрасывается сразу и повторно
  через 350 мс;
- естественный autofocus до `DOMContentLoaded` обрабатывается;
- phone/pin focus и blur используют общий путь;
- вертикальная коррекция использует `window.scrollTo(..., left: 0)`;
- dialog, form и непосредственные grid-дети ограничены
  `min-width: 0; max-width: 100%`;
- длинный заголовок использует `white-space: normal` и
  `overflow-wrap: anywhere`;
- все 11 ролевых origin возвращают общий login-контракт.

### 4.1. Живая мобильная проверка

На `390×844` без ручного reload проверены десять разрешенных origin:

```text
driver
excavator
mining-master
deputy
dispatcher
oup
timekeeper
site-manager
mechanic
management
```

Для каждого проверены четыре состояния:

1. естественный autofocus и ожидание более 350 мс;
2. blur телефона;
3. focus пинкода и ожидание более 350 мс;
4. blur пинкода.

Итог:

```text
10 origin × 4 состояния = 40/40 PASS
```

Во всех состояниях:

```text
main.scrollLeft = 0
main.scrollWidth <= main.clientWidth
document/body horizontal overflow = false
main, form и h2 находятся внутри viewport
contract = pwa-contract-v1
ready = true
locked = false
contract banner = отсутствует
```

Контрольные значения естественного autofocus:

| Origin | `main.scrollLeft` | `scrollWidth / clientWidth` |
|---|---:|---:|
| `excavator.localhost` | 0 | `358 / 358` |
| `deputy.localhost` | 0 | `358 / 358` |
| `oup.localhost` | 0 | `358 / 358` |
| `mechanic.localhost` | 0 | `362 / 362` |

Длинный мобильный заголовок ОУП осмотрен визуально: обрезки, наложения и
горизонтального смещения нет.

### 4.2. Живая desktop-проверка

На `1280×720` для тех же десяти origin:

```text
10 origin × 4 состояния = 40/40 PASS
```

Форма Диспетчера осмотрена визуально. Горизонтального переполнения, обрезки и
перекрытия элементов нет.

Общий независимый живой результат:

```text
80/80 состояний PASS
ошибки консоли: 0
```

`admin.localhost` живым браузером не открывался из-за ранее установленного
ограничения текущей браузерной сессии. Его общий шаблон, autofocus и
login-контракт входят в зеленый автоматический тест всех 11 origin. Это
ограничение независимой среды, а не воспроизведенный дефект продукта.

## 5. QA-REG-P2-PRIVATE-MEDIA-001

Статус: **PASS**.

Подтверждено:

- `PortalTestCase` подменяет `MEDIA_ROOT` и
  `PORTAL_PRIVATE_MEDIA_ROOT`;
- import-time singleton `portal_private_storage` перенаправляется во
  временный каталог;
- cached `base_location` и `location` очищаются;
- cleanup восстанавливает storage, снимает override и удаляет временный
  каталог в безопасном порядке;
- все четыре private ImageField используют изолированный singleton;
- исходный security-тест доступа к чужому черновику остается зеленым.

Результат:

```text
manage.py test portal
50/50 OK
```

Рабочий `private_media`:

| Контрольная точка | Файлов | Байт | SHA-256 manifest |
|---|---:|---:|---|
| до тестов | 78 | 3042 | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |
| после portal | 78 | 3042 | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |
| после целевых и полного набора | 78 | 3042 | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |
| после Browser QA | 78 | 3042 | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |

```text
добавлено файлов: 0
удалено файлов: 0
изменено файлов: 0
остаточных portal-private-tests-* каталогов: 0
```

## 6. Автоматические проверки

JavaScript:

```text
node --test static/js/tests/*.test.js
23/23 PASS
```

Обязательный регресс документа 104:

```text
47/47 OK
8 skipped — PostgreSQL-only
```

Целевой набор трех карточек:

```text
35/35 OK
```

Портал:

```text
50/50 OK
```

Полный Django-набор:

```text
666/666 OK
13 skipped — PostgreSQL-only
```

Системные проверки:

```text
manage.py check
PASS

manage.py makemigrations --check --dry-run
No changes detected

node --check static/js/deputy-mining-manager-v3.js
PASS

git diff --check
PASS
```

`git diff --check` вывел только предупреждения Windows о возможном будущем
LF → CRLF; whitespace-ошибок нет.

## 7. Неизменность рабочей базы

До тестов, после полного набора и после Browser QA:

```text
SHA-256: A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222
размер:  2584576 байт
mtime:   2026-07-23T17:11:26.0764680Z
```

Рабочая база побайтово не изменилась.

## 8. Ограничения, которые не закрыты этим этапом

Не объявлены пройденными:

1. настоящие параллельные транзакции на отдельной PostgreSQL;
2. живой изменяющий DnD на отдельной тестовой БД;
3. специальные Android-сценарии потери ответа, двух устройств, старой
   сессии и фонового восстановления;
4. живая Browser QA `admin.localhost` в текущей запрещенной сессии.

Обычный пользовательский цикл всех PWA ранее лично проверен владельцем на
реальном Android, но это не заменяет перечисленные специальные проверки.

## 9. Следующий этап

Карточки документа 105 повторно не открывать.

Следующий блокирующий контур:

1. исправить и независимо проверить `QA-CHAOS-P1-008-R1` документа 100:
   Механик не может закрывать простой Самосвала;
2. передать и исправить оставшиеся подтвержденные карточки документа 101,
   включая автоматический `ЕТО/ТО` и повторное включение техники;
3. получить решения владельца по смене свыше 12 моточасов и отображению
   переименованных справочников в старых рейсах;
4. выполнить отдельную PostgreSQL-приемку после появления разрешенной
   тестовой СУБД.

Окончательный статус всей системы пока остается `NO-GO`, хотя пакет
исправлений документов `103–106` принят.

## 10. Граница результата

Код продукта не изменялся. Создан только этот отчет и обновлены документы
прогресса.

Локальный сервер запущен только через `START_SERVER_MVP.bat` и остановлен
через `STOP_SERVER_MVP.bat`. Порт 8000 свободен.

Production, `.env`, рабочая база, `private_media`, миграции и deploy не
изменялись. Commit, push и deploy не выполнялись.
