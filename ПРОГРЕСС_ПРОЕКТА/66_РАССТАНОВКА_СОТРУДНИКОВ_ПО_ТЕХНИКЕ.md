# Расстановка сотрудников по технике

Дата фиксации: 13.07.2026.

Статус: новый контур реализован, проверен и опубликован на `driverform.ru` из commit `66bd17468a33924b30a3d85e081483701d8ffc14`.

## Назначение контура

Контур предназначен для заместителя начальника горного участка и решает одну оперативную задачу: подготовить расстановку сотрудников по технике на дневную и ночную смены, проверить ее и только после этого опубликовать как действующие назначения.

Новая роль доступа:

- внутренний код: `deputy_mining_manager`;
- пользовательское название: `Заместитель начальника горного участка`;
- после входа сотрудник с этой ролью попадает на экран расстановки.

## Маршруты

- `/deputy-mining-manager/` — основной экран расстановки;
- `/deputy-mining-manager/slot/` — серверное автосохранение изменения одного сменного слота;
- `/deputy-mining-manager/publish/` — атомарная публикация проверенного черновика;
- `/deputy-mining-manager/reports/` — вторая страница профиля с историей опубликованных расстановок.

Шапка является общей для профиля заместителя начальника и содержит две рабочие вкладки: `Расстановка` и `Отчеты`. Архитектура навигации допускает последующее добавление новых страниц роли без изменения основного экрана.

## Группы сотрудников и техники

Пользователь выбирает одну рабочую категорию. Отдельно выбирать группу сотрудников и группу техники не требуется: соответствие задается сервером.

| Рабочая роль сотрудников | Группа техники |
|---|---|
| `driver` | Самосвалы |
| `excavator_operator` | Экскаваторы |

При переключении категории одновременно меняются список доступных сотрудников и матрица подходящей техники. Сотрудник или техника несовместимого вида не могут быть записаны в слот.

## Контракт интерфейса

Экран рассчитан на плотную работу с десятками единиц техники и сотней сотрудников. Основа — не крупные карточки, а компактная двухзонная рабочая доска:

- слева — только свободные и подходящие сотрудники выбранной категории;
- справа — плотная матрица, где одна строка соответствует одной единице техники;
- в каждой строке техники два слота: `День` и `Ночь`;
- назначение выполняется перетаскиванием, а также кликом или клавиатурой через тот же серверный сценарий;
- общий поиск находит сотрудника или технику;
- фильтры показывают все строки, незаполненные, конфликтные или измененные;
- счетчики внутри фильтров одновременно служат краткой сводкой, поэтому отдельная дублирующая панель показателей не используется;
- отдельной кнопки `Сохранить` нет: каждое изменение слота автоматически сохраняется в черновик;
- единственное итоговое действие — `Опубликовать расстановку`.

В бейдже сотрудника используется фотография. Нажатие именно на фотографию открывает ее увеличенную версию, а взаимодействие с остальной частью бейджа выполняет назначение. Для техники используются проектные иконки самосвала или экскаватора; текстовые статусы показываются только для исключений, требующих внимания.

## Черновик и автосохранение

Расстановка хранится отдельно от действующих назначений в моделях `CrewPlan` и `CrewPlanSlot`.

`CrewPlan` фиксирует:

- производственную дату;
- рабочую роль;
- ревизию плана;
- статус `draft`, `published` или `superseded`;
- номер версии;
- авторов создания, изменения и публикации;
- время публикации.

`CrewPlanSlot` фиксирует одну пару `техника + смена` и выбранного сотрудника. Для каждой единицы техники создаются отдельные слоты `day` и `night`. В слоте также хранится исходное назначение, относительно которого определяется изменение и проверяется устаревание черновика.

Каждое назначение, перемещение, обмен или освобождение слота:

1. проверяет совместимость роли, вида техники, активности сотрудника и техники;
2. проверяет, что сотрудник не занят в другом слоте плана;
3. принимает ожидаемую версию черновика;
4. сохраняет изменение в транзакции;
5. увеличивает версию плана;
6. возвращает обновленное состояние доски.

Если другой сеанс уже изменил план, сервер отклоняет устаревшую версию. Клиент должен получить актуальную доску вместо тихого перезаписывания чужого изменения.

## Публикация

Публикация выполняется одной атомарной серверной операцией. До записи сервер повторно проверяет:

- актуальность версии черновика;
- состав активной техники;
- отсутствие повторного назначения одного сотрудника;
- совместимость ролей, смен и видов техники;
- отсутствие изменения исходных действующих назначений другим процессом.

После успешной проверки план переводится в `published`, а его слоты становятся источником действующих постоянных назначений `EquipmentAssignment`:

- неизмененные действующие назначения сохраняются без создания лишней истории;
- измененные назначения корректно завершаются;
- новые назначения создаются только для реально изменившихся слотов;
- активное назначение сотрудника в другой рабочей роли считается конфликтом и не закрывается молча;
- пустой слот означает отсутствие действующего назначения для этой техники и смены.

Пока план остается черновиком, приложения Водителя и Машиниста экскаватора продолжают использовать прежние опубликованные `EquipmentAssignment`. После публикации следующий вход или старт смены получает новое действующее назначение через уже существующий серверный контракт.

## Открытые смены и история

`EmployeeShift` хранит snapshot техники, роли и смены на момент открытия. Публикация новой расстановки меняет постоянное назначение для следующего старта, но не перепривязывает уже открытую смену и не изменяет ее производственные события.

Страница `Отчеты` использует историю опубликованных `CrewPlan` и показывает расстановки по дате, категории, ревизии, времени публикации и автору. История планов и история постоянных `EquipmentAssignment` дополняют друг друга: первая отвечает на вопрос, какая расстановка была опубликована целиком, вторая хранит фактические периоды отдельных назначений.

## Границы текущего шага

Опубликованы роль, маршруты, модели планов, серверные операции черновика и публикации, dense UI расстановки, навигация профиля и страница отчетов. Роль создана и активна на production; конкретному сотруднику доступ выдается через существующую административную карточку.

## Локальная проверка

- `manage.py check` — без замечаний;
- `makemigrations --check --dry-run` — новых миграций не обнаружено;
- полный набор Django — `323/323` теста успешно;
- Browser QA выполнен на фактическом объеме `53` самосвала и `100` водителей;
- desktop `1280x900`: одновременно видны обе смены, фильтры, 8–10 строк техники и отдельные прокручиваемые списки; горизонтального переполнения страницы нет;
- tablet `1024x768`: рабочая матрица и список сотрудников прокручиваются внутри компактной рабочей зоны, страница не растягивается на все 53 строки;
- mobile `390x844`: горизонтального переполнения страницы нет, длинные категории, фильтры, сотрудники и матрица используют собственную прокрутку;
- подтверждены поиск, дневная и ночная тема, увеличение фотографии, назначение кликом, обмен двух сотрудников, автосохранение, переключение `Самосвалы` / `Экскаваторы`, окно публикации и переход в `Отчеты`;
- ошибки консоли и битые изображения отсутствуют.

Серверные тесты отдельно подтверждают DnD-семантику перемещения и обмена, конфликт версии, атомарную публикацию, сохранение snapshot открытой смены и получение нового назначения приложением сотрудника. Физический жест перетаскивания остается пунктом ручной пользовательской приемки на обычной мыши или тачпаде: Browser QA проверил тот же сценарий через альтернативное назначение кликом, но браузерный контроллер не воспроизвел HTML5 DataTransfer.

## Публикация 13.07.2026

- commit `66bd17468a33924b30a3d85e081483701d8ffc14` отправлен в ветки `main` и `codex/deputy-crew-planning`;
- перед deploy созданы и проверены резервные копии кода `/srv/accounting-mvp/backups/code/deputy-crew-planning-before-20260713_040306.tar.gz` и PostgreSQL `/srv/accounting-mvp/backups/db/deputy-crew-planning-before-20260713_040306.sql.gz`;
- migration `assignments.0006_crewplan_crewplanslot_and_more` применена;
- на сервере прошли `manage.py check`, `makemigrations --check --dry-run`, `collectstatic --noinput`, restart `accounting-mvp` и `nginx -t`;
- роль `deputy_mining_manager` активна, таблицы `assignments_crewplan` и `assignments_crewplanslot` созданы;
- публичные CSS и JS отвечают `200` и совпадают с release-сборкой по SHA-256;
- защищенные маршруты `/deputy-mining-manager/` и `/deputy-mining-manager/reports/` без авторизации ожидаемо перенаправляют на `/home/`.
- production Browser QA с существующей сессией Водителя подтвердил ролевую защиту обоих маршрутов: они перенаправляют на `/driver/`; на `1280x900` и `390x844` нет горизонтального переполнения и ошибок консоли.

Остается пользовательская приемка самого production-экрана и физического DnD обычной мышью или тачпадом под реальным доступом заместителя начальника. Альтернативное назначение кликом и серверная семантика перемещения и обмена уже проверены локально.

## Локальное исправление DnD и сообщений 13.07.2026

- системный полупрозрачный drag ghost заменен собственным непрозрачным прямоугольным бейджем с фотографией, именем и краткой подписью сотрудника;
- занятый слот можно перетащить в другой слот техники или смены, а в занятом целевом слоте сервер выполняет безопасный обмен сотрудниками;
- список `Свободные сотрудники` стал явной drop-зоной: перенос назначенного сотрудника в эту область очищает исходный слот и возвращает человека в свободный список;
- обычные операции черновика не требуют подтверждения и сразу проходят через существующее автосохранение;
- ошибки сохранения показываются в отдельной читаемой панели с заголовком, пояснением, закрытием и действием `Обновить данные` для устаревшей версии;
- окно публикации явно сообщает, что только после публикации новые техника и смена попадут в карточки сотрудников и рабочие приложения;
- регрессионные тесты подтверждают перенос между техникой и сменами, снятие назначения, неизменность действующего `EquipmentAssignment` в черновике и обновление административной карточки после публикации;
- локально прошли `manage.py check`, `makemigrations --check --dry-run`, профильный набор `11/11` и полный набор `327/327` тестов;
- Browser QA подтвердил читаемость публикационного окна и панели ошибки на desktop `1280x900`, tablet `1024x768`, mobile `390x844`, в дневной и ночной темах, без горизонтального переполнения;
- автоматизированный контроллер браузера по-прежнему не воспроизводит нативный HTML5 `DataTransfer`, а отдельная проверка физического жеста через Windows-браузер остановлена политикой локальных URL. Поэтому финальный физический DnD остается пунктом пользовательской приемки после переноса изменений в рабочую среду.

## Публикация исправления DnD 13.07.2026

- release commit `14f9a0992041a569488b6a01e36c239a3708cd5b` отправлен в ветки `main` и `codex/deputy-dnd-fix`;
- перед deploy созданы и проверены резервные копии кода `/srv/accounting-mvp/backups/code/deputy-dnd-v3-before-20260713-072243.tar.gz` и PostgreSQL `/srv/accounting-mvp/backups/db/deputy-dnd-v3-before-20260713-072243.sql.gz`;
- миграции и структура БД не изменялись: до и после deploy `manage.py check` прошел без замечаний, `makemigrations --check --dry-run` сообщил `No changes detected`;
- `collectstatic --noinput` скопировал 23 файла, сервис `accounting-mvp` перезапущен и активен, `nginx -t` успешен, ошибок сервиса после рестарта нет;
- публичные `deputy-mining-manager-v3.css` и `deputy-mining-manager-v3.js` отвечают `200` и побайтно совпадают с release по SHA-256;
- защищенные маршруты `/deputy-mining-manager/` и `/deputy-mining-manager/reports/` без нужной роли ожидаемо перенаправляют на `/home/`, а существующую сессию Водителя — на `/driver/`;
- production хранит маркер опубликованного кода `14f9a0992041a569488b6a01e36c239a3708cd5b`.

Исправление опубликовано на `driverform.ru`. Остается только пользовательская приемка физического DnD обычной мышью или тачпадом под реальным доступом заместителя начальника участка; альтернативное назначение кликом и серверная семантика перемещения, обмена и снятия проверены.

## Production-hotfix PostgreSQL-блокировок 13.07.2026

- production-журнал подтвердил причину всех ответов `500` на `/deputy-mining-manager/slot/`: PostgreSQL запрещал обычный `FOR UPDATE` по nullable-стороне `LEFT JOIN` сотрудника;
- запросы изменения слота и публикации ограничивают блокировку только базовой таблицей `CrewPlanSlot` через `select_for_update(of=('self',))`;
- добавлены регрессионные проверки ORM-контракта для снятия сотрудника и публикации; локально прошли профильные `13/13` и полные `329/329` тестов;
- hotfix commit `e1cd8f92aacf537974a2ac21c263c776c0a3d0e2` отправлен в `main` и `codex/deputy-dnd-fix`;
- перед deploy созданы и проверены backups кода `/srv/accounting-mvp/backups/code/deputy-pg-lock-before-20260713-074131.tar.gz` и PostgreSQL `/srv/accounting-mvp/backups/db/deputy-pg-lock-before-20260713-074131.sql.gz`;
- после deploy прошли `manage.py check`, `makemigrations --check --dry-run`, `collectstatic --noinput`, restart `accounting-mvp` и `nginx -t`; сервис активен, ошибок после рестарта нет;
- production-smoke вызвал реальный endpoint снятия сотрудника на PostgreSQL: получены HTTP `200` и корректный JSON, изменение внутри транзакции выполнено, затем принудительно отменено и исходные данные подтверждены неизменными;
- отдельный production-запрос блокировки публикации с nullable-связями выполнен успешно; новых ошибок `FOR UPDATE cannot be applied to the nullable side of an outer join` после deploy нет.

Пользователю достаточно обновить страницу и повторить снятие либо перенос: клиентская статика не менялась, поэтому очистка браузерного кэша не требуется.

## PWA рабочего места заместителя 13.07.2026

- `Расстановка` и `Отчёты` объединены отдельным manifest: установленное приложение стартует с `/deputy-mining-manager/`, работает в `standalone` и ориентировано на горизонтальный экран ноутбука или планшета;
- подготовлены отдельные PNG-иконки `180`, `192`, `512` и `maskable 512`, визуально связанные с матрицей техники, смен и сотрудников;
- service worker имеет узкую область `/deputy-mining-manager/` и не перехватывает соседние роли;
- HTML расстановки всегда загружается с сервера и не сохраняется в Cache Storage, потому что содержит персональные данные и актуальный версионный черновик;
- фотографии `/media/`, JSON и POST назначения/публикации не входят в precache и не перехватываются worker;
- безопасная статика обновляется по network-first с каноническим ключом без query-параметров и остается доступной как offline fallback;
- при отсутствии сети вместо устаревшей расстановки показывается нейтральный экран `Нет подключения к серверу`;
- root-scope service worker Горного мастера больше не обслуживает навигацию заместителя и очищает только собственные кэши; версия shell поднята до `mining-master-mobile-shell-v107`;
- локально проверены manifest, MIME и cache-заголовки, размеры иконок, JavaScript-синтаксис, серверные контракты и адаптивность обоих экранов.

PWA реализовано и проверено локально. Для production остаются отдельные push/deploy, HTTPS-проверка фактической установки на `driverform.ru` и пользовательская приемка входа/выхода из установленного приложения.

## Печатный Excel и адаптивная геометрия 14.07.2026

- в рабочей панели добавлена одна компактная кнопка `Excel`; отдельная кнопка печати не создается, потому что подготовленный файл печатается стандартными средствами Excel;
- выгрузка доступна только роли заместителя начальника горного участка и строится по точному идентификатору открытого `CrewPlan`;
- сразу после публикации кнопка выгружает именно опубликованную ревизию; после первого нового изменения ссылка штатно переключается на текущий черновик;
- клиентские фильтры бригады и поиска не сокращают официальный документ: в него попадают все строки выбранной категории техники текущей ревизии;
- техника на экране и в книге использует естественную сортировку гаражных номеров, поэтому `2` располагается перед `10`;
- книга содержит статус и ревизию, дату, категорию, ответственного, время формирования, сводку, технику, модель, дневного и ночного сотрудника и примечание;
- пустые слоты выделяются желтым, конфликтные строки — красным; печать настроена на A4 в альбомной ориентации, ширину одной страницы и повтор заголовка;
- формульные префиксы в пользовательских значениях экранируются, ответ запрещает кеширование персонального документа;
- шапка увеличена до `76 px`, высота рабочей области учитывает `zoom: 0.90`, панель сотрудников расширена до `360–400 px`, колонки техники/дня/ночи распределены как `24% / 38% / 38%`;
- версия PWA-кеша поднята до `v3`, чтобы установленное приложение не сохранило прежнюю компоновку;
- локально прошли профильные `24/24` и `81/81`, полный набор `396/396`, `manage.py check`, проверка отсутствия миграций, JS-синтаксис и diff-check;
- тестовый файл на 50 единиц техники визуально проверен и преобразован в PDF из 4 страниц A4 альбомной ориентации.

Release `ffaf94f22a7e6a8428e082d963d182611418bd3b` отправлен в `main` и `codex/deputy-pwa` и опубликован на `driverform.ru`. Перед deploy созданы и проверены rollback-копии кода `/srv/accounting-mvp/backups/code/deputy-excel-layout-before-20260714_090349.tar.gz` (SHA-256 `8cfba1312aeef7b8f8b9569510d43c6417a8239a21fc76b0b12f86fc5def5a07`) и PostgreSQL `/srv/accounting-mvp/backups/db/deputy-excel-layout-before-20260714_090349.sql.gz` (SHA-256 `7caf7bb9468ed7104ff7e3754603b92c21324c1aaddba62c182a2d5b7020a74b`).

Новых миграций нет: `makemigrations --check --dry-run` и `migrate --plan` чистые, `migrate --noinput` не запускался. На production прошли `manage.py check`, `collectstatic --noinput` (`33` файла скопировано), restart `accounting-mvp`, `nginx -t` и проверка журнала gunicorn. Сервисы активны.

Публичные CSS/JS совпадают с release-архивом и `staticfiles` по SHA-256, `/deputy-mining-manager-sw.js` отдает `v3`, `/` отвечает `200`, защищенные экран и Excel-маршрут без сессии — штатным `302`. Read-only smoke сформировал из production-плана №4 книгу `Расстановка` на 8 единиц техники, подтвердил A4 landscape и fit-to-width; данные БД не менялись. Остается пользовательская визуальная приемка экрана и скачивания файла в авторизованной сессии.

## Единые заголовки рабочей матрицы 14.07.2026

- заголовок панели `Свободные сотрудники` и заголовки таблицы `Техника`, `День`, `Ночь` выровнены по единой высоте `42 px` и общей базовой линии;
- для всех четырёх заголовков используются одинаковые семейство шрифта, размер `13 px`, насыщенность `900`, верхний регистр и межбуквенный интервал;
- панель сотрудников сохранила компактную геометрию, а подсказка снятия назначения скорректирована под новую высоту заголовка;
- версия PWA-кеша поднята с `v3` до `v4`, чтобы установленное приложение удалило прежний shell-кеш и загрузило актуальный CSS;
- локально прошли `manage.py check`, профильные тесты `24/24`, `makemigrations --check --dry-run` и diff-check.

Release `bbed5e99844b8104df47e95a948894143b0a79a3` создан поверх актуального `main` без force-push и опубликован на `driverform.ru`; соответствующий commit ветки `codex/deputy-pwa` — `df8c26b9c431c6f683c496d94345092ab38794b9`.

Перед deploy создан и проверен rollback-архив кода `/srv/accounting-mvp/backups/code/deputy-headings-before-20260714_120450.tar.gz` (SHA-256 `f69ddb68408435073874e9f8d7ce5a5f221a7a5233736f393c4a15f51a54c381`). Модели, данные и миграции не менялись, поэтому резервная копия БД не создавалась и `migrate --noinput` не запускался.

На production прошли `manage.py check`, `makemigrations --check --dry-run`, `migrate --plan`, `collectstatic --noinput` (`1` файл скопирован), restart `accounting-mvp` и `nginx -t`; оба сервиса активны, новых ошибок в журнале нет. Четыре deploy-маркера указывают на `bbed5e99844b8104df47e95a948894143b0a79a3`.

Публичные проверки подтвердили `/` → `200`, защищённый `/deputy-mining-manager/` без сессии → `302 /home/`, service worker → `200` и PWA-кеш `v4`. Исходный, собранный и публичный `deputy-mining-manager-v3.css` совпадают по SHA-256 `6aa0de213fb9526445e5a0cf55abe4be79855f2e801c9940e93f35297600d49d`. Остаётся пользовательская визуальная приёмка выравнивания в авторизованной сессии заместителя.
