# Production: переработка графика готовности 10.09.2026

## Статус

**ОПУБЛИКОВАНО / COMMIT, PUSH И PRODUCTION DEPLOY ВЫПОЛНЕНЫ.**

Реализация сохранена в ветке
`codex/management-registration-insights-2026-09-09`, commit
`29e57a9b3407023d2bc0d6b936c8631c61300bb0` и 10.09.2026 точечно
опубликована на `driverform.ru`.

Перед заменой четыре production-файла были побайтно сопоставлены с предыдущим
runtime-коммитом `3edb669180b8b4344a267743f81422e936d96ddc` через Git blob ID.
Старые общие marker-файлы сервера не отражали этот точечный релиз, поэтому их
значения не использовались как доказательство и не изменялись.

Rollback-архив предыдущих файлов:
`/srv/accounting-mvp/backups/code/management-registration-chart-20260909T223535Z-before-3edb6691/runtime-before.tar.gz`,
SHA-256 `9f32b9b251359a9ddb29c2b85566b62f07b3656a01b66498b0cdc7ed9920e57d`.

## Новый график

- прежний длинный холст `44 px × день` и горизонтальная прокрутка удалены;
- 7, 14 и 30 дней показываются дневными интервалами, 90 дней — 13 интервалами
  до семи дней, при этом точный дневной ряд сохраняется на сервере;
- накопительная готовность показана ступенчатой областью на фиксированной
  шкале `0/25/50/75/100%`, прирост — отдельной выровненной гистограммой;
- линия и столбцы используют единый центр интервала, общий crosshair и
  инспектор выбранного дня/интервала;
- клик или касание сначала выбирает интервал, отдельная кнопка открывает
  точный серверный срез; доступны стрелки, Home, End, Enter и Escape;
- события без подтверждённой даты и будущие даты отделены от линии, а подписи
  прямо говорят, что это распределение текущих готовых сотрудников, не
  исторический снимок состояния;
- нулевые столбцы не изображают ложную активность, анимация отключается через
  `prefers-reduced-motion`.

## Визуальная и адаптивная приёмка

Авторизованная Browser QA выполнена реальным Chromium на стандартном локальном
сервере для администратора и руководства.

- desktop `1280×720`: основная область `260 px`, гистограмма `80 px`;
- tablet `768×1024`: `220 px` и `72 px`;
- portrait `390×844`: `190 px` и `60 px`;
- landscape `844×390`: `156 px` и `52 px`;
- на всех размерах страница имеет нулевое горизонтальное переполнение;
- 30 дней целиком помещаются в ширину, видны первая и последняя дата;
- погрешность центра линии и столбца — `0,00–0,01 px`;
- дневная и ночная темы, клавиатура, дневной и недельный drill-down работают;
- временный QA-файл, сотрудник, доступ и сессия удалены; локальный сервер
  остановлен штатным скриптом.

В ходе Browser QA найден и закрыт критический дефект, не обнаруженный
статическим аудитом: фиксированная grid-оболочка администратора сжимала строку
панели примерно до `30 px` и обрезала график. Неявные строки теперь имеют
`max-content`; панель занимает полную высоту, а следующий блок начинается
ниже неё с проектным зазором.

## Проверки

- `users.test_registration_dashboard` — `24/24 OK`;
- `manage.py check` — без ошибок;
- `makemigrations --check --dry-run` — изменений моделей нет;
- `py_compile`, `node --check`, `git diff --check` — успешно;
- три независимых аудита backend-метрик, UX и frontend-интеграции — `GO`.

Общий GitHub workflow остаётся красным на известном независимом baseline
пользовательского модуля; профильный набор переработанного дашборда зелёный.

## Production-приёмка

- source SHA-256 совпадают с runtime-коммитом для серверного расчёта, шаблона,
  CSS и JavaScript;
- собранные `staticfiles` CSS и JavaScript совпадают с исходниками, публичные
  ответы с cache-bust `20260909-6` имеют те же SHA-256;
- `manage.py check` — без ошибок, `makemigrations --check --dry-run` — без
  изменений, `migrate --plan` — без запланированных операций;
- `collectstatic` перенёс два изменённых статических файла, `nginx -t`
  успешен, `accounting-mvp` и `nginx` активны;
- журнал после restart содержит только штатную остановку, запуск Gunicorn и
  загрузку трёх worker-процессов, traceback/exception/error отсутствуют;
- защищённые маршруты без сессии ожидаемо отвечают `302`;
- read-only расчёт на production-данных подтвердил: дата расстановки
  `08.09.2026`, всего `22`, доступ подготовлен у `21`, готовы `12`, требуют
  действия `10`; с подтверждённой датой `11`, без даты `1`, будущих дат `0`;
- периоды 7/14/30 содержат 7/14/30 дневных точек, период 90 — 90 дневных
  точек и 13 непересекающихся интервалов `[6] + 12×[7]`.

На двух предварительных запусках защита сработала штатно: первый завершился до
замены из-за прав чтения root-only backup, второй обнаружил опечатку в
контрольной строке SHA до `collectstatic`, автоматически восстановил все четыре
старых файла и перезапустил сервис. Перед окончательной попыткой прежние Git
blob ID и активность сервисов были повторно подтверждены.

Авторизованная визуальная Browser QA выполнена локально на точном runtime-коде;
на production подтверждены те же исходники и публичная статика. Отдельный
авторизованный production-сценарий роли не выполнялся из-за отсутствия боевой
сессии в доступном браузере.

База данных, `.env`, модели, миграции и рабочие production-данные не менялись.
