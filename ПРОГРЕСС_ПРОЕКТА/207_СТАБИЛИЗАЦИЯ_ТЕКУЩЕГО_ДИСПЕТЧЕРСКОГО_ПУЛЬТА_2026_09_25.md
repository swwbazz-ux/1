# Стабилизация текущего Диспетчерского пульта — 25.09.2026

## Цель

Сделать единой отправной точкой не старую feature-ветку, а фактически
работающий на `driverform.ru` Диспетчерский пульт, сохранить его поведение и
поэтапно разделить накопившиеся контуры без большого переписывания.

## Подтверждённый baseline и уточнение 26.09.2026

- Последняя серверная публикация кода выполнена GitHub Actions из полного SHA
  `749d10356d7a510f1061f8781c570988b48a8e84`: `VERIFY_MIGRATIONS` и
  `DEPLOY_MIGRATIONS` завершились успешно 25.09.2026, payload содержал `82` файла.
- Production является составной выкладкой, а не Git checkout. Последний payload
  не содержал `templates/includes/dispatcher_header.html`, поэтому общий header
  не был заменён версией из SHA `749d1035`.
- Публичные файлы 26.09.2026 подтверждают смешанный снимок: цельный
  `dispatcher-control-v1.js` побайтово соответствует blob из commit `c3cf465a`,
  цельный `dispatcher-control-v1.css` — blob из `ee0266a9`; вынесенные локально
  `dispatcher-transport-v1.js`, `dispatcher-detail-v1.js`,
  `dispatcher-board-v1.js`, `dispatcher-realtime-v1.js` и четыре CSS-слоя на
  production ещё отсутствуют (`404`).
- В боевом HTML на снимке пользователя от 24.09.2026 присутствует
  `data-dispatcher-equipment-search`, а публичный цельный JS содержит рабочий
  `bindDispatcherEquipmentSearch`. Это поведение было создано в отдельной ветке
  `codex/dispatcher-canvas-2026-09-09` и не входило в ancestry релизной основы.
- Следовательно, исходной точкой является подтверждённый manifest-scoped состав
  production плюс отдельно восстановленные боевые контракты, а не полное дерево
  любой одной Git-ветки.

Чистая рабочая ветка стабилизации:
`codex/dispatcher-stabilization-20260925`. Она основана на production SHA и
содержит первый отдельный commit `b37834e1` с исправлением возврата из
фрагментного URL после закрытия смены.

## Исправленный эксплуатационный дефект

После фонового fragment-refresh скрытое поле `next` могло получить
`?_operational_fragment=dispatcher`. Обычный POST завершения смены затем
перенаправлял основное окно на JSON-фрагмент — пользователь видел сырой JSON
вместо пульта.

Исправление состоит из двух защит:

1. форма всегда получает полный безопасный URL экрана без внутренних
   fragment-параметров;
2. сервер повторно очищает `next` и `Referer`, допускает только текущий host и
   сохраняет только пользовательские query-параметры.

## Фактическая структура до стабилизации

| Файл | Размер до этапа | Назначение |
|---|---:|---|
| `templates/trips/dispatcher_control.html` | 7 357 строк | общий HTML Диспетчера и мобильного Горного мастера, включая большой inline runtime мастера |
| `static/js/dispatcher-control-v1.js` | 6 610 строк / 364 394 байта | desktop runtime Диспетчера вместе с повторной копией мобильной логики Горного мастера |
| `static/css/dispatcher-control-v1.css` | 14 464 строки / 397 081 байт | исторически наращиваемые desktop-стили |
| `trips/views.py` | 8 531 строка | несколько доменных контуров, включая экран и действия Диспетчера |

Основной обнаруженный риск первого этапа: браузер Диспетчера загружал почти
половину JavaScript, предназначенного только для мобильного Горного мастера.
Этот код не исполнялся на desktop-экране, но увеличивал payload и делал любое
изменение одной роли опасным для другой.

## Выполненный этап 1

- Из `static/js/dispatcher-control-v1.js` удалена повторная мобильная ветка
  Горного мастера: mobile fragment-refresh, PWA update UI, touch/drag runtime и
  связанные недостижимые desktop-ветки.
- Канонический mobile runtime Горного мастера сохранён в условной ветке
  `mining_master_mobile_enabled` общего шаблона и не менялся.
- Desktop runtime уменьшен примерно вдвое: с `364 394` до `178 164`
  байт и с `6 610` до `3 317` строк.
- Оставшийся runtime размечен семью стабильными зонами: старт/смена, карточка
  техники, транспорт запросов, realtime reconciliation, карточка/тема,
  service worker/диагностика, desktop-доска/DnD.
- Публичный диагностический объект получил корректное имя
  `window.DispatcherSyncDebug`.
- Добавлен контрактный тест границы ролей: мобильные функции не должны снова
  попадать в desktop bundle, при этом обязаны оставаться в mobile-ветке.
- Версия оболочки Диспетчера подготовлена как
  `dispatcher-desktop-shell-v132`, чтобы будущая публикация не оставила у
  браузеров закэшированный `v131`.

## Проверка этапа 1

- `23/23` адресных Django-теста Диспетчера и системного push прошли;
  `manage.py check` завершился без замечаний.
- Все `87/87` профильных Node-тестов Диспетчера, fragment runtime,
  responsive-контрактов, звука, push и границы ролей прошли.
- В изолированной временной SQLite Browser QA прошёл реальный цикл
  `открыть смену -> получить fragment-refresh -> закрыть смену`.
  Версия доски обновилась с `8` до `9`, безопасный `next` остался
  `/dispatcher/control/`, закрытие вернуло `text/html`, а не JSON.
- На `1280 px` и `390 px` горизонтального overflow нет; итоговый журнал
  браузера пуст. Временные БД, сотрудник, доступ, журналы и QA-сервер удалены.
- Карточки техники, поиск и drag-and-drop на этом пустом Browser QA-наборе не
  воспроизводились; их действующие контракты покрыты профильными Node-тестами,
  а полный наполненный Browser QA остаётся обязательным release gate.
- Новый include формы смены явно добавлен в production-files manifest; все
  изменённые рабочие файлы текущего пакета теперь входят в будущий release.

## Выполненный этап 2

- Transport запросов и durable offline queue вынесены из общего runtime в
  `static/js/dispatcher-transport-v1.js`.
- Сохранены без миграции production-ключ
  `mining-master-mobile-sync-queue-v3`, формат записей, префикс
  `client_action_id`, CSRF, таймауты, coalescing и различие сетевой/серверной
  ошибки. Очереди `v1/v2` по-прежнему удаляются как небезопасные.
- Основной runtime использует узкий API фабрики: POST, timeout-fetch, readonly,
  чтение/flush очереди и состояние realtime. Внутренняя реализация больше не
  смешана с карточками, fragment reconciliation и drag-and-drop.
- `dispatcher-control-v1.js` уменьшился с `3 317` строк / `178 164` байт до
  `3 093` строк / `168 951` байта. Новый transport занимает `318` строк /
  `12 486` байт; суммарный неминифицированный исходник стал на `3 273` байта
  больше из-за явной модульной границы и API, но основной рабочий файл стал
  меньше и независимее.
- Transport подключается перед основным runtime, входит в service worker cache
  и production-files manifest. Shell подготовлен как
  `dispatcher-desktop-shell-v133`.
- Добавлены семь runtime-тестов очереди/transport и два boundary-контракта:
  production-ключ и старые записи сохраняются, сетевой сбой ставится в очередь,
  серверный конфликт не маскируется, readonly ничего не отправляет, успешный
  flush удаляет запись только после ответа.

## Проверка этапа 2

- Адресно прошли `117/117` Node-контрактов и `25/25` Django-тестов;
  `manage.py check` завершился без замечаний.
- Полный backend JS-набор: `746 passed`, `1` штатный skip. Все контракты
  мобильной оболочки: `44/44`.
- Browser QA на стандартном временном локальном сервере подтвердил загрузку
  transport до control runtime, shell `v133` и полный цикл
  `открыть смену -> fragment 8 -> 9 -> закрыть смену` с возвратом HTML-пульта.
- На `1280 px` и `390 px` горизонтального overflow нет, браузерная консоль
  пустая. QA-сервер, SQLite, временный сотрудник, доступ и журналы удалены.

## Выполненный этап 3

- Realtime version tracking, проверка занятости экрана и узкое обновление DOM
  вынесены в `static/js/dispatcher-realtime-v1.js`.
- Новый модуль принимает только явные зависимости: transport, синхронизацию
  состояния смены, чтение/обновление данных карточек, повторную привязку
  взаимодействий и восстановление целостности доски.
- Сохранены прежние правила: версия `operational-state-version`, окно доверия
  локальному `assignment_changed`, блокировка обновления при активном вводе,
  модальном окне, drag-and-drop и незавершённой offline-очереди, keyed
  reconciliation по `equipmentId`/`zoneId` и полный fallback при нарушении
  DOM-контракта или большом отставании.
- `dispatcher-control-v1.js` уменьшился с `3 093` строк / `168 951` байта до
  `2 784` строки / `154 118` байт. Новый realtime-модуль занимает `399` строк /
  `18 797` байт. Суммарный неминифицированный исходник вырос на `3 964` байта
  из-за явной границы зависимостей, но основной изменяемый файл стал меньше на
  `309` строк / `14 833` байта.
- Порядок загрузки закреплён как `sounds -> transport -> realtime -> control`.
  Модуль добавлен в service worker cache и production-files manifest; shell
  подготовлен как `dispatcher-desktop-shell-v134`.
- Добавлены runtime-тесты фабрики realtime и DOM-контракта: отсутствие лишнего
  fragment-запроса для нерелевантного события, полный fallback при усечённой
  истории, восстановление callback-хуков и точечная замена изменившейся плитки.

## Проверка этапа 3

- `manage.py check` завершился без замечаний; `31/31` адресных Django-тестов
  Диспетчера, realtime и service worker прошли.
- Полный backend JS-набор: `749 passed`, `1` штатный skip (`750` тестов всего).
- Browser QA на стандартном локальном запуске подтвердил фактический порядок
  `transport -> realtime -> control`, shell `v134` и цикл
  `открыть смену -> fragment 8 -> 9 -> закрыть смену`.
- После завершения смены остались `/dispatcher/control/` и `text/html`, сырой
  JSON не появился. На `1280 px` и `390 px` горизонтального overflow нет;
  мобильная кнопка открывает диалог смены, консоль пуста.
- Временные SQLite, QA-сотрудник/доступ, журнал, сервер и junction удалены.

## Выполненный этап 4

- Карточка техники вынесена в `static/js/dispatcher-detail-v1.js`: загрузка
  versioned detail, рендер, отчёт смены, простой, настройки, ручной рейс и
  служебное завершение смены остаются единым контуром.
- Desktop-доска вынесена в `static/js/dispatcher-board-v1.js`: сортировка,
  поиск, адаптивная раскладка машин, drag-and-drop, подтверждения и
  оптимистические DOM-изменения отделены от transport/realtime.
- Между модулями сохранён узкий API: control передаёт URL и callbacks,
  detail публикует только карточки/слой/рендер и presentation helpers, board —
  повторную привязку, проверку целостности и перерасчёт rack layout.
- Серверные маршруты, CSRF, data-атрибуты, assignment state id, offline queue,
  версии состояния и fallback не менялись.
- `dispatcher-control-v1.js` уменьшен с `2 784` строк / `154 118` байт до
  `343` строк / `16 573` байта. Detail занимает `1 464` строки / `85 812`
  байт, board — `1 089` строк / `63 811` байт.
- Порядок загрузки закреплён как
  `sounds -> transport -> detail -> board -> realtime -> control`. Оба файла
  добавлены в service worker cache и production-files manifest; shell
  подготовлен как `dispatcher-desktop-shell-v135`.

## Проверка этапа 4

- Синтаксис всех трёх изменённых JS-файлов проверен `node --check`.
- Адресные контракты Диспетчера прошли `80/80`; полный backend JS-набор —
  `749 passed`, `1` штатный skip (`750` тестов всего).
- `28/28` адресных Django-тестов и `manage.py check` прошли без замечаний.
- Browser QA запущен штатным `START_SERVER_MVP.bat` на наполненной временной
  SQLite. На `1280×800` карточка экскаватора открылась и отрисовалась; на
  `390×844` horizontal overflow не обнаружен.
- Реальный цикл `закрыть смену -> открыть смену` вернул HTML-пульт с сообщением,
  не сырой JSON. После повторного открытия смены карточка снова открылась,
  `shiftOpen=true`, readonly снят, консоль пуста.
- Временная БД, QA-сотрудник/доступ, сервер и junction удалены.

## Выполненный этап 5

- Монолитный `static/css/dispatcher-control-v1.css` разделён непрерывными
  диапазонами на пять функциональных слоёв без перестановки селекторов,
  изменения значений и удаления исторических overrides:
  - `dispatcher-control-v1.css` — `3 661` строка / `87 076` байт;
  - `dispatcher-workspace-v1.css` — `6 018` строк / `155 275` байт;
  - `dispatcher-detail-v1.css` — `1 508` строк / `36 921` байт;
  - `dispatcher-adaptive-v1.css` — `2 122` строки / `69 905` байт;
  - `dispatcher-detail-overrides-v1.css` — `1 155` строк / `47 904` байта.
- Суммарный каскад остался равен исходным `14 464` строкам / `397 081` байту.
  Последовательное объединение пяти файлов даёт исходный Git blob
  `602ac12ae7fb1f8449ccf37e15f029509d9b9fa6`.
- Шаблон загружает слои в исходном порядке до существующего
  `dispatcher-canvas-v1.css`. Все новые файлы включены в service worker cache,
  production-files manifest и общий тестовый CSS-reader; shell подготовлен как
  `dispatcher-desktop-shell-v137`.
- В ходе Browser QA обнаружена отдельная ошибка этапа 4: detail-модуль потерял
  доступ к `escapeHtml` после извлечения из общего runtime. Локальный helper и
  регрессионный тест добавлены отдельным commit `caaf93f6`; CSS-декомпозиция с
  этим исправлением не смешана.

## Проверка этапа 5

- Побайтовая эквивалентность каскада подтверждена через `git hash-object`;
  `git diff --check` не обнаруживает ошибок форматирования.
- `28/28` адресных Django-тестов и `manage.py check` прошли без замечаний.
- Полный backend JS-набор: `750 passed`, `1` штатный skip (`751` тест всего).
- Browser QA выполнен штатным локальным запуском на чистом shell `v137`.
  На `1280×800` и `390×844` загружены и разобраны все пять CSS-слоёв,
  наполненный комплекс и карточка К-530 отображаются без horizontal overflow
  и ошибок консоли; мобильная карточка укладывается в viewport.
- Временная SQLite, media, QA-доступ, журнал, сервер и junction удалены; порты
  локальной проверки освобождены.

## Выполненный этап 6

- Добавлен `tools/audit_dispatcher_css.cjs`, анализирующий все шесть CSS-слоёв
  в фактическом порядке каскада и разделяющий точные повторы деклараций и
  свойства с изменёнными значениями.
- Исходный аудит: `37` точных повторов деклараций, `0` полностью совпадающих
  блоков правил, `69` changed-value overrides в `36` селекторах.
- Удалена только первая группа из `23` точных повторов деклараций раннего
  оформления диалога общей смены. CSS уменьшен на `23` строки / `777` байт;
  после очистки осталось `14` точных повторов, число changed-value overrides
  не изменилось.
- Во время Browser QA выявлена отдельная потеря обработчика «Начать смену»
  после realtime reconciliation. Общий диалог получил идемпотентную повторную
  инициализацию, вызываемую realtime-модулем; исправление сохранено отдельным
  локальным commit `ecaf672a`.
- Shell после CSS-этапа подготовлен как `dispatcher-desktop-shell-v140`.
  Подробный снимок и список оставшихся повторов — в документе 208.

## Проверка этапа 6

- Полный backend JS-набор: `754 passed`, `1` штатный skip (`755` тестов всего).
- `28/28` адресных Django-тестов и `manage.py check` прошли без замечаний.
- Browser QA `1280×800` и `390×844` подтвердил актуальные shell-ресурсы,
  открытие и закрытие диалога после realtime-обновления, отсутствие horizontal
  overflow и ошибок/предупреждений консоли.
- На узком viewport сохранено текущее масштабирование desktop-canvas; новый
  адаптивный дизайн или визуальные компоненты не вводились.

## Выполненный этап 7

- Удалены следующие `4` точных повтора деклараций, у которых идентичная более
  поздняя декларация находится в том же файле:
  - `.complex-equipment: color` и `.complex-plan: justify-self, width` в
    `dispatcher-control-v1.css`;
  - `.dispatcher-board: grid-template-rows` внутри одного `max-width: 1180px`
    в `dispatcher-adaptive-v1.css`.
- `dispatcher-control-v1.css` уменьшился с `3 638` до `3 635` строк и с
  `86 299` до `86 223` байт; `dispatcher-adaptive-v1.css` — с `2 122` до
  `2 121` строки и с `69 905` до `69 866` байт.
- Аудит теперь показывает `10` точных межфайловых повторов, `0` same-file
  повторов, `0` полностью совпадающих блоков и прежние `69` changed-value
  overrides в `36` селекторах. Shell поднят до `dispatcher-desktop-shell-v141`.

## Проверка этапа 7

- Полный backend JS-набор: `754 passed`, `1` штатный skip (`755` тестов всего).
- `28/28` адресных Django-тестов, `manage.py check`, синтаксис аудитора и
  manifest allowlist прошли без замечаний.
- Browser QA штатного локального сервера выполнен на заполненной доске с
  комплексом К-530 и самосвалом 82. На `1280×800` и `390×844` карточка видна,
  horizontal overflow отсутствует, консоль пуста; диалог начала смены
  открывается и закрывается.
- Существующие visual-компоненты и масштабирование desktop-canvas сохранены;
  новый стиль не добавлялся.

## Выполненный этап 8

- Подтверждено, что `dispatcher-control-v1.css` и
  `dispatcher-workspace-v1.css` в рабочем контуре всегда загружаются вместе и
  в одном порядке: шаблон, service worker, тестовый CSS-reader и production
  allowlist содержат оба файла.
- Из control удалены `9` ранних деклараций, точные копии которых остаются в
  workspace: `display` у topbar; `margin`, `padding`, `cursor` у кнопки темы;
  `grid-column` и `display` у контейнера комплексов;
  `grid-template-columns` и `gap` у карточки комплекса; `grid-column` у правого
  гаража.
- `dispatcher-control-v1.css` уменьшился с `3 635` до `3 626` строк и с
  `86 223` до `86 014` байт. Аудит оставляет `1` точный повтор, `0` полностью
  совпадающих блоков и прежние `69` changed-value overrides в `36` селекторах.
- Shell поднят до `dispatcher-desktop-shell-v142`.

## Проверка этапа 8

- Полный backend JS-набор: `754 passed`, `1` штатный skip (`755` тестов всего).
- `28/28` адресных Django-тестов, `manage.py check`, синтаксис аудитора и
  manifest allowlist прошли.
- Browser QA штатного локального сервера выполнен на трёхколоночной доске с
  активным комплексом, свободным экскаватором и свободным самосвалом. На
  `1280×800` левая, центральная и правая области видимы в колонках `1/2/3`,
  кнопка темы переключает night/day.
- На `390×844` области переходят в адаптивную первую колонку, карточки и
  элементы управления остаются внутри viewport; horizontal overflow и ошибки
  консоли отсутствуют. Диалог начала смены открывается и закрывается.
- Новый visual-компонент или стиль не добавлялся.

## Выполненный этап 9

- Подтверждён единый порядок `workspace -> adaptive` в рабочем шаблоне,
  service worker, тестовом CSS-reader и production allowlist.
- Из позднего правила `.complex-truck-tile .dispatcher-plan-loop-badge`
  удалён только точный повтор `right: 3px`; рабочее адаптивное изменение
  `top: 17px` сохранено.
- `dispatcher-adaptive-v1.css` уменьшился с `2 121` до `2 120` строк и с
  `67 749` до `67 733` байт в Git-представлении.
- Аудит показывает `0` точных повторов деклараций, `0` полностью совпадающих
  блоков и прежние `69` changed-value overrides в `36` селекторах.
- Shell поднят до `dispatcher-desktop-shell-v143`.

## Проверка этапа 9

- Полный backend JS-набор: `754 passed`, `1` штатный skip (`755` тестов всего).
- `28/28` адресных Django-тестов и `manage.py check` прошли; оба изменяемых
  CSS-файла и остальные runtime-файлы остаются в production allowlist.
- Browser QA на `1280×800` подтвердил видимый бейдж `×1`, computed
  `top: 17px; right: 3px`, его положение внутри плитки и рабочую
  трёхколоночную доску; переключатель темы работает.
- На `390×844` бейдж, плитка, карточка, topbar и правый гараж находятся внутри
  viewport; horizontal overflow и ошибки/предупреждения консоли отсутствуют.
- Новый visual-компонент или стиль не добавлялся.

## Карта дальнейшего разделения

Следующие этапы выполняются отдельными малыми commit с Browser QA после
каждого, без одновременного редизайна:

1. [x] Вынести transport/offline queue в отдельный модуль, сохранив текущий
   ключ localStorage для восстановления уже поставленных в очередь действий.
2. [x] Вынести realtime fragment reconciliation и зафиксировать его DOM-контракт
   отдельными тестами.
3. [x] Разделить карточку техники и desktop board/DnD, оставив общий публичный
   bootstrap минимальным.
4. [x] Разобрать CSS по функциональным секциям с visual-regression проверкой до
   удаления перекрывающих исторических правил.
5. [x] Добавить машинный аудит CSS и удалить первую группу только точных
   повторов деклараций, не затрагивая overrides с изменёнными значениями.
6. [x] Удалить `4` same-file повтора и подтвердить нулевое число таких
   повторов аудитом и Browser QA.
7. [x] Удалить `9` повторов control/workspace после проверки единого порядка
   загрузки и трёхколоночной Browser QA.
8. [x] Отдельно удалить последний повтор workspace/adaptive у бейджа цикла.
9. [x] После стабилизации frontend начать выделение dispatcher-обработчиков из
   `trips/views.py`; перенос backend нельзя смешивать с изменением поведения.

Первой изолированной группой стали PWA manifest/service worker обработчики. Карта оставшихся
операционных границ зафиксирована в
`209_КАРТА_BACKEND_ДИСПЕТЧЕРСКОГО_ПУЛЬТА_2026_09_25.md`.

10. [x] Отдельным этапом выделить общие dispatcher access/response guards с
    сохранением redirect/JSON-контрактов и адресными тестами.
11. [ ] Следующим отдельным этапом отделить read-model рабочего экрана и карточки,
    сохранив renderer-контракт Горного мастера.

## Обязательные release gates

- профильные Django-тесты Диспетчера и проверка `manage.py check`;
- все Node-контракты Диспетчера, fragment runtime и boundary ролей;
- Browser QA `1280×800` и `390×844`: открытие/закрытие смены, fragment-refresh,
  карточка техники, поиск, drag-and-drop, отсутствие overflow и ошибок консоли;
- проверка deployment manifest: каждый новый production-файл должен быть явно
  включён;
- публикация только отдельным разрешённым этапом через `VERIFY`, затем
  `DEPLOY` того же полного SHA с rollback-комплектом.

## Статус публикации

Все изменения этого документа пока только локальные. Push, deploy,
`collectstatic`, миграции, база данных и production не выполнялись.

## Выполненный этап 10

- Построена карта 13 диспетчерских маршрутов: 2 PWA и 11 операционных.
- PWA service worker и PWA HTTP-обработчики вынесены из `trips/views.py` в
  `trips/dispatcher_pwa.py`.
- Удалён неиспользуемый локальный `DISPATCHER_MANIFEST`; источником manifest остаётся
  `users/role_apps.py`.
- URL, scope, push-контракт и shell `dispatcher-desktop-shell-v143` сохранены.
- Новый runtime-файл включён в production allowlist.

## Проверка этапа 10

- Профильные Django/PWA-тесты: `81/81`; `manage.py check` — без замечаний.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- Прямые HTTP-проверки manifest/service worker подтвердили status `200`, content type,
  scope `/dispatcher/` и shell `dispatcher-desktop-shell-v143`.
- Browser QA на `1280×800` и `390×844`: экран и диалог смены открываются,
  horizontal overflow и ошибки/предупреждения консоли отсутствуют.
- Новый визуальный компонент или стиль не добавлялся.

## Статус публикации этапа 10

Изменения пока только локальные. Push, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 11

- В новый `trips/dispatcher_guards.py` вынесены восемь access/response guards.
- Старые имена доступны через `trips.views`; wrapper блокирующей проверки сохраняет
  patch seam `trips.views.role_session_state`.
- Зафиксированы безопасный redirect, удаление внутренних fragment query,
  JSON `409`, HTML redirect/message, active-role recheck и renderer Горного мастера.
- Новый runtime-файл включён в production allowlist.

## Проверка этапа 11

- Адресные Django-тесты: `84/84`; `manage.py check` — без замечаний.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- Browser QA `1280×800` и `390×844`: экран и защищённое начало тестовой смены
  работают; horizontal overflow и ошибки/предупреждения консоли отсутствуют.
- Новый визуальный компонент или стиль не добавлялся.

## Статус публикации этапа 11

Изменения пока только локальные. Push, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 12

- Ветка стабилизации без конфликтов синхронизирована с release-основой `749d1035`;
  прежняя вершина сохранена отдельной локальной страховочной веткой.
- Desktop-приглашение системных уведомлений дословно вынесено в
  `templates/trips/includes/dispatcher_push_invite.html`.
- Условие роли, DOM-контракт и порядок разметки сохранены; мобильная ветка Горного
  мастера не изменялась.
- Include добавлен в `production-files.txt` тем же набором изменений и защищён
  точечным release-тестом отсутствия пропуска и дубля.

## Проверка этапа 12

- Обратная подстановка include полностью восстанавливает исходный текст шаблона.
- Адресные проверки: Node `7/7`, shared-shift Django `18/18`, release protocol `18/18`.
- Расширенный dispatcher-профиль: `174` теста, `169` пройдено, `5` штатно пропущено;
  полный backend JS: `778 passed`, `1` skip из `779`; `manage.py check` чистый.
- Browser QA `1280×800` и `390×844` подтвердил доску, detail-карточку `822`, нулевой
  horizontal overflow и пустую console error/warn. Временный QA-контур удалён.

## Статус публикации этапа 12

Изменения только локальные. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 13

- Полная сверка diff ветки с `production-files.txt` классифицировала `65` изменённых
  файлов и `26` рабочих production-кандидатов.
- Найден единственный накопленный пропуск: `trips/urls.py`, переключённый на
  `dispatcher_pwa.py` ещё на этапе выделения PWA-обработчиков.
- Маршрутный файл добавлен в manifest и защищён exact-once release-тестом.

## Проверка этапа 13

- Повторная сверка: `26/26` production-кандидатов входят в manifest.
- Дубли manifest: `0`; пропущенные production-файлы: `0`.
- Итоговый release protocol: `19/19`.

## Статус публикации этапа 13

Исправление только локальное. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 14

- Общая серверная доска дословно вынесена в
  `templates/trips/includes/dispatcher_board.html`; главный шаблон уменьшился
  с `7491` до `7349` строк.
- Include владеет верхней панелью, гаражом экскаваторов, комплексами и гаражом
  самосвалов. DOM/data-атрибуты, ветки ролей и порядок разметки не менялись.
- Новый production-файл добавлен в manifest и exact-once release-тест.
- Node-тесты, которым нужен собранный экран, переведены на единый
  `dispatcher-screen-source.js`; сырой шаблон и include отдельно продолжает
  проверять boundary-тест.

## Проверка этапа 14

- Обратная подстановка include полностью восстанавливает исходный текст шаблона.
- Boundary Node: `8/8`; release protocol: `20/20`; shared-shift Django: `18/18`.
- Полный backend JS: `779 passed`, `1` штатный skip из `780`; расширенный
  dispatcher-профиль Django: `174` найдено, `169` пройдено, `5` штатно
  пропущено на SQLite; `manage.py check` чистый.
- Финальная сверка упаковки: `27/27` production-кандидатов входят в manifest;
  пропусков и дублей нет.
- Browser QA `1280×800` и `390×844` подтвердил одну доску, комплекс K-530,
  самосвалы 821/822, detail-карточку ремонта, отсутствие horizontal overflow
  и пустую error/warn console. Временный QA-контур удалён.

## Статус публикации этапа 14

Изменения только локальные. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 15

- Скрытая служебная панель дословно вынесена в
  `templates/trips/includes/dispatcher_service_lists.html`; главный шаблон
  уменьшился с `7349` до `7107` строк.
- Новый include владеет фильтрами, списками рейсов и назначений, незакрытыми
  сменами, механическими простоями и их существующими служебными формами.
- URL, CSRF, hidden-поля возврата, проверки роли, порядок и скрытое состояние
  секции не менялись.
- Include добавлен в составной источник Node-тестов, production manifest,
  boundary-тест и exact-once release-тест.

## Проверка этапа 15

- Обратная подстановка `243` строк include полностью восстанавливает прежний
  шаблон из `7349` строк.
- Boundary/focused Node: `47/47`; release protocol: `21/21`.
- Полный backend JS: `780 passed`, `1` штатный skip из `781`; расширенный
  dispatcher-профиль Django: `174` найдено, `169` пройдено, `5` штатно
  пропущено на SQLite; `manage.py check` чистый.
- Browser QA `1280×800` и `390×844` подтвердил одну доску, одну скрытую
  служебную секцию с шестью карточками, отсутствие horizontal overflow и пустую
  error/warn console. Временный QA-контур удалён.
- Финальная сверка упаковки: `28/28` production-кандидатов входят в manifest;
  пропусков и дублей нет.

## Статус публикации этапа 15

Изменения только локальные. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 16

- Единая detail-карточка техники и комплекса дословно вынесена в
  `templates/trips/includes/dispatcher_equipment_detail.html`; главный шаблон
  уменьшился с `7107` до `6902` строк.
- Все `data-gd-*` якоря, CSRF и формы служебного закрытия смены, простоя,
  настроек работы и ручного рейса сохранены без изменения порядка и поведения.
- Include добавлен в составной источник Node-тестов, production manifest,
  boundary-тест и exact-once release-тест.

## Проверка этапа 16

- Обратная подстановка `206` строк include полностью восстанавливает прежний
  шаблон из `7107` строк.
- Focused Node: `37/37`; release protocol: `22/22`.
- Полный backend JS: `781 passed`, `1` штатный skip из `782`; расширенный
  dispatcher-профиль Django: `174` найдено, `169` пройдено, `5` штатно
  пропущено на SQLite; `manage.py check` чистый.
- Browser QA `1280×800` и `390×844` подтвердил одну доску, одну скрытую detail-
  карточку, её ключевые формы, отсутствие horizontal overflow и пустую error/warn
  console. Временный QA-контур удалён.
- Финальная сверка упаковки: `29/29` production-кандидатов входят в manifest;
  пропусков и дублей нет.

## Статус публикации этапа 16

Изменения только локальные. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 17

- Desktop landscape-scaler вынесен из `dispatcher_control.html` в существующий
  `dispatcher-canvas-v1.js`; шаблон уменьшился с `6902` до `6830` строк.
- Атрибут `data-dispatcher-phone-fit` ограничивает формулу `1400×800` только пультом;
  сменный отчёт продолжает использовать общий холст без телефонной трансформации.
- Исправлены шесть недопустимых `.calc(...)` в canvas CSS.
- Canvas JS/CSS добавлены в production allowlist и PWA `CORE_ASSETS`; оболочка поднята
  до `dispatcher-desktop-shell-v144`.

## Проверка этапа 17

- Canvas/sounds Node: `11/11`; release protocol: `23/23`.
- Полный backend JS: `786` пройдено, `1` штатный skip из `787`.
- Расширенный dispatcher/PWA Django-профиль: `173/173`; `manage.py check` чистый.
- Browser QA: на `1280×800` и `390×844` canvas включён без horizontal overflow; на
  `784×336` включается прежний landscape-режим пульта; shell `v144` реально загружен,
  browser log пуст.
- Финальная сверка упаковки: `32/32` production-кандидата входят в manifest, дублей
  нет. Временный QA-сервер, SQLite и junction venv удалены.

## Статус публикации этапа 17

Изменения только локальные. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 18 — восстановление живого поиска production

- После ручной проверки заполненного пульта обнаружено, что локальный desktop-экран
  не показывал поле поиска и не принимал номер техники без предварительного фокуса.
- Причина локализована на границе составной production-выкладки: логика поиска и CSS
  были сохранены при декомпозиции, но разметка поля находилась только в отдельной
  production-ветке и отсутствовала в релизной основе `749d1035`.
- Разметка `dispatcher-equipment-search` возвращена в общий
  `templates/includes/dispatcher_header.html` с прежними ограничениями: только
  вкладка пульта, только desktop, максимум три символа, без placeholder.
- В header одновременно сохранены более поздние элементы релизной основы, включая
  кнопку звука. Старый файл целиком не копировался.
- Header добавлен в `production-files.txt`: дальнейшая manifest-scoped публикация
  больше не сможет случайно оставить на сервере другую версию шапки.
- Добавлен отдельный Node-контракт, который связывает четыре обязательные части:
  разметку header, keyboard runtime в `dispatcher-board-v1.js`, стили подсветки и
  наличие header в release manifest.

## Проверка этапа 18

- Адресный Node-профиль поиска, runtime boundary и responsive layout: `32/32`.
- Полный backend JS-профиль: `833` пройдено, `1` штатный skip из `834`;
  полный профиль `trips`: `434` теста, `13` штатных skip; release protocol:
  `27/27`; `manage.py check` чистый.
- Browser QA на заполненной временной базе подтвердил набор `10` при фокусе на
  `BODY`: поле само получило фокус, значение стало `10`, найден ровно самосвал №10.
- Набор `K-2` без выбора поля нашёл ровно карточку комплекса K-2; `Esc` очистил
  поле, подсветку и класс поиска.
- Индикаторы плана после проверки не изменились: комплекс K-1 — `28%`, самосвал
  №10 — `25%`; browser error log пуст.
- Дополнительная сверка цельного production JavaScript нашла `293` именованные
  функции. В локальных desktop-модулях и сохранённой mobile-ветке присутствуют все,
  кроме пяти старых внутренних имён: четыре относятся к удалённому legacy drag
  Горного мастера и заменены единым `bindMobileTruckTransferDrag`, одно — к старому
  long-tap и заменено текущим double-tap pointer-контрактом. Иных пропавших
  именованных функций production не обнаружено.

## Статус публикации этапа 18

Исправление только локальное. Push, PR, VERIFY, DEPLOY и production не выполнялись.

## Выполненный этап 19 — аудит параллельных веток

- Проверены все remote-ветки `dispatcher*`, которые Git формально считает неслитыми.
- Функциональные линии сведены по владельцам: canvas/search/layout, conflict recovery,
  sound, system push, responsive/keyed realtime и detail/downtime.
- Для каждой линии найден действующий модуль и профильный тест в текущей ветке.
  Postdeploy-ветки содержат только фиксацию уже выполненного выпуска.
- Единственным подтверждённым production-контрактом без локальной разметки оказался
  уже восстановленный поиск в общей шапке. Целиком переносить старые монолитные ветки
  не требуется и небезопасно.
- Backend-карта также исправлена: выполненный перенос приоритетов состояния техники
  теперь отмечен завершённым в чек-листе.

## Статус публикации этапа 19

Аудит и документация только локальные. Push, PR, VERIFY, DEPLOY и production не
выполнялись.

## Выполненный этап 20 — единый локальный приёмочный baseline

На заполненной временной базе выполнен связный проход по рабочему контуру диспетчера:

| Сценарий | Результат |
|---|---|
| Пульт и навигация | Пульт, Аналитика, Журнал и Отчёты открываются; активные вкладки определяются правильно |
| Поиск техники | Набор `10` и `K-2` без предварительного выбора поля находит нужную технику; `Esc` очищает поиск |
| Карточка самосвала | Карточка №10 открывается; план `1 / 4 рейса`, индикатор `25%` |
| Карточка комплекса | Карточка K-1 открывается; план `114 / 400 м³`, индикатор `28%` |
| Перетаскивание техники | Работоспособность подтверждена пользователем на заполненном локальном пульте |
| Завершение смены | Открывается штатное окно подтверждения; проверка отменена без изменения смены |
| Аналитика и журнал | Сводка и 58 записей хронологии загружаются без browser errors |
| Отчёты | Главный экран и 7 диспетчерских отчётов открываются без server error и horizontal overflow |
| Связь и браузер | Индикатор показывает «Связь есть», error log пуст |

Во время прохода обнаружено смешение двух разных дат в общей шапке: производственной
даты смены и календарной даты часов. На ночной смене одна из них неизбежно была
неверной. Контракт разделён: `current_date` остаётся датой производственной смены,
`clock_date` показывает фактическую календарную дату Владивостока. Шаблон теперь
выводит их в разные места. `trips/dispatcher_header.py` одновременно добавлен в
production allowlist, поскольку раньше этот backend-файл отсутствовал в manifest.

## Проверка этапа 20

- Новый unit-контракт дат: `1/1`.
- Dispatcher header/read-model и отчёты: `33/33`.
- Release protocol: `27/27`; `manage.py check` чистый.
- Browser QA после явного перезапуска локального `--noreload`-сервера: дата смены
  `25.09.2026`, дата часов `26.09.2026`, фильтр отчёта `2026-09-25`; browser error
  log пуст.
- Release-scope audit: `RELEASE_SCOPE_OK`, `111` записей manifest, `35`
  production-файлов в текущем dispatcher-релизе, пропусков и удалений нет.

## Статус публикации этапа 20

Baseline и исправление только локальные. Commit, push, PR, VERIFY, DEPLOY и production
на этом этапе не выполнялись.

## Выполненный этап 21 — синхронизация релизного кандидата с production

- По GitHub-ветке и журналам защищённого workflow независимо подтверждено, что
  фактическая версия production перед выпуском пульта —
  `f99c48cacdc718c303d6b30197701107d33e688c`.
- Production-основа влита в ветку пульта отдельным merge-коммитом
  `6a74b5febdf66cdcf1de4e3cb1f2ec01901273ed`, без rebase. Пересечения в
  `production-files.txt`, `trips/tests.py` и `users/role_apps.py` объединены без
  конфликтов; изменения Водителя из production сохранены.
- В `production-files.txt` после слияния `114` уникальных записей, задвоенных строк
  нет. `audit_release_scope.py` вернул `RELEASE_SCOPE_OK`: `85` изменённых файлов,
  `35` production-файлов, удалённых production-файлов нет.
- После слияния зелёные: release protocol `27/27`, tools `44` с одним штатным skip,
  backend JavaScript `798/799` с одним штатным skip, mobile shell `44/44`, `users`
  `609` с тремя штатными skip, `core + trips + downtimes` `644` с сорока штатными
  skip. `manage.py check` чистый, новых миграций нет.
- Browser QA объединённого кода подтвердил shell `dispatcher-desktop-shell-v144`,
  набор `10` без фокуса с единственным результатом — самосвал №10, план комплекса
  K-1 `28%`, план самосвала №10 `25%`, отсутствие horizontal overflow на
  `1280×900` и `390×844`, а также пустой browser error/warning log.

## Статус публикации этапа 21

Пользователь разрешил push, PR, защищённый VERIFY и production deploy пульта.
На момент фиксации этого этапа релизный кандидат подготовлен и проверен локально;
публикация ещё не выполнена и должна пройти только через канонический GitHub Actions
контур с точным SHA и точкой отката `f99c48ca`.
