# Production: агрегация простоев в сменных отчётах — 06.09.2026

## Правило отображения

- события одной причины суммируются по единице техники до округления;
- итог менее одной минуты не показывается;
- служебный комментарий «Автоматически по производственному событию» в отчёт
  не выводится;
- в отчёте по выемочному оборудованию суммарный простой показывается только в
  первой производственной строке экскаватора;
- в отчёте по самосвалам агрегат показывается в единственной строке самосвала;
- ручные `Trip.downtime_text` и `Trip.note` не изменялись.

## Публикация

Runtime-коммиты:

- `83674e7f06e00fa8e1127c12a3f80cfb3eed4b18` — суммирование причин и скрытие
  нулевых итогов;
- `21a7fbef12ed574640bf29698247d301165cc3b4` — показ итога только один раз для
  каждого экскаватора.

Дополнительный тест отчёта по самосвалам зафиксирован коммитом
`4c4191fdbdbc40b1c6ba5620090f3e5d39f363bc`.

Rollback-архив последнего runtime deploy:

- `/srv/accounting-mvp/backups/code/deploy-20260906T131748Z-dispatcher-report-downtime-once-before.tar.gz`;
- SHA-256: `6b5929a160089cb4264c85fc7710a55fd7e2626da11a93c09fd2cb9f12e67125`.

## Проверка

- целевой набор `DispatcherShiftReportTests`: `8/8`;
- `manage.py check`: без ошибок;
- production service: `active`, публичный вход: HTTP `200`;
- Browser QA обоих отчётов за ночную смену 06.09.2026: нулей и
  автокомментариев нет;
- экскаватор 99: `Ожидание самосвалов—1ч 47 мин` только в одной строке;
- самосвал 11: `ТО—4ч 19 мин (открыт)`, самосвал 44: `ТО—1ч 2 мин`;
- Excel QA: значения обоих листов совпадают с экранными строками.

База данных, модели, миграции и `.env` не изменялись.
