# Карта шаблона Диспетчерского пульта

Дата: 26.09.2026.

Рабочий файл:
`СИСТЕМА_MVP/backend/templates/trips/dispatcher_control.html`.

Назначение документа: перед дальнейшим разделением большого шаблона зафиксировать,
какому рабочему месту принадлежит каждый блок, какие DOM-атрибуты уже являются
контрактами JavaScript и какие функции обязаны повторно инициализироваться после
realtime fragment. Номера строк относятся к локальному состоянию шаблона из `7508`
строк на дату карты и после структурных изменений должны обновляться.

## Главный вывод

Текущий файл — это не один экран. В нём физически совмещены:

1. desktop-пульт Диспетчера;
2. мобильный пульт Горного мастера;
3. общая серверная разметка доски и карточки техники;
4. небольшие ветки масштабирования;
5. большой старый inline runtime Горного мастера;
6. уже разделённый внешний runtime desktop-Диспетчера.

Поэтому блок `1314–7499` нельзя считать «оставшимся JavaScript Диспетчера» и
переносить в `dispatcher-control-v1.js`: он находится внутри
`{% if mining_master_mobile_enabled %}` и принадлежит прежде всего мобильному
Горному мастеру. Desktop-Диспетчер на текущем контуре загружает шесть внешних
модулей в строках `7501–7506`.

## Верхнеуровневая построчная карта

| Строки | Содержимое | Владелец и правило изменения |
|---|---|---|
| `1–39` | Наследование `base.html`, выбор CSS, manifest и head-assets | Общая точка развилки двух рабочих мест; версии и порядок ресурсов менять только с PWA/release-проверкой |
| `41–63` | `body`, canvas, `<main>`, runtime URL/config и сообщения | Общая оболочка; `data-dispatcher-*` читает desktop runtime |
| `64–206` | Верхняя панель, гараж экскаваторов, комплексы, гараж самосвалов | Основная серверная доска; desktop-Диспетчер и общий renderer Горного мастера |
| `208–710` | Полный мобильный интерфейс Горного мастера | Контур Горного мастера; не включать в обычный рефакторинг desktop-Диспетчера |
| `713–955` | Скрытая служебная панель: фильтры, рейсы, назначения, смены, простои | Общая серверная/операционная разметка; формы сохраняют текущие URL и CSRF |
| `957–1162` | Единая detail-карточка техники и комплекса | Для desktop ею владеет `dispatcher-detail-v1.js`; DOM-якоря `data-gd-*` являются контрактом |
| `1165–1183` | Приглашение включить системные уведомления | Только desktop-Диспетчер |
| `1187–1257` | Inline-подгонка `.dispatcher-shell` для desktop-ветки | Старый узкий layout-контур; не объединять с canvas без отдельной визуальной проверки |
| `1259–1260` | Подключение `dispatcher-canvas-v1.js` | Общий холст диспетчерских desktop-экранов |
| `1262–1306` | Отдельная inline-подгонка shell Горного мастера | Контур Горного мастера |
| `1309–1310` | JSON `equipment_cards` и `equipment_state_ui` | Общий сервер → клиент контракт detail-карточки |
| `1313–7499` | Inline runtime мобильного Горного мастера | Отдельный большой объект будущего разделения; содержит template-вставки и повторную инициализацию после fragment |
| `7500–7507` | Внешние JS-модули desktop-Диспетчера | Канонический runtime пульта; порядок загрузки уже защищён тестами |

## Карта серверной HTML-разметки

### Оболочка и параметры runtime — строки `41–64`

Элемент `<main>` передаёт клиенту:

- URL перемещения экскаватора и назначения самосвала;
- шаблоны URL detail-карточки и закрытия простоя;
- static prefix и service-worker scope;
- тип клиента, role code и shell version.

Корневой `.dispatcher-board` передаёт состояние смены, собственную смену,
`operational_state_version` и `server_now`. Это входные данные
`dispatcher-control-v1.js`, `dispatcher-transport-v1.js` и
`dispatcher-realtime-v1.js`; переименование этих атрибутов без одновременного
изменения модулей запрещено.

### Основная доска — строки `64–206`

- `65–67` — общий include `includes/dispatcher_header.html`.
- `69–91` — гараж экскаваторов.
- `93–184` — активные производственные комплексы.
- `186–205` — гараж самосвалов.

Ключевые DOM-контракты доски:

- `data-dispatcher-drag` и `data-dispatcher-drop` — drag-and-drop;
- `data-equipment-card-id`, `data-equipment-id`, `data-equipment-name` — адресация
  плитки и открытие detail-карточки;
- `data-equipment-state` — семантическое состояние техники;
- `data-haul-assignment-state-id` — optimistic concurrency назначения;
- `data-zone-id`, `data-assigned-zone`, `data-placement-zone` — топология доски;
- `data-plan-*` — отображение плана, циклов и заполнения;
- `data-dispatcher-free-bucket-expires-at` — локальное окончание маркера свободного
  ковша.

Realtime-модуль ожидает три именованных региона и стабильные ключи:

- `.dispatcher-excavators` → `.dispatcher-equipment-tile` по `equipmentId`;
- `.dispatcher-zone-grid` → `.dispatcher-complex-card[data-zone-id]` по `zoneId`;
- `.dispatcher-trucks` → `.dispatcher-truck-tile[data-equipment-id]` по
  `equipmentId`.

### Мобильный Горный мастер — строки `208–710`

- `209–294` — домашний экран с комплексами и нижней навигацией;
- `296–349` — гараж экскаваторов;
- `351–402` — гараж самосвалов;
- `404–554` — отчёт и действие со сменой;
- `556–607` — отдельные экраны набора состава каждого комплекса;
- `609–658` — модальное выполнение плана смены;
- `662–671` — общий notice-dialog;
- `673–710` — browser/PWA update dialogs Горного мастера.

Все `data-mm-*` — контракт мобильного Горного мастера. Их нельзя переименовывать в
рамках задачи desktop-Диспетчера. Особенно чувствительны `data-mm-mobile-fill-*`,
`data-mm-mobile-home-truck-id`, `data-mm-mobile-activate-excavator`,
`data-mm-mobile-nav` и `data-mm-open-shift-plan-detail`: они участвуют в локальной
оптимистической раскладке, жестах и восстановлении экрана после fragment.

### Служебные списки — строки `713–955`

Секция сейчас скрыта, но содержит реальные формы и источники данных:

- фильтры;
- активные и завершённые рейсы;
- ожидающие и принятые назначения;
- открытые смены и их служебное закрытие;
- открытые простои.

Это не мёртвая разметка. При выносе в include нужно сохранить имена GET-фильтров,
CSRF, URL действий, hidden-поля возврата и проверки роли.

### Detail-карточка — строки `957–1162`

Разметка делится на:

- hero/паспорт и план — `965–981`;
- сотрудник и служебное закрытие смены — `983–1042`;
- простой и состав — `1044–1059`;
- настройки работы — `1064–1100`;
- ручной рейс — `1102–1145`;
- загрузка и отчёт смены — `1147–1157`;
- индикатор дальнейшей прокрутки — `1160`.

Якоря `data-gd-detail-*`, `data-gd-setting-*` и `data-gd-destination-*` принадлежат
`dispatcher-detail-v1.js`. Include можно выделить без изменения имён, вложенности
форм и расположения CSRF. Сам слой detail находится вне `.dispatcher-board`, поэтому
обычный realtime fragment доски не заменяет его; JSON карточек обновляется отдельно.

## Карта JavaScript

### Desktop-Диспетчер: канонические внешние модули

Порядок строк `7501–7506` является зависимостью:

| Модуль | Ответственность |
|---|---|
| `dispatcher-sounds-v1.js` | Звуковые переходы и пользовательское отключение звука |
| `dispatcher-transport-v1.js` | POST/fetch, таймауты и durable offline queue с legacy-ключом |
| `dispatcher-detail-v1.js` | Данные, открытие, rendering и формы detail-карточки |
| `dispatcher-board-v1.js` | Сетка, поиск, DnD и optimistic DOM доски |
| `dispatcher-realtime-v1.js` | Версии, fragment-запрос и точечное reconciliation |
| `dispatcher-control-v1.js` | Композиция модулей, notice/conflict recovery и debug API |

`dispatcher-canvas-v1.js` подключается раньше, в строке `1260`, потому что должен
установить геометрию холста до первого заметного отображения.

### Inline runtime Горного мастера — строки `1314–7499`

Фактические подблоки:

| Строки | Ответственность |
|---|---|
| `1315–1425` | Bootstrap, shift state и presentation состояния техники |
| `1426–1692` | Legacy transport/offline queue |
| `1693–1882` | Assignment state, версии и снимок мобильного экрана |
| `1883–2299` | Fragment refresh мобильного/desktop представления и notice |
| `2305–2745` | PWA update Горного мастера |
| `2746–3187` | Тема и rendering detail-карточки |
| `3188–6198` | Навигация, локальная раскладка, набор состава, жесты и смена |
| `6200–6289` | Стартовая плашка и пяти-минутная полная сверка |
| `6290–6388` | Service worker, online/storage события и sync debug |
| `6389–7498` | Desktop-представление/DnD внутри ветки Горного мастера |

В этом блоке остались Django-вставки: static prefix, action URL, флаг смены,
service-worker scope, native/browser ветка и несколько template-условий. Поэтому его
нельзя просто скопировать в `.js`. Сначала параметры должны стать явным data/JSON
bootstrap-контрактом, затем нужно переносить весь согласованный подмодуль с тестами.

## Что происходит после realtime fragment

### Desktop-Диспетчер

`dispatcher-realtime-v1.js`:

1. получает и разбирает только `.dispatcher-board`;
2. пытается точечно согласовать три keyed-региона;
3. при неполном контракте заменяет всю `.dispatcher-board`;
4. повторно вызывает `dispatcherBoard.bindInteractions()`;
5. повторно вызывает общие `initAppConfirmForms`, `initSharedShiftLogin`,
   `initDispatcherThemeControls`, `initDispatcherRadialClocks`;
6. восстанавливает выбранную/открытую карточку и scroll state;
7. запускает integrity/layout и обновляет индикатор связи.

Следовательно, новый интерактивный блок внутри доски обязан либо входить в
`bindInteractions`, либо иметь отдельный идемпотентный rebind callback в realtime.

### Мобильный Горный мастер

`refreshMobileBoardFromServer` заменяет целиком `.mm-mobile-shell`, затем вызывает
`bindMiningMasterMobileScreens()`, восстанавливает выбранный экран и прокрутку и
обновляет PWA-индикатор. Глобальные listeners защищены флагом
`window.__mmMobileGlobalHandlersBound`; общий `ResizeObserver` перед повторным bind
отключается и подписывается на новые DOM-узлы. Это обязательный контракт, иначе после
каждого fragment будут накапливаться обработчики.

## Тесты, которые защищают карту

- Граница desktop/mobile и порядок модулей:
  `dispatcher-runtime-boundary.test.js`.
- Realtime replacement и повторный bind:
  `dispatcher-realtime-runtime.test.js`,
  `dispatcher-fragment-shift-runtime.test.js`.
- Detail DOM и формы:
  `dispatcher-detail-card-runtime.test.js`,
  `dispatcher-detail-helper-runtime.test.js`,
  `dispatcher-manual-trip-runtime.test.js`.
- Доска и адаптивность:
  `dispatcher-responsive-layout-runtime.test.js`,
  `dispatcher-mobile-empty-garages-runtime.test.js`.
- Мобильный Горный мастер:
  `mining-master-board-refresh-runtime.test.js`,
  `mining-master-truck-transfer-runtime.test.js`.
- Общие смена, связь и transport:
  `dispatcher-shared-shift-runtime.test.js`,
  `dispatcher-transport-runtime.test.js`.

Любой перенос HTML должен дополнительно пройти Browser QA desktop `1280×800` и mobile
`390×844`: три колонки, detail-карточка, отсутствие horizontal overflow, пустая
console и повторное применение realtime fragment.

## Безопасный порядок дальнейшего разделения

1. Сначала выделять только HTML includes без изменения классов, атрибутов, порядка и
   JavaScript.
2. Первые кандидаты: основная доска `64–206`, служебные списки `713–955`,
   detail-карточка `957–1162` и push invite `1165–1183`.
3. Мобильную оболочку Горного мастера `208–710` выносить отдельным role-owned include,
   не смешивая с изменениями desktop-Диспетчера.
4. Малые scaler-блоки `1187–1306` анализировать отдельно от HTML includes; совпадение
   текста ещё не доказывает одинаковое поведение canvas и shell.
5. Перед внешним `.js` для блока `1314–7499` сначала создать явный bootstrap config и
   boundary-тест отсутствия Django syntax в будущем модуле.
6. После каждого include или JS-модуля запускать адресные Node tests, полный backend
   JS-набор и Browser QA. Редизайн и новые функции на этих commit не добавлять.

## Что пока нельзя трогать

- DOM-ключи трёх realtime-регионов;
- порядок внешних desktop-модулей;
- legacy offline queue key;
- `data-gd-*`, `data-mm-*`, `data-plan-*` без отдельной миграции контракта;
- повторный bind и защиту от дублирования global listeners;
- мобильный Горный мастер в рамках чисто dispatcher-задачи;
- PWA shell/cache/release manifest без отдельного release-аудита.

Production, deploy, миграции и база данных на этом этапе не менялись.
