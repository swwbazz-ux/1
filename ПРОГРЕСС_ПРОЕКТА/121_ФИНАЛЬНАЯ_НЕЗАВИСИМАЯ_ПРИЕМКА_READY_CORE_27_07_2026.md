# Финальная независимая приёмка ready-core

Дата: 27.07.2026

Основание:

- `119_НЕЗАВИСИМАЯ_ПРИЕМКА_ДОКУМЕНТА_118_И_ФИНАЛЬНЫЕ_TEST_ONLY_КАРТОЧКИ_27_07_2026.md`;
- `120_ОТЧЕТ_ОБ_ИСПРАВЛЕНИИ_QA_REG_P2_006_И_QA_REG_P3_007_27_07_2026.md`;
- SHA-256 документа 120:
  `CDEAB2E8526F3EEE8E38124CEBCF7F7773E84574A54375C445F54DBA9CFB3EE5`.

## 1. Итог

Обе финальные test-only карточки независимо приняты:

- `QA-REG-P2-006` — `PASS`;
- `QA-REG-P3-007` — `PASS`.

Финальный результат готового производственного ядра:

```text
адресные тесты:        2/2 OK
полный SQLite:         705/705 OK, skipped=31
полный PostgreSQL 16:  705/705 OK, skipped=0
AST return-аудит:      41 файл, findings=0
manage.py check:       PASS
новые миграции:        отсутствуют
git diff --check:      PASS
```

В рамках проверенного ready-core новых блокирующих production-дефектов не
обнаружено.

Текущая расширенная QA-кампания завершена. Вердикт:

**GO К ОТДЕЛЬНОЙ GIT-ФИКСАЦИИ И ОГРАНИЧЕННОМУ ПИЛОТУ ГОТОВЫХ РОЛЕЙ**.

Это не означает готовность ещё не разработанных ролей и интерфейсов.

## 2. Независимая проверка `QA-REG-P2-006`

Подтверждено:

- разбор JSON и четыре assertions находятся внутри
  `test_excavator_downtime_action_rejects_reason_outside_role_reference`;
- helper `post_truck_loaded_cancel()` начинается после завершения теста;
- тест действительно проверяет HTTP `400`, `ok=false`, сообщение
  `недоступна` и отсутствие `DowntimeEvent`;
- production-код не менялся.

Адресный тест прошёл самостоятельно.

## 3. Независимая проверка `QA-REG-P3-007`

Подтверждена полная симметричная цепочка Машиниста:

1. исходный простой экскаватора начинается в `18:55`;
2. Диспетчер служебно закрывает смену в `19:00`;
3. сменщик открывает новую смену;
4. видит ту же запись простоя;
5. закрывает её в `19:05`;
6. `DowntimeEvent` остаётся ровно один;
7. исходный сотрудник, причина и время начала не переписываются;
8. полный интервал составляет `10 минут / 0,17 часа`;
9. отчёт Диспетчера и его Excel совпадают;
10. отчёт Руководителя и его Excel совпадают;
11. имя сменщика не подменяет автора в обоих Excel.

Независимый code review блокеров не нашёл. Исходный простой в этом тесте
создаётся через ORM, но настоящий POST начала и закрытия уже отдельно
покрыт `ExcavatorWorkServerIntegrationTests`. Для цели регрессии
пересменки это допустимая изоляция сценария.

Production-расхождения не выявлено; production-код не менялся.

## 4. Независимые полные прогоны

### 4.1. SQLite

Окружение очищено от PostgreSQL-переменных.

```text
Found 705 test(s)
Ran 705 tests in 33.872s
OK (skipped=31)
```

Все 31 пропуск относятся только к PostgreSQL-only проверкам:

- 14 concurrency-тестов;
- 17 последовательных POST-smoke.

### 4.2. PostgreSQL 16.14

Использован только изолированный переносимый кластер:

```text
127.0.0.1:55432
C:\Users\swwba\AppData\Local\Temp\codex-pg16-qa-20260727-01
```

Кластер запускался с явными параметрами:

```text
-p 55432 -h 127.0.0.1
```

Порт `5432`, production и рабочая SQLite не использовались.

```text
Found 705 test(s)
Ran 705 tests in 93.312s
OK
```

Пропущенных тестов нет.

После проверки тестовая база удалена штатным Django runner, а переносимый
PostgreSQL остановлен через `pg_ctl stop -m fast -w`.

## 5. Статический и системный контроль

Независимо подтверждено:

```text
manage.py check
System check identified no issues (0 silenced).

manage.py makemigrations --check --dry-run
No changes detected

AST:
FILES=41
RETURN_BLOCK_FINDINGS=0

git diff --check
exit code 0
```

Первый локальный вариант AST-команды потребовал чтения файлов через
`utf-8-sig`, потому что один старый тестовый файл содержит BOM. После
корректного чтения аудит прошёл. Это не дефект приложения.

## 6. Контроль файлов исправлений

| Файл | SHA-256 |
|---|---|
| `СИСТЕМА_MVP/backend/trips/tests.py` | `A105139A4807C314F8E9F49223DED7A43A35EFB9C075F7516831497EDCFD194A` |
| `СИСТЕМА_MVP/backend/trips/test_chaos_p01_shift_downtime.py` | `ED4A4B188C3998959E8B572DDE956F98310001B532063906B822877EB002AAAF` |
| `ПРОГРЕСС_ПРОЕКТА/120_ОТЧЕТ_ОБ_ИСПРАВЛЕНИИ_QA_REG_P2_006_И_QA_REG_P3_007_27_07_2026.md` | `CDEAB2E8526F3EEE8E38124CEBCF7F7773E84574A54375C445F54DBA9CFB3EE5` |

Независимый review подтвердил, что в пакете документа 119 изменены только
два разрешённых тестовых участка. Блокеров нет.

## 7. Неизменность рабочих данных

Рабочая SQLite:

| Параметр | Значение |
|---|---:|
| размер | `2584576` |
| mtime UTC | `2026-07-23T17:11:26.0764680Z` |
| SHA-256 | `A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222` |

`private_media`:

| Параметр | Значение |
|---|---:|
| файлов | `78` |
| байт | `3042` |
| SHA-256 manifest | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |

Рабочие данные не изменились.

## 8. Browser и физический Android

Предыдущая независимая проверка документа 119 подтвердила для PWA
Машиниста:

- `1280 × 720` и `390 × 844`;
- отсутствие horizontal overflow;
- отсутствие ошибок console;
- настоящий long-press и начало штатного жеста возврата рейса.

Доступный автоматизированный in-app Browser не смог передать завершающий
`pointerup` захваченной карточке. Искусственный `fetch`, синтетический
JavaScript и подмена DOM не применялись.

Серверный POST `409`, точный русский текст и отсутствие побочных изменений
надёжно покрыты интеграционным тестом. Владелец ранее самостоятельно
проверил все PWA на физическом Android. Визуальный показ именно нового
stale-сообщения можно кратко повторить на устройстве, но это не
подтверждённый production-дефект и не основание продолжать цикл исправлений.

## 9. Границы готовности

Вердикт распространяется на уже готовые и введённые в систему контуры:

- Системный администратор;
- ОУП;
- Заместитель начальника участка в реализованной части;
- Диспетчер;
- Горный мастер;
- Водитель самосвала;
- Машинист экскаватора;
- Руководство;
- общие расстановки, смены, рейсы, простои, realtime, отчёты и Excel,
  относящиеся к этим ролям.

Вердикт не распространяется на ещё не спроектированные либо не завершённые
контуры Механика, Табельщика, Делопроизводителя, Охраны труда и будущих
отделов.

## 10. Следующий шаг

Текущий QA-цикл завершить. Новые гипотетические вариации без реального
воспроизведения не открывать как дефекты.

Следующее отдельное решение владельца:

1. провести полный Git-scope аудит большого локального пакета;
2. включить все обязательные неотслеживаемые production- и test-файлы;
3. не использовать один `git add -u`;
4. создать осмысленный commit;
5. после commit отдельно решить push;
6. после push отдельно решить deploy и границы ограниченного пилота.

До отдельного разрешения commit, push, deploy и production запрещены.

## 11. Состояние среды после приёмки

```text
listener 8000:  0
listener 5432:  0
listener 55432: 0
postgres processes: 0
```

Переносимый PostgreSQL сохранён остановленным вне проекта для возможной
повторной проверки.

Все изменения остаются только локальными. Commit, push и deploy не
выполнялись. Production не затрагивался.
