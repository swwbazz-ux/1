# Перевод самосвалов между экскаваторами — 12.09.2026

## Решение

- Сервер хранит один срок перехода назначения в `HaulAssignment.effective_at`; штатный срок — 5 минут, для `ТЕСТ-1` сохранён нулевой стендовый срок.
- До серверного завершения старый машинист видит затухающую карточку «Перевод на …», новый — проявляющуюся карточку «Перевод с …». Перезагрузка не меняет срок.
- Старый машинист не имеет права создавать новый рейс. Новый может до срока выполнить существующее удержание; только успешное атомарное создание рейса досрочно завершает перевод.
- Ноль экранного таймера не меняет права. Клиент запрашивает серверное состояние и ждёт подтверждённый фрагмент.
- Повторное переназначение отменяет прежнюю ожидающую цель и уведомляет все затронутые экскаваторы. Ошибка рейса откатывает и рейс, и перевод.
- История прежних отгрузок, ручное управление пассивными самосвалами, блокирующие простои, единственный открытый рейс и индикатор связи водителя сохраняют самостоятельную семантику.

## Независимый срок карточки ручной отправки у точки разгрузки

- Для рейса, созданного ручным управлением пассивным самосвалом (`driver_participation_recorded=True`, `driver_control_shift IS NULL`), badge у точки разгрузки показывается 5 минут от серверного `Trip.created_at`.
- По истечении срока скрывается только badge конкретного рейса. Статус рейса, разгрузка, время, объёмы, счётчики и основная карточка самосвала не меняются.
- Повторная отправка получает новый `trip_id` и новый срок; обработчик старого badge не может удалить новую карточку того же самосвала.
- Подключение водителя и последующее переназначение не меняют сохранённый признак ручного рейса. Этот срок не использует таймер или модель перевода назначения.

## Проверки перед публикацией

- Целевые серверные сценарии перевода: 12/12.
- Полный набор ручного управления, включая срок badge: 21/21.
- JavaScript захвата, анимации перевода и точного удаления badge: 14/14.
- `manage.py check`, `makemigrations --check --dry-run`: успешно.
- Browser QA на изолированной SQLite QA-базе: оба направления перевода, серверное завершение, обычный и reduced-motion режимы, `390×844` и `1280×900`, отсутствие горизонтального переполнения, скрытие badge при сохранении основной карточки.
- В объединённой фактической production-базе остаются 13 ранее несовместимых тестовых ожиданий соседних контуров (3 горного мастера, 10 диспетчера/старого UI). Целевые тесты выпуска проходят; эти ожидания должны быть отдельно синхронизированы с уже действующим production UI.

## Состояние публикации

Код опубликован в ветке `codex/truck-excavator-transfer-2026-09-12`, основной
release-коммит — `ab1bed75e4f2d52a059404dbbcd584b1cd62bb18`. 12.09.2026
изменение адресно выложено на `driverform.ru` поверх повторно снятых live-файлов.
Production shell Экскаваторщика — `excavator-mobile-shell-v229`.

Перед заменой hash guard подтвердил точные восемь исходных runtime-файлов.
Создан rollback-каталог
`/srv/accounting-mvp/tmp/backup-truck-transfer-v229-20260912-162205` и проверенный
PostgreSQL dump `database.dump`. Сохранены соседние production-маркеры:
Dispatcher `v104`, `STATIC_ASSET_RELEASE=v157`, обе функции отчёта карточки
Диспетчера и существующий контур ручного управления.

Production-история миграций отличается от локального объединённого графа:
на сервере уже применены `shifts.0019_employeeshift_service_close_kind` и
`trips.0011_manual_loading_participation`. Конфликтующие локальные merge-файлы
не переносились; `migrate --plan` до и после deploy показывает отсутствие
операций. Выполнены `manage.py check`, `migrate --noinput`, `collectstatic`,
`nginx -t` и restart `accounting-mvp`; публичный service worker и CSS подтверждены.

Отдельный PostgreSQL race-test в временной базе выполнил два одновременных
запроса нового машиниста: получены ровно один `200`, один `409`, один открытый
рейс и одно разрешение перехода. Временные базы удалены, production-рейсы для
проверки не создавались.
