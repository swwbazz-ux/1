# Алгоритмы и пользовательские сценарии

Версия: 0.1

## 1. Основной процесс

1. Уполномоченный источник утверждает состав и период заезда.
2. Система фиксирует неизменяемую версию cohort.
3. Делопроизводитель открывает авторосселение и выбирает утверждённый cohort.
4. Система создаёт новый `AutoSettlementRun` и рассчитывает строки.
5. Делопроизводитель видит итог, причины конфликтов и изменения относительно текущего проживания.
6. Делопроизводитель разбирает конфликтные строки вручную либо оставляет их нерасселёнными.
7. Делопроизводитель явно подтверждает Apply безопасных строк.
8. Система повторно проверяет входы под блокировками и применяет safe rows одной транзакцией.
9. Итог и все созданные записи связываются с run.

## 2. Алгоритм preview

### 2.1. Подготовка

1. Проверить, что cohort имеет статус `APPROVED`.
2. Проверить конечный корректный период каждой строки.
3. Загрузить официальные кадровые признаки.
4. Загрузить room profiles, transfer status, anchor-bed assignments и resolver version.
5. Загрузить существующие occupancies целевого периода.
6. Загрузить постоянные bindings.
7. Загрузить только доказанные baseline-производственные назначения для сотрудников без binding.

### 2.2. Обработка сотрудника

Порядок строгий:

1. Если сотрудник уже имеет допустимую occupancy на весь требуемый период — `KEEP`.
2. Если существующая occupancy конфликтует с обязательными правилами — `CONFLICT/existing_occupancy_invalid`.
3. Если есть подтверждённый binding:
   - разрешить calendar slot → anchor → bed;
   - проверить доступность и совместимость;
   - вернуть `SETTLE` либо `CONFLICT`;
   - текущее оперативное EquipmentAssignment не меняет этот выбор.
4. Если binding отсутствует и есть доказанное baseline-назначение на тяжёлую технику:
   - найти два атомарных anchor техники;
   - выбрать свободный calendar slot детерминированно;
   - проверить пару `An+Bn`, room profile и 2+1;
   - вернуть `SETTLE_NEW_BINDING` либо `CONFLICT`.
5. Если сотрудник относится к non-equipment категории:
   - вызвать соответствующий versioned resolver;
   - вернуть `SETTLE_NEW_BINDING`, `SETTLE` либо `CONFLICT`.
6. Если обязательных данных или resolver нет — `CONFLICT`, без fallback-угадывания.

### 2.3. Проверка результата как единого набора

После индивидуального выбора проверяются:

- конкуренция строк run за одну койку;
- конкуренция строк за один calendar slot;
- пересечения сотрудников;
- room-level пол, режим, организация и protected-политика;
- полная схема трёх пар и 2+1 для технической комнаты;
- вместимость;
- интервалы чередующихся групп.

Конфликт одной строки не должен скрывать другие причины. Строка содержит полный набор применимых reason codes.

## 3. Детерминированность

Одинаковые входы и одинаковая версия правил должны давать одинаковый результат.

Порядок выбора:

1. существующая occupancy;
2. существующий binding;
3. стабильный anchor, уже использовавшийся сотрудником;
4. явно заданный приоритет resolver;
5. room/building/floor/room/bed stable order;
6. stable employee ID для разрешения равных кандидатов.

Случайный выбор запрещён.

## 4. Reason codes

Минимальный каталог:

| Код | Смысл |
|---|---|
| `cohort_not_approved` | Состав не утверждён |
| `cohort_member_period_invalid` | Некорректен период сотрудника |
| `required_hr_fact_missing` | Нет обязательного кадрового признака |
| `rotation_group_unknown` | Не определена чередующаяся вахта |
| `schedule_regime_unknown` | Не определён ранний/стандартный режим |
| `permanent_assignment_ambiguous` | Нельзя отличить постоянное назначение от временного |
| `multiple_active_bindings` | Несколько действующих постоянных закреплений |
| `equipment_anchor_pair_missing` | У техники нет двух атомарных мест |
| `anchor_bed_assignment_missing` | Нет подтверждённой связи anchor с койкой |
| `room_not_transferred` | Комната недоступна |
| `bed_not_transferred` | Койка недоступна |
| `bed_overlap` | Койка занята в пересекающийся период |
| `employee_overlap` | Сотрудник уже занимает другую койку |
| `gender_conflict` | Конфликт пола комнаты |
| `schedule_regime_conflict` | Ранний/стандартный режим смешиваются |
| `organization_conflict` | Недопустимое смешение организаций |
| `protected_place_forbidden` | Нет права на защищённое место |
| `reserve_place_forbidden` | Нет права на резервное место |
| `room_2plus1_conflict` | Не выполняется схема 2+1 |
| `resolver_not_configured` | Нет правила категории |
| `existing_occupancy_invalid` | Текущее размещение нарушает hard rule |
| `preview_stale` | Входы изменились после расчёта |

Коды являются стабильным API. Текстовое описание может локализоваться и уточняться.

## 5. Apply

Вход команды:

- `run_id`;
- явное подтверждение;
- `idempotency_key`;
- ожидаемый `input_hash`/version.

Алгоритм:

1. Начать транзакцию.
2. Заблокировать run.
3. Если этот idempotency key уже применён — вернуть сохранённый результат.
4. Проверить допустимый статус run.
5. Заблокировать входные сущности в стабильном порядке.
6. Перечитать cohort, transfer status, profiles, bindings, anchor-bed и occupancies.
7. Пересчитать input hash.
8. При несовпадении отметить `STALE`, откатить запись результата заселения и вернуть новый preview-required ответ.
9. Для каждой safe row выполнить общий COMMIT validator.
10. Если любая safe row больше не безопасна — ничего не применять, run становится `STALE` или `FAILED_VALIDATION` согласно техническому контракту.
11. Создать новые bindings только для `SETTLE_NEW_BINDING`.
12. Создать конечные occupancies для `SETTLE` и `SETTLE_NEW_BINDING`.
13. Для `KEEP` не создавать дубликат, а связать строку с существующей occupancy.
14. Сохранить audit/result и статус `APPLIED`.
15. Зафиксировать транзакцию.

## 6. Ручные операции

### 6.1. Ручное заселение без изменения постоянного места

Используется для сотрудника без текущей occupancy, когда место выбрано человеком. Требует конечный период, источник и COMMIT validator. Если выбранная койка не совпадает с binding, операция должна быть явно оформлена как временное исключение либо постоянная коррекция.

### 6.2. Временное переселение

- обязательны начало, конец и причина;
- постоянный binding не меняется;
- создаётся/заменяется фактическая occupancy на период исключения;
- после окончания следующий заезд снова разрешается через постоянный binding;
- чужая постоянная койка может использоваться только при доказанной недоступности/отсутствии владельца на весь период.

### 6.3. Постоянная коррекция

- обычный drag-and-drop не должен незаметно выполнять её;
- пользователь выбирает «изменить постоянное место»;
- требуется повышенное подтверждение и основание;
- старый binding закрывается без удаления;
- создаётся новый confirmed binding, ссылающийся на предыдущий;
- фактическая occupancy текущего периода синхронизируется отдельной частью команды.

### 6.4. Досрочный выезд

Первоначальный `ends_at` не переписывается. Фактическое прекращение фиксируется `terminated_at` и событием аудита.

## 7. Изменение входов после preview

Следующие изменения делают run устаревшим:

- новая версия cohort;
- изменение arrival/departure;
- изменение transfer status затронутой комнаты/койки;
- новая occupancy или binding на затронутом интервале;
- изменение anchor-bed assignment;
- изменение room profile;
- изменение baseline assignment;
- новая версия resolver/rules.

UI должен показывать, что расчёт устарел, и предлагать пересчитать, а не применять старые строки.

## 8. UI-контракт

Preview показывает:

- период и версию состава;
- число `KEEP`, `SETTLE`, `SETTLE_NEW_BINDING`, `CONFLICT`;
- сотрудника, источник маршрута и назначенную койку;
- отличие от текущего факта;
- все причины конфликта;
- отметку существующего ручного исключения;
- актуальность run;
- кнопку Apply только при наличии safe rows и актуальном run.

Непереданные комнаты остаются видимыми на карте, но неактивными. UI-ограничение всегда дублируется серверным guard.
