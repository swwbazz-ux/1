# Дорожная карта разработки и критерии приёмки

Версия: 0.6
Принцип: каждый этап отдельно реализуется, отдельно проверяется и только после приёмки становится основанием следующего.

## 0. Сверка фактического HEAD

Цель: заполнить доказательную карту текущей реализации, не меняя код.

Результаты:

- точный HEAD и рабочее дерево;
- карта моделей, сервисов, API, UI, миграций и тестов;
- фактическая трассировка preview и ручных writers;
- подтверждение либо опровержение заявленного состояния;
- таблица `READY/PARTIAL/ABSENT/CONTRADICTED` против целевой архитектуры.

Статус: завершено 13.08.2026. Подтверждён HEAD `4df3c62`; заполнен `07_ТЕКУЩЕЕ_СОСТОЯНИЕ_HEAD.md`.

Приёмка: каждый вывод имеет `file:line`, SHA или команду теста. Архитектурные предложения Codex не смешаны с фактами.

## 0A. Нормализация baseline

Цель: не потерять принятую заказчиком v29 и начать доменные изменения из чистого, воспроизводимого состояния.

Статус: завершено 13.08.2026. Создан локальный commit `e71dc4e0b7ca26c0402e6e2ac990c8cae5fd1d1b`; worktree чистый; 272/272 settlement tests PASS. Push и deploy не выполнялись.

Результаты:

- точечный обзор пяти содержательно изменённых settlement-файлов;
- подтверждение соответствия принятой UI-итерации v29;
- сохранение оформления admin-console button и пяти settlement-summary карточек;
- исключение room-transfer legend и сохранение числового количества переданных комнат;
- отделение шести LF/CRLF-only файлов от содержательных изменений;
- отдельная проверка settlement tests;
- фиксация принятого UI-diff отдельным commit только после прямого разрешения заказчика;
- чистый worktree перед архитектурной миграцией.

Приёмка:

- ни одно пользовательское изменение не потеряно;
- в diff нет LF/CRLF-шума;
- cache-buster и UI соответствуют принятой v29;
- 272 settlement tests проходят;
- commit/push/deploy не объединяются в одну операцию без отдельного разрешения.

## 0B. Восстановление и фиксация проектной документации

Цель: до программирования собрать единый непротиворечивый комплект требований, архитектуры, алгоритмов, миграции и приёмки.

Статус: завершено 14.08.2026 commit `45de5d068f3b8c7f971e6065b974e4857e872f76`. Версия 0.3 была зафиксирована локальным commit `aba37c44e39b6f6f3bc0ab2caa51d9dae4c4ed9c`; проверен архив первичных источников SHA-256 `b9d89fb04d039e5a717f3418e64765808e286dc71d587a94b4f5f621297a8442`; последние решения заказчика перенесены в десять документов версии 0.4 и сквозно сверены. Версия 0.4 независимо проверена, побайтово принята и зафиксирована в репозитории как документационный baseline.

Приёмка:

- каждое открытое бизнес-решение сначала проверено по репозиторию, истории и evidence bundle;
- подтверждённые решения отражены во всех связанных документах, а не только в журнале;
- требования, модель, алгоритм, roadmap, миграция и трассируемость не противоречат друг другу;
- рабочие данные, которых пока нет, перечислены как входы внедрения и не подменены догадками;
- Codex получает только зафиксированную версию после отдельной сквозной проверки.

## 1. Стабилизация существующего фундамента

Зависит от этапа 0.

Цель: сохранить рабочую карту и ручной контур, закрыть доказанные bypass и временные противоречия до расширения модели.

Статус: в работе для оставшихся общих DB-level interval guarantees, полного единого COMMIT validator и production-приёмки. В commits `b8b54cbe`, `4e88278e`, `1e701208` и `f7719c7d` устранены PostgreSQL employee-lock defect, публичные массовые ORM-обходы anchor-bed/occupancy и deadlock ручных writers. В историческом implementation baseline `902e3886` локально существовали только schema control lease/event, migration `0007`, внутренний lifecycle и административный takeover; последующие этапы подключили write gate, HTTP/session lifecycle, UI и M8, включая раздельное применение смен. Ни одна локальная settlement migration этим документом не объявляется применённой к production.

Состав этапа:

- единый предикат `[starts_at, effective_end)`;
- корректная семантика `terminated_at`;
- защита подтверждённых source/revision/anchor-bed;
- запрет обхода writers через update/bulk/delete;
- единый COMMIT validator для manual settle/relocate;
- конкурентные тесты PostgreSQL.
- singleton `SettlementControlLease`, fencing revision и атомарные acquire/heartbeat/release;
- административный takeover с обязательной причиной и аудитом;
- сериализация всех ручных writers и будущего Apply на control row до доменных блокировок.
- разделение единственной открытой бизнес-смены делопроизводителя (`EmployeeShift` + `WatchPeriod`) и технического browser lease; отдельный follow-up — проверка этой смены при acquire.

Приёмка:

- существующая карта и DnD не сломаны;
- непереданный фонд серверно недоступен;
- два параллельных заселения не создают overlap;
- все разрешённые writers проходят один COMMIT validator;
- тесты воспроизводимы на целевой СУБД.
- две сессии не получают право записи одновременно;
- параллельные запросы одной владеющей сессии сериализуются;
- административный takeover инвалидирует старую revision без частичных записей и без обхода hard constraints;
- закрытие браузера и потеря сети восстанавливаются через конечный lease timeout, а не через недоказанное событие клиента.
- expiry lease не выдаёт бизнес-право второму делопроизводителю, пока предыдущая смена не закрыта; этот precondition считается принятым, но ещё не доказан текущим acquire-кодом.

## 2. Постоянная жилищная структура техники

Цель: одна тяжёлая машина получает два атомарных места, не нарушая «один anchor — одна койка».

Результаты:

- `group_key/slot_ordinal` либо эквивалентная модель пары;
- возможность двух active equipment anchors;
- миграция существующих anchor без потери данных;
- валидация пары `An+Bn` в одной комнате;
- room-level представление трёх пар.

Приёмка:

- у каждой включённой тяжёлой машины ровно два подтверждённых атомарных места;
- места связаны с разными койками `An` и `Bn` одной пары;
- один anchor не превращён в пул коек;
- старые записи однозначно промигрированы либо попали в отчёт ручной сверки.

## 3. Календарные слоты и постоянное закрепление

Цель: представить двух чередующихся владельцев одного атомарного места и отделить право от фактического проживания.

Статус M4: `COMPLETE`. Calendar slots/binding реализованы migrations `0008` и `0011`; migration `0011` заменила прямой subject-FK `Employee` на `SettlementResident` без изменения actor/audit FK на `Employee`.

Результаты:

- `AccommodationAnchorCalendarSlot` как конкретный экземпляр `anchor + WatchComposition + WatchPeriod` с identity `(anchor, watch_period)`;
- включительные границы slot, точно равные `WatchPeriod.starts_on/ends_on`, и stale detection без молчаливого переписывания;
- `EmployeeAccommodationBinding` с subject `resident` → `SettlementResident` (`PROTECT`);
- команды создания, закрытия и постоянной коррекции binding;
- immutable source/revision/audit;
- ограничения пересекающихся binding по resident identity.

Приёмка:

- временная occupancy не меняет binding;
- DAY/NIGHT не является ключом постоянной койки;
- две чередующиеся группы могут использовать одну койку при доказанном календарном непересечении;
- пересекающиеся подтверждённые slots одного физического места запрещены независимо от состава/смены;
- новый WatchPeriod создаёт новый slot; повторяемое правило остаётся в M5/M6;
- неизвестная группа блокирует совместное использование.

## 4. Авторитетный состав заезда

Цель: preview получает точный scope и период всех прибывающих.

Статус M5: `COMPLETE`. Authoritative cohort реализован migrations `0009` и `0011`; `SettlementCohortMember` использует subject `resident` → `SettlementResident`, а uniqueness и overlapping APPROVED memberships проверяются по resident identity.

Результаты:

- сохраняемые `SettlementCohort`/`Member` как жилищная проекция официальных `WatchComposition`, `WatchPeriod`, ротационных решений, продлений и подтверждённых поправок;
- версии и статусы утверждения;
- прибытие/выбытие, продления, незаезды и дополнительные сотрудники;
- принадлежность к календарной группе;
- snapshot производственного контекста.

Приёмка:

- в scope входят техника, ИТР, службы и остальные ожидаемые категории;
- EquipmentAssignment не является единственным источником состава;
- изменение состава создаёт новую версию и делает старый preview устаревшим.

## 5. Первичное закрепление и защита от оперативных перестановок

Цель: исключить смену койки из-за оперативной перестановки.

Результаты:

- обычный опубликованный `CrewPlan`/`EquipmentAssignment` используется как основание только при отсутствии binding;
- снимок основания сохраняется в binding и run;
- существующий binding имеет приоритет над всеми последующими оперативными назначениями;
- постоянное изменение выполняется отдельной подтверждённой командой.

Приёмка:

- временная перестановка EquipmentAssignment не меняет существующую постоянную койку;
- новый сотрудник получает binding только из утверждённого основания;
- отдельный «постоянный CrewPlan» и дополнительное жилищное утверждение заместителем не требуются;
- неоднозначное или отсутствующее первичное основание даёт conflict.

## 6. Room profiles, hard constraints и soft preferences

Перед M6 вводится authoritative resident prerequisite:

- общий `SettlementResident` для внутреннего `Employee` и внешней карточки;
- внешние типы `CONTRACTOR/BUSINESS_TRIP/EXTERNAL_OTHER` без login и `EmployeeAccess`;
- нормализованная текстовая организация внешнего жильца;
- control-gated create/update/archive/reactivate с optimistic revision;
- завершённый migration `0011` перевод M4/M5 с прямого subject-FK `Employee` на resident identity; actor/audit остаются `Employee`.

Статус: минимальное безопасное read-only ядро M6 для MVP — `COMPLETE`; persistence и legacy preview не изменены. Resolver обрабатывает волны `CONFIRMED binding → EquipmentAssignment → PersonnelPosition → controlled unresolved`, возвращает immutable deterministic result/fingerprint и не выполняет ORM writes. Equipment wave использует тот же официальный interval/status contract, что production preview, и требует однозначную цепочку `assignment → active equipment → active equipment anchor → effective CONFIRMED bed assignment → CONFIRMED slot текущего WatchPeriod → bed`. Binding имеет приоритет над equipment route, equipment route — над position route; DAY/NIGHT не создаёт отдельную identity койки; неоднозначность и конкуренция за одну bed завершаются controlled conflict.

Persistent versioned RoomUseProfile/ResolverRule, специальные protected/reserve/service, 2+1, EARLY/STANDARD/SPECIAL и trainee-specific routing без конфигурации остаются `resolver_not_configured`. Реализованный M7 сохраняет таких residents в immutable списке unresolved с reason codes. Сменная идентичность состава, плана и фактического проживания, раздельное применение и ручной контур реализованы позднее миграциями `0015` и `0016`. UI внешних карточек и ручного внешнего заселения, точечные исправления будущего плана и trainee adapter не реализованы.

Цель: централизовать пол, режим, организацию, protected/reserve и 2+1.

Результаты:

- периодический room profile;
- официальные источники пола и графика внутреннего Employee, а также тип и организация внешнего SettlementResident;
- единый PRECHECK/COMMIT набор hard rules;
- отдельный ranking мягких предпочтений по компактности, группам и графику;
- reason codes.

Приёмка:

- ни один неизвестный обязательный факт не заменяется fallback;
- ранняя/стандартная смены и мужчины/женщины не смешиваются;
- protected/reserve права проверяются сервером;
- 2+1 проверяется на уровне комнаты и периода.
- внутренние и внешние resident никогда не смешиваются в комнате;
- для внешних resident одинаковая организация является мягким предпочтением, но не запрещает совместимое смешение разных внешних организаций при нехватке места; сравнение выполняется через `trim` + case-insensitive exact match.
- мягкое предпочтение никогда не нарушает hard rule и не создаёт отказ само по себе.

## 7. Resolver-правила non-equipment

Цель: обеспечить управляемое расселение всех категорий.

Волны конфигурации:

1. тяжёлая техника по типам, включая бульдозеры, грейдеры и прочую технику;
2. постоянная функциональная группа помощников машиниста экскаватора;
3. лёгкий транспорт и «Газели»;
4. отдельные вахтовки/автобусы;
5. механическая служба вместе с водителем тягача;
6. явная категория резерва;
7. стажёры как временный структурированный статус внутреннего Employee с маршрутом по его настоящей должности, без Vacancy и отдельного пула;
8. женщины как общий фонд с мягкой оптимизацией по службам и графику;
9. мужские ИТР и службы, включая совместимую группу АСУ ГТК с ОТ/ПБ/БДД;
10. диспетчеры, горные мастера и оперативное руководство;
11. protected-фонд и подрядчики;
12. остальные доказанные должности и группы.

Приёмка каждой волны:

- официальный predicate;
- versioned rule;
- eligible pool;
- hard constraints;
- deterministic ranking;
- позитивные, негативные и граничные тесты.

До настройки конкретной волны система возвращает `resolver_not_configured`.

## 7A. Сверка рабочей конфигурации

До включения resolver-волны загружаются и сверяются:

- действующий реестр техники, её типы и количество; отдельно подтверждается фактическое число грейдеров;
- актуальная карта `RoomUseProfile`, включая protected, contractor, gender и schedule policies;
- официальные должности, службы, пол и `WatchComposition` внутренних Employee, а также типы и организации внешних SettlementResident;
- постоянные и временные назначения явных категорий;
- authoritative структурированный trainee state действующих стажёров либо отдельный явно спроектированный adapter;
- матрица совместимости групп и владельцы разрешений.

Приёмка: в коде нет констант «шесть бульдозеров», «две/три грейдера», фамилий или номеров специальных комнат; из одних и тех же входных данных строится воспроизводимый отчёт конфигурации.

## 8. Сохраняемый preview

Статус: `COMPLETE` для сохранения и подтверждения preview; Apply и UI не входят в M7.

Реализованы `SettlementPreviewRun`, `SettlementPreviewPlacement` и `SettlementPreviewUnresolved`, lifecycle `DRAFT → CONFIRMED → SUPERSEDED`, immutable provenance, resolver/normalized fingerprints, exact server-side `SettlementControlWriteContext`, повторный M6 при подтверждении, stale-source detection и controlled concurrent confirmation. На один `WatchPeriod` допускается один `CONFIRMED` run; unresolved residents сохраняются с reason codes и не запрещают confirmation.

Результаты:

- versioned run и отдельные immutable placement/unresolved rows;
- resolver fingerprint, normalized result fingerprint и source snapshot;
- reason codes и normalized provenance;
- детерминированный расчёт и повторная проверка M6 при confirmation;
- read-only stale helper без автоматического изменения status;
- schema-only migration `settlement.0012_m7_saved_previews` без backfill.

Приёмка:

- preview не пишет occupancy/binding;
- одинаковые входы дают одинаковые строки;
- каждый active cohort member представлен ровно один раз как placement либо unresolved;
- изменение источника блокирует confirmation как stale, не переписывая run;
- два конкурентных confirmation одного baseline дают одного победителя и controlled conflict проигравшему;
- подтверждение не меняет M4/M5, residents, rooms, beds или anchors;
- наличие unresolved не запрещает confirmation.

## 8A. Resident occupancy subject transition

Статус: `COMPLETE`. Migration `settlement.0013_resident_occupancy_subject` перевела фактическое проживание на обязательный `SettlementResident`, сохранив legacy-имя `EmployeeBedOccupancy` и Employee actor/audit-поля.

Реализованы canonical команды `settle_resident_on_bed`, `relocate_resident_to_bed`, `release_resident_from_bed`; внутренний HTTP UI продолжает работать через Employee-адаптеры без скрытого создания resident. External resident поддержан domain API без Employee/Access/Role/PIN. Internal sex читается только из Employee, external sex обязателен; unknown/incompatible sex fail-closed, изменение external sex входит в M6 fingerprint и stale-проверку M7.

Forward migration переиспользует/создаёт internal wrappers для существующих Employee occupancy без EmployeeAccess. Existing external resident без authoritative sex блокирует forward, external occupancy блокирует reverse; данные не удаляются и фиктивный Employee не создаётся.

Не входили в завершённый этап `0013`: применение плана и сменная идентичность реализованы последующими M8, `0015` и `0016`; UI внешней карточки и ручного внешнего заселения по-прежнему отсутствует.

## 9. Транзакционное применение плана

Статус серверного контура и интерфейса: `COMPLETE`, включая независимое применение ночной и дневной смены.

Цель: безопасно применить показанный результат.

Результаты:

- canonical API `apply_confirmed_settlement_preview(*, run_id, work_shift, control_context, confirm_replace_manual=False)`;
- первый Apply создаёт AUTO baseline только целевого WatchPeriod;
- штатные MANUAL-корректировки доступны после первого Apply;
- повторный Apply заменяет только доказанный baseline текущего cohort/WatchPeriod; MANUAL требует `confirm_replace_manual=True`;
- неизменившаяся AUTO occupancy переиспользуется как `REUSED` без изменения timestamps;
- стабильный lock order;
- повторная проверка M6/M7 fingerprints, immutable rows и replaceable baseline;
- общий COMMIT validator;
- отдельная транзакция и отдельный `SettlementPreviewApplication` для каждой смены одного утверждённого плана;
- immutable `SettlementPreviewApplication/ApplicationItem`, exact Access и связь AUTO occupancy с run/placement.

Приёмка:

- stale run ничего не пишет;
- повтор Apply одного run не создаёт дубликаты;
- параллельный Apply безопасен;
- конфликт при повторной проверке откатывает весь safe batch;
- заранее конфликтные строки не применяются и остаются ручными;
- unchanged AUTO occupancy не дублируется и получает `REUSED` application item;
- relocation, swap и переход resident в unresolved корректно заменяют только текущий baseline;
- MANUAL без явного подтверждения даёт controlled отказ, с подтверждением сохраняется в истории;
- unrelated resident, другой WatchPeriod и неоднозначная provenance остаются hard conflict;
- изменение replaceable baseline делает M7 preview stale, а порядок ORM не меняет fingerprint.

Миграция `settlement.0015_cohort_member_work_shift` добавляет официальную смену участника и её происхождение без угадывания исторических данных. Миграция `settlement.0016_shift_scoped_apply_and_occupancy` добавляет сменную идентичность применению и фактическому проживанию; обратный переход после появления сменных данных завершается fail-closed.

Старый серверный маршрут применения всего плана сохранён только как управляемый отказ HTTP 409 с кодом `settlement.apply.shift_split_required`. Точечные исправления и отдельный режим плановой карты завершены локально M9; следующий обязательный межмодульный этап — единый реестр заезда T1–T3.

### 9A. Следующий заезд и двухдневная пересменка

Статус: `COMPLETE` локально. Официальная смена, календарно защищённое применение по сменам, две команды интерфейса, сменная идентичность ручного проживания, M9 и отдельный режим плановой карты реализованы.

Результаты этапа:

- структурированная дневная или ночная смена каждого участника с состоянием «Требуется проверка» при отсутствии либо неоднозначности источника — `COMPLETE`;
- независимые идемпотентные применения ночной и дневной смены — `COMPLETE`;
- ночная смена разрешена не раньше `WatchPeriod.starts_on - 1 день`, дневная — не раньше `WatchPeriod.starts_on`; досрочное применение запрещено сервером — `COMPLETE`;
- две команды интерфейса и состояния «Ещё рано», «Готово к применению», «Применена» — `COMPLETE`;
- сменная идентичность ручного фактического проживания — `COMPLETE`;
- отдельный режим плановой карты и неизменяемая история точечных исправлений поверх `CONFIRMED` плана — `COMPLETE`; активная версия ресурсов карты — `settlement-map-v34`.

Приёмка:

- подготовка и подтверждение будущего плана не меняют текущее фактическое проживание;
- изменение одного resident не пересчитывает остальных;
- конфликт целевой койки отклоняется без частичной записи;
- ночное применение не затрагивает дневное фактическое проживание и наоборот;
- физическое пересечение вахт не создаёт двойное фактическое проживание и не продлевает прежнее право;
- список табельщика не подменяет официальную расстановку смен;
- дата прибытия в Хабаровск является датой заселения независимо от дальнейшей дороги до Малмыжа.

## 10. Ручные исключения и постоянная коррекция

Статус: `COMPLETE` локально для ручных операций после применения соответствующей смены и точечных исправлений ещё не применённого плана.

Цель: разделить точечные исправления ещё не применённого плана и фактические ручные операции после применения, не обходя архитектуру.

Результаты:

- новое ручное заселение только после применения соответствующей смены; период и смену определяет сервер;
- заселение принимает точного жильца, переселение и освобождение — точную запись фактического проживания;
- переселение исторической строки без смены запрещено, освобождение разрешено по точному PK;
- ручное происхождение и смена сохраняются, другая смена не затрагивается;
- отдельные точечные исправления будущего плана и UI их истории — `COMPLETE` локально в M9.

Приёмка:

- drag-and-drop не меняет постоянное право молча;
- временная коррекция заканчивается по периоду;
- постоянная коррекция сохраняет историю старого binding;
- чужое место используется только при доказанной доступности.
- точечные исправления будущего плана не создают MANUAL occupancy; после применения используются фактические ручные операции.

## T1–T3. Обязательный единый реестр заезда

Статус: T1.1–T1.3b — `COMPLETE` локально; T1.4, T2 и T3 — `NEXT`.

### T1.1–T1.3b. Реализованный фундамент и интерфейс

- T1.1: закрытое хранение Excel и SHA-256, безопасный разбор, исходные и нормализованные строки, вопросы и предварительное сопоставление без побочных бизнес-записей;
- T1.2: ручная проверка, исправления, основания, точный доступ табельщика и распределение блокирующих вопросов по ответственным ролям;
- T1.3a: новая версия из базы `Employee`, точный FK сотрудника без обязательного `SettlementResident`, неизменяемая история и отсутствие скрытого создания карточек;
- T1.3b: раздел «Рабочее место табельщика → Перевахта и состав заезда», поиск по всей кадровой базе, добавление существующей внешней карточки и идемпотентное массовое подтверждение.

### T1.4. Окончательное утверждение версии — NEXT

- повторно проверить фактическую готовность всех строк;
- запретить утверждение при блокирующих вопросах;
- заменить предыдущую подтверждённую версию только новой версией с сохранением истории;
- создать `SettlementSource` и `SettlementRevision` только из доказанного утверждённого результата;
- не создавать `SettlementCohort` на этом этапе.

### T2. Передача заместителю начальника участка — NEXT

- передать общий утверждённый список для официальной техники, роли и смены;
- сохранить связь опубликованной расстановки с точной версией строки;
- не менять состав реестра производственной публикацией.

### T3. Передача делопроизводителю — NEXT

- передать только готовый состав без нерешённых вопросов;
- создать черновой `SettlementCohort` с доказанным происхождением;
- не менять фактическое проживание автоматически.

Полноценный модуль табельщика остаётся шире реализованного раздела. Учёт рабочего времени, табели, заезды и выезды, продления, билеты и трансфер, документальное изменение смен, отсутствия и переработки, документы и отчёты остаются `NEXT`.

Приёмка T1.4–T3 блокирует расселение, если версия не подтверждена, есть несопоставленные строки или критические конфликты, обязательная расстановка не опубликована, официальная смена внутреннего жильца не определена либо обязательные сведения внешнего жильца не подтверждены.

## 11. Пилот и ввод в работу

1. Прогон на обезличенной копии реальных данных.
2. Сравнение результата с ручной контрольной картой.
3. Shadow mode: preview без Apply для одного заезда.
4. Разбор всех conflicts и корректировка правил, не данных алгоритмом.
5. Отдельная проверка сценария дефицита: система показывает руководителю группу и нехватку, не применяя скрытый приоритет.
6. Controlled Apply на тестовой БД.
7. Резервная копия и production pilot на одном утверждённом cohort.
8. Наблюдение и акт приёмки.

Production Apply запрещён до прохождения этапов 0–10 и явного решения заказчика.

## Матрица общей готовности

| Возможность | Готово, когда |
|---|---|
| Карта | Фонд совпадает с источником, недоступные места защищены сервером |
| Постоянная карта | Все используемые места имеют подтверждённую структуру и ревизию |
| Scope | Каждый прибывающий входит в утверждённый cohort с периодом |
| Preview | Сохраняем, повторяем, объясняем и не меняем фактические данные |
| Apply | Атомарен, идемпотентен, защищён от stale и конкурентности |
| Ручная работа | Различает временное исключение и постоянное изменение |
| Управление | Ровно одна session-bound lease владеет write-контуром; takeover атомарен, fenced и аудируем |
| Аудит | Любое размещение восстанавливается до источников и автора |
| Единый реестр заезда | Одна подтверждённая версия обслуживает все роли через фильтры; T1–T3 пройдены без копий и обходов |
