# Матрица разрывов и трассируемости

Версия: 0.5
Проверенный HEAD перед текущей документационной актуализацией: `7c21f3ec4248ad216e9cdb323ac207b0f3e0ffab`; migration leaf `settlement.0014_m8_apply_provenance`
Дата: 18.08.2026

## Правило использования

Каждая реализационная задача Codex должна ссылаться на один или несколько ID этой матрицы. Задача считается принятой только после выполнения указанных проверок. Статус меняется по доказательствам нового HEAD, а не по отчёту «сделано».

### Implementation checkpoint перед текущей документационной актуализацией

В baseline `902e3886` локально реализованы schema `SettlementControlLease/SettlementControlEvent`, migration `0007` с FREE singleton, внутренние acquire/heartbeat/release/expire, административный takeover, fencing/token/HMAC и application-level неизменяемый аудит. Эти primitives проверены на SQLite и PostgreSQL, но не подключены к manual settlement writers, HTTP/session или UI. Поэтому end-to-end требования TR-043–TR-047 не считаются закрытыми: write gate и общий порядок Employee/EmployeeAccess отсутствуют, а TR-048 остаётся полностью `ABSENT`. Migration `0007` не объявляется применённой к production.

После этого historical checkpoint migrations `0008/0009` реализовали M4 calendar binding и M5 authoritative cohort, `0010_settlement_residents` ввела общий `SettlementResident`, а migrations `0011/0013` завершили resident subject transition M4/M5/occupancy. Минимальное безопасное read-only ядро M6 для MVP завершено: binding → equipment assignment → position → controlled unresolved. Migration `0012_m7_saved_previews` завершила M7 persistence/confirmation. Backend M8 Apply и AUTO replacement реализованы migration `0014_m8_apply_provenance`, затем подключён единый HTTP/UI workflow. Не реализованы требования ADR-031, UI внешних карточек/ручного внешнего заселения, persistent RoomUseProfile/ResolverRule и trainee adapter.

## Матрица

| ID | Требование | Фактическое состояние или незакрытый runtime-разрыв | Статус полного требования | Требуемое изменение | Обязательная проверка |
|---|---|---|---|---|---|
| TR-001 | Все физические комнаты видимы, непереданные недоступны | v29 сохранён commit `e71dc4e0`; server guard присутствует | READY | Сохранять при следующих изменениях | UI contract + POST 409 |
| TR-002 | Одна Equipment имеет два atomic slots | Schema допускает N anchors; preview требует ровно один | CONTRADICTED | Использовать anchors ordinal 1/2 | Model/service tests |
| TR-003 | Пара техники соответствует `An+Bn` | Только физические A/B койки | ABSENT | Pair validator общей room/position и разных A/B | Позитивные и негативные pair tests |
| TR-004 | A/B не являются DAY/NIGHT | Preview использует shift в capacity key | CONTRADICTED | Удалить `(bed, shift)` как ёмкость | DAY/NIGHT flip test |
| TR-005 | Смена DAY/NIGHT не меняет постоянную койку | Resident binding не использует shift в identity; legacy preview ещё не подключён к нему | READY/PARTIAL | Подключить будущий preview без возврата `(bed, shift)` semantics | Shift-flip persistence test |
| TR-006 | Две месячные группы используют место непересекающе | Concrete WatchPeriod slots и calendar overlap guards реализованы M4 | READY | Сохранять slot identity/boundaries/stale guards | Alternating/non-overlap tests |
| TR-007 | Постоянное право отдельно от occupancy | `EmployeeAccommodationBinding` хранит право для `SettlementResident`; occupancy остаётся отдельным фактом | READY | Сохранять разделение binding/occupancy | Temporary override test |
| TR-008 | Factual occupancy имеет конечный интервал и resident subject | Migration `0013` перевела subject на `SettlementResident`; canonical writers применяют interval guards, но model сохраняет legacy open permanent/proposed | READY/PARTIAL | Новые AUTO occupancies только конечные; legacy reconciliation | Resident constraint/service tests |
| TR-009 | Полный утверждённый состав заезда | Versioned `SettlementCohort/Member` реализован M5; member subject переведён на resident migration `0011` | READY | Подключить будущий preview без изменения authoritative cohort | Version/stale/scope tests |
| TR-010 | Scope включает сотрудников без техники | M6 читает полный APPROVED cohort; M7 сохраняет каждого active member ровно как placement либо unresolved | READY/PARTIAL | Добавить конфигурацию специальных волн без потери M7 coverage | ITR/service/no-equipment tests |
| TR-011 | Первичный план создаёт binding один раз; последующие назначения не переселяют | Binding и immutable basis snapshot реализованы; текущий M8 применяет placement, но не расширяет контракт автоматического создания binding | READY/PARTIAL | Отдельно доказать initial-binding creation без изменения последующими EquipmentAssignment | Initial binding + temporary reassignment tests |
| TR-012 | Допустимая existing occupancy даёт KEEP | Совпадение молча проходит | PARTIAL | Explicit immutable KEEP row | Repeat-run test |
| TR-013 | Недопустимая occupancy не перемещается автоматически | Полного preview validator нет | PARTIAL | Conflict reason без relocate | Invalid-existing test |
| TR-014 | Пол и room sex restriction соблюдаются | Canonical resident writers, M6 и M8 Apply используют Employee.sex для internal и обязательный external_sex для external; unknown fail-closed | READY/PARTIAL | Расширять только через общий validator | Preview/apply/manual parity tests |
| TR-015 | Ранний и стандартный режимы раздельны | Room period regime отсутствует | ABSENT | RoomUseProfile на период, установленный уполномоченной ролью | Mixed-regime rejection |
| TR-016 | Техническая комната соблюдает 2+1 | Нет модели/алгоритма | ABSENT | Room-level orientation validator | 2+1, mirror, 3+0 tests |
| TR-017 | Категорийные и функциональные правила версионируются | Минимальный read-only resolver есть; persistent ResolverRule отсутствует | PARTIAL | Versioned resolver rules без хардкода фамилий/комнат | Category matrix tests |
| TR-018 | Неизвестный обязательный факт блокирует | M6 fail-closed возвращает controlled reason codes; M7 неизменяемо сохраняет unresolved и отклоняет stale confirmation | READY/PARTIAL | Сохранить fail-closed в будущих configured resolvers и Apply | Missing-fact tests |
| TR-019 | Bed overlap запрещён глобально | Только application writer; DB constraint нет | PARTIAL | PostgreSQL DB guarantee + writer | Concurrent DB test |
| TR-020 | Resident overlap запрещён глобально | Application writer проверяет `resident_id`; полная DB interval guarantee отсутствует | PARTIAL | PostgreSQL DB guarantee + writer | Concurrent DB test |
| TR-021 | Anchor-bed overlap нельзя обойти публичными bulk/update | Публичные update/bulk_create/bulk_update запрещены; private/raw и finite DB overlap остаются | READY/PARTIAL | Сохранить QuerySet guard; отдельно добавить PostgreSQL interval constraint | Bypass tests + DB overlap probe |
| TR-022 | Все writers используют полный COMMIT validator | Используются только SET-R033/R034 | PARTIAL | Единый rule registry в manual/apply | Parity contract tests |
| TR-023 | Preview сохраняется и воспроизводится | `SettlementPreviewRun` + immutable placement/unresolved rows, version и exact coverage реализованы M7; M8 читает только сохранённый run | READY | Подключить будущий UI без обхода сохранённого run | Persistence/determinism tests |
| TR-024 | Preview хранит input/rule version | Resolver fingerprint, normalized fingerprint и immutable source snapshot реализованы; confirmation и Apply повторно сверяют M6/baseline | READY | Сохранять stale contract в HTTP orchestration | Hash-change tests |
| TR-025 | Apply отклоняет stale input | M8 под locks повторно сверяет current CONFIRMED run, fingerprints, immutable rows и replaceable baseline | READY | Сохранить в HTTP endpoint | Stale rollback test |
| TR-026 | Safe rows применяются атомарно | M8 выполняет один resident-based batch; любой conflict/revalidation error откатывает occupancy и Application | READY | Повторить на PostgreSQL перед deploy | One-row-fails/all-rollback test |
| TR-027 | Apply идемпотентен | One-to-one run→Application; повтор одного run возвращает тот же result | READY | Не вводить обходной UI idempotency path | Replay test |
| TR-028 | Temporary override не меняет binding | Binding существует отдельно; M8 заменяет MANUAL только по явному confirmation и сохраняет историю | READY/PARTIAL | Доделать UI предупреждения/ручных операций | Override expiry test |
| TR-029 | Permanent correction — отдельная команда | M4 correction/supersede работает по resident identity и сохраняет историю | READY | Сохранять единый lock plan и provenance | History/audit test |
| TR-030 | Решения имеют actor/reason/source/revision/run | M8 Application хранит exact Access, run, fingerprints, actions/counts и MANUAL-confirmation; полная UI reason orchestration ещё отсутствует | READY/PARTIAL | Подключить HTTP/UI без потери provenance | Audit reconstruction test |
| TR-031 | Deadlock manual writers не выдаёт необработанный 500 | Единый Employee→Beds→Rooms→Occupancies; pairwise matrix без 40P01/55P03/500 | READY | Сохранять lock order; отдельный retry contract нужен только для иных DB failures | PostgreSQL concurrency + mutation test |
| TR-032 | Settlement-фундамент доказан на PostgreSQL | Последняя подтверждённая settlement suite после takeover: 332/332 PASS на PostgreSQL 14.24; это не полный тестовый набор всего проекта | READY | Повторять после каждого interval/concurrency изменения | Full settlement suite on PostgreSQL |
| TR-033 | Резерв назначается явно постоянно/временно | Явной category assignment модели нет | ABSENT | SettlementCategory + EmployeeSettlementCategoryAssignment с обязательным концом temporary | Assignment lifecycle tests |
| TR-034 | Стажёр следует настоящей должности Employee без отдельного пула | Структурированный trainee state/adapter ещё не найден; Vacancy исключена ADR-030 | ABSENT | Подключить существующий authoritative trainee state либо отдельно спроектировать adapter; JSON-fallback запрещён | State/no-state/promotion tests |
| TR-035 | Техника группируется по типу, три пары на комнату | Preview идёт по отдельной Equipment и одной bed | CONTRADICTED | Group-first allocator, An+Bn, совместимое заполнение неполной комнаты | 6 bulldozers, 2/3 graders, mixed-tail tests |
| TR-036 | Женщины размещаются общим фондом с мягкой оптимизацией графика | Preview не учитывает пол; manual проверяет только room restriction | ABSENT/PARTIAL | Gender-first route + soft ranking «2 day + night dispatcher» | Gender hard rule + soft fallback tests |
| TR-037 | Функциональные группы соответствуют решениям владельца | Resolver-каталога нет | ABSENT | Versioned mappings для assistants, transport, mechanical, ASU/HSE, ITR, operations | Per-group and temporary-transfer tests |
| TR-038 | Дефицит отображается группой без автоматического выбора человека | M6 возвращает equal-priority group как controlled unresolved, M7 сохраняет reason/details; отдельной persistent decision-модели нет | PARTIAL | Добавить immutable group-conflict/decision persistence без изменения M7 run | Whole-group shortage/decision/new-run tests |
| TR-039 | Protected не используется общим пулом; внутренние и внешние resident разделены | Authoritative внешняя карточка и текстовая организация реализованы; M6 уточняет одинаковую организацию как soft preference и допускает смешение разных внешних организаций при нехватке; room policy ещё не материализована | PARTIAL | Hard internal/external pool policy и versioned room profiles; внешняя организация ранжирует, но не запрещает | Protected scarcity, internal/external separation, external organization preference/fallback tests |
| TR-040 | Fixed fund используется компактно без пустых переменных пулов | Общего allocator нет | ABSENT | Soft compactness ranking; no trainee/unused category reservation | Capacity utilization tests |
| TR-041 | Рабочая конфигурация сверяется до включения resolver | Production DB и актуальные справочники не проверялись | UNKNOWN | Reconciliation report for equipment, rooms, HR, resident cards, watches, categories and trainee state | Deterministic config report + owner sign-off |
| TR-042 | Hard constraints отделены от soft preferences | Единого allocator/validator нет | ABSENT | Typed hard validation before ranking; soft score cannot permit or reject by itself | Hard-vs-soft precedence tests |
| TR-043 | Ровно один активный `SettlementControlLease` владеет settlement write-контуром | Singleton model, migration `0007`, FREE bootstrap и runtime ensure/acquire реализованы; HTTP/session и обязательное использование manual writers отсутствуют | ABSENT | Подключить существующий singleton к HTTP/session и общему write gate, сохранив текущий `owner_access` → `EmployeeAccess` | Two-session atomic acquire test |
| TR-044 | Heartbeat, expiry и безопасное освобождение после потери связи | Внутренние heartbeat, expiry и release реализованы и проверены; HTTP/session binding и browser heartbeat отсутствуют | ABSENT | Подключить существующий lifecycle к HTTP/session и browser heartbeat с expiry/reconnect/network-loss contract | Expiry/reconnect/network-loss tests |
| TR-045 | Административный takeover требует подтверждения и обязательной причины | Внутренний atomic takeover с обязательной причиной, новым token/revision и audit event реализован; admin endpoint, writer fencing и UI modal отсутствуют | CONTRADICTED | Подключить существующий takeover к admin endpoint, общему write gate и подтверждаемому UI modal | In-flight writer/takeover/old-session 409 tests |
| TR-046 | Все writers проходят транзакционный gate по `lease_token` и `fencing_revision`; запросы одной сессии сериализуются | Lease/token/fencing primitives реализованы; общий control lock, Employee/EmployeeAccess alignment и wiring writers отсутствуют | ABSENT | Control row первым; неполный plan отклоняется fail-closed, иначе заранее полный Employee-set по PK → полный EmployeeAccess-set по PK с `OF self` → revalidation и запрет позднего Employee-lock → Beds → Rooms → Occupancies | Old-token/no-partial-write + complete-plan/order/revalidation/joined-lock + same-session concurrency tests |
| TR-047 | События управления полностью аудируются | `SettlementControlEvent`, четыре event type, append-only application guard и атомарные lifecycle/takeover events реализованы; end-to-end HTTP/write-gate/UI integration отсутствует | ABSENT | Сохранить существующий history store и подключить к нему полный HTTP/write-gate/UI lifecycle без обходных журналов | Transition/audit reconstruction tests |
| TR-048 | UI и read-only режим расширяют существующую карту v29 без переписывания | Control UI v30 реализован для внутреннего Employee-контура; UI внешней карточки и ручного внешнего заселения отсутствует | READY/PARTIAL | Подключить внешний UI без обхода lease/canonical API | UI contract + server write-gate tests |
| TR-049 | Общий resident subject различает Employee и внешнего жильца | Migrations `0010/0011/0013`: M4, M5 и occupancy используют resident; internal связан с Employee, external не имеет Employee/Access/Role/PIN и поддержан canonical occupancy API | READY | Сохранять resident identity; отдельно остаётся внешний UI | Resident identity/sex/occupancy + FK forward/reverse tests |
| TR-050 | Плановая карта следующего заезда не изменяет фактическое проживание | M7 хранит план отдельно, но отдельного режима карты и планового права ещё нет | PARTIAL | Разделить «Текущую вахту» и «Следующий заезд»; до сменного применения occupancy не писать | No-write planning + two-mode UI tests |
| TR-051 | Каждый участник имеет authoritative DAY/NIGHT | Equipment provenance содержит сменный контекст не для всех resolver routes; обязательной нормализованной смены M5/M7 нет | ABSENT | Official assignment для internal, явный ввод для external, сверка табельщика и «Требуется проверка» | Missing/ambiguous/conflicting shift tests |
| TR-052 | Ночная и дневная смены применяются независимо и не раньше даты | Текущий M8 применяет весь run одной OneToOne Application | CONTRADICTED | Shift-scoped Application: ночь с 13-го, день с 14-го, без override | Date gate + night/day isolation + idempotency tests |
| TR-053 | Точечное исправление не переписывает `CONFIRMED` plan | Отдельной correction-сущности нет; immutable M7 rows менять нельзя | ABSENT | Append-only correction overlay и effective-plan resolver с одной действующей поправкой на resident/смену | Move/exclude/restore/history/conflict tests |
| TR-054 | Плановое право и физическая задержка старой вахты не создают двойную occupancy | План и факт разделены M7/M8, но переход права при пересменке не материализован | PARTIAL | Прекратить прежнее право по смене без создания второй occupancy задержавшемуся resident | Overlap handover tests |

## Группы реализации

| Волна | ID | Результат |
|---|---|---|
| Baseline | TR-001 | Завершено: чистая сохранённая v29 |
| Integrity | TR-019–TR-022, TR-031–TR-032 | Надёжный существующий фундамент |
| Exclusive control | TR-043–TR-048 | Единственный session-bound управляющий, lifecycle, takeover, write gate, аудит и UI/read-only |
| Permanent map | TR-002–TR-008, TR-011 | Пара техники, календарь и binding |
| Cohort and rules | TR-009–TR-018, TR-033–TR-037, TR-039–TR-042, TR-049 | M5/resident transition и минимальное M6 MVP-ядро завершены; persistent rules и специальные волны остаются отдельной конфигурацией |
| Saved preview | TR-012–TR-013, TR-023–TR-024, TR-038 | M7 persistence/confirmation COMPLETE; unresolved сохраняются с reason codes, отдельная полная group-conflict конфигурация остаётся M6 follow-up |
| Apply and audit | TR-025–TR-030 | Whole-run M8/HTTP/UI workflow реализован; сменное применение остаётся отдельным переходом |
| Next arrival handover | TR-050–TR-054 | Следующий активный этап ADR-031; не `COMPLETE` |

## Критерий закрытия разрыва

Для смены статуса на READY обязательны одновременно:

1. реализация в утверждённом HEAD;
2. миграция или доказательство, что она не нужна;
3. позитивный, негативный и граничный тест;
4. для interval/concurrency — PostgreSQL-проверка;
5. обновление `07_ТЕКУЩЕЕ_СОСТОЯНИЕ_HEAD.md` и этой матрицы;
6. независимая проверка агентом, не реализовывавшим изменение.
