# Независимая перепроверка документа 98

Дата: 26.07.2026  
Проверяемый документ: `98_ОТЧЕТ_ОБ_ИСПРАВЛЕНИИ_QA_CHAOS_P2_001_R3_И_QA_REG_P2_001_26_07_2026.md`  
Итоговый статус документа 98: **ПРИНЯТО**

## 1. Краткий итог

Обе карточки документа 96 независимо подтверждены как исправленные:

```text
QA-CHAOS-P2-001-R3 = PASS
QA-REG-P2-001 = PASS
CHAOS-P01-008 = СНЯТ ВЛАДЕЛЬЦЕМ / НЕПРИМЕНИМО
DOCUMENT 98 = ПРИНЯТО
POSTGRESQL CONCURRENCY = NOT RUN / НЕ ПРОВЕРЕНО
```

Новых дефектов в проверенном контуре не найдено.

Документ 98 и исправления можно закрыть. Возвращать их чату исправлений не требуется.

## 2. Целостность результата

Внешний SHA-256 документа 98:

```text
A69F4E89A5FAE4691F2141B1F3BF5D5CF4FA055F47BD1FD4580DAD45B696FCE4
```

Фактически вычисленный SHA-256 полностью совпал.

Проверка таблицы раздела 8 документа 98:

- семь заявленных code/test-файлов: `7/7 MATCH`;
- отсутствующие файлы: `0`;
- все семь файлов действительно отличаются от отпечатков документа 95;
- оставшиеся 26 файлов прежнего 33-файлового контрольного набора не изменились;
- общий контрольный набор: `33/33 MATCH`;
- незаявленные изменения внутри контрольного набора: `0`.

Текущая ветка и HEAD совпали с отчетом:

```text
branch = codex/new-public-site-ux-prototype
HEAD = e75922ce7d26c1b9cf4335d8c1af67064a40d440
```

## 3. Статический и JavaScript-аудит

Независимо подтверждено:

- точный `/logout/` при `role_active=false` выполняет безопасный прямой переход;
- изменяющие ссылки, формы и запросы не получили обхода read-only;
- готовое изменяющее подтверждение по-прежнему обезвреживается;
- вызовов отсутствующей `resetHold()` в шаблоне Водителя: `0`;
- `shiftCloseHoldGuard.cancel()` сбрасывает таймер, RAF, progress и состояние подтверждения;
- новый тест извлекает и исполняет production-код обработчика формы;
- `driver-mobile-shell-v104`: `0` вхождений;
- `driver-mobile-shell-v105`: `7` согласованных вхождений.

Исполняемый Node-набор:

```text
tests = 6
pass = 6
fail = 0
skipped = 0
```

Ограничение теста учтено: клиентский Node-тест не заменяет серверную проверку очистки сессии. Серверный маршрут отдельно подтвержден Django-тестом и Browser QA.

## 4. Django-регресс

### 4.1. Узкий набор текущих исправлений

```text
Found 13 tests
Ran 13 tests in 0.486s
OK
```

Результат:

- `13/13 PASS`;
- skipped: `0`;
- failures: `0`;
- errors: `0`.

### 4.2. Расширенный набор документа 96

```text
Found 67 tests
Ran 67 tests in 2.022s
OK (skipped=5)
```

Результат:

- выполнено без пропуска: `62 PASS`;
- PostgreSQL-only: `5 skipped`;
- failures: `0`;
- errors: `0`.

### 4.3. Полный набор

```text
Found 641 test(s)
Ran 641 tests in 30.484s
OK (skipped=13)
```

Все 13 пропусков сохранены как честно непроверенные специализированные проверки; они не объявлены пройденными.

### 4.4. Системные проверки

| Проверка | Результат |
|---|---|
| `manage.py check` | без ошибок |
| `makemigrations --check --dry-run` | `No changes detected` |
| `git diff --check` | exit code `0` |

## 5. Независимый Browser QA

### 5.1. Изолированное окружение

Проверка выполнена на отдельной временной SQLite с сотрудниками и техникой, имеющими единый маркер `QA99`.

Использовались:

- временный settings-модуль;
- временная management-команда фикстуры;
- `qa99_browser.sqlite3`;
- отдельные временные media-каталоги;
- host-only-сессии ролевых поддоменов.

Сервер запущен только через `START_SERVER_MVP.bat` и остановлен только через `STOP_SERVER_MVP.bat`.

SHA-256 рабочей `db.sqlite3`:

```text
до Browser QA:    A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222
после Browser QA: A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222
```

Рабочая база не изменялась.

### 5.2. QA-REG-P2-001 и Driver PWA — 1280×720

На настоящей форме `/driver/shift/close/` выполнено:

```text
вход Водителем
→ открыть закрытие смены
→ заполнить конечные показания
→ Проверить показания
→ Изменить показания
→ изменить числовое поле
→ повторно Проверить показания
```

Наблюдаемый результат:

```text
data-driver-pwa-version = driver-mobile-shell-v105
после редактирования shift_action = review
кнопка = Проверить показания
hold progress = 0%
повторная проверка shift_action = close
horizontal overflow = false
console errors = 0
```

`ReferenceError` и вызова `resetHold()` не возникло.

### 5.3. QA-REG-P2-001 и Driver PWA — 390×844

Тот же пользовательский путь повторен при настоящем viewport `390×844`.

Результат:

```text
width = 390
scrollWidth = 390
horizontal overflow = false
PWA = driver-mobile-shell-v105
после input shift_action = review
hold progress = 0%
после повторной проверки shift_action = close
console errors = 0
```

Точную скрытую ветку `input` при еще установленном `shift_action=close` подтверждает исполняемый Node-интеграционный тест production-обработчика. В обычном интерфейсе пользователь получает доступ к полям только после нажатия `Изменить показания`.

### 5.4. QA-CHAOS-P2-001-R3 — 1280×720

В браузере выполнена полная реальная последовательность двух role-origin:

```text
вход Горным мастером
→ оставить /mining-master/assignments/ открытым
→ в другом role-origin войти Диспетчером тем же сотрудником
→ старый экран не перезагружать
→ дождаться realtime
```

Старый экран без reload получил:

```text
role_active = false
data-role-readonly = true
баннер = Роль неактивна — доступен только просмотр
Начать смену: disabled = true
aria-disabled = true
data-role-readonly-blocked присутствует
horizontal overflow = false
console errors = 0
```

Видимая обычная ссылка `Выйти` с `href="/logout/"` нажата непосредственно в браузере.

Результат:

```text
URL = http://mining-master.localhost:8000/
title = Вход · Горный мастер
confirm не появился
console errors = 0
```

После выхода старая host-only-сессия очищена, а отдельная активная сессия Диспетчера после reload осталась на `/dispatcher/control/`.

### 5.5. QA-CHAOS-P2-001-R3 — 390×844

Мобильный экран Горного мастера был открыт и оставлен активным при настоящем viewport `390×844`. Для сохранения выбранного мобильного viewport роль Диспетчера активирована через тот же production login-view независимым Django Client с другим host.

Старая мобильная страница без reload получила:

```text
width = 390
height = 844
scrollWidth = 390
role_active = false
data-role-readonly = true
баннер read-only отображается
data-role-readonly-blocked: 24 элемента
horizontal overflow = false
console errors = 0
```

Единственный незаблокированный submit оказался невидимым элементом `0×0` с методом `GET` и подписью `Обновить пульт`; он не является изменяющим действием.

Мобильная разметка не вывела видимую ссылку выхода. Прямой безопасный `/logout/` очистил неактивную сессию и открыл форму входа:

```text
URL = /
title = Вход · Горный мастер
width = 390
scrollWidth = 390
console errors = 0
```

## 6. PostgreSQL

Настоящая отдельная PostgreSQL в текущем окружении отсутствует и не запускалась.

```text
POSTGRESQL CONCURRENCY = NOT RUN / НЕ ПРОВЕРЕНО
```

Последовательный SQLite, Django test DB и Browser QA не объявляются заменой проверки настоящих параллельных транзакций.

## 7. Очистка

После Browser QA:

- сервер остановлен штатным BAT-файлом;
- порт 8000 свободен;
- временный settings-модуль удален;
- временная management-команда удалена;
- временная SQLite удалена;
- временные media-каталоги удалены;
- QA99-файлов и `pyc` осталось: `0`;
- SHA-256 рабочей базы не изменился.

## 8. Итог для кампании P01

Исправления документа 98 приняты. Две последние продуктовые карточки документа 96 закрыты.

Решение владельца по `CHAOS-P01-008` также закрыто: простоем самосвала управляет только Водитель, путь Механика не создается.

Функциональные исправления P01, проверяемые последовательными тестами и браузером, подтверждены. Однако окончательную приемку всего пакета P01 нельзя объявлять завершенной, пока не выполнены:

1. финальный повтор всех 17 сценариев P01 и критического регресса;
2. настоящие конкурентные проверки на отдельной разрешенной PostgreSQL.

Текущий статус:

```text
DOCUMENT 98 = ПРИНЯТО
PRODUCT DEFECTS FROM DOCUMENT 96 = CLOSED
P01 FUNCTIONAL FIXES = PASS
P01 FINAL ACCEPTANCE = ОЖИДАЕТ ФИНАЛЬНЫЙ ПРОГОН И POSTGRESQL
```

## 9. Граница изменений

Код проекта текущим тестовым чатом не изменялся. Создан только этот отчет и обновлены файлы прогресса.

Commit, push, deploy, production, `.env`, рабочая база и миграции не затрагивались.
