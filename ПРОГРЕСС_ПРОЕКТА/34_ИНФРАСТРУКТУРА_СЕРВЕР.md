# Инфраструктура и сервер для производственного контура

## Дополнение 04.07.2026: опубликован PWA Водителя `driver-mobile-shell-v12`

На `driverform.ru` опубликовано обновление водительского PWA для справочника простоев. Перед изменением создан серверный бэкап `/srv/accounting-mvp/backups/code/driver-downtime-reference-v12-before-20260704_052739.tar.gz`.

Production-состояние после публикации:

- `downtimes.0003_alter_downtimereason_options_and_more` уже применена;
- шаблон `templates/users/driver_shift.html` отдает `data-driver-pwa-version="driver-mobile-shell-v12"`;
- вкладка `Простои` содержит формы с `data-driver-downtime-reason-id` и hidden `reason_id`;
- `users/views.py` использует `DowntimeReason.for_workplace('truck_driver', ...)` для выдачи и серверной проверки причины;
- выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`;
- `accounting-mvp` и `nginx` active;
- публичный `https://driverform.ru/driver-sw.js` отдает `driver-mobile-shell-v12`;
- browser QA `https://driverform.ru/driver/?tab=downtimes` под демо-водителем: 14 кнопок из справочника, совпадение `data-driver-downtime-reason-id` и hidden `reason_id`, horizontal overflow 0, console errors 0.

## Дополнение 03.07.2026: начинается перенос на новый сервер

После стабилизации текущего production-контура на `driverform.ru` пользователь поставил задачу перенести проект на новый сервер. Подробный рабочий план переноса вынесен в отдельный документ `47_ПЕРЕНОС_НА_НОВЫЙ_СЕРВЕР.md`.

Текущий сервер перед переносом:

- HOSTKEY RU;
- IP `77.91.93.47`;
- домены `driverform.ru`, `www.driverform.ru`, `demo.driverform.ru`;
- Ubuntu 24.04;
- PostgreSQL `accounting_mvp`;
- приложение `/srv/accounting-mvp`;
- systemd `accounting-mvp.service`;
- nginx + Let's Encrypt;
- актуальные shell-версии после последнего production-обновления: Горный мастер `mining-master-mobile-shell-v87`, Горный диспетчер `dispatcher-desktop-shell-v15`.

Для нового переноса нельзя ограничиваться копированием кода. Нужно перенести или восстановить PostgreSQL, `.env`, секреты, staticfiles, nginx, systemd, SSL, service worker/PWA-версии и realtime-контур. Критерий успеха - открытие рабочих интерфейсов в браузере через новый сервер, включая диспетчерский интерфейс, мобильный Горный мастер, service worker и `/realtime/state/`.

## Оценка нагрузки

Исходные ориентиры:

- до 60 самосвалов;
- около 10 экскаваторов;
- 2 смены;
- примерно 20 рейсов на самосвал за смену;
- около 500 сотрудников;
- оперативное хранение производственных данных минимум за 3 месяца.

Расчетная базовая нагрузка по рейсам:

- 60 самосвалов x 20 рейсов x 2 смены = около 2400 рейсов в сутки;
- 90 дней = около 216000 рейсов;
- с событиями, простоями, изменениями статусов, журналами и отчетными срезами база может быстро выйти на сотни тысяч или несколько миллионов строк, что нормально для PostgreSQL при правильных индексах.

## Минимальная и рекомендуемая конфигурация

Для реального производственного запуска не использовать SQLite и обычный виртуальный хостинг. Нужен PostgreSQL.

Минимум для пилота:

- 4-6 vCPU;
- 16 ГБ RAM;
- 200-300 ГБ NVMe;
- ежедневные резервные копии;
- мониторинг диска, CPU, RAM и PostgreSQL.

Комфортная конфигурация для спокойной работы:

- 8 vCPU или выделенный CPU сопоставимого класса;
- 32 ГБ RAM;
- 500 ГБ-1 ТБ NVMe;
- отдельное место под резервные копии;
- PostgreSQL, приложение и фоновые задачи можно начать на одном сервере, но с возможностью дальнейшего разделения.

## Ориентир по SprintHost / Sprintbox на 21.06.2026

Публичные тарифы Sprintbox можно использовать для теста и пилота, но они выглядят тесными для спокойного промышленного запуска.

Подходящие из VDS только условно:

- `Про 12`: 12 ядер, 16 ГБ RAM, 120 ГБ NVMe, 10700 руб/мес - минимально допустимо для пилота, но мало диска и RAM без запаса;
- `AMS x16`: 10 ядер, 16 ГБ RAM, 200 ГБ NVMe, 6999 руб/мес - лучше по диску, но зарубежная локация и 200 Мбит/с, для российской производственной эксплуатации спорно.

Предпочтительный вариант у SprintHost для реальной эксплуатации - выделенный сервер, а не VDS:

- целевой уровень: 32 ГБ RAM, 6-8+ физических ядер / 12-16 потоков, NVMe от 500 ГБ, лучше 2 x NVMe и отдельный бекапный диск;
- публичная страница выделенных серверов SprintHost показывает варианты с 2 x 1 ТБ SSD NVMe и ценами порядка 12500-18500 руб/мес в зависимости от акции/конфигурации.

## Практический вывод

Для проекта лучше планировать:

1. Разработка и демонстрация - локально или младший VDS.
2. Пилот - старший VDS уровня 10-12 vCPU / 16 ГБ RAM, если данных немного и есть ежедневные бекапы.
3. Производственный контур - выделенный сервер с 32 ГБ RAM и NVMe, PostgreSQL, регулярными бекапами и мониторингом.

## Дополнение 21.06.2026: сравнение российских провайдеров с оплатой в рублях

Критерии исследования:

- российский провайдер или российская площадка;
- возможность оплаты в рублях;
- конфигурация не слабее пилотного уровня: примерно 8 vCPU / 16 ГБ RAM / NVMe;
- для промышленного запуска предпочтительнее 32 ГБ RAM и выделенный сервер.

Предварительный рейтинг:

1. `HOSTKEY vm.v3-heavy` - 8 vCPU, 32 ГБ RAM, 240 ГБ NVMe, около 5050 руб/мес. Лучшее соотношение цена/ресурсы среди найденных VDS, но это все равно виртуальный сервер и нужно отдельно контролировать бекапы/лимит трафика.
2. `Timeweb Cloud High` - 8 CPU, 16 ГБ RAM, 160 ГБ NVMe, около 4200 руб/мес. Выгодно для пилота, но без запаса по RAM и диску.
3. `REG.RU High C8-M16-D180` - 8 vCPU, 16 ГБ RAM, 180 ГБ NVMe, около 8000 руб/мес. Дороже HOSTKEY/Timeweb, но понятная российская площадка и несколько регионов.
4. `Sprintbox Про 12` - 12 CPU, 16 ГБ RAM, 120 ГБ NVMe, около 10700 руб/мес. По CPU хорошо, но мало диска и RAM за свои деньги.
5. `REG.RU dedicated Ryzen 7 7700X` - 8 ядер / 16 потоков, 32 ГБ RAM, 2 x 1 ТБ NVMe, около 19100 руб/мес, при оплате на год около 16235 руб/мес. Практичный вариант для промышленного контура.
6. `Selectel AR43-NVMe` - около 24900 руб/мес. Дороже, но сильнее как корпоративный вариант.

Вывод:

- самый выгодный бюджетный старт: HOSTKEY VDS с 32 ГБ RAM;
- самый разумный промышленный вариант: физический сервер REG.RU или аналогичный dedicated с 32 ГБ RAM и 2 x NVMe;
- крупные облака Yandex Cloud, VK Cloud, Cloud.ru удобны корпоративно, но по цене для этой задачи обычно хуже VDS/dedicated.

## Дополнение 21.06.2026: сервер для демонстрации производственного контура

Для демонстрации только производственного контура с диспетчерской и архивом данных за 1 месяц выделенный сервер не нужен.

Оценка демонстрационного объема:

- 60 самосвалов x 20 рейсов x 2 смены x 30 дней = около 72000 рейсов;
- даже с событиями рейсов, простоями, журналами действий и служебными статусами это остается умеренной нагрузкой для PostgreSQL.

Минимально разумная конфигурация:

- 4 vCPU;
- 8 ГБ RAM;
- 120 ГБ NVMe;
- PostgreSQL на том же сервере;
- ежедневный бекап базы.

Нормальная конфигурация для спокойной демонстрации:

- 8 vCPU;
- 16 ГБ RAM;
- 160 ГБ NVMe;
- PostgreSQL, приложение и фоновые задачи на одном сервере;
- ежедневные бекапы и контроль свободного места.

Предпочтительный вариант из найденных тарифов:

- `HOSTKEY vm.v2-medium`: 8 vCPU, 16 ГБ RAM, 160 ГБ NVMe, около 3390 руб/мес - оптимально для демонстрации;
- `HOSTKEY vm.v2-heavy`: 8 vCPU, 32 ГБ RAM, 240 ГБ NVMe, около 4420 руб/мес - лучше, если разница в цене некритична и хочется запас.

## Дополнение 30.06.2026: куплен демонстрационный сервер HOSTKEY

Для показа руководству и демонстрации диспетчерского контура на 1 месяц куплен тестовый VPS HOSTKEY:

- тариф: `v2-medium`;
- регион: `FI`;
- ресурсы: 8 vCPU, 16 GB RAM, 160 GB NVMe;
- сеть: 3 TB / 1 Gbps VM, 1 IPv4;
- IP: `82.38.40.17`;
- ОС: Ubuntu 24.04;
- период оплаты: 1 month (hourly billing);
- сумма счета: 14.16 EUR с VAT.

Сервер используется как временный демонстрационный контур без реальных персональных данных сотрудников. Для демо-базы использовать условные ФИО, условные телефоны и не загружать реальные фотографии/кадровые данные. Если руководство примет решение продолжать работу и переходить к реальному тестированию с персональными данными, следующий контур планировать на российской локации.

Базовая подготовка сервера:

- обновлены пакеты Ubuntu, сервер перезагружен на ядро `6.8.0-124-generic`;
- создан рабочий пользователь `deploy`, настроен SSH-доступ по ключу Codex и sudo;
- включен firewall `ufw`: разрешены `OpenSSH` и `Nginx Full`;
- установлен nginx, публичный HTTP по `http://82.38.40.17` открывает стандартную страницу nginx;
- установлен PostgreSQL 16, Python 3.12 tooling, build-essential, libpq-dev и git;
- создана база `accounting_mvp` и пользователь `accounting_mvp`, секреты сохранены в `/home/deploy/accounting-secrets/db.env`.

Подготовка Django-проекта к деплою:

- локальный режим по умолчанию остается на SQLite;
- серверный режим включается через `.env`: `DJANGO_DB_ENGINE=postgres`;
- для сервера предусмотрены `DJANGO_ALLOWED_HOSTS`, `DJANGO_CSRF_TRUSTED_ORIGINS` и PostgreSQL-переменные;
- в зависимости добавлен `gunicorn`;
- временные tunnel-логи и локальные резервные копии исключены из git/deploy;
- локальные проверки перед заливкой: `manage.py check`, `makemigrations --check --dry-run`, полный `manage.py test` на 103 теста.

## Дополнение 30.06.2026: Django MVP развернут на демо-сервере

Текущий MVP развернут на сервере HOSTKEY по адресу `http://82.38.40.17/`.

Фактическая схема демо-контура:

- приложение: `/srv/accounting-mvp`;
- пользователь запуска: `deploy`;
- база данных: PostgreSQL `accounting_mvp`;
- секреты базы: `/home/deploy/accounting-secrets/db.env`;
- виртуальное окружение: `/srv/accounting-mvp/.venv`;
- WSGI runner: `gunicorn`;
- systemd service: `accounting-mvp.service`;
- reverse proxy и static: nginx;
- static files: `/srv/accounting-mvp/staticfiles`;
- публичный доступ: HTTP по IP, без домена и HTTPS.

Выполнено:

- установлен код текущего checkpoint;
- создан серверный `.env` с `DJANGO_DEBUG=False`, `DJANGO_DB_ENGINE=postgres`, `DJANGO_ALLOWED_HOSTS=82.38.40.17,localhost,127.0.0.1`;
- применены миграции Django в PostgreSQL;
- выполнен `collectstatic`;
- загружены стартовые справочники из `ПРОГРЕСС_ПРОЕКТА/11_ДАННЫЕ_СПРАВОЧНИКОВ_MVP`;
- выполнена команда демо-сценария `seed_demo_scenario`;
- исправлен запуск через unix-socket в `/run/accounting-mvp/accounting-mvp.sock`;
- проверено, что `accounting-mvp` и `nginx` активны;
- проверено извне: `/`, `/interfaces/`, `/static/css/app.css`, `/mining-master-sw.js` отдают HTTP 200.

Ограничения текущего демо-контура:

- HTTPS и домен пока не настроены;
- PWA на телефоне для полноценной установки потребует HTTPS;
- временный `NOPASSWD` sudo для пользователя `deploy` оставлен для ускорения настройки, перед более строгим контуром его нужно убрать или ограничить;
- используются только демо-данные без реальных персональных данных.

## Дополнение 01.07.2026: перенос с зарубежного VPS на российский HOSTKEY

Из-за блокировки зарубежного сервиса принято решение заново развернуть демо-контур на российском сервере HOSTKEY.

Новый сервер:

- провайдер: HOSTKEY;
- регион: `RU`;
- тариф: `ru-vmv3-medium`;
- ресурсы: 8 vCore, 16 GB RAM, 160 GB NVMe;
- IP: `77.91.93.47`;
- ОС: Ubuntu 24.04;
- домены: `driverform.ru`, `www.driverform.ru`, `demo.driverform.ru`.

Фактическая схема:

- приложение: `/srv/accounting-mvp`;
- пользователь запуска: `deploy`;
- база данных: PostgreSQL `accounting_mvp`;
- секреты базы: `/home/deploy/accounting-secrets/db.env`;
- виртуальное окружение: `/srv/accounting-mvp/.venv`;
- WSGI runner: `gunicorn`;
- systemd service: `accounting-mvp.service`;
- reverse proxy и static: nginx;
- SSL: Let's Encrypt, сертификат `/etc/letsencrypt/live/driverform.ru/fullchain.pem`;
- nginx-конфиг: `/etc/nginx/sites-available/accounting-mvp`.

Настройка nginx:

- `http://77.91.93.47` оставлен рабочим и проксирует в приложение;
- `http://driverform.ru`, `http://www.driverform.ru`, `http://demo.driverform.ru` редиректят на HTTPS;
- `https://driverform.ru`, `https://www.driverform.ru`, `https://demo.driverform.ru` проксируют в `gunicorn` через unix-socket `/run/accounting-mvp/accounting-mvp.sock`.

Проверки 01.07.2026:

- `nginx -t` - успешно;
- `accounting-mvp` - active;
- `nginx` - active;
- `http://77.91.93.47` - 200 OK;
- `http://driverform.ru` - 301 на `https://driverform.ru/`;
- `https://driverform.ru` - 200 OK;
- `http://www.driverform.ru` - 301 на `https://www.driverform.ru/`;
- `https://www.driverform.ru` - 200 OK;
- `https://driverform.ru/system-admin/employees/` - 302 на `/home/`, без SSL-ошибки и без 502;
- `https://demo.driverform.ru/` - 200 OK.

Ограничение: на сервере временно оставлен `deploy ALL=(ALL) NOPASSWD:ALL` для автоматической настройки. После стабилизации контура его нужно убрать или заменить на обычный sudo-пароль/ограниченные команды.

## Дополнение 03.07.2026: Экскаваторщик downtime/PWA v10 перенесен на `driverform.ru`

На основной сервер перенесена правка активного простоя Экскаваторщика: серверный таймер `DowntimeEvent.started_at`, `EquipmentState`-цвета, расширенный realtime `downtime_changed` и PWA-оболочка `excavator-mobile-shell-v10`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/excavator-downtime-server-state-v10-before-20260703_161737.tar.gz`. Обновлены `trips/views.py`, `core/signals.py`, `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/tests.py`; выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`. Сервисы `accounting-mvp` и `nginx` active.

Публичная проверка подтвердила: `https://driverform.ru/excavator-sw.js` содержит `excavator-mobile-shell-v10` и `/static/js/realtime-client.js`. Production rollback-проверка через Django Client подтвердила `downtime_started`, `downtime_changed` и `downtime_closed` без сохранения тестового простоя в базе. Production Browser QA `390x844` на `/excavator/work/` показал вкладку `События` без horizontal overflow и ошибок консоли.
## Дополнение 02.07.2026: сервер обновлен до мобильного Горного мастера v66

- Перед обновлением на сервере создан резервный архив текущего кода: `/srv/accounting-mvp/backups/pre-mobile-version-v66-.tar.gz`.
- На сервер `77.91.93.47` выгружены актуальные папки кода Django MVP, включая мобильный Горный мастер, realtime/PWA-слой и static/templates.
- На сервере выполнены `manage.py check`, `manage.py migrate --noinput`, `manage.py collectstatic --noinput`.
- Сервис `accounting-mvp.service` перезапущен и находится в состоянии `active`.
- Публичная проверка подтвердила: `https://driverform.ru/mining-master-sw.js` и `https://www.driverform.ru/mining-master-sw.js` отдают `mining-master-mobile-shell-v66`.
- Browser QA через `https://www.driverform.ru/mining-master/assignments/` под ролью Горного мастера: мобильный shell открывается, в шапке виден `v66`, горизонтального переполнения и ошибок консоли нет.

## Дополнение 02.07.2026: dispatcher service worker обновлен до v3 на боевом сервере

- Перед точечной заменой создан резервный архив серверных файлов: `/srv/accounting-mvp/backups/pre-dispatcher-sw-v3-.tar.gz`.
- На сервер `77.91.93.47` точечно выгружены файлы `trips/views.py` и `templates/trips/dispatcher_control.html`.
- На сервере выполнены `python manage.py check`, `python manage.py migrate --noinput`, `python manage.py collectstatic --noinput`.
- Сервис `accounting-mvp.service` перезапущен и находится в состоянии `active`.
- Публичная проверка подтвердила: `https://www.driverform.ru/dispatcher-sw.js` и `https://driverform.ru/dispatcher-sw.js` отдают `dispatcher-desktop-shell-v3`, а также команды `SKIP_WAITING` и `GET_VERSION`.
- Цель обновления: быстрее вытеснять старый кэш dispatcher-PWA и закрепить сортировку гаража экскаваторов по рабочему номеру техники после возврата комплекса в гараж.

## Дополнение 02.07.2026: realtime/PWA v77 перенесен на боевой сервер

Причина переноса: установленный PWA Горного мастера на телефоне мог слабо синхронизироваться с desktop-экранами, потому что боевой сервер еще отдавал `mining-master-mobile-shell-v76`, а `/static/js/realtime-client.js` возвращал 404.

- Перед заменой создан серверный бэкап: `/srv/accounting-mvp/backups/code/realtime-pwa-v77-before-20260702_110806.tar.gz`.
- На сервер точечно перенесены `templates/base.html`, `templates/trips/dispatcher_control.html`, `static/js/realtime-client.js`, `assignments/views.py`, `assignments/tests.py`, `trips/views.py`, `trips/tests.py`.
- На сервере выполнены `manage.py check`, `manage.py collectstatic --noinput`, перезапуск `accounting-mvp`.
- Проверено: `accounting-mvp` active, `nginx` active.
- Публичная проверка подтвердила: `https://driverform.ru/mining-master-sw.js` и `https://www.driverform.ru/mining-master-sw.js` отдают `mining-master-mobile-shell-v77`, содержат `/static/js/realtime-client.js` и `ignoreSearch: true`.
- Публичная проверка подтвердила: `https://driverform.ru/dispatcher-sw.js` и `https://www.driverform.ru/dispatcher-sw.js` отдают `dispatcher-desktop-shell-v5`, содержат `/static/js/realtime-client.js` и `ignoreSearch: true`.
- Публичная проверка подтвердила: `https://driverform.ru/static/js/realtime-client.js` и `https://www.driverform.ru/static/js/realtime-client.js` отдают 200.
- Авторизованный Chrome-smoke на `https://www.driverform.ru`: мобильный Горный мастер показывает `v77`, `window.AppRealtime` активен, `/realtime/state/` отвечает 200; Горный диспетчер работает как `dispatcher-control / custom`; витрина руководства работает как `management-reports / manual`; ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production перенесен PWA/realtime fix v79/v7

После повторного замечания о рассинхроне установленной PWA Горного мастера на `driverform.ru` перенесено исправление XHR/app-shell:

- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v79`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v7`;
- `/static/js/realtime-client.js` содержит `initialVersion` и `previousVersion`;
- service worker Горного мастера и Горного диспетчера больше не отдают app-shell fallback на AJAX-запросы с `X-Requested-With: XMLHttpRequest`;
- рабочий HTML получает `data-operational-state-version`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/pwa-xhr-cache-v79-before-20260702_120937.tar.gz`. На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичная проверка подтвердила новые service worker и общий JS. Авторизованный Chrome-smoke подтвердил: мобильный Горный мастер применил production-версии 813-816 через `previousVersion`, Горный диспетчер открыт как `dispatcher-control / custom`, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production включена оптимизация realtime-трафика v80/v8

После вопроса об уменьшении нагрузки на канал связи внедрена оптимизация без изменения механики рабочих экранов:

- фоновый realtime-polling теперь использует `/realtime/state/?after=<currentVersion>&include_events=0`;
- события продолжают запрашиваться отдельно только при изменении версии состояния;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v80`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v8`;
- в nginx включен gzip для JSON, JavaScript/service worker, manifest, CSS, XML и SVG.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/realtime-traffic-pwa-v80-before-20260702_122924.tar.gz`. Дополнительно на сервере остался ранний резервный архив без timestamp `/srv/accounting-mvp/backups/code/realtime-traffic-pwa-v80-before-.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Серверный замер Django Client: полный `/realtime/state/` - 10866 байт, легкий `/realtime/state/?after=...&include_events=0` - 190 байт. Публично проверено: `/static/js/realtime-client.js` отдается с `Content-Encoding: gzip`, `/mining-master-sw.js` содержит `mining-master-mobile-shell-v80`, `/dispatcher-sw.js` содержит `dispatcher-desktop-shell-v8`. Авторизованный Chrome-smoke подтвердил: мобильный Горный мастер `v80`, Горный диспетчер `dispatcher-control / custom`, оба экрана ходят легкими запросами `include_events=0`, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production исправлена очистка mobile-очереди PWA v81/v9

После замечания пользователя, что действия с телефона доходят до desktop-экранов, но на телефоне остаются "висящими", на `driverform.ru` перенесена правка mobile-очереди:

- queued mobile-действия получают `refreshMobileBoard: true`;
- после очистки последнего элемента очереди телефон подтягивает свежий `/mining-master/assignments/` фрагмент;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v81`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v9`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/pwa-mobile-queue-refresh-v81-before-20260702_124328.tar.gz`. На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v81`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v9`. Авторизованный Chrome-smoke на production подтвердил безопасный сценарий queued-очереди без изменения данных: fake queued-запрос очистился из `localStorage`, `is-sync-pending` снялся, клиент запросил свежий `/mining-master/assignments/` фрагмент, `window.AppRealtime` остался активным, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production ускорена mobile-очередь PWA v82/v10

После выяснения, что направление `телефон -> desktop` было медленнее из-за искусственной 5-секундной задержки queued-отправки, на `driverform.ru` перенесена точечная правка app-shell:

- mobile-очередь Горного мастера использует `dispatcherMobileSyncFlushDelayMs = 300`;
- локальная optimistic-анимация телефона не ждет ответа сервера;
- offline-очередь и refresh мобильной доски после очистки очереди сохранены;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v82`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v10`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/pwa-mobile-queue-speed-v82-before-20260702_200245.tar.gz`. На сервере выполнены content-check, `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v82`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v10`, `/static/js/realtime-client.js` отдается с gzip. Авторизованный Chrome-smoke на production подтвердил безопасный сценарий queued-очереди без изменения данных: мобильный Горный мастер `v82`, fake queued-запрос очистился из `localStorage`, `is-sync-pending` снялся, клиент запросил свежий `/mining-master/assignments/` фрагмент, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production добавлено постоянное уведомление PWA v83/v11

На `driverform.ru` перенесена правка mobile shell Горного мастера:

- мобильный shell периодически проверяет серверную версию `/mining-master-sw.js?version_check=...`;
- если серверная версия новее текущей, на нижней вкладке `Отчеты` показывается маленький красный индикатор;
- индикатор переживает закрытие попапа, переходы между вкладками и partial-refresh;
- штатный realtime-контур не делает reload до явного согласия пользователя на обновление;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v83`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v11`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/20260702_2030_pwa_update_indicator_v83-before.tar.gz`. На сервере выполнены content-check, `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active. Попытка запустить серверные Django-тесты уперлась в ограничение PostgreSQL на создание test database, поэтому результат тестов берется из локального полного набора.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v83`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v11`. Авторизованный Chrome-smoke на production подтвердил: мобильный Горный мастер `v83`, индикатор скрыт в норме, при имитации `v84` появляется на вкладке `Отчеты`, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production выложен тестовый выпуск PWA v84/v12

Для ручной проверки постоянного уведомления о новой версии на телефоне создан тестовый выпуск без изменения рабочей логики:

- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v84`;
- мобильный shell Горного мастера показывает `v84`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v12`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/20260702_2035_pwa_test_v84-before.tar.gz`. На сервере выполнены content-check, `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v84`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v12`. Авторизованный Chrome-smoke на production подтвердил: свежий мобильный Горный мастер `v84`, индикатор скрыт при актуальной версии, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production усилено уведомление PWA v85/v13

На `driverform.ru` перенесена правка заметности обновления PWA:

- красный индикатор на нижней вкладке `Отчеты` увеличен;
- индикатор получил мягкую пульсацию;
- кнопка во вкладке `Отчеты` остается `Проверить обновления`, если новой версии нет;
- при найденной новой версии кнопка становится `Обновить`, красной и пульсирующей;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v85`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v13`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/20260702_2045_pwa_update_attention_v85-before.tar.gz`. На сервере выполнены content-check, `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v85`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v13`, `/static/css/app.css` содержит `mm-mobile-update-button-pulse` и `mm-mobile-update-badge-pulse`. Авторизованный Chrome-smoke на production подтвердил: свежий мобильный Горный мастер `v85`, при имитации `v86` кнопка `Обновить` пульсирует, точка крупная, попап показывает `v85 -> v86`, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production выложен пустой выпуск PWA v86/v14

Для ручной проверки усиленного уведомления на телефоне создан пустой выпуск без изменения рабочей логики:

- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v86`;
- мобильный shell Горного мастера показывает `v86`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v14`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/20260702_2055_pwa_empty_test_v86-before.tar.gz`. На сервере выполнены content-check, `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v86`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v14`. Авторизованный Chrome-smoke на production подтвердил: свежий мобильный Горный мастер `v86`, индикатор скрыт при актуальной версии, кнопка `Проверить обновления`, ошибок консоли и горизонтального overflow нет.

## Дополнение 02.07.2026: на production исправлен rebind карточек самосвалов v87/v15

На `driverform.ru` перенесена правка мобильного гаража самосвалов Горного мастера:

- после partial-refresh/realtime-замены DOM карточки самосвалов в панели `data-mm-mobile-panel="trucks"` заново получают обработчик карточки техники;
- долгий тап по самосвалу в гараже больше не должен зависеть от полной перезагрузки PWA;
- `/mining-master-sw.js` поднят до `mining-master-mobile-shell-v87`;
- `/dispatcher-sw.js` поднят до `dispatcher-desktop-shell-v15`.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/pwa-truck-card-rebind-v87-before-20260702_215247.tar.gz`. На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публично проверено: `/mining-master-sw.js` отдает `mining-master-mobile-shell-v87`, `/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v15`. Production Chrome-smoke подтвердил: свежий мобильный Горный мастер `v87`, после принудительного realtime-refresh 40 карточек самосвалов в гараже остались с обработчиком, долгий тап открыл карточку техники, ошибок консоли и горизонтального overflow нет.
# 2026-07-03. На driverform.ru перенесен PWA-экран Водителя самосвала

На основной сервер `driverform.ru` перенесена правка `/driver/shift/`: обновлены `users/views.py`, `users/urls.py`, `templates/users/driver_shift.html`, `static/css/app.css`. Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/driver-shift-pwa-before-20260703_142512.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Chrome QA на `https://driverform.ru/driver/shift/` под ролью Водителя подтвердил: экран открывается с заголовком `Смена водителя`, статус `В РЕЙСЕ`, 4 вкладки переключаются, горизонтального overflow нет, нижняя навигация не перекрывает рабочую панель, ошибок консоли и failed requests нет.

## Дополнение 03.07.2026: на production доведен визуальный экран Водителя самосвала

После сравнения с эталонным скрином на `driverform.ru` точечно обновлены `users/views.py`, `templates/users/driver_shift.html`, `static/css/app.css`. Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/driver-shift-reference-redraw-before-20260703_145317.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Производственная проверка `https://driverform.ru/driver/shift/` подтвердила: HTTP 200, текущий статус водителя `ПУСТОЙ`, body/shell без вертикального и горизонтального overflow на 853x1844 и 1920x1200, внутренний scrollbar убран, нижняя навигация стала темной иконной панелью. Для активного рейса серверный view теперь отдает статус `ГРУЖЕНЫЙ`, а не `В РЕЙСЕ`.

## Дополнение 03.07.2026: на production перенесены серверные состояния гаражей Горного диспетчера v16

После пользовательского скрина с `https://driverform.ru/dispatcher/control/` выяснено, что локальная правка гаражей Горного диспетчера не была перенесена на production: публичный `/dispatcher-sw.js` все еще отдавал `dispatcher-desktop-shell-v15`, а серверный `trips/views.py` содержал старые демо-проценты самосвалов.

Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/dispatcher-garage-current-state-v16-before-20260703_150702.tar.gz`. На сервер точечно перенесен `trips/views.py`; выполнены `manage.py check` и перезапуск `accounting-mvp`. `accounting-mvp` и `nginx` active.

Публичная проверка подтвердила: `https://driverform.ru/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v16`. Серверный авторизованный рендер `/dispatcher/control/` под ролью диспетчера вернул 52 гаражных самосвала: 43 `status-yellow`, 9 `status-green`, 0 `status-gray`; демо-строка `truck_percent = 62` отсутствует.

## Дополнение 03.07.2026: короткий экран Водителя и доведение `EquipmentState` на production

Для правки `/driver/shift/` под короткий viewport `386x747` перед заменой создан code backup `/srv/accounting-mvp/backups/code/driver-shift-386-short-viewport-before-20260703_151954.tar.gz`. На сервер перенесены `templates/users/driver_shift.html` и `static/css/app.css`; собранная статика `staticfiles/css/app.css` содержит media-блок `@media (max-width: 520px) and (max-height: 800px)`.

Во время `manage.py check` обнаружена несостыковка production-кода: текущий `trips/views.py` импортировал `EquipmentState`, но на сервере отсутствовали модель, миграция и начальные состояния справочника. Перед исправлением созданы бэкапы `/srv/accounting-mvp/backups/code/equipment-state-support-before-20260703_152222.tar.gz` и `/srv/accounting-mvp/backups/db/equipment-state-support-before-20260703_152222.sql.gz`. На сервер перенесены `references/models.py`, `references/admin.py`, `references/equipment_states.py`, `references/migrations/0002_equipmentstate.py`, `references/management/commands/load_initial_references.py`; применена миграция `references.0002_equipmentstate`, команда `load_initial_references` загрузила 15 состояний.

Финальная проверка production: `manage.py showmigrations references` показывает `[X] 0002_equipmentstate`, `manage.py check` без ошибок, `EquipmentState.objects.count()` = 15, `accounting-mvp` active, `nginx` active. `collectstatic --noinput` выполнен; публичный CSS cache-buster обновлен до `/static/css/app.css?v=1783056492`.

Production Browser QA на `https://driverform.ru/driver/shift/` под ролью водителя подтвердил рабочий экран на viewport `386x747`: body/shell `386x747`, overflow 0, карточки и индикатор не перекрываются, ошибок консоли нет. Дополнительно проверены viewport `853x1844` и `1920x1200`.

## Дополнение 03.07.2026: canonical route PWA Водителя перенесен на production

После уточнения, что главный экран PWA Водителя должен открываться на `/driver/`, перед заменой создан бэкап `/srv/accounting-mvp/backups/code/driver-canonical-route-before-20260703_155928.tar.gz`. На сервер перенесены `users/views.py`, `users/urls.py`, `templates/users/driver_shift.html`.

Выполнены `manage.py check` и перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA подтвердил: вход водителя `+79000000002 / 200000` редиректит на `https://driverform.ru/driver/`, title `Работа водителя`, `.driver-shell` отрисован, ошибок консоли нет. Маршрут `https://driverform.ru/driver/shift/` оставлен рабочим алиасом.

## Дополнение 03.07.2026: мобильный Горный мастер v89 на production

На `driverform.ru` перенесена правка мобильного Горного мастера под единый стандарт `EquipmentState`: обновлены `trips/views.py`, `templates/trips/dispatcher_control.html`, `static/css/app.css`, `assignments/views.py`, `assignments/tests.py`. Перед заменой создан code backup `/srv/accounting-mvp/backups/code/mining-master-equipment-states-v89-before-20260703_083416.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичная проверка подтвердила: `https://driverform.ru/mining-master-sw.js` отдает `mining-master-mobile-shell-v89`, CSS содержит мобильные статусы `status-gray`, `status-blue`, `status-orange` для гаража экскаваторов. Production Chrome mobile QA `430x932` под ролью Горного мастера подтвердил `V89`, серверные состояния `free`, `garage`, `working`, `in_transit`, `loaded_waiting_unload`, отсутствие horizontal overflow и console errors.

После локального повторного прогона тестов дополнительно перенесена правка `trips/views.py`: активный рейс теперь сам включает экскаватор в список показываемых активных комплексов, даже если для него нет отдельного активного placement/assignment. Перед заменой создан backup `/srv/accounting-mvp/backups/code/mining-master-v89-active-trip-display-fix-before-20260703_084138.tar.gz`; серверный `manage.py check` чистый, `accounting-mvp` и `nginx` active. Финальный production-smoke `430x932` подтвердил `V89`, состояния `garage/free/in_transit/loaded_waiting_unload`, отсутствие horizontal overflow и console errors.

## Дополнение 03.07.2026: мобильный Горный мастер v90 на production после удаления `in_transit`

После удаления устаревшего состояния `in_transit` runtime Горного мастера уже использовал `EquipmentState`, но установленное PWA не получало запрос обновления, так как shell-версия оставалась `mining-master-mobile-shell-v89`. На `driverform.ru` поднята версия Горного мастера до `mining-master-mobile-shell-v90`, видимый номер интерфейса - `V90`.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/mining-master-shell-v90-before-20260703_115954.tar.gz`; обновлены `assignments/views.py`, `assignments/tests.py`, `templates/trips/dispatcher_control.html`. Серверный `manage.py check` чистый, `accounting-mvp` и `nginx` active. Публично `/mining-master-sw.js` отдает `mining-master-mobile-shell-v90`; production Chrome mobile `430x932` под Горным мастером подтвердил `V90`, отсутствие `in_transit` и `В пути`, наличие `Свободен` и `На разгрузку`, состояния `garage/assigned/working/loaded_waiting_unload/free`, отсутствие horizontal overflow и console errors.

## Дополнение 03.07.2026: inline critical CSS для короткого экрана Водителя на production

После повторного пользовательского скриншота `https://driverform.ru/driver/shift/` выяснено, что production HTML уже обновлен, но открытая вкладка продолжала применять старую CSS-раскладку. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-inline-short-viewport-before-20260703_160551.tar.gz`. На сервер перенесен `templates/users/driver_shift.html` с inline critical responsive-слоем для `386x747`.

На сервере выполнен `manage.py check`, перезапущен `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA подтвердил: `https://driverform.ru/driver/shift/` и `https://driverform.ru/driver/` при viewport `386x747` отрисовывают title `Работа водителя`, inline-слой присутствует, body/shell `386x747`, overflow нет, иконка экскаватора `42x29`, круг `280x280` расположен выше нижней навигации, console errors 0. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\driver_prod_shift_inline_386x747.png`.

## Дополнение 03.07.2026: production-доводка Водителя под Samsung Galaxy S8+ 360x740

После скрина пользователя с DevTools-режимом `Samsung Galaxy S8+ 360x740` причина отличия была уточнена: на 360px ширины заголовок `Самосвал 10` не помещался в отведенную колонку и переносился на две строки. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-360-short-viewport-before-20260703_091850.tar.gz`. На сервер перенесен `templates/users/driver_shift.html` с дополнительным inline-блоком `@media (max-width: 370px) and (max-height: 800px)`.

На сервере выполнен `manage.py check`, перезапущен `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA на `https://driverform.ru/driver/?tab=work` при viewport `360x740` подтвердил: inline-блок активен, body/shell `360x740`, горизонтального и вертикального overflow нет, `Самосвал 10` в одну строку, заголовок не пересекается с online-индикатором, иконка экскаватора `42x29`, круг `274x274` выше нижней навигации, console errors 0. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\driver_prod_360x740_after_fix.png`.

## Дополнение 03.07.2026: production-механика PWA Водителя по ТЗ

На `driverform.ru` перенесена механика PWA Водителя по ТЗ и референсам: обновлены `templates/users/driver_shift.html`, `static/css/app.css`, `trips/models.py`, `trips/forms.py`, `trips/views.py`, `trips/urls.py`, `trips/migrations/0006_trip_actual_dump_point_trip_assigned_dump_point.py`, `users/views.py`, `users/tests.py`. Перед заменой созданы бэкапы `/srv/accounting-mvp/backups/code/driver-pwa-mechanics-before-20260703_095743.tar.gz` и `/srv/accounting-mvp/backups/db/driver-pwa-mechanics-before-20260703_095743.sql.gz`.

На сервере применена миграция `trips.0006_trip_actual_dump_point_trip_assigned_dump_point`, выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA подтвердил оба адреса `https://driverform.ru/driver/?tab=work` и `https://driverform.ru/driver/shift/?tab=work` при viewport `360x740`: главный экран без отдельной кнопки `Разгрузился` и блока `Активный рейс`, завершение рейса доступно через hold-форму, карточка `ЕХАТЬ` открывает выбор точки, текущая точка отмечена, overflow и console errors отсутствуют. Проверочные скрины сохранены в `C:\Users\swwba\AppData\Local\Temp\driver_prod_work_360x740_mechanics.png`, `C:\Users\swwba\AppData\Local\Temp\driver_prod_points_360x740_mechanics.png`, `C:\Users\swwba\AppData\Local\Temp\driver_prod_shift_url_360x740.png`.

## Дополнение 03.07.2026: Горный диспетчер `dispatcher-desktop-shell-v19` на production

На `driverform.ru` перенесена правка пульта Горного диспетчера под единый справочник `EquipmentState`: обновлены `trips/views.py`, `templates/trips/dispatcher_control.html`, `trips/tests.py`. Перед заменой создан code backup `/srv/accounting-mvp/backups/code/dispatcher-equipment-state-v19-before-20260703_170451.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичная проверка подтвердила: `https://driverform.ru/dispatcher-sw.js` отдает `dispatcher-desktop-shell-v19`; авторизованный `/dispatcher/control/` содержит `gd-equipment-states-data` с 15 состояниями, 53 плитки техники с `data-equipment-state`, несовпадений между состоянием и цветовым классом нет.

## Дополнение 03.07.2026: запрет выделения интерфейса на production

На `driverform.ru` добавлен общий CSS-слой, запрещающий выделение текстов, картинок и визуальных деталей интерфейса. Перед изменением создан backup `/srv/accounting-mvp/backups/code/no-ui-selection-before-20260703_104923.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA на `https://driverform.ru/driver/?tab=work` при viewport `360x740` подтвердил: `body` и `.driver-shell` имеют `user-select: none`, после протяжки мышью выделение пустое, картинка имеет `-webkit-user-drag: none`, поле логина сохраняет `user-select: text`, horizontal overflow и console errors отсутствуют. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\no_select_driver_prod.png`.

## Дополнение 03.07.2026: production-исправление hold-progress и selection-артефактов Водителя

На `driverform.ru` перенесена доработка центральной кнопки PWA Водителя: убрано остаточное выделение текста/прямоугольника внутри круглой кнопки и восстановлен видимый progress удержания рейса. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-hold-selection-before-20260703_105922.tar.gz`.

На сервер перенесены `templates/users/driver_shift.html` и `static/css/app.css`. Выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA на `https://driverform.ru/driver/shift/?tab=work` при viewport `360x740` подтвердил: активный статус `ГРУЖЕНЫЙ`, hold-кнопка есть, при удержании `--driver-hold=52%`, progress visible `opacity=1`, выделение пустое, `selectionBg=transparent`, `ringOutline=none`, horizontal overflow и console errors отсутствуют. Проверочные скрины сохранены в `C:\Users\swwba\AppData\Local\Temp\driver_prod_hold_progress_mid.png` и `C:\Users\swwba\AppData\Local\Temp\driver_prod_no_select_after_hold_fix.png`.

## Дополнение 03.07.2026: production PWA-контур Водителя `driver-mobile-shell-v1`

На `driverform.ru` перенесен отдельный PWA-контур Водителя: добавлены маршруты `/driver.webmanifest` и `/driver-sw.js`, template metadata, регистрация service worker со scope `/driver/`, update badge на вкладке `Путевка` и модалка явного применения обновления. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-pwa-v1-before-20260703_111742.tar.gz`.

На сервер перенесены `users/views.py`, `users/urls.py`, `templates/users/driver_shift.html`, `static/css/app.css`, `users/tests.py`. Выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичная проверка подтвердила: `/driver.webmanifest` отдает manifest `Водитель самосвала` со start_url `/driver/`, `/driver-sw.js` содержит `driver-mobile-shell-v1`, `SKIP_WAITING`, `/driver/` и `/driver.webmanifest`.

Production Browser QA на `https://driverform.ru/driver/` при viewport `360x740` подтвердил: manifest подключен, service worker зарегистрирован со scope `https://driverform.ru/driver/`, активный worker отвечает версией `driver-mobile-shell-v1`, badge и modal существуют и скрыты до появления новой версии, horizontal overflow и console errors отсутствуют. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\driver_prod_pwa_v1.png`.

## Дополнение 03.07.2026: production-адаптив Водителя `driver-mobile-shell-v3`

На `driverform.ru` перенесена адаптивная верстка PWA Водителя для разных экранов: обновлены `users/views.py`, `templates/users/driver_shift.html`, `static/css/app.css`, `users/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-adaptive-layout-v3-before-20260703_113411.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/driver-sw.js` отдает `driver-mobile-shell-v3` и `SKIP_WAITING`.

Production Browser QA на `https://driverform.ru/driver/` подтвердил адаптивную геометрию на viewport `360x740`, `386x747`, `390x844`, `430x932`, `1536x864`, `1536x1032`: shell занимает корректную ширину, title не пересекается с online-индикатором, карточки не пересекаются с кругом, круг не пересекается с нижней навигацией, подпись `Точковка` стоит на 18-20px ниже круга, horizontal/vertical overflow и console errors отсутствуют. Проверочные скрины сохранены как `C:\Users\swwba\AppData\Local\Temp\driver_prod_adaptive_v3_*.png`.

## Дополнение 03.07.2026: production-адаптив Водителя `driver-mobile-shell-v4`

На `driverform.ru` перенесена доводка адаптивной верстки PWA Водителя под высокий desktop/DevTools viewport: обновлены `users/views.py`, `templates/users/driver_shift.html`, `static/css/app.css`, `users/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-adaptive-layout-v4-before-20260703_114736.tar.gz`.

На сервере выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/driver-sw.js` отдает `driver-mobile-shell-v4` и `SKIP_WAITING`.

Production Browser QA на `https://driverform.ru/driver/` подтвердил адаптивную геометрию на viewport `1606x1032` и `360x740`: shell занимает корректную ширину, title не пересекается с online-индикатором, карточки не пересекаются с кругом, круг не пересекается с нижней навигацией, horizontal/vertical overflow и console errors отсутствуют. Проверочные скрины сохранены как `C:\Users\swwba\AppData\Local\Temp\driver_prod_adaptive_v4_desktop-screenshot-like-1606x1032.png` и `C:\Users\swwba\AppData\Local\Temp\driver_prod_adaptive_v4_s8-360x740.png`.

## Дополнение 03.07.2026: production-адаптив PWA Водителя `driver-mobile-shell-v8`

На `driverform.ru` перенесена финальная доводка адаптивной раскладки PWA Водителя после скринов с вкладками `Работа`, `Смена`, `Простои`, `Путевка`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-adaptive-layout-v8-before-20260703_191536.tar.gz`; обновлены `users/views.py`, `users/tests.py`, `templates/users/driver_shift.html`, `static/css/app.css`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/driver-sw.js` отдает `driver-mobile-shell-v8`, `SKIP_WAITING` и `/driver/`.

Production Browser QA подтвердил: `ГРУЖЕНЫЙ` помещается внутри центрального круга на `1920x1032` и `360x740`, title не пересекается с online-индикатором, рабочая панель не уходит под nav, вкладки `Смена`, `Простои`, `Путевка` прокручиваются внутри своей области, horizontal/vertical body overflow и console errors отсутствуют. Проверочные скрины сохранены как `C:\Users\swwba\AppData\Local\Temp\driver_prod_v8_wide_1920x1032_work.png` и `C:\Users\swwba\AppData\Local\Temp\driver_prod_v8_mobile_360x740_work.png`.

## Дополнение 04.07.2026: production-адаптив PWA Экскаваторщика `excavator-mobile-shell-v11`

На `driverform.ru` перенесена правка адаптивной раскладки рабочего места Экскаваторщика `/excavator/work/`: обновлены `static/css/app.css`, `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-adaptive-layout-v11-before-20260704-084449.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v11`.

Production Browser QA на `https://driverform.ru/excavator/work/` подтвердил: на `1920x1032`, `390x844`, `360x740` вкладки `Погрузка`, `Смена`, `Забой`, `События` имеют `bodyOverflowX=0`, `shellOverflowX=0`, нижнюю навигацию `display:grid`, console errors 0. Проверочный скрин широкого viewport сохранен в `C:\Users\swwba\AppData\Local\Temp\excavator_prod_wide_1920x1032_fixed.png`.

## Дополнение 04.07.2026: production-стиль кнопки смены Экскаваторщика `excavator-mobile-shell-v12`

На `driverform.ru` перенесена правка кнопки смены рабочего места Экскаваторщика `/excavator/work/`: обновлены `static/css/app.css`, `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-button-v12-before-20260704-090041.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v12`.

Production Browser QA на `https://driverform.ru/excavator/work/` при `1920x1032` подтвердил: вкладка `Смена`, кнопка `Завершить смену` имеет размер `362x54`, цвет текста `rgb(255, 81, 76)`, фон `rgba(119, 26, 31, .22)`, рамку `rgb(255, 69, 64)`, зазор до нижней навигации `0`, `bodyOverflowX=0`, `shellOverflowX=0`, console errors 0. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\excavator_shift_button_prod_v12.png`.

## Дополнение 04.07.2026: production-состояния самосвалов Экскаваторщика `excavator-mobile-shell-v13`

На `driverform.ru` перенесена правка источника состояния самосвалов на рабочем месте Экскаваторщика `/excavator/work/`: обновлены `trips/views.py`, `templates/trips/excavator_work.html`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-equipment-state-v13-before-20260704-105623.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v13`.

Production Browser QA на `https://driverform.ru/excavator/work/` подтвердил: самосвал `10` на экране Экскаваторщика отображается `assigned / Назначена / status-blue`, `data-eo-equipment-state="assigned"`, `draggable=true`, `bodyOverflowX=0`, `shellOverflowX=0`, console errors 0. Проверочный скрин сохранен в `C:\Users\swwba\AppData\Local\Temp\excavator_prod_states_v13.png`.

## Дополнение 04.07.2026: production-исправление статусов Водителя `driver-mobile-shell-v10`

На `driverform.ru` перенесена правка pending-состояния назначения Водителя: `ПРИНЯТЬ` больше не обрезается в желтом круге, кнопка подписана командой `Принять`, после принятия prompt исчезает, а принятый экскаватор остается в верхней карточке. Тем же обновлением закреплены финальные размеры для `ГРУЖЕНЫЙ`, чтобы поздние responsive-слои не возвращали обрезку текста.

Перед заменами созданы backup `/srv/accounting-mvp/backups/code/driver-pending-assignment-v10-before-20260704_084957.tar.gz` и `/srv/accounting-mvp/backups/code/driver-status-text-final-v10-before-20260704_085435.tar.gz`; обновлены `users/views.py`, `users/tests.py`, `templates/users/driver_shift.html`, `static/css/app.css`. На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/driver-sw.js` отдает `driver-mobile-shell-v10`.

## Дополнение 04.07.2026: production-фильтр мобильного гаража Горного мастера

На `driverform.ru` перенесена правка серверной выборки мобильного Горного мастера: `mobile_truck_garage_tiles` больше не включает самосвалы, уже назначенные в комплексы, независимо от состояния `EquipmentState`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/mining-master-mobile-garage-filter-before-20260704_033735.tar.gz`; обновлены `trips/views.py`, `assignments/tests.py` и проектные документы.

На сервере выполнен `.venv/bin/python manage.py check`, перезапущен `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA на `https://driverform.ru/mining-master/assignments/` при viewport `430x932` подтвердил: самосвалы из комплексов не пересекаются с вкладкой `Самосвалы` и гаражами экранов набора, console errors 0, horizontal overflow false.

## Дополнение 04.07.2026: production-ускорение рейсовых событий Горного мастера `v94`

На `driverform.ru` перенесена правка realtime-фильтра мобильного Горного мастера: событие `trip_changed` теперь считается релевантным и вызывает тихий refresh mobile shell сразу, без ожидания пассивной сверки. PWA Горного мастера поднята до `mining-master-mobile-shell-v94`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/mining-master-trip-changed-v94-before-20260704_035549.tar.gz`; обновлены `assignments/views.py`, `assignments/tests.py`, `templates/trips/dispatcher_control.html` и проектные документы.

На сервере выполнен `.venv/bin/python manage.py check`, перезапущен `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/mining-master-sw.js` отдает `mining-master-mobile-shell-v94`. Production Browser QA на `https://driverform.ru/mining-master/assignments/` при viewport `430x932` подтвердил `v94`, наличие `"trip_changed"` в shell, `realtimeMode=custom`, `realtimeScreen=mining-master`, console errors 0, horizontal overflow false.

## Дополнение 04.07.2026: production-справочник простоев Экскаваторщика `excavator-mobile-shell-v15`

На `driverform.ru` перенесено подключение PWA Машиниста экскаватора `/excavator/work/` к серверному ролевому справочнику простоев. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-downtime-reasons-v15-before-20260704_120016.tar.gz`; обновлены `downtimes/models.py`, `downtimes/admin.py`, `downtimes/forms.py`, миграция `downtimes/migrations/0003_alter_downtimereason_options_and_more.py`, `trips/views.py`, `templates/trips/excavator_work.html`, `trips/tests.py`, а также админские шаблоны/настройки справочников.

На сервере применена миграция `downtimes.0003_alter_downtimereason_options_and_more`. Первая попытка миграции на PostgreSQL выявила ошибку `cannot CREATE INDEX ... because it has pending trigger events`; миграция исправлена через `atomic = False` и успешно применена. После этого выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Серверные unit-тесты `trips.tests.ExcavatorWorkServerIntegrationTests` не запускались до конца из-за отсутствия у deploy-пользователя права PostgreSQL `CREATE DATABASE` для test DB.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: shell `excavator-mobile-shell-v15`, вкладка `События` содержит 12 кнопок простоев из `DowntimeReason.for_workplace('excavator_operator', текущий тип экскаватора)`, каждая кнопка имеет `data-eo-downtime-reason-id`, `data-eo-reason`, `data-eo-equipment-state`, локального списка `downtimeReasons` в HTML/JS нет, horizontal overflow и console errors отсутствуют. Временная production-причина `QA sync` с флагом `Показывать машинисту экскаватора` появилась в уже открытой вкладке после нажатия refresh как `reason_id=51`, `state=waiting`, `status-yellow`; после проверки удалена, production-справочник вернулся к 12 причинам.

## Дополнение 04.07.2026: production-сетка событий Экскаваторщика `excavator-mobile-shell-v16`

На `driverform.ru` перенесена доводка вкладки `События` PWA Экскаваторщика: причина `Перегон` больше не выводится отдельной широкой кнопкой, а занимает ячейку общей 3-колоночной сетки причин. Отдельный блок `.eo-event-actions` удален из разметки, disabled-состояние кнопки `Завершить простой` сделано серым и визуально недоступным.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-events-grid-v16-before-20260704_121838.tar.gz`; обновлены `trips/views.py`, `trips/tests.py`, `templates/trips/excavator_work.html`, `static/css/app.css`. На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичные файлы отдают `excavator-mobile-shell-v16`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: 12 плиток причин в одной сетке, `Перегон` находится в сетке с `data-eo-equipment-state="downtime"` и `status-red`, отдельного `.eo-event-actions` нет, `Завершить простой` имеет `disabled`, `aria-disabled`, класс `is-disabled`, цвет `rgba(176, 190, 198, 0.44)`, opacity `0.62`, horizontal overflow и console errors отсутствуют.

## Дополнение 04.07.2026: ручные сменные планы техники перенесены на demo

На общий демонстрационный сервер `77.91.93.47`, доступный через `demo.driverform.ru`, перенесен ручной контур сменных планов техники. Перед заменой создан backup `/srv/accounting-mvp/backups/code/shift-plans-before-20260704_053202.tar.gz`.

Обновлены runtime-файлы: `shifts/models.py`, `shifts/admin.py`, новый `shifts/services.py`, миграция `shifts/migrations/0003_shiftplan_equipmentshiftplan.py`, `users/views.py`, `reports/views.py`, `templates/reports/management_dashboard.html`, `templates/users/system_admin_references.html`, `templates/users/system_admin_reference_detail.html`.

На сервере выполнены `.venv/bin/python manage.py migrate shifts --noinput`, `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Миграция `shifts.0003_shiftplan_equipmentshiftplan` применена.

Публичная Browser QA на `https://demo.driverform.ru` под админским доступом подтвердила: в `/system-admin/references/` видны `Сменные планы` и `Планы техники`, прямые страницы `/system-admin/references/shift-plans/` и `/system-admin/references/equipment-shift-plans/` открываются и содержат нужные поля, desktop `1280x900` и mobile `390x844` без horizontal overflow и console errors. Витрина руководства `/reports/management/?date=2026-07-04` открывается без серверных ошибок.

## Дополнение 04.07.2026: production-сетка простоев Водителя `driver-mobile-shell-v13`

На `driverform.ru` перенесена доводка вкладки `Простои` PWA Водителя `/driver/` и `/driver/shift/`: причины простоев выводятся 3-колоночной плиточной сеткой, источник причин остается серверным `DowntimeReason.for_workplace('truck_driver', текущий_тип_техники)`, отправка идет через `reason_id`.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-downtime-grid-v13-before-20260704_083940.tar.gz`; обновлены `templates/users/driver_shift.html`, `static/css/app.css`, а в `users/views.py` точечно поднята версия `DRIVER_SHELL_VERSION` до `driver-mobile-shell-v13`. На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Production Browser QA на `https://driverform.ru/driver/?tab=downtimes` при viewport `390x844` и `360x740` подтвердил: `/driver-sw.js` отдает `driver-mobile-shell-v13`, DOM содержит 14 кнопок простоев из серверного справочника, каждая форма имеет `data-driver-downtime-reason-id` и hidden `reason_id`, сетка строится рядами `3/3/3/3/2`, вкладка скроллится вертикально, нижняя навигация видима, horizontal overflow 0, console errors 0.
## Дополнение 04.07.2026: production-настройка забоя Экскаваторщика `excavator-mobile-shell-v17`

На `driverform.ru` перенесена правка вкладки `Забой` PWA Машиниста экскаватора `/excavator/work/`: обновлены `trips/views.py`, `trips/urls.py`, `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-face-settings-v17-before-20260704_161209.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v17`. Серверные unit-тесты `trips.tests.ExcavatorWorkServerIntegrationTests` не были запущены до конца на production из-за отсутствия у deploy-пользователя PostgreSQL-права `CREATE DATABASE` для test DB; локально тот же набор из 23 тестов прошел успешно.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: авторизованный экран содержит shell `excavator-mobile-shell-v17`, вкладка `Забой` получает 10 пород из `RockType` и 6 точек из `DumpPoint`, `/excavator/work/settings/` сохраняет `Гор.75 / Блок 52` и выбранные `dump_point_ids=1,3`, после тихого refresh вкладка `Погрузка` показывает слоты `ККД` и `Отвал`, horizontal/vertical overflow и console errors отсутствуют.
## Дополнение 04.07.2026: production-исправление раскладки `Погрузка` Экскаваторщика `excavator-mobile-shell-v18`

На `driverform.ru` перенесена правка раскладки главной вкладки `Погрузка` PWA Машиниста экскаватора `/excavator/work/`: обновлены `trips/views.py`, `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-work-layout-v18-before-20260704_092304.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичные файлы содержат `excavator-mobile-shell-v18`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: при 1 назначенном самосвале грид получает `eo-truck-grid is-single`, карточка самосвала не пересекается с блоком точки разгрузки `ККД`, зазор между ними 10px, horizontal/vertical overflow и console errors отсутствуют.

## Дополнение 04.07.2026: production-исправление кнопки сохранения в `Планы техники`

На `demo.driverform.ru` перенесена точечная правка шаблона `templates/users/system_admin_reference_detail.html` для экрана `/system-admin/references/equipment-shift-plans/`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/equipment-plan-save-button-before-20260704_173418.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Production Browser QA под временным QA-админом подтвердил desktop `1920x900` и `1920x760`: кнопка `Сохранить запись` видна в viewport, поля правой формы прокручиваются внутри карточки, horizontal overflow 0, console errors 0. На mobile `390x844` кнопка доступна после прокрутки к форме, horizontal overflow 0. Временный доступ `+79099999999 / 109999` удален.

## Дополнение 04.07.2026: production-механика простоев Водителя `driver-mobile-shell-v15`

На `driverform.ru` перенесена доработка вкладки `Простои` PWA Водителя `/driver/` и `/driver/shift/`: старт и завершение простоя выполняются JSON-действиями без полной перезагрузки страницы и без верхних Django-сообщений `Простой завершен`. Механика синхронизирована с удачным паттерном Экскаваторщика: выбранная причина сразу включает активную карточку простоя, таймер, selected-состояние плитки и активную кнопку `Завершить простой`; закрытие простоя сбрасывает карточку, таймер и кнопку.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/driver-downtime-ajax-v15-before-20260704_092446.tar.gz`. При деплое восстановлен production `users/views.py` из backup после обнаружения несовместимого локального импорта `ShiftPlanScope`, затем внесена точечная серверная правка без переноса локальной схемы. На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/driver-sw.js` отдает `driver-mobile-shell-v15`.

Production Browser QA на `https://driverform.ru/driver/?tab=downtimes` при viewport `390x844` подтвердил: 14 причин простоев из серверного справочника, сетка рядов `3/3/3/3/2`, до старта `duration=00:00:00` и `closeDisabled=true`; после выбора `Ожидание погрузки` появляется `activeId=21`, `cardActive=true`, таймер дошел до `00:00:05`, `closeDisabled=false`, `selectedCount=1`; после закрытия `activeId=""`, `duration=00:00:00`, `closeDisabled=true`, `selectedCount=0`. Сообщения сверху отсутствуют, horizontal overflow 0, console errors 0. Серверный запуск `manage.py test users.tests.AccessLoginTests` остановился на `permission denied to create database`, потому что у production-пользователя PostgreSQL нет права `CREATE DATABASE` для test DB.

## Дополнение 04.07.2026: логика `Сменные планы` перенесена на `demo.driverform.ru`

После уточнения бизнес-логики планирования на основной сервер перенесена исправленная форма `/system-admin/references/shift-plans/`: общий план задается в м3 и не выглядит как обязательная ручная запись на каждую дату/смену с рейсами и тоннажем.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/shift-plan-scope-before-20260704_170240.tar.gz`. На сервер перенесены `shifts/models.py`, `shifts/admin.py`, `shifts/services.py`, `shifts/migrations/0004_shiftplan_plan_scope.py`, `users/views.py`, `templates/users/system_admin_reference_detail.html`. Применена миграция `shifts.0004_shiftplan_plan_scope`, выполнены `manage.py check`, `collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Production Browser QA на `https://demo.driverform.ru/system-admin/references/shift-plans/` под временным QA-админом подтвердил desktop `1280x900` и mobile `390x844`: HTTP 200, видны `Тип плана`, `План объема, м3`, `Месячный план`, `Суточный план`, `Дневная смена`, `Ночная смена`; отсутствуют `Дата смены`, `План рейсов`, `План тоннажа` и блок `Действия`; кнопка `Сохранить запись` видима, тестовая запись создается, horizontal overflow 0, console errors 0. Временная тестовая запись `QA Codex shift plan` и временный доступ `+79099999999 / 109999` удалены.

## Дополнение 04.07.2026: production-обновление простоев Экскаваторщика `excavator-mobile-shell-v20`

На `driverform.ru` перенесена точечная правка PWA Машиниста экскаватора `/excavator/work/`: `trips/views.py`, `templates/trips/excavator_work.html`, `trips/tests.py`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-downtime-effective-state-v20-before-20260704_121743.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v20`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердила: 12 причин простоев Экскаваторщика загружаются из серверного справочника, кнопки имеют `data-eo-downtime-reason-id`, состояние берется из `DowntimeReason.effective_equipment_state_code`, цвет - из `DowntimeReason.effective_color_group`. Штатные причины и `Перегон` отображаются как `waiting/yellow`, `ТО` и `Ремонт` - `orange`, `Поломка` - `breakdown/red`; horizontal/vertical overflow и console errors отсутствуют.

## Дополнение 05.07.2026: сменная аналитика подключена к витрине руководства на `driverform.ru`

На `driverform.ru` перенесен первый слой сменной аналитики и его подключение к витрине руководства. Перед заменой создан backup `/srv/accounting-mvp/backups/code/shift-analytics-management-before-20260705_034703.tar.gz`.

На сервер перенесены `reports/views.py`, `reports/urls.py`, `reports/shift_analytics.py`, `templates/reports/management_dashboard.html`, `templates/reports/shift_analytics.html`, include-шаблоны отчета сменной аналитики и `static/css/app.css`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Production Browser QA подтвердила: `https://driverform.ru/reports/management/?date=2026-07-05` на `1440x900` и `390x844` показывает блоки `Погрузка`, `Выгрузка`, `Открыто`, `Простои` и `Поток смены`; `https://driverform.ru/reports/shift-analytics/?date=2026-07-05` открывает отдельный отчет `Сменная аналитика`; horizontal overflow и console errors отсутствуют. Endpoints `/reports/management/export/` и `/reports/shift-analytics/export/` возвращают XLSX с HTTP 200.

## Дополнение 05.07.2026: realtime-обновление сменной аналитики на `demo.driverform.ru`

На `demo.driverform.ru` опубликована точечная правка `/reports/shift-analytics/`: экран выведен из общего ручного режима `/reports/*` и подключен как `management-shift-analytics / custom`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/shift-analytics-realtime-before-20260705_071555.tar.gz`.

На сервер перенесены `templates/base.html`, `templates/reports/shift_analytics.html`, `templates/reports/includes/management_report_theme.html`; выполнен `.venv/bin/python manage.py check`, перезапущен `accounting-mvp`, `accounting-mvp` и `nginx` active.

Production Browser QA на `https://demo.driverform.ru/reports/shift-analytics/?date=2026-07-05&shift_type=` подтвердил: HTTP 200, `body.dataset.realtimeScreen=management-shift-analytics`, `body.dataset.realtimeMode=custom`, `window.refreshShiftAnalyticsFromServer()` возвращает `{applied: true}`, видимый текст `Обновить` отсутствует, console errors 0, horizontal overflow 0.

## Дополнение 05.07.2026: production-обновление главного экрана Экскаваторщика `excavator-mobile-shell-v21`

На основной `driverform.ru` перенесена замена главной вкладки `Погрузка` PWA Машиниста экскаватора `/excavator/work/` на dashboard-дизайн. Перед заменой создан backup `/srv/accounting-mvp/backups/excavator-dashboard-20260705-082439`.

На сервер перенесены `trips/views.py`, `templates/trips/excavator_work.html`, `static/css/app.css`. Для `trips/views.py` использован патч от текущего production-файла, чтобы не переносить несвязанные локальные изменения диспетчерского блока. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v21`. Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: новая dashboard-вкладка активна, старая нижняя навигация `mm-mobile-bottom-nav` сохранена, макетный `bottom-nav` отсутствует, hardcoded `const trucks` отсутствует, вкладка `Смена` переключается штатно, horizontal/vertical overflow и console errors отсутствуют.

## Дополнение 05.07.2026: production-фикс слоя drag-карточки Экскаваторщика `excavator-mobile-shell-v22`

На основной `driverform.ru` перенесена точечная CSS-правка главной вкладки `Погрузка` PWA Машиниста экскаватора: перетаскиваемая карточка самосвала больше не уходит под слой зоны разгрузки. Перед заменой создан backup `/srv/accounting-mvp/backups/excavator-drag-layer-20260705-083533`.

На сервер перенесен `static/css/app.css`; версии в `trips/views.py` и `templates/trips/excavator_work.html` точечно подняты с `excavator-mobile-shell-v21` до `excavator-mobile-shell-v22`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active.

Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v22`. Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: `truckGridOverflow=visible`, `truckGridZ=4`, `dumpGridZ=1`, `dragZ=120`, drag-карточка визуально поверх зоны разгрузки, horizontal/vertical overflow и console errors отсутствуют.

## Дополнение 06.07.2026: кнопка выхода в PWA Экскаваторщика на `driverform.ru`

На основной `driverform.ru` перенесена кнопка `Выйти` на вкладке `Смена` PWA Машиниста экскаватора. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-logout-hold-v23-before-20260706_090757.tar.gz`.

На сервер перенесены `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/views.py`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v23`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `386x747` подтвердил: кнопка `Выйти` стоит рядом с кнопкой управления сменой, короткое удержание отменяется и остается на `/excavator/work/`, полное удержание 2 секунды переводит на `/` с полями `phone` и `access_code`; console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: production-фикс управления сменой Экскаваторщика `excavator-mobile-shell-v24`

На основной `driverform.ru` перенесено серверное действие `/excavator/shift/` для кнопки `Завершить смену` / `Начать смену` PWA Машиниста экскаватора. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-action-v24-before-20260706_092715.tar.gz`; после обнаружения PostgreSQL-регресса перед точечным исправлением создан дополнительный backup `/srv/accounting-mvp/backups/code/excavator-shift-postgres-lock-before-*.tar.gz`.

На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`, `trips/urls.py`, `static/css/app.css`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v24`.

При production QA первое закрытие смены выявило `500` на PostgreSQL: `FOR UPDATE cannot be applied to the nullable side of an outer join`. Причина устранена в `trips/views.py`: запрос закрытия смены использует `select_for_update(of=('self',))`, чтобы блокировать только `EmployeeShift`. Повторный production Browser QA на `https://driverform.ru/excavator/work/` при viewport `386x747` подтвердил POST `/excavator/shift/` `200 / shift_closed`, затем `200 / shift_opened`, корректную смену текста кнопки, console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: production-обновление удержания завершения смены Экскаваторщика `excavator-mobile-shell-v25`

На основной `driverform.ru` перенесена клиентская механика удержания кнопки `Завершить смену` в PWA Машиниста экскаватора. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-hold-v25-before-*.tar.gz`.

На сервер перенесены `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/views.py`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v25`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `386x747` подтвердил: короткое удержание `Завершить смену` показывает заполнение около 28%, возвращает кнопку в исходное состояние и не отправляет POST; полное удержание отправляет `/excavator/shift/` и получает `200 / shift_closed`; затем обычное нажатие `Начать смену` получает `200 / shift_opened`; console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: production-обновление удержания исполнительных действий Экскаваторщика `excavator-mobile-shell-v26`

На основной `driverform.ru` перенесена общая механика удержания для исполнительных кнопок PWA Машиниста экскаватора: `Применить настройки`, кнопки причин простоя и `Завершить простой`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-action-hold-v26-before-*.tar.gz`.

На сервер перенесены `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/views.py`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v26`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `386x747` подтвердил: короткие удержания `Применить настройки`, причины простоя и `Завершить простой` показывают заполнение 27-28%, сбрасываются и не отправляют POST; полные удержания отправляют серверные действия и получают `200` для `/excavator/work/settings/`, `200 / downtime_started`, `200 / downtime_closed`. После проверки активный простой закрыт, console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: production-обновление PWA-версии Экскаваторщика `excavator-mobile-shell-v27`

На основной `driverform.ru` перенесена правка окна обновления PWA Машиниста экскаватора: новая версия больше не отображается словом `new`, а определяется по реальному номеру `excavator-mobile-shell-v...` из waiting-service-worker или `/excavator-sw.js`. Экран также игнорирует версии чужих service worker и не показывает модалку без подтвержденной новой версии.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-pwa-version-v27-before-20260706_112651.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html` и `trips/views.py`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v27` и содержит ответ `GET_VERSION` через `event.ports && event.ports[0]`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: экран открывается под ролью Экскаваторщика, в HTML есть `excavator-mobile-shell-v27`, fallback `newVersion || "new"` отсутствует, `normalizeExcavatorVersion` и `renderUpdateModal` присутствуют, fallback на чужой `registration.active || navigator.serviceWorker.controller` отсутствует, модалка обновления скрыта без подтвержденной новой версии, текст `new` в модалке отсутствует, console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: production-отступ над нижними вкладками Экскаваторщика `excavator-mobile-shell-v28`

На основной `driverform.ru` перенесена правка PWA Машиниста экскаватора: между рабочей областью экранов и нижней навигацией добавлен единый зазор `10px`. Нижняя панель сохранила стандартную высоту `66px`, но ее grid-строка стала `calc(66px + var(--eo-bottom-nav-gap))`, поэтому кнопки, зона разгрузки и блоки простоев больше не прилипают к вкладкам.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-bottom-gap-v28-before-20260706_113812.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`; `static/css/app.css` изменен точечным серверным патчем только по правилам отступа. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v28`, публичный `/static/css/app.css` содержит `--eo-bottom-nav-gap: 10px`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: на вкладках `Погрузка`, `Смена`, `Забой`, `События` фактический зазор между последним видимым элементом экрана и верхом нижней панели равен `10px`; console errors 0, horizontal overflow false.

## Дополнение 06.07.2026: модальный выбор экскаваторов на вкладке динамики руководства

На `demo.driverform.ru` перенесена правка `/reports/management/dynamics/`: выбор экскаваторов больше не раскрывается inline-блоком внутри фильтров, а открывается компактным модальным окном. Перед заменой созданы серверные backup-архивы `management-dynamics-picker-before-*.tar.gz` и `management-dynamics-picker-css-before-20260706_1200.tar.gz` в `/srv/accounting-mvp/backups/code/`.

На сервер перенесены `templates/reports/management_dynamics.html` и `static/css/app.css`; выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` и `nginx` active, `nginx -t` успешен.

Production Browser QA на `https://demo.driverform.ru/reports/management/dynamics/` подтвердила: HTTP `200`, модальное окно выбора экскаваторов помещается в desktop `1366x768` и mobile `390x844`, кнопки `Отмена` и `Применить` видимы, horizontal overflow false, console errors 0, фильтры и KPI остаются отдельными рядами и не сдвигаются раскрытым списком.

## Дополнение 06.07.2026: production-концепт вкладки `Смена` Экскаваторщика `excavator-mobile-shell-v29`

На основной `driverform.ru` перенесен новый визуальный концепт вкладки `Смена` PWA Машиниста экскаватора. Экран разделен на зоны `Показатели техники`, `Забой` и `Точки разгрузки`; в блоке точек показывается выбранная основная точка и кнопка `+ Добавить` с подписью `Выбор из справочника`. Кнопки `Изменить`, выбранные точки и `+ Добавить` переводят на существующую вкладку `Забой`.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-concept-v29-before-20260706_121425.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`; `static/css/app.css` изменен точечным серверным патчем только по правилам новой вкладки `Смена`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v29`, публичный `/static/css/app.css` содержит `.eo-shift-dump-panel`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: вкладка `Смена` показывает `Показатели техники`, `Забой`, `Точки разгрузки`, выбранную `ККД` как основную и `+ Добавить`; кнопка `+ Добавить` переключает активную вкладку на `Забой`; вертикального скролла на вкладке `Смена` нет, horizontal overflow false, console errors 0.

## Дополнение 06.07.2026: production-правка вкладки `Смена` Экскаваторщика `excavator-mobile-shell-v30`

На основной `driverform.ru` перенесена корректировка вкладки `Смена` PWA Машиниста экскаватора: экран больше не содержит выбор или добавление точек разгрузки. Эти действия остаются во вкладке `Забой`, а `Смена` показывает read-only сводку зафиксированных показателей техники, забоя, точек разгрузки и итогов смены.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-readonly-v30-before-20260706_123357.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`; `static/css/app.css` изменен точечным серверным патчем только по CSS-блоку вкладки `Смена`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v30`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` подтвердил: вкладка `Смена` показывает `Показатели техники`, `Забой`, `Точки разгрузки`, `Итог смены`, `ККД` как основную точку и факт рейсов; кнопки `+ Добавить` и `Изменить` отсутствуют, `data-eo-open-face-settings` отсутствует, gap до нижней панели `10px`, horizontal overflow false, console errors 0.

## Дополнение 06.07.2026: production-уплотнение вкладки `Смена` Экскаваторщика `excavator-mobile-shell-v31`

На основной `driverform.ru` перенесена правка мобильного масштаба вкладки `Смена`: показатели техники снова отображаются компактной строкой из трех карточек, секции и цифровые значения уменьшены до рабочего размера, а поле `Порода` вынесено в широкую строку с переносом текста, чтобы название породы читалось полностью.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-compact-v31-before-20260706_132506.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`; `static/css/app.css` изменен точечными серверными патчами по CSS-блокам вкладки `Смена`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v31`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` и `386x747` подтвердил: `Негабарит` отображается полностью, `white-space: normal`, `text-overflow: clip`, показатели техники стоят в 3 колонках шириной около `109-111px`, gap до нижней панели `10px`, screen не заезжает под nav, horizontal overflow false, console errors 0.

## Дополнение 06.07.2026: production-правка строк показателей техники `excavator-mobile-shell-v32`

На основной `driverform.ru` перенесена правка блока `Показатели техники` вкладки `Смена`: `Топливо`, `Пробег` и `Моточасы` отображаются отдельными горизонтальными строками, где подпись стоит слева, а значение справа. Высота строк адаптивно использует свободное место, но не возвращает прежние гигантские карточки.

Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-shift-metric-rows-v32-before-20260706_134454.tar.gz`. На сервер перенесены `templates/trips/excavator_work.html`, `trips/views.py`; `static/css/app.css` изменен точечными серверными патчами по CSS-блокам вкладки `Смена`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v32`.

Production Browser QA на `https://driverform.ru/excavator/work/` при viewport `390x844` и `386x747` подтвердил: строки показателей отдельные, подписи видимы (`129-131px`), значения справа (`175-177px`), `Негабарит` отображается полностью, gap до нижней панели `10px`, screen не заезжает под nav, horizontal overflow false, console errors 0.
## Дополнение 06.07.2026: production-правка вкладки `Смена` Экскаваторщика `excavator-mobile-shell-v34`

На основной `driverform.ru` перенесена финальная правка вкладки `Смена` PWA Машиниста экскаватора. Перед заменой созданы серверные backup-архивы:

- `/srv/accounting-mvp/backups/code/excavator-shift-final-polish-v33-before-20260706_142914.tar.gz`;
- `/srv/accounting-mvp/backups/code/excavator-shift-result-gap-v34-before-20260706_143207.tar.gz`.

На сервер перенесены `trips/views.py`, `templates/trips/excavator_work.html`, `static/css/app.css`, `trips/tests.py`. Выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; `accounting-mvp` active, `nginx -t` успешен. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v34`.

Серверный запуск `trips.tests.ExcavatorWorkServerIntegrationTests` на production не выполнен из-за ограничения прав PostgreSQL на создание тестовой базы: `permission denied to create database`. Локально этот профильный класс тестов прошел полностью: 27 тестов OK.

Production Browser QA `https://driverform.ru/excavator/work/?qa_v=34` при viewport `390x844` подтвердил: вкладка `Смена` активна, дробных значений нет, показатели техники `104 л`, `1 704 км`, `1 264 м/ч`, строки показателей одинаковые `344x51`, порода `Негабарит` читается полностью, итог смены `ФАКТ 988 м³ / 20 МАШ.`, `ВЫЙТИ` нейтральная графитовая, gap до нижней навигации `10px`, horizontal overflow false, console errors 0.
# Дополнение 06.07.2026: опубликован `excavator-mobile-shell-v36` для PWA Экскаваторщика

На основной сервер `driverform.ru` перенесены файлы: `trips/views.py`, `trips/tests.py`, `templates/trips/excavator_work.html`, `static/css/app.css`, `static/css/excavator-work-v36.css`. Перед заменой создан серверный бэкап `/srv/accounting-mvp/backups/code/excavator-shift-v35-before-20260706_151950.tar.gz`; предупреждение бэкапа касалось только отсутствовавшего на тот момент нового CSS-файла.

Проверки и публикация: `manage.py check` на сервере прошел; запуск server-side tests ограничен правами PostgreSQL (`permission denied to create database`); выполнены `collectstatic --noinput`, restart `accounting-mvp`, `systemctl is-active accounting-mvp`, `nginx -t`. Публичный `https://driverform.ru/excavator-sw.js` отдает `excavator-mobile-shell-v36` и `/static/css/excavator-work-v36.css`.

Production browser QA выполнен под ролью Экскаваторщика на 386x747: после явного PWA-обновления с `v35` на `v36` экран `Смена` работает без horizontal overflow, shell имеет ширину 386px, кнопки и нижняя навигация разделены зазором 10px, console errors 0.
# Дополнение 06.07.2026: опубликован `excavator-mobile-shell-v37` с единым мобильным CSS-контрактом Экскаваторщика

На основной сервер `driverform.ru` перенесены файлы: `trips/views.py`, `trips/tests.py`, `templates/trips/excavator_work.html`, `static/css/excavator-work-v37.css`. Перед заменой создан серверный backup `/srv/accounting-mvp/backups/code/excavator-mobile-design-v37-before-20260706_155610.tar.gz`; предупреждение backup касалось только отсутствия нового CSS-файла до первой публикации.

Проверки и публикация: локально прошли `manage.py check`, `manage.py test trips.tests.ExcavatorWorkServerIntegrationTests` и Browser QA 390x844/386x747/360x740. На сервере прошел `.venv/bin/python manage.py check`, выполнены `collectstatic --noinput`, restart `accounting-mvp`, `accounting-mvp` active. Публичный `https://driverform.ru/excavator-sw.js` отдает `excavator-mobile-shell-v37` и `/static/css/excavator-work-v37.css`.

Production Browser QA на `https://driverform.ru/excavator/work/` под ролью Экскаваторщика подтвердила: для 10 назначенных самосвалов включен `eo-dashboard-truck-grid is-many`, карточки имеют компактный размер без наложений, topbar не обрезает значения, вкладки `Погрузка`, `Смена`, `Забой`, `События` не имеют horizontal/vertical overflow, gap до нижней навигации 8-10px, console errors 0.
## 06.07.2026 17:35 — PWA Экскаваторщика опубликована как `excavator-mobile-shell-v40`

На основной `driverform.ru` перенесена правка функционального распределения пространства PWA Машиниста экскаватора: `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`, `static/css/excavator-work-v40.css`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-mobile-v40-before-20260706_173051.tar.gz`; предупреждение касалось только отсутствия нового CSS-файла до первой публикации.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`, `nginx -t`. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v40` и `/static/css/excavator-work-v40.css`.

Production Browser QA на `https://driverform.ru/excavator/work/` подтвердила: при `390x844` 10 самосвалов отображаются карточками около `117.7x67.4px`, одиночная точка разгрузки около `148.5px`, нижний зазор до nav около `28.6px`; вкладка `Смена` держит действия над nav с зазором около `8.5px`; на `386x747` и `390x720` horizontal overflow false, console errors 0, карточки самосвалов не меньше `50px` по высоте.
## 06.07.2026 17:58 — PWA Экскаваторщика опубликована как `excavator-mobile-shell-v41`

На основной `driverform.ru` перенесена правка нижней навигации PWA Машиниста экскаватора: `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`, `static/css/excavator-work-v41.css`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-mobile-nav-v41-before-20260706_1758_nav_v41.tar.gz`; предупреждение касалось только отсутствия нового `excavator-work-v41.css` до первой публикации.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`, `nginx -t`. Публичный `/excavator-sw.js` отдает `excavator-mobile-shell-v41` и `/static/css/excavator-work-v41.css`; публичный CSS отвечает `200 OK`.

Production Browser QA на `https://driverform.ru/excavator/work/` подтвердил fixed/full-width навигацию на всех вкладках. `390x844`: nav `left=0`, `right=390`, `bottom=844`, `390x77.6px`, вкладка `97.5x77.6px`. `386x747`: nav `386x74px`, вкладка `96.5x74px`. `390x720`: nav `390x74px`, вкладка `97.5x74px`. `position=fixed`, `border-radius=0`, horizontal overflow false, console errors 0.

## 06.07.2026 18:30 - PWA Экскаваторщика опубликована как `excavator-mobile-shell-v42`

На основной `driverform.ru` перенесена правка верхней fixed-шапки PWA Машиниста экскаватора: `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`, `static/css/excavator-work-v42.css`. Перед заменой создан backup `/srv/accounting-mvp/backups/code/excavator-fixed-header-v42-before-20260706_182727.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; сервис `accounting-mvp` active. Публичный `/excavator-sw.js` содержит `excavator-mobile-shell-v42` и `/static/css/excavator-work-v42.css`; публичный CSS отвечает `200 OK`.

Production Browser QA на `https://driverform.ru/excavator/work/` под ролью Экскаваторщика подтвердила: на вкладках `Погрузка`, `Смена`, `Забой`, `События` header `position=fixed`, `top=0`, `width=390`, `height=53.6`; nav `position=fixed`, `width=390`, `height=77.6`, `bottom=844`; рабочая зона расположена между ними с gap `8px` до nav; horizontal overflow false, console errors 0.

## 07.07.2026 09:06 - PWA Экскаваторщика опубликована как `excavator-mobile-shell-v43`

На основной сервер `driverform.ru` перенесена правка scroll-контракта первого экрана PWA Машиниста экскаватора: `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`, `static/css/excavator-work-v43.css`. Перед заменой создан rollback backup `/srv/accounting-mvp/backups/code/excavator-v43-scroll-before-20260707-090433.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, перезапуск `accounting-mvp`; сервис `accounting-mvp` active. Публичный `/excavator-sw.js` содержит `excavator-mobile-shell-v43` и `/static/css/excavator-work-v43.css`; публичный CSS отвечает `200 OK`.

Production Browser QA `https://driverform.ru/excavator/work/` при `390x844`: после добавления 15 тестовых карточек `.eo-dashboard-screen` имеет `overflow-y: auto`, `clientHeight=696`, `scrollHeight=1947`, после прокрутки `scrollTop=1251`, `window.scrollY=0`; header остается `0..54`, nav остается `766..844`, nav width `390`, horizontal overflow `0`, console errors `0`.

## 07.07.2026 09:11 - production-публикация переключателя режима графика динамики

На основной сервер `driverform.ru` перенесена правка экрана `reports/management/dynamics/`: переключатель `Накопительно м3 / Темп м3/час / Рейсы` размещен внутри карточки графика и передает режим через параметр `chart_mode`.

Перед заменой создан rollback backup `/srv/accounting-mvp/backups/code/management-dynamics-chart-mode-before-20260707_090918.tar.gz`.

Перенесены файлы: `reports/shift_analytics.py`, `reports/views.py`, `reports/tests.py`, `templates/reports/management_dynamics.html`, `static/css/app.css`. На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, restart `accounting-mvp`, `nginx -t`. Сервис `accounting-mvp` active. Публичный `https://driverform.ru/static/css/app.css` отдает классы `.management-dynamics-chart-mode`, серверный шаблон содержит `data-management-dynamics-chart-mode`.
## 07.07.2026 10:02 - PWA Экскаваторщика опубликована как `excavator-mobile-shell-v44`

На основной сервер `driverform.ru` перенесена правка экрана `Погрузка` PWA Машиниста экскаватора: `templates/trips/excavator_work.html`, `trips/views.py`, `trips/tests.py`, `static/css/excavator-work-v44.css`. Перед заменой создан rollback backup `/srv/accounting-mvp/backups/code/excavator-loading-v44-before-20260707-100212.tar.gz`.

На сервере выполнены `.venv/bin/python manage.py check`, `.venv/bin/python manage.py collectstatic --noinput`, restart `accounting-mvp`; сервис `accounting-mvp` active. Публичный `/excavator-sw.js` содержит `excavator-mobile-shell-v44` и `/static/css/excavator-work-v44.css`; публичный CSS отвечает `200 OK`.

Production Browser QA `https://driverform.ru/excavator/work/` под ролью Экскаваторщика: при `390x844` реальные 10 самосвалов видимы без скролла, карточки около `117x82px`, зона самосвалов/разгрузки около `69.5%/29.8%`, одна точка разгрузки занимает всю ширину; при временной DOM-проверке двух точек ширины равны около `181/181px`, высота нижней зоны остается `207px`. При `360x720` карточки самосвалов около `108x70px`, одна точка шириной `338px`, horizontal/vertical overflow отсутствует, console errors 0.

## 14.07.2026 15:18 — опубликована компактная расстановка заместителя начальника участка

На основной `driverform.ru` архивом release `9192149f96fcecf3020d682583073e1ad86b94f8` перенесен актуальный Django backend с компактной шапкой `/deputy-mining-manager/`, фильтром бригад, карточками сотрудника и техники и центрированным DnD-бейджем. До изменения созданы и проверены rollback-копии `/srv/accounting-mvp/backups/code/deputy-compact-v2-before-20260714_081640.tar.gz` и `/srv/accounting-mvp/backups/db/deputy-compact-v2-before-20260714_081640.sql.gz`.

Новых миграций нет. Выполнены `manage.py check`, `makemigrations --check --dry-run`, `migrate --plan`, `collectstatic --noinput`, restart `accounting-mvp`, `nginx -t`; сервисы активны. Публичные CSS/JS совпадают с release и `staticfiles` по SHA-256, service worker отдает `deputy-mining-manager-desktop-shell-v2`, manifest отвечает `200`. Read-only production-рендер существующего черновика вернул `200` и 53 строки техники. Встроенный Browser QA заблокирован ошибкой среды Codex, поэтому визуальная и физическая DnD-приемка остаются отдельным пунктом.
