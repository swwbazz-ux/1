# 103. Первичный прогон P09–P13

Дата: 26.07.2026  
Статус: `NO-GO / ДЕФЕКТЫ ГОТОВЫ К ПЕРЕДАЧЕ`

## 1. Объем проверки

Проверены все 57 сценариев пяти пакетов документа 88:

- `P09-001…014` — сеть, два устройства, PWA и realtime;
- `P10-001…010` — время и календарные границы;
- `P11-001…013` — отчеты, Excel и детерминированный расчетный оракул;
- `P12-001…010` — права, старые сессии, CSRF и аудит;
- `P13-001…010` — перезапуск, восстановление, нагрузка и отказоустойчивость.

Проверка выполнялась по текущему локальному состоянию Django MVP. Использовались:

- статическая трассировка фактических серверных, JavaScript и service-worker путей;
- штатные Django- и JavaScript-тесты;
- реальные `TestClient POST` на автоматически создаваемых и уничтожаемых test DB;
- одноразовые изолированные тестовые модули;
- два отдельных Django-процесса с резервной и восстановленной временной SQLite-базой;
- детерминированные отчетные данные с ручным расчетом;
- многопоточный прогон 50 обработчиков realtime polling.

Рабочая база, production, `.env` и deploy не использовались и не изменялись. Файлы миграций не менялись; штатные миграции применялись только при создании временных test DB. Локальный рабочий сервер и браузер не запускались.

## 2. Итог

| Статус | Количество |
|---|---:|
| `PASS` | 37 |
| `PARTIAL` | 16 |
| `FAIL` | 4 |
| `UNDEFINED` | 0 |
| Всего | 57 |

По пакетам:

| Пакет | PASS | PARTIAL | FAIL | UNDEFINED |
|---|---:|---:|---:|---:|
| P09 | 4 | 8 | 2 | 0 |
| P10 | 10 | 0 | 0 | 0 |
| P11 | 11 | 1 | 1 | 0 |
| P12 | 9 | 0 | 1 | 0 |
| P13 | 3 | 7 | 0 | 0 |

Ключевые результаты:

1. Все десять временных сценариев P10 прошли реальные POST; в сценариях с отчетным результатом дополнительно сверены HTML и Excel. Границы `07:00/19:00`, месяц, год, 29 февраля, ночной рейс после полуночи, раннее завершение смены и отложенное переназначение работают по `Asia/Vladivostok`.
2. Детерминированный оракул P11 подтвердил правильные итоги завершенных рейсов, пересменку, фактическую точку, простои и равенство `Все = День + Ночь`.
3. Раздельные Django-процессы на восстановленной копии test DB сохранили открытую смену, рейс, простой, назначения, аудит и realtime-версию без нарушений FK.
4. Экран расстановки обработал 100 сотрудников и 30 единиц техники: HTTP `200`, 30 строк, 60 назначений, конфликтов `0`.
5. Для 1000 завершенных рейсов локальная сменная аналитика сформировалась за `0,836 с`, Excel — за `0,838 с`.
6. Пятьдесят параллельных Django polling-обработчиков завершились за `0,411 с`; все ответы `200`, одна итоговая версия, события строго монотонны.
7. Найдены два `FAIL` P09: realtime-клиент не завершает открытую PWA после серверного `401`, а service worker допускает смешение нового HTML со старым кэшированным JavaScript.
8. Найден `FAIL` P11: завершенный legacy-рейс с пустыми `volume_m3/tonnage` динамически пересчитывается по текущим справочникам, поэтому один и тот же исторический набор дал сначала `258/942`, затем `214/612`.
9. Найден `FAIL` P12: отмена и служебное завершение рейса принимают пустую причину и записывают аудит без объяснения действия.

Вердикт относится к пакетам P09–P13. Он не отменяет ранее подтвержденные рабочие цепочки, но до устранения P1 нельзя считать расширенную нештатную приемку завершенной.

## 3. P09 — сеть, два устройства, PWA и realtime

| ID | Статус | Фактический результат |
|---|---|---|
| `P09-001` | `PARTIAL` | При ошибке погрузки интерфейс откатывает pending-состояние и не показывает серверный успех; общий клиент показывает offline/stale. Физический обрыв до отправки на Android не воспроизводился. |
| `P09-002` | `PARTIAL` | После неуспешного ответа сохраняется тот же `client_action_id`; сервер возвращает ранее созданный рейс, один рейс и один action. Реальная потеря уже отправленного HTTP-ответа не выполнялась. |
| `P09-003` | `PARTIAL` | Повтор разгрузки по тому же ключу не меняет `completed_at` и не создает второй `TripClientAction`. Физический lost response не выполнялся. |
| `P09-004` | `PARTIAL` | Poll, `online`, `focus`, `pageshow`, `resume` и возврат видимости запускают сверку; Горный мастер дополнительно делает полный passive refresh. Реальные паузы 1/5/30 минут в фоне Android не выжидались. |
| `P09-005` | `PASS` | При 205 событиях endpoint вернул лимит 50, `events_truncated=true`, возрастающие версии и актуальную итоговую версию; рабочие экраны переходят к полному refresh. |
| `P09-006` | `PASS` | Сервер отдает события по версии, скачок `10 → 15` вызвал один custom refresh и применил версию 15; клиент не проиграл пачку в неверном порядке. |
| `P09-007` | `PARTIAL` | Две независимые Django-сессии подтвердили: новый хост активен, старый GET read-only, POST отклоняется, realtime сообщает `role_active=false`. Два физических телефона не проверялись. |
| `P09-008` | `PARTIAL` | Последовательные повторы с одинаковыми и разными ключами не удваивают факт. Настоящие одновременные вкладки входят в PostgreSQL-only набор и локально пропущены. |
| `P09-009` | `PASS` | Перед мутацией сервер повторно блокирует и проверяет сотрудника, назначение, смену и рейс; конкурирующие последовательные терминальные действия оставляют один итог. |
| `P09-010` | `FAIL` | После увольнения/блокировки poll получает `401`, POST — `409 inactive_role`, мутации нет. Но `realtime-client.js` превращает `401` в `null`: перехода на вход нет, `roleAccessActive` остается истинным. |
| `P09-011` | `PARTIAL` | Сброс PIN инвалидирует poll и POST. При изменении роли того же `EmployeeAccess` старый endpoint уже недоступен, но poll остается `authenticated=true`, `role_active=true` с новым кодом роли; старая PWA не переходит в read-only. |
| `P09-012` | `FAIL` | Навигация service worker использует network-first, а `/static/` — cache-first с `ignoreSearch:true`; новый HTML может получить старый JavaScript до принятия новой shell. Атомарного version/contract gate нет. |
| `P09-013` | `PARTIAL` | Новый GET после перезапуска экрана показывает фактический серверный `LOADED_WAITING_UNLOAD`. Физический force-close/relaunch Android не выполнялся. |
| `P09-014` | `PASS` | При клиентской зоне `Europe/Samara` UTC/Samara/Vladivostok дали одинаковые серверные производственную дату и смену Владивостока. |

Основные доказательства:

- `СИСТЕМА_MVP/backend/static/js/realtime-client.js:399-527`;
- `СИСТЕМА_MVP/backend/templates/base.html:206`;
- `СИСТЕМА_MVP/backend/templates/trips/excavator_work.html:620,1746-1765`;
- `СИСТЕМА_MVP/backend/templates/trips/dispatcher_control.html:1575-1639`;
- `СИСТЕМА_MVP/backend/core/views.py:55-92`;
- `СИСТЕМА_MVP/backend/users/views.py:275-324,1825-1943`;
- `СИСТЕМА_MVP/backend/users/test_chaos_active_role_and_pin.py`;
- `СИСТЕМА_MVP/backend/trips/test_chaos_trip_terminal_idempotency.py`;
- `СИСТЕМА_MVP/backend/core/production_time.py`;
- `СИСТЕМА_MVP/backend/core/tests_production_time.py`.

## 4. P10 — время и календарные границы

| ID | Статус | Фактический результат |
|---|---|---|
| `P10-001` | `PASS` | POST погрузки `18:59:59` и разгрузки `19:00:00` создали один переходящий рейс: loading=`day`, unloading=`night`, производственная дата обоих этапов `23.07.2026`; HTML и Excel дали по одному факту. |
| `P10-002` | `PASS` | POST погрузки `06:59:59` и разгрузки `07:00:00` дали один рейс: loading=`night` за `24.07`, unloading=`day` за `25.07`; ошибочная дата в горном отчете дала `0`. |
| `P10-003` | `PASS` | Реальный POST открытия смены ровно `07:00:00` открыл дневную смену с точным серверным `opened_at`. |
| `P10-004` | `PASS` | Реальный POST открытия смены ровно `19:00:00` открыл ночную смену с точным серверным `opened_at`. |
| `P10-005` | `PASS` | Погрузка `23:50`, разгрузка `01:30` следующего календарного дня относятся к производственной дате предыдущего дня; горный, транспортный и заказной HTML/Excel совпали. |
| `P10-006` | `PASS` | `31 января 23:50 → 1 февраля 01:30` осталось январским производственным фактом; дневные и месячные HTML/Excel не потеряли рейс. |
| `P10-007` | `PASS` | `31 декабря 23:50 → 1 января 01:30` осталось фактом `31.12`; декабрьский HTML/Excel содержит рейс, ошибочная дата нового года — нет. |
| `P10-008` | `PASS` | `29.02.2024 23:50 → 01.03.2024 01:30` осталось фактом февраля; дневные и месячные HTML/Excel совпали. |
| `P10-009` | `PASS` | Pending-переназначение в `18:56` с `effective_at=19:01` не применилось в `19:00:59`, применилось в `19:01`; разгрузка в `19:02` не переписала исходный Экскаватор и смену старого рейса. |
| `P10-010` | `PASS` | Фактическое закрытие дневных смен в `18:00` и ночных в `06:00` не изменило принадлежность уже выполненных рейсов; FK смен, HTML и Excel сохранены. |

Временной контракт:

- `СИСТЕМА_MVP/backend/core/production_time.py:10-45` — `Asia/Vladivostok`, границы `[07:00,19:00)`;
- `СИСТЕМА_MVP/backend/config/settings.py:189,193` — `TIME_ZONE='Asia/Vladivostok'`, `USE_TZ=True`;
- отчеты используют авторитетные `loading_shift.opened_at` и `unloading_shift.opened_at`;
- отложенное переназначение применяется по серверному `effective_at <= now` и не переписывает существующий `Trip`.

P10-009 динамически проверен через `19:00`; ветвь `07:00` использует тот же серверный числовой контракт, но отдельной динамической пробой не повторялась.

## 5. P11 — отчеты, Excel и расчетный оракул

Детерминированный набор на `20.07.2026`, `Asia/Vladivostok`:

- завершенные рейсы: `4 / 60 м3 / 150 т`;
- день: `2 / 30 м3 / 75 т`;
- ночь: `2 / 30 м3 / 75 т`;
- Самосвал №1: `3 / 60 м3 / 150 т`;
- Водитель А: `1 / 10 м3`; Водитель Б: `2 / 50 м3`; legacy без смены: `1 / 0`;
- Машинист А: `2 / 30 м3`; Машинист Б: `1 / 30 м3`;
- точка А: `40 м3`; фактическая точка Б: `20 м3`;
- открытый загруженный: `1`; отмененный: `1`; переходящий: `1`;
- простои: `2 / 1,5 ч`; день `1 / 1 ч`, ночь `1 / 0,5 ч`.

Оперативный слой намеренно отличается от завершенного факта: `loaded=5`, `unloaded=4`, `open=1`. Отмененный рейс не попал ни в один разрешенный итог.

| ID | Статус | Фактический результат |
|---|---|---|
| `P11-001` | `PASS` | Общий итог техники `3 / 60 / 150` сохранен целиком; Водитель А=`1/10`, Водитель Б=`2/50`. Экран и Excel совпали. |
| `P11-002` | `PASS` | Переходящий рейс учтен один раз: погрузка осталась за дневной сменой/Машинистом А, разгрузка — за ночной сменой/Водителем Б. |
| `P11-003` | `PASS` | Смена Диспетчера не изменила рейс и итог `4 / 60 / 150`; Диспетчер Б появился только автором своего действия. |
| `P11-004` | `PASS` | В завершенный производственный факт вошли ровно `4 / 60 / 150`; открытый отображается только в явно оперативном слое, отмененный исключен. |
| `P11-005` | `PASS` | Служебно завершенный рейс представлен одной строкой, в аудите ровно одно отдельное действие Диспетчера Б. |
| `P11-006` | `PASS` | Сохранено `assigned=A`, `actual=B`, совместимое `dump_point=B`; отчеты используют фактическую точку Б. |
| `P11-007` | `PASS` | После изменения вместимости и плотности завершенные факты остались `60 м3 / 150 т` на экранах и в Excel. |
| `P11-008` | `PASS` | Простой `18:30–19:30` целиком отнесен к дневной смене как `1 ч`; ночь содержит только отдельные `0,5 ч`. |
| `P11-009` | `PASS` | Реальное восстановление сотрудника сохранило старую закрытую смену и показания; исторический итог снова доступен. |
| `P11-010` | `PASS` | Горный итог `4=2+2`, транспортный `4=1+3`, оперативный `loaded 5=3+2`, `unloaded 4=1+3`, `258=129+129`, `942=471+471`, простои `1,5=1+0,5`. |
| `P11-011` | `PASS` | Для `hour/day/shift/month` KPI, сумма корзин и последний cumulative совпали: день=`129/3`, ночь=`129/2`. |
| `P11-012` | `FAIL` | Legacy-рейс с `volume=NULL`, `tonnage=NULL` пересчитывается текущими справочниками. Без изменения рейсов экран дал `258/942`, а свежий Excel после изменения справочников — `214/612`; транспорт назвал смену неопределенной, суточный отчет отнес тот же рейс к ночи. |
| `P11-013` | `PARTIAL` | Коррекция `777,77` видна Диспетчеру, Администратору и Руководителю; закрытая смена не переписана. Excel журнала Диспетчера и Руководства коррекцию не экспортирует. |

Основная причина `P11-012`:

- `СИСТЕМА_MVP/backend/reports/shift_analytics.py:111` вычисляет пустые исторические значения через текущие `TruckCapacityRule`, объем модели и плотность;
- обычные завершенные отчеты используют сохраненные значения рейса;
- разные fallback определения смены расходятся между отчетами.

Причина `P11-013`:

- UI получает коллекцию расхождений;
- `СИСТЕМА_MVP/backend/reports/views.py:1362` формирует Excel журнала из общих строк и не сериализует коррекции показаний.

## 6. P12 — права, старые сессии и аудит

| ID | Статус | Фактический результат |
|---|---|---|
| `P12-001` | `PASS` | Представительный прямой POST из чужой роли дал redirect/отказ без изменения рейса. |
| `P12-002` | `PASS` | Подмена ID чужого Самосвала/рейса не изменила объект. |
| `P12-003` | `PASS` | Повтор POST после logout не выполнил мутацию. |
| `P12-004` | `PASS` | Состояние сессии повторно проверяет активность доступа, роли и сотрудника; старая cookie после блокировки/увольнения не восстанавливает права. |
| `P12-005` | `PASS` | После переключения роли старые вкладки получают read-only; изменяющие серверные пути повторно проверяют активную роль. |
| `P12-006` | `PASS` | Служебное закрытие смены без причины отклоняется до мутации. |
| `P12-007` | `FAIL` | Отмена и служебное завершение рейса с `reason=''` выполняются. Аудит содержит автора, объект, время и итог, но причина остается пустой. |
| `P12-008` | `PASS` | Восстановление сотрудника создает отдельную административную запись восстановления. |
| `P12-009` | `PASS` | Обычный сотрудник не получает административные данные и не выполняет административную мутацию. |
| `P12-010` | `PASS` | POST без CSRF при `enforce_csrf_checks=True` получил `403`, объект не изменился. |

Точное воспроизведение `P12-007`:

- `СИСТЕМА_MVP/backend/trips/views.py:4782` и `4841` читают и очищают причину, но не проверяют пустое значение;
- `СИСТЕМА_MVP/backend/templates/trips/dispatcher_control.html:595,601` не помечает оба поля `required`;
- отмена переводит рейс в `CANCELLED`;
- служебное завершение переводит рейс в `COMPLETED`;
- обе записи `DispatcherActionLog.reason` равны пустой строке.

Для сравнения `dispatcher_service_close_shift_view` уже требует непустую причину на сервере.

## 7. P13 — перезапуск, восстановление и нагрузка

| ID | Статус | Фактический результат |
|---|---|---|
| `P13-001` | `PASS` | После завершения первого Django-процесса второй процесс открыл восстановленную test DB и увидел ту же открытую смену. |
| `P13-002` | `PASS` | После отдельного старта Django сохранился тот же `LOADED_WAITING_UNLOAD`, без второго рейса и смены статуса. |
| `P13-003` | `PASS` | Открытый простой сохранил `ended_at=NULL` и исходный `started_at=2026-07-26T10:29:41.479180+00:00`; длительность продолжает считаться от этой отметки. |
| `P13-004` | `PARTIAL` | Seed, backup и restored SQLite имели один SHA-256; смена, рейс, простой, два вида назначения и аудит согласованы, `PRAGMA foreign_key_check=0`. Настоящее восстановление PostgreSQL не выполнялось. |
| `P13-005` | `PARTIAL` | Новый Django-процесс выполнил realtime GET: HTTP `200`, версия `16`, события монотонны, фактические объекты доступны. Полный refresh реального JavaScript-клиента после рестарта не выполнялся. |
| `P13-006` | `PARTIAL` | 100 сотрудников и 30 Самосвалов дали 60 точных серверных назначений и 0 конфликтов. Серверный экран: HTTP `200`, 30 строк, HTML `110944` байт за `0,088 с`; payload поиска содержит правильные ФИО и гаражные номера. Фактический JavaScript-поиск в браузере не выполнялся. |
| `P13-007` | `PARTIAL` | 1000 рейсов дали ровно `40000 м3 / 100000 т`; отчет `0,836 с`, Excel `0,838 с`, файл `6497` байт. Ошибок нет, но формальный приемочный SLA и PostgreSQL-нагрузка не утверждены. |
| `P13-008` | `PARTIAL` | 50 параллельных Django TestClient poll завершились за `0,411 с`, все `200`, одна версия, порядок событий строгий. Это не заменяет 50 сетевых PWA-клиентов и PostgreSQL. |
| `P13-009` | `PARTIAL` | Предельные Unicode ФИО `255`, модель/порода/точка `128` сохранились полностью и присутствуют в HTML/Excel. Визуальная читаемость реальным браузером на узком экране не проверялась. |
| `P13-010` | `PARTIAL` | Искусственный отказ event-store после попытки разгрузки дал HTTP `500`; транзакция откатила статус, `completed_at` и `TripClientAction`, ложного успеха нет. Реальные отказы диска, PostgreSQL и процесса не вводились. |

SHA-256 трех файлов отдельной SQLite-проверки:

```text
seed.sqlite3     37313857A172172F2267E8653067F9496C0D0E63DFEC86A0EE4AC4D7A7725010
backup.sqlite3   37313857A172172F2267E8653067F9496C0D0E63DFEC86A0EE4AC4D7A7725010
restored.sqlite3 37313857A172172F2267E8653067F9496C0D0E63DFEC86A0EE4AC4D7A7725010
```

После проверки временные БД, настройки, scripts и тестовые модули удалены.

## 8. Карточки исправления

### QA-CHAOS-P1-P09-012

Название: новая HTML-shell может работать со старым кэшированным JavaScript.

Фактический результат:

- навигационный запрос получает новый HTML по network-first;
- `/static/` обслуживается cache-first с `ignoreSearch:true`;
- до активации/принятия новой shell старый service worker может вернуть JavaScript прежнего контракта;
- `controllerchange → reload` сокращает окно, но не обеспечивает атомарность.

Требуемый результат:

1. Экран не запускает изменяющий интерфейс при несовпадении версий HTML, JavaScript и серверного контракта.
2. Обновление либо атомарно активирует совместимый комплект, либо выполняет контролируемое обновление до разрешения действий.
3. Не возникает бесконечной перезагрузки, потери offline fallback или смешения кэшей разных ролевых PWA.
4. Для каждого затронутого PWA согласованно повышается shell/service-worker версия.

Обязательные регрессии:

- старая установленная shell + новый серверный HTML/JS;
- старая shell offline, затем online;
- обновление во время открытого рейса;
- два последовательных релиза;
- все ролевые кэши остаются изолированными.

### Расширение QA-CHAOS-P1-P06-015-P08-006 — P11-012

Название: исторический legacy-рейс с пустым фактом меняет итог от текущих справочников и определяется по-разному в отчетах.

Фактический результат:

- один и тот же завершенный рейс с `volume_m3=NULL`, `tonnage=NULL` пересчитывается при каждом построении;
- после изменения справочников итог изменился `258/942 → 214/612` без изменения рейса;
- транспортный и суточный отчеты расходятся по сменной принадлежности.

Требуемый результат:

1. Legacy fallback определяется один раз и не зависит от последующих изменений справочников.
2. Все экраны и Excel используют одну производственную дату, смену, объем и тоннаж.
3. Новые JSON и резервные HTML-пути создают одинаковое состояние и не порождают новые пустые завершенные факты.
4. Завершенная история не переписывается случайно при обычном просмотре отчета.
5. Если требуется одноразовое заполнение legacy-данных или миграция, это отдельно обосновывается и не запускается без разрешения.

Обязательные регрессии:

- отчет и Excel до/после изменения правила вместимости и плотности;
- `Все = День + Ночь`;
- одинаковая дата/смена в transport, daily, analytics и management;
- повторное формирование отчета не изменяет БД и цифры;
- новый рейс обоими путями не попадает в legacy fallback.

### QA-CHAOS-P2-P09-010

Название: realtime `401` не завершает открытую PWA-сессию.

Фактический результат:

- сервер правильно блокирует poll и POST;
- общий realtime-клиент поглощает `401`, не открывает вход и оставляет локальный экран в активном состоянии.

Требуемый результат:

1. Первый подтвержденный `401` переводит приложение в завершенное auth-состояние.
2. Готовые подтверждения, удержания и ожидающие мутации сбрасываются.
3. Пользователь получает понятное сообщение и переход на вход.
4. Повторные poll не создают цикл.

### QA-CHAOS-P2-P09-011

Название: изменение роли не инвалидирует уже открытую PWA того же `EmployeeAccess`.

Фактический результат:

- старый ролевой endpoint больше не принимает действие;
- realtime считает ту же сессию активной с новым кодом роли;
- старая вкладка не получает ожидаемый read-only/auth transition.

Требуемый результат:

1. Изменение роли обновляет login/session generation либо иной серверный revision.
2. Старый poll получает `role_active=false` или `401`.
3. Старый экран становится read-only, сохраняет безопасные live-обновления либо завершает сессию по единому правилу.
4. Ни один старый POST не получает новые полномочия.

### QA-CHAOS-P2-P11-013

Название: коррекции начальных показаний отсутствуют в Excel Диспетчера и Руководителя.

Фактический результат:

- UI трех разрешенных ролей показывает расхождение;
- закрытая смена не переписывается;
- соответствующие Excel не содержат эту историческую запись.

Требуемый результат:

1. Excel журнала Диспетчера и управленческого отчета содержат технику, предыдущую и новую смену, показатель, переданное и фактическое значения, сотрудника и время.
2. Коррекция остается информационной и не блокирует работу.
3. Экспорт не меняет старые показания.

### QA-CHAOS-P2-P12-007

Название: отмена и служебное завершение рейса допускают пустую причину.

Фактический результат:

- оба POST завершаются успешно с `reason=''`;
- статус рейса и realtime меняются;
- `DispatcherActionLog` не объясняет действие.

Требуемый результат:

1. Сервер отклоняет пустую или состоящую из пробелов причину до любой мутации.
2. Оба UI-поля обязательны и показывают понятную ошибку.
3. Аудит всегда содержит автора, причину, объект, время и итог.
4. Повтор/конкурирующее терминальное действие не создает дублирующий аудит.

Обязательные регрессии:

- пустая строка;
- только пробелы;
- допустимая причина;
- прямая отправка без браузерного `required`;
- отмена и служебное завершение;
- прежнее обязательное поле служебного закрытия смены не регрессирует.

## 9. Остаточные обязательные проверки

Нельзя объявлять пройденными без отдельной среды:

1. Реальный Android: потеря отправки/ответа, фон 1/5/30 минут, два телефона, старая shell, force-close в середине рейса.
2. Настоящая PostgreSQL-конкуренция P01, P06, P07 и P09.
3. Backup/restore и отказ транзакции на штатной тестовой PostgreSQL.
4. Пятьдесят сетевых PWA-клиентов, а не только параллельные Django-обработчики.
5. Согласованный SLA для отчетов на 1000 и большем количестве рейсов.
6. Визуальная Browser QA предельных строк на desktop и mobile.

Личная проверка владельцем всех PWA на реальном Android остается принятой физической проверкой обычного рабочего цикла, но не заменяет перечисленные специальные сетевые сценарии.

## 10. Выполненные проверки

P09:

- Django: `39/39`;
- адресный повтор: `13/13`;
- дополнительные наборы: `5/5` и `3/3`;
- Node read-only runtime: `6/6`;
- PostgreSQL concurrency: `8 skipped`;
- одноразовая DB: `205` событий, limit `50`, `events_truncated=true`.

P10:

- все `10/10` сценариев — реальные TestClient POST;
- для отчетных сценариев сверены HTML и Excel;
- дополнительный штатный набор: `9/9`;
- продуктовых дефектов не найдено.

P11:

- детерминированный оракул сверен с экранами и Excel;
- адресный штатный набор: `9/9`;
- рабочий путь Экскаватор → Водитель: `1/1`;
- воспроизведены `P11-012` и отсутствие коррекций в двух Excel.

P12:

- объединенный ролевой/аудитный набор: `101 OK`, `2 skipped`;
- одноразовые отрицательные пробы: `5/5`;
- CSRF дал `403`, чужая роль/ID/logout не изменили данные;
- два пустых reason фактически выполнили терминальные действия и подтвердили дефект.

P13:

- одноразовый пакет persistence/backup/scale/poll/failure: `8/8`;
- отдельный серверный экран 100×30: `1/1`;
- два отдельных Django-процесса seed/restore/verify — успешно;
- 1000 рейсов: точные `40000 м3 / 100000 т`;
- 50 параллельных poll: `50/50 HTTP 200`;
- искусственный отказ: HTTP `500`, полная транзакционная отмена.

Финальный контроль текущего локального дерева:

- полный `manage.py test` — `641/641 OK`, `13 PostgreSQL-only skipped`;
- `manage.py check` — без ошибок;
- `makemigrations --check --dry-run` — изменений нет;
- `git diff --check` — код возврата `0`, только информационные предупреждения Git о будущем `LF → CRLF`;
- временных файлов с `qa103/test_qa103` после очистки — `0`;
- слушателей порта 8000 — `0`.

## 11. Граница результата

Код продукта не изменялся. Создан только этот отчет и обновляются файлы прогресса.

Рабочая `db.sqlite3` до и после этапа:

```text
SHA-256 A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222
```

Все временные test DB, backup/restore-файлы, настройки, scripts и тестовые модули удалены. Порт 8000 не использовался.

Commit, push и deploy не выполнялись. Production, рабочая база, `.env` и миграции не затрагивались.
