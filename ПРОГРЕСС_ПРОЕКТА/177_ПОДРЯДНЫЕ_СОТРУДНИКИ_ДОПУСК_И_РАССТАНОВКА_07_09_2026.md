# Подрядные сотрудники: допуск, приложение и расстановка

Дата: 07.09.2026.

## Результат

На `driverform.ru` реализован единый контур подрядных сотрудников. Отдельный дублирующий
справочник людей не создаётся: подрядчик остаётся обычным `Сотрудником`, получает
действующую производственную специальность и роль, но дополнительно связан с
`Подрядной организацией` и ограничен датами допуска.

Система использует эту связь во всех ключевых точках:

- по номеру телефона на `/start/` распознаёт только сотрудника с действующим
  допуском и показывает ему только назначенное ролевое приложение;
- в расстановке показывает только действующих на выбранную дату сотрудников;
- не позволяет поставить сотрудника на технику другой подрядной организации;
- после окончания допуска перестаёт принимать PIN-сессию и исключает человека
  из кандидатов, не удаляя его историю;
- в карточке и реестре администратора показывает тип занятости, организацию и
  срок допуска.

## Рабочий сценарий администратора

1. В `Системный администратор → Справочники → Подрядные организации` создать
   организацию и при необходимости указать номер и срок договора.
2. В справочнике `Техника` у подрядной единицы снять признак собственной
   техники и выбрать её подрядную организацию.
3. В карточке сотрудника выбрать тип `Подрядчик`, организацию, даты допуска,
   производственную специальность и роль `Машинист экскаватора`.
4. Создать или активировать обычный доступ сотрудника и передать ему ссылку
   `/start/`. По номеру телефона система покажет приложение Экскаваторщика;
   отдельная ссылка на вручную выбранное приложение не нужна.
5. Заместитель начальника по горным работам включает сотрудника в расстановку
   на экскаватор той же организации. Несовпадение организаций блокируется на
   сервере, в том числе при обмене местами и публикации плана.

## Данные и ограничения

Добавлены:

- справочник `ContractorOrganization` с реквизитами договора и признаком
  активности;
- тип занятости и организация в `Employee`, а также начало и окончание допуска;
- организация-владелец для подрядной `Equipment`;
- миграции `users.0024` и `references.0008`.

Договор организации и персональный допуск имеют собственные сроки. Рабочим
считается только пересечение этих сроков с активностью организации, сотрудника,
роли и доступа. Для штатного сотрудника подрядные реквизиты запрещены; для
подрядчика организация и обе даты обязательны. Собственная техника не может
быть привязана к подрядной организации, а подрядная техника обязана иметь её.

## Проверка

- `manage.py check` — без замечаний;
- `makemigrations --check --dry-run` — новых незафиксированных изменений схемы
  нет;
- 8/8 новых тестов подрядной логики и 27/27 тестов актуальной двухместной
  расстановки пройдены на финальной production-интеграции;
- Browser QA выполнен на локальной одноразовой базе через штатный запуск:
  `1280×900` и `390×844`, без горизонтального переполнения; проверены реестр,
  карточка сотрудника, оба справочника и ссылка `/start/`;
- устранён старый ложный отчёт о незагруженном ресурсе от скрытого изображения
  с пустым `src` в карточке сотрудника.

Расширенные тесты исходной ветки содержат устаревшие ожидания относительно
фактического составного production-кода. Они не используются как критерий
данной публикации; профильные проверки подрядчиков и текущей расстановки
выполнены отдельно на восстановленном live-baseline.

## Граница публикации

Release `48e5b2643b6cc86f03fcb154b2f8babbb5dba3c6` отправлен в ветку
`codex/contractor-workforce-production-final-2026-09-07` и точечно опубликован
на production поверх актуального live-состояния `c718e385`. Параллельно
вышедшие изменения Экскаваторщика и двухместной расстановки сохранены.

Перед deploy созданы и проверены rollback-копии:

- код: `/srv/accounting-mvp/backups/code/contractor-workforce-20260907T045229Z-before-48e5b264.tar.gz`,
  SHA-256 `1eb55e1344db8a944f724e5561524007d4e0d42595f6942d48a83c931f98395d`;
- PostgreSQL: `/srv/accounting-mvp/backups/db/contractor-workforce-20260907T045229Z-before.dump`,
  SHA-256 `6fe273d242703e38df8051734becc291162e796867e2c1b803e9ca74b65daa7b`.

На production применены `users.0024` и `references.0008`, выполнены
`manage.py check`, `collectstatic`, restart `accounting-mvp` и `nginx -t`.
Все 19 runtime-файлов совпали с release-архивом; сервис активен,
`NRestarts=0`, `/start/` отвечает `200`, закрытые экраны администратора —
штатным `302`. Справочник организаций после миграции пуст: рабочие записи и
сотрудники не создавались автоматически. Следующий шаг — администратору
заполнить фактические организации, договоры, сотрудников и принадлежность
техники, затем принять полный сценарий на реальном Android машиниста.

## Исправление PostgreSQL-ошибки назначения 07.09.2026

После первого рабочего ввода подрядчика выявлена production-only ошибка:
блокировка `Employee` одновременно пыталась заблокировать необязательную строку
подрядной организации через `LEFT OUTER JOIN`. PostgreSQL возвращал
`FOR UPDATE cannot be applied to the nullable side of an outer join`, а POST
расстановки завершался HTTP 500.

Блокировка ограничена строкой сотрудника через `select_for_update(of=('self',))`.
Добавлен регрессионный тест на точный ORM-контракт. Пройдены 28/28 профильных
тестов расстановки, `manage.py check`, migration guard и фактический локальный
Browser QA назначения подрядчика на экскаватор в desktop `1280x900` и mobile
`390x844`; ошибок консоли и выхода документа за ширину нет. Широкий набор из
55 тестов имеет одну не связанную с правкой устаревшую проверку Deputy service
worker, которая ожидает `v14` при фактическом `v17`.

Исправление опубликовано commit `70e72a2df` точечной live-aware поставкой:
параллельные изменения production `assignments/services.py` сохранены, заменена
только проблемная строка. Миграций и collectstatic не было. Production-файл
имеет SHA-256 `3ef4c038fce911d7eeb56ec402fc81c5427687aa7cbe32cac0bc9c53288bcae2`,
`DEPLOYED_COMMIT=70e72a2d`, сервис активен, `NRestarts=0`, `/start/` отвечает
HTTP 200, закрытая расстановка — штатным HTTP 302.

Rollback-копии:

- код: `/srv/accounting-mvp/backups/code/contractor-placement-20260907T194300Z-before-70e72a2d/services.py`,
  SHA-256 `f151fe36c84d92929341c0c76e24905d31b31d741b72da9200e7a9057a2476b4`;
- PostgreSQL: `/srv/accounting-mvp/backups/db/contractor-placement-20260907T194300Z-before.dump`,
  SHA-256 `2b9ec19973de01e24c3a0c88c9e54d04579465e125d493e1919cc9678e1fecb8`.

После deploy production-транзакция назначения прошла и была контрольным образом
откачена. Затем Крицкий Владимир Викторович назначен в дневную смену на CAT 395
в черновике плана `73`; версия плана стала `2`. План не публиковался.
