# Production: сменные отчёты Диспетчера 06.09.2026

## Статус

Реализовано в ветке `codex/dispatcher-shift-reports-2026-09-06`, commit
`7d72e16804c84d638fc7fd2da479603b8f18b217`. Ветка отправлена в `origin`,
точный runtime-пакет опубликован на `driverform.ru`. Миграций нет; сама
публикация не изменяла рабочие записи.

## Что добавлено

Во вкладке Диспетчера `Отчёты` появились две отдельные формы:

1. `Итоги смены по самосвалам` — номер техники, количество рейсов,
   средневзвешенное плечо, объём, `м³×км`, примечания и простои. БелАЗ и NHL
   разделены привычной цветовой заливкой, техника без рейсов остаётся в форме.
2. `Работа выемочного оборудования` — порода, экскаватор, план, фактический
   объём, горизонт, блок, место разгрузки, плечо, простои и примечания.

Обе формы строятся при открытии из завершённых рейсов и пересекающихся со
сменой событий простоя. Доступны выбор даты и смены, экранный просмотр,
печать и отдельная числовая Excel-выгрузка в привычном табличном оформлении.

Перед выгрузкой экран показывает незакрытые рейсы, завершённые рейсы без
объёма или плеча и открытые простои. Диспетчер и Администратор могут раскрыть
исходный рейс, исправить объём, плечо, породу, фактическую разгрузку,
горизонт, блок, простой и примечание. Причина обязательна. Исправляется
первичная запись `Trip`, поэтому обе формы пересчитываются одинаково;
действие фиксируется в `DispatcherActionLog`. Руководитель имеет только
просмотр, Excel и печать.

## Расчётные правила

- рейсы — количество завершённых `Trip` выбранной смены;
- объём — сумма `Trip.volume_m3`;
- `м³×км` — сумма по каждому рейсу `volume_m3 × transport_distance_km`;
- плечо самосвала и итоговое плечо — `сумма м³×км / сумма м³`, округление до
  одного знака;
- место разгрузки — фактическое, затем назначенное, затем исходное;
- границы смен берутся только из `core.production_time`.

## Проверки

- новые целевые тесты: `6/6 OK`;
- регресс существующих отчётов: `24/24 OK`;
- статический release-контракт: `4/4 OK`;
- `manage.py check`: без ошибок;
- `makemigrations --check --dry-run`: изменений схемы нет;
- Browser QA: desktop `1280×800` и mobile `390×844`, переполнения страницы и
  ошибок консоли нет; широкая таблица прокручивается внутри своего блока.

## Production-публикация

Перед deploy подтверждено, что production имеет более новый маркер
`673a2ba5`, но целевые `reports/views.py`, `reports/urls.py` и `app.css`
побайтово совпадают с базой отчётов. `users/role_apps.py` на сервере уже
содержал требуемый статический релиз `ready-core-traffic-v100`, поэтому его не
заменяли. Это сохранило новые функции production, не относящиеся к отчётам.

Опубликованы только пять runtime-файлов: два новых файла формы/шаблона и
целевые `reports/views.py`, `reports/urls.py`, `static/css/app.css`.
Production-маркер установлен в
`7d72e16804c84d638fc7fd2da479603b8f18b217`.

Rollback-комплект:

- код: `/srv/accounting-mvp/backups/code/dispatcher-shift-reports-20260906T064953Z-before-7d72e168.tar.gz`, SHA-256 `a13e31616040a8fcd76e39833479005f07c86acf6bc140118c4c3cc5c3705645`;
- PostgreSQL: `/srv/accounting-mvp/backups/db/dispatcher-shift-reports-20260906T064953Z-before-7d72e168.dump`, SHA-256 `b5144a19afd49eb3b932cf831d64985bc3828ff4ada5cbfc975f8013b5855b85`.

После deploy: `manage.py check` без ошибок, новых миграций и операций нет,
`collectstatic` скопировал один CSS, `nginx -t` успешен,
`accounting-mvp`/nginx активны, `NRestarts=0`, предупреждений сервиса после
рестарта нет. Пять серверных файлов совпали с локальным релизом; SHA-256
source/staticfiles/public `app.css` одинаковый:
`4aa5f9cb712ad5f770db4822733fe10e689cfe30277cf53d4175adf5ed1ecd47`.

Авторизованная Browser QA на production проверила вкладку `Отчёты`, обе
формы и заполненный период 28.08.2026. На `1280×800` документ не имеет
горизонтального переполнения. На `390×844` документ остаётся шириной 390 px,
а таблица прокручивается только внутри блока `365 → 1080 px`; кнопки Excel и
печати видимы. Обе Excel-ссылки вернули HTTP `200` (`7421` и `6385` байт).
Кнопка печати активна и вызывает `window.print()`. Изменяющую корректировку
реального рейса в production не выполняли; текущая диспетчерская сессия была
read-only из-за входа на другом устройстве, а серверный контракт закрыт
целевыми тестами.

## Границы смен и открытый предметный вопрос

Действующий производственный календарь системы использует смены
`07:00–19:00` и `19:00–07:00`. В предоставленном образце выемочного отчёта
напечатано `08:00–20:00` и `20:00–08:00`. Опубликованная реализация
сознательно использует действующие границы системы, чтобы не смешивать рейсы
соседних смен. Если фактический регламент предприятия изменился, нужно
отдельно согласованно менять общий производственный календарь. Менять только
подпись отчёта без календаря нельзя.

## Компактная панель действий 06.09.2026

Кнопки `Excel` и `Печать` перенесены из отдельной пустой полосы в строку
фильтров даты и смены. Runtime commit `e63b25b8` отправлен в `origin` и
опубликован точным пакетом из шаблона и CSS; версия статики повышена до
`ready-core-traffic-v101`. Rollback-архив:
`/srv/accounting-mvp/backups/code/dispatcher-report-actions-20260906T071758Z-before-e63b25b8.tar.gz`,
SHA-256 `f3c93cd0e71ba3f318a9310b30cf9c9bedd0a14591fd57444dd0be0313875c6c`.
После deploy `check`, контроль миграций, `collectstatic`, restart и `nginx -t`
успешны; service-журнал чист. Production Browser QA подтвердила отсутствие
старой полосы и нахождение обеих кнопок внутри фильтров на desktop/mobile.

## Полная прокрутка длинной формы 06.09.2026

Устранено обрезание экранного отчёта примерно после самосвала №25. Причина
была в общем правиле пульта `overflow: hidden` у `body`: сама форма имела
полную высоту, но браузер не мог прокрутить документ. Для страниц сменных
отчётов добавлен отдельный класс `dispatcher-shift-report-screen` с обычной
вертикальной прокруткой; остальные экраны пульта не изменены. Версия статики
повышена до `ready-core-traffic-v102`.

Runtime commit `b2dd31b9` отправлен в `origin` и опубликован точечным пакетом
из шаблона, CSS и release-маркера. Rollback-архив:
`/srv/accounting-mvp/backups/code/dispatcher-shift-report-scroll-20260906T073233Z-before-b2dd31b9.tar.gz`,
SHA-256 `5e84e700da1d9d36a85b91c913487db8da443cccd08e41a022b4360cb24ca8da`.
Миграций и изменений данных нет. После deploy `check`, контроль миграций,
`collectstatic`, restart и `nginx -t` успешны; service-журнал чист.

Авторизованная production Browser QA подтвердила 54 строки техники, последнюю
строку `NHL №63` и строку `Итого`. На desktop документ прокручивается до
`scrollY=1143`; на ширине 390 px — до `scrollY=1474`, при этом ширина самой
страницы остаётся ровно 390 px, а широкая таблица прокручивается внутри
собственного контейнера.

## Живые фильтры даты и смены 06.09.2026

Лишняя кнопка `Показать` удалена. Поля `Дата` и `Смена` теперь сразу
отправляют GET-фильтр и заново формируют отчёт после изменения; рабочие данные
при этом не изменяются. Подсказка в панели фильтров объясняет это поведение.

Runtime commit `2c3d2586` отправлен в `origin` и опубликован одним шаблоном.
Rollback-архив:
`/srv/accounting-mvp/backups/code/dispatcher-live-report-filters-20260906T074229Z-before-2c3d2586.tar.gz`,
SHA-256 `79c7a60220e5e69d206e4b42599f896dd0195c8d6634efd1434e37da6b206747`.
Миграций и изменений данных нет. После deploy `check`, контроль миграций,
`collectstatic`, restart и `nginx -t` успешны; service-журнал чист.

Авторизованная production Browser QA подтвердила отсутствие кнопки и оба
автосценария: переключение `Дневная → Ночная` сразу изменило адрес и заголовок
на `19:00–07:00`, изменение даты на `05.09.2026` сразу переформировало отчёт.
На ширине 390 px оба фильтра видимы, горизонтального переполнения страницы нет.
