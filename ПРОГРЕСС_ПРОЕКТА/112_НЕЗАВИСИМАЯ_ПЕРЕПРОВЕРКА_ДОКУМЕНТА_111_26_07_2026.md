# Независимая перепроверка документа 111

Дата: 26.07.2026  
Основание: документ `111_ОТЧЕТ_О_ПОДГОТОВЛЕННЫХ_ИСПРАВЛЕНИЯХ_ЭТАПА_A_26_07_2026.md`  
Проверяемый контур: только четыре изменения этапа A, разрешенные документом 110  
Документ 108: не возобновлялся

## 1. Итог

Основные функциональные исправления этапа A подтверждены:

| Карточка | Независимый результат |
|---|---|
| `QA-CHAOS-P1-008-R1` | **PASS в разрешенной защитной границе** |
| `QA-CHAOS-P1-P02-016` | **ФУНКЦИОНАЛЬНЫЙ PASS**, найден отдельный неблокирующий UI-остаток |
| `QA-CHAOS-P2-P04-006` | **PASS**, найдено только косметическое замечание к пунктуации |
| `QA-CHAOS-P1-P04-009` | **ПОСЛЕДОВАТЕЛЬНАЯ ЧАСТЬ PASS / POSTGRESQL НЕ ПРОВЕРЕНО** |

Новых P1-блокеров для ограниченного пилота готового производственного ядра не найдено.

Контур Механика целиком не принимался. Проверен только защитный запрет,
разрешенный документом 110. Архивирование и восстановление сотрудников также
не входят в штатные операции первой пилотной волны.

## 2. Идентичность проверяемой версии

- ветка: `codex/new-public-site-ux-prototype`;
- HEAD: `e75922ce7d26c1b9cf4335d8c1af67064a40d440`;
- SHA-256 документа 111:
  `120C37D0AF246B5CD39CC4EDF923A67D7EB220C6874847D27F12E8ADEFCDCFA1`;
- все `14/14` файлов раздела 5 документа 111 совпали по размеру и SHA-256;
- отклонений отпечатка не найдено.

## 3. Автоматические проверки

Все команды выполнены независимо от чата исправлений:

| Проверка | Результат |
|---|---:|
| Точный Django-набор этапа A | `14 PASS / 1 PostgreSQL skip` |
| Расширенный Django-набор | `103 PASS / 1 PostgreSQL skip` |
| JavaScript-регрессии | `23/23 PASS` |
| Полный `manage.py test` | `681 tests: 667 PASS / 14 skipped / 0 failures` |
| `manage.py check` | PASS |
| `makemigrations --check --dry-run` | `No changes detected` |
| `git diff --check` | код `0` |

Все 14 пропусков полного набора относятся к тестам, которые штатно требуют
PostgreSQL. Они не объявлялись пройденными.

## 4. Безопасная Browser QA

Проверка выполнена на отдельной копии рабочей SQLite с отдельными временными
`MEDIA_ROOT` и `PORTAL_PRIVATE_MEDIA_ROOT`.

Для тестовых сущностей использован единый маркер `QA112`. Они существовали
только во временной базе. После проверки:

- сервер остановлен только `STOP_SERVER_MVP.bat`;
- порт 8000 свободен;
- временная база и временные каталоги удалены;
- временный settings-модуль и его `pyc` удалены;
- рабочая база и `private_media` не изменились.

Сервер запускался только `START_SERVER_MVP.bat`.

Размеры:

- desktop: `1280×720`;
- mobile: `390×844`.

Для проверенных страниц:

- `data-app-contract-version = pwa-contract-v1`;
- `ready = true`;
- `locked = false`;
- код роли соответствует приложению;
- глобального горизонтального overflow страницы нет;
- ошибок и предупреждений консоли нет.

На странице Механика таблицы на мобильном размере продолжают прокручиваться
внутри собственных контейнеров. Это не считается приемкой всего интерфейса
Механика и остается в отложенном контуре.

## 5. QA-CHAOS-P1-008-R1

Подтверждено:

1. у открытого простоя Самосвала в интерфейсе Механика отсутствуют форма и
   кнопка закрытия;
2. выводится пояснение `Закрывает только водитель`;
3. прямой POST Механика возвращает `403`;
4. `ended_at` простоя Самосвала не меняется;
5. событие аудита закрытия не создается;
6. простой Экскаватора Механик по-прежнему может закрыть;
7. разрешенное закрытие Экскаватора через браузер создало ровно одно закрытие
   и одну запись аудита.

Вердикт: **PASS в узкой защитной границе**.

Это не означает готовность полного рабочего места Механика к пилоту.

## 6. QA-CHAOS-P1-P02-016

Подтверждено:

1. прямой серверный unblock архивного сотрудника не меняет сотрудника,
   доступ, закрепление техники и аудит;
2. в карточке архивного сотрудника обычная кнопка разблокировки отсутствует;
3. штатная последовательность `Восстановить сотрудника → Разблокировать`
   работает;
4. активный временно заблокированный сотрудник разблокируется обычным путем;
5. закрепление `EquipmentAssignment` сохраняется.

После штатной последовательности:

- сотрудник: `active`;
- доступ: `activated`;
- закрепление: `accepted`;
- техника закрепления не изменилась.

### 6.1. Новый остаток интерфейса

Создана отдельная карточка:

`QA-CHAOS-P2-P02-016-R1`

Название: глобальная drag-and-drop-зона `Разблокировать` остается видимой для
архивного сотрудника и принимает перенос как молчащий no-op.

Фактическое поведение:

- на desktop и mobile общая зона `Разблокировать · Вернуть вход` видна;
- архивную строку можно перетащить в эту зону;
- запрос не выполняется и данные не меняются;
- пользователь не получает пояснения, почему действие не сработало.

Серверная защита корректна, поэтому исходный P1-дефект не воспроизводится.
Остаток относится только к понятности интерфейса.

Ожидаемое поведение при будущем исправлении:

1. при начале перетаскивания архивного, уволенного или удаленного сотрудника
   зона обычной разблокировки скрывается либо становится явно недоступной;
2. вместо молчащего no-op показывается пояснение
   `Сначала восстановите сотрудника`;
3. для активного временно заблокированного сотрудника зона остается рабочей;
4. JavaScript-регрессия подтверждает `fetch count = 0` для архива и ровно один
   запрос для активного заблокированного сотрудника;
5. поведение проверяется на `1280×720` и `390×844`.

Статус новой карточки: **BACKLOG / НЕ БЛОКИРУЕТ ПЕРВУЮ ВОЛНУ**, потому что
архивирование и восстановление исключены из штатных операций ограниченного
пилота документом 109.

## 7. QA-CHAOS-P2-P04-006

### 7.1. Водитель

При назначении на занятый Самосвал:

- видны гаражный номер и владелец открытой смены;
- форма начала смены недоступна;
- кнопка начала смены отключена;
- прямой POST не создает новую смену;
- HTML-ответ остается безопасным ответом формы со статусом `200`.

При назначении на свободный Самосвал:

- браузерное начало смены прошло;
- создана ровно одна открытая смена;
- создано ровно одно действие `driver_shift_opened`.

### 7.2. Машинист

При назначении на занятый Экскаватор:

- поля показаний отключены;
- кнопка начала смены отключена;
- показаны техника и владелец открытой смены;
- прямой JSON POST возвращает `409`;
- новая смена не создается.

При назначении на свободный Экскаватор:

- браузерное начало смены прошло;
- создана ровно одна открытая смена;
- создано ровно одно действие `excavator_shift_opened`;
- повтор с тем же `client_action_id` вернул `deduplicated = true`;
- число смен и действий после повтора осталось `1 / 1`.

Вердикт: **PASS**.

### 7.3. Косметическое наблюдение

Если ФИО владельца уже заканчивается точкой, сообщение добавляет еще одну
точку: например, `П.П..`.

Это не меняет смысл конфликта, не создает смену и не блокирует ограниченный
пилот. Рекомендованный статус: `P3 / BACKLOG`, исправить при следующей
адресной правке текста.

## 8. QA-CHAOS-P1-P04-009

Статически и исполняемыми тестами подтверждено:

1. после `IntegrityError` сначала проверяется существующее
   `ShiftClientAction`;
2. затем проверяется реально открытая смена техники;
3. затем проверяется реально открытая смена сотрудника;
4. `409` формируется только при подтвержденном конфликте;
5. при отсутствии подтверждения выполняется чистый повторный `raise` того же
   исходного исключения;
6. посторонняя ошибка базы не маскируется под занятость техники;
7. транзакция полностью откатывается.

Последовательная часть: **PASS**.

Настоящие две параллельные транзакции не запускались, потому что разрешенного
тестового PostgreSQL-окружения нет.

Окончательный статус:
**РЕАЛИЗОВАНО / POSTGRESQL НЕ ПРОВЕРЕНО**.

## 9. PWA-ресурсы

Все восемь ресурсов вернули HTTP `200` с корректным content type:

- `/system-admin.webmanifest`;
- `/system-admin-sw.js`;
- `/driver.webmanifest`;
- `/driver-sw.js`;
- `/excavator.webmanifest`;
- `/excavator-sw.js`;
- `/mechanic.webmanifest`;
- `/mechanic-sw.js`.

Подтвержденные shell-версии:

| Роль | Shell |
|---|---|
| Системный администратор | `system-admin-shell-v18` |
| Водитель | `driver-mobile-shell-v106` |
| Машинист | `excavator-mobile-shell-v121` |
| Механик | `mechanic-shell-v6` |

Все четыре service worker содержат обработчик `GET_VERSION`.

## 10. Контроль неизменности рабочих данных

До и после независимой перепроверки:

### Рабочая SQLite

- размер: `2584576` байт;
- mtime UTC: `2026-07-23T17:11:26.0764680Z`;
- SHA-256:
  `A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222`.

### `private_media`

- файлов: `78`;
- общий размер: `3042` байта;
- SHA-256 манифеста:
  `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35`.

Рабочие данные не изменились.

## 11. Решение для дальнейшей работы

1. Не возобновлять документ 108 целиком.
2. Считать три функциональных исправления этапа A подтвержденными.
3. Считать последовательную часть `P04-009` подтвержденной, но оставить
   отдельную PostgreSQL-приемку.
4. Перенести `QA-CHAOS-P2-P02-016-R1` и косметическую двойную точку в
   неблокирующий backlog.
5. Не возвращать Механика и архивные операции в первую волну.
6. Следующий основной шаг — короткий целевой R05 только по готовому
   производственному ядру:
   Системный администратор, ОУП, Заместитель начальника горного участка,
   Диспетчер, Горный мастер, Машинист, Водитель и Руководство.

## 12. Публикация

- изменения продукта после получения документа 111 не выполнялись;
- создан только настоящий независимый QA-документ 112 и обновлены файлы
  прогресса;
- commit: не выполнялся;
- push: не выполнялся;
- deploy: не выполнялся;
- production: не затрагивался.

Проверять результат следует в текущем локальном Django MVP после запуска
`START_SERVER_MVP.bat`. Для повторения Browser QA необходимо использовать
только изолированную копию базы и временные media-каталоги.
