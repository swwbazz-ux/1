# Полная недельная KPI-симуляция полным составом

Дата проверки: **29.07.2026**.

## 1. Итоговый статус

Полная изолированная симуляция семи дневных и семи ночных смен завершена.

Основной производственный и KPI-контур:

**PASS С ОДНИМ ПОВТОРНО ПОДТВЕРЖДЁННЫМ P1**.

Подтверждено:

- полный тестовый штат принят, активирован и связан с одним утверждённым
  составом вахты;
- Заместитель начальника участка опубликовал расстановки по технике,
  дневной и ночной смене;
- отработаны `14` смен: `7` дневных и `7` ночных;
- все `53` самосвала выполняли от `17` до `23` рейсов за каждую смену;
- все `8` экскаваторов участвовали в производственном цикле;
- завершено `14 840` рейсов;
- рассчитано `836 545,00 м³` и `1 850 733,07 т`;
- все рейсы, смены, переходящие рейсы, показания и отчёты согласованы;
- все `742` закрытые водительские смены связаны с календарной вахтой;
- создано `742` неизменяемых диагностических паспорта Водителей;
- очередь формирования паспортов не содержит ошибок;
- официальный рейтинг, баллы, места и веса не включились;
- полный PostgreSQL-набор: `872/872 OK`, пропусков нет;
- реалистичный одновременный HTTP-soak `67` сессий: `PASS`.

Повторно подтверждён ранее известный дефект:

`QA-CHAOS-P1-P03-021` / сценарное подтверждение `P07-013` — после закрытия
смены система не создаёт ожидаемый открытый простой `ЕТО/ТО`.

Это не нарушило рейсы, объёмы, отчёты или сохранение KPI-паспортов, но
операционная логика завершения смены не соответствует решению владельца и
требует исправления с независимой перепроверкой.

## 2. Граница проверки

Проверка выполнена только локально.

| Параметр | Значение |
|---|---|
| ветка | `codex/ready-core-pilot-2026-07-27` |
| исходный HEAD | `71f02b3058617fbff7935fced0178cccb96aacb2` |
| QA-СУБД | PostgreSQL `16.14` |
| QA-база | `copper_week_kpi_qa_20260729` |
| безопасное подключение | `127.0.0.1:55432`, локальный пользователь `copper_qa_runner` |
| период | производственные даты `20.07.2026–26.07.2026` |
| часовой пояс | `Asia/Vladivostok` |
| дневная смена | `07:00–19:00` |
| ночная смена | `19:00–07:00` |
| run id | `QA-WEEK-KPI-20260729` |
| маркер сотрудников | `ТЕСТ_НЕДЕЛЯ_KPI_20260729` |

Пароли в документ не записываются.

Новая база создана отдельно. Старая QA-база
`copper_week_qa_20260727`, рабочая SQLite и production не использовались как
приёмник тестовых записей.

Экспорт или копирование тестовой базы не выполнялись. База сохранена локально
только для независимой перепроверки.

## 3. Реальные справочники и синтетические факты

Справочники загружены из ранее подготовленной обезличенной копии:

`C:\Users\swwba\AppData\Local\Temp\copper-week-qa-20260727\production_reference_fixture_nobom.json`

SHA-256:

`DB55234E164EEA54B344E22CD6F8EE7235E37CE1B2E5C0387AD7BA9455FD7D0A`.

Использованы фактические записи справочников:

- `53` активных самосвала;
- `8` активных экскаваторов;
- `8` активных пород;
- `7` точек разгрузки;
- `16` правил кубатуры;
- фактические модели техники, плотности пород и правила расчёта объёма.

Два ранее подтверждённых дубля `Окисленная` и `Рыхлая` деактивированы только
в новой QA-базе. Канонические записи `Окисленная руда` и `Рыхлая порода`
остались активны. Источник справочников, рабочая база и production не
изменялись.

Рейсы, смены, сотрудники, простои и показания являются синтетическими
QA-фактами. Этот прогон нельзя выдавать за реальную производственную вахту
или использовать для утверждения нормативов и весов KPI.

Подтверждённого справочника плеч маршрутов в доступных данных нет.
`transport_distance_km` не выдумывался.

## 4. Полный тестовый штат

Создано и активировано `256` маркированных сотрудников:

| Роль | Количество |
|---|---:|
| Администратор | 1 |
| ОУП | 1 |
| Заместитель начальника участка | 1 |
| Руководитель | 1 |
| Диспетчер | 4 |
| Горный мастер | 4 |
| Машинист экскаватора | 32 |
| Водитель самосвала | 212 |
| **Итого** | **256** |

Каждый доступ активирован через штатную цепочку первичного PIN и постоянного
PIN.

Созданы:

- один `WatchComposition` — утверждённый состав;
- один связанный `WatchPeriod`;
- структурное членство всех `256` сотрудников в составе;
- `14` опубликованных планов;
- `854` опубликованных слота расстановки.

Состав вахты не выводился из графика, номера бригады, свободного текста или
дат приезда.

## 5. Выполненные смены

Рейсы по сменам:

| № | Производственная дата | Смена | Рейсов |
|---:|---|---|---:|
| 1 | 20.07.2026 | день | 1 054 |
| 2 | 20.07.2026 | ночь | 1 058 |
| 3 | 21.07.2026 | день | 1 062 |
| 4 | 21.07.2026 | ночь | 1 066 |
| 5 | 22.07.2026 | день | 1 063 |
| 6 | 22.07.2026 | ночь | 1 060 |
| 7 | 23.07.2026 | день | 1 057 |
| 8 | 23.07.2026 | ночь | 1 054 |
| 9 | 24.07.2026 | день | 1 058 |
| 10 | 24.07.2026 | ночь | 1 062 |
| 11 | 25.07.2026 | день | 1 066 |
| 12 | 25.07.2026 | ночь | 1 063 |
| 13 | 26.07.2026 | день | 1 060 |
| 14 | 26.07.2026 | ночь | 1 057 |
| **Итого** | | | **14 840** |

Итоговые смены по рабочему месту:

| Рабочее место | Смен |
|---|---:|
| Водитель | 742 |
| Машинист экскаватора | 112 |
| Диспетчер | 14 |
| Горный мастер | 14 |
| ОУП | 1 |

Открытых смен после прогона: `0`.

Длительность водительских смен:

- минимум: `40 491 с` — `11:14:51`, предусмотренный досрочный сценарий;
- среднее: `42 563,12 с` — примерно `11:49:23`;
- максимум: `42 600 с` — `11:50:00`.

Каждый самосвал выполнил ровно `280` завершённых рейсов за неделю.

## 6. Производственные факты

| Показатель | Результат |
|---|---:|
| завершённые рейсы | 14 840 |
| открытые рейсы после недели | 0 |
| переходящие рейсы | 13 |
| объём | 836 545,00 м³ |
| тоннаж | 1 850 733,07 т |
| расхождения показаний пересменки | 26 |
| корректировки пробега | 13 |
| корректировки моточасов | 13 |
| события простоев | 542 |
| открытые простои после недели | 0 |

Состав простоев:

| Причина | Количество |
|---|---:|
| Ожидание самосвалов | 428 |
| Перегон экскаватора | 112 |
| Заправка | 1 |
| Подготовка забоя | 1 |

Переходящие рейсы зачтены только смене разгрузки. Двойного зачёта нет.

Переданные простои Водителя и Машиниста сохраняют автора и закрываются
сменщиком без переписывания исходной смены.

## 7. Вахта и диагностические паспорта KPI

Связь с вахтой:

| Проверка | Результат |
|---|---:|
| закрытые водительские смены-кандидаты | 742 |
| связаны с выбранным `WatchPeriod` | 742 |
| без вахты | 0 |
| связаны с другой вахтой | 0 |
| выбранная вахта вне периода | 0 |
| `linkage_ready` | `true` |

Формирование паспортов:

| Проверка | Результат |
|---|---:|
| outbox-запросы | 742 |
| статус `completed` | 742 |
| `pending` | 0 |
| `processing` | 0 |
| `failed` | 0 |
| snapshots | 742 |
| ревизия 1, ровно одна на смену | 742 |

Повторная команда восстановления без `--force`:

```text
Обработано: 0; ошибок: 0; паспортов текущей версии: 742.
```

Сумма паспортов совпала с производственной базой:

- `14 840` рейсов;
- `836 545,00 м³`;
- `1 850 733,07 т`.

Во всех payload:

- `official=false`;
- `official_rating_eligible=false`;
- отсутствуют запрещённые ключи `score`, `place`, `weight`;
- совпадают `WatchPeriod` и `WatchComposition`;
- fingerprints источника и результата имеют корректный формат;
- причина снимка — штатное закрытие смены Водителем;
- запоздалых снимков нет.

## 8. Почему формула KPI всё ещё удержана

Все `742` смены корректно накоплены, но намеренно не объявлены готовыми к
официальной формуле:

| Причина удержания | Результат |
|---|---:|
| рейсы без подтверждённого плеча для м³·км | 14 840 |
| рейсы без подтверждённого плеча для т·км | 14 840 |
| смены с `unexplained_time` | 742 |
| закрытые смены с переходящим открытым рейсом | 13 |
| пригодные к официальной формуле смены | 0 |

Покрытие классифицированным временем:

- минимум: `35,07%`;
- среднее: `42,38%`;
- максимум: `53,32%`.

Это не потеря рейсов и не ошибка хранения паспорта. Это честный
диагностический результат:

- плечо перевозки отсутствует в доступном структурном справочнике;
- промежуток между разгрузкой и следующей погрузкой не имеет отдельного
  подтверждённого серверного источника;
- искусственно заполнять эти значения запрещено;
- дополнительные ручные комментарии от Водителя не вводятся.

Следовательно, текущая неделя подтверждает механизм сбора и накопления KPI,
но не является основанием для утверждения весов, баллов и мест.

## 9. Отчёты и Excel

Основной независимый oracle:

- `65` проверок — `PASS`;
- `1` проверка — неблокирующий `FAIL`;
- блокирующих падений — `0`.

Пройдены:

- отчёты Диспетчера;
- сменная аналитика;
- суточный отчёт;
- отчёт Руководителя;
- недельная динамика;
- недельный отчёт по объёмам;
- сверка рейсов, объёмов и тоннажа;
- `14` сменных Excel;
- `7` суточных Excel;
- `1` недельный Excel;
- всего `22` Excel-файла.

Единственный неблокирующий `FAIL` — повторное подтверждение
`QA-CHAOS-P1-P03-021`.

## 10. Одновременная нагрузка

Проверено `67` одновременно авторизованных сессий:

- `53` Водителя;
- `8` Машинистов;
- Администратор;
- ОУП;
- Заместитель начальника участка;
- Руководитель;
- Диспетчер;
- Горный мастер.

### 10.1. Экстремальный профиль: realtime каждую секунду

| Показатель | Результат |
|---|---:|
| длительность | 90 с |
| запросы | 5 694 |
| HTTP 200 | 5 686 |
| кратковременно отвергнутые соединения | 8 |
| доля отказов | 0,14% |
| realtime p95 | 852,817 мс |
| realtime p99 | 1 790,215 мс |
| страницы p95 | 5 314,948 мс |
| итог | `FAIL` |

Локальный Django development server продолжил работу, исключений приложения
и HTTP 500 не было. Отказы имеют вид Windows `10061` при перегружающем
профиле примерно `63,27 запроса/с`.

Этот результат фиксирует предел локального development server. Он не
объявляется дефектом production без отдельного production-like soak
nginx/gunicorn.

### 10.2. Повышенный реалистичный профиль: realtime раз в пять секунд

| Показатель | Результат |
|---|---:|
| длительность | 90 с |
| запросы | 1 158 |
| HTTP 200 | 1 158 |
| ошибки | 0 |
| realtime p95 | 367,628 мс |
| realtime p99 | 798,730 мс |
| страницы p95 | 2 478,712 мс |
| максимум | 2 673,928 мс |
| итог | `PASS` |

Этот профиль всё ещё заметно чаще штатного оптимизированного обмена и
подтверждает достаточный запас для полного состава на локальном контуре.

## 11. Полный PostgreSQL-набор

```text
Found 872 test(s).
System check identified no issues (0 silenced).
Ran 872 tests in 127.280s
OK
```

Дополнительно:

- `manage.py check` — успешно;
- `makemigrations --check --dry-run` — `No changes detected`;
- `git diff --check` — успешно;
- синтаксис трёх QA-инструментов проверен;
- тестовая PostgreSQL test-база после набора удалена штатно.

## 12. Повторно подтверждённый P1

### `QA-CHAOS-P1-P03-021` — не создаётся автоматический простой ЕТО/ТО

Статус:

**ПОВТОРНО ВОСПРОИЗВЕДЁН / ТРЕБУЕТ ИСПРАВЛЕНИЯ**.

Факт недельной симуляции:

- проверено `14` завершений смен;
- в каждой смене участвовал полный комплект из `61` единицы техники;
- две единицы в отдельных сценариях были исключены из автоматической
  постановки из-за передачи уже открытого простоя сменщику;
- суммарно ожидалось `852` автоматических maintenance-состояния;
- обнаружено `0`;
- каждая из `14` проверок получила `passed=false`;
- после недели открытых простоев `ЕТО/ТО` нет.

Ожидаемое правило владельца:

- после штатного закрытия смены Водителя или Машиниста техника автоматически
  переводится в открытый простой `ЕТО` либо `ТО`;
- если сотрудник передаёт сменщику уже открытый простой, исходное событие
  сохраняется и не заменяется дубликатом;
- параметры техники и показания закрытой смены сохраняются;
- повторный запрос закрытия не создаёт второй простой;
- начало следующей смены и первая разрешённая производственная операция
  должны обрабатывать maintenance-состояние по единому серверному правилу.

Критерии перепроверки:

1. обычное закрытие смены Водителя;
2. обычное закрытие смены Машиниста;
3. служебное закрытие уполномоченным сотрудником;
4. закрытие при уже открытом передаваемом простое;
5. повтор того же `client_action_id`;
6. одновременное закрытие на PostgreSQL;
7. открытие следующей смены;
8. первая разрешённая производственная операция;
9. отсутствие дублей и сохранность показаний;
10. повторный двухсменный и недельный QA-срез.

Новой дублирующей карточки создавать не нужно. Исправлять и закрывать следует
существующий `QA-CHAOS-P1-P03-021`.

## 13. Артефакты

Основной каталог:

`C:\Users\swwba\AppData\Local\Temp\copper-week-kpi-qa-20260729`

| Файл | SHA-256 |
|---|---|
| `preflight.json` | `DAD17065C3B4B7DC249B6110F675D639A87C6B0CF53B1D64DF591C762E323998` |
| `generation_summary.json` | `69E4F6DD8B0909310ECEF9F0E88946EE935105EE3BD25B9D51F5A135412B209C` |
| `verification_report.json` | `71421D669B6E2917945D633B800306495813E4C46B2F3D87D3EF5AC956CF0DB5` |
| `driver_passport_audit.json` | `E9A859DE95B661EAFD494019541213CA9ADE2A453B91DE1A54ED47007134D031` |
| `driver_watch_observation.json` | `6C9676F1B783B56B377C786CD1EA3A46ACE5AF679D8328A2AB9F5D66288D46B1` |
| `run_metadata.json` | `05FEB7F85C3656B3DDF5640DD907387CB22E9E987F777AC8DA4FADF728669F86` |
| `action_log.jsonl` | `DF686DDF167720CDB27717D70AE8C43E9C4B163E93AA74CAE1FE79A055983EB5` |
| `trip_manifest.jsonl` | `15C0807B028ED219BB998F3F1A33D77FF97E560AA76D628236D4704829539C23` |

Экстремальный soak:

`C:\Users\swwba\AppData\Local\Temp\copper-week-kpi-http-soak-20260729`

| Файл | SHA-256 |
|---|---|
| `summary.json` | `2EE4011F29A485FD2DCE7309E66F456B0542C0879EC7FC01FD767F5F1A4D3D7E` |
| `http_requests.jsonl` | `D196AFBA3661ACC5196BC234C10C9C7E6DA23925636D20A29CC8DF9F25F6778E` |

Повышенный реалистичный soak:

`C:\Users\swwba\AppData\Local\Temp\copper-week-kpi-http-soak-5s-20260729`

| Файл | SHA-256 |
|---|---|
| `summary.json` | `890A3CF0B48C71FEDD4A324AE21A55A4F5CC8883071D4242A53986129E130160` |
| `http_requests.jsonl` | `AEF0A10ADA946EDBE741612626DBCB89418BC305B0E7CD2731712395DD21CA15` |

## 14. Неизменность рабочих данных

После симуляции:

| Контроль | Результат |
|---|---|
| рабочая SQLite, размер | `2 584 576` байт |
| рабочая SQLite, SHA-256 | `A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222` |
| `private_media`, файлов | `78` |
| `private_media`, байт | `3042` |
| `private_media`, SHA-256 manifest | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |
| `.env` | не изменён |
| рабочие миграции | не применялись |
| production | не изменялся |
| deploy | не выполнялся |
| commit | не выполнялся |
| push | не выполнялся |
| порт 8000 | свободен |

## 15. Вердикт

Система способна:

- принять полный состав;
- опубликовать недельные расстановки;
- провести полный производственный цикл всей техники;
- накопить рейсы, м³ и тонны по каждому Водителю;
- сохранить связь каждой смены с утверждённым составом и календарной вахтой;
- сформировать отдельный неизменяемый паспорт каждой смены;
- построить отчёты Диспетчера и Руководителя;
- выдержать полный состав одновременно при повышенном реалистичном HTTP-профиле.

До следующего этапа необходимо:

1. исправить существующий `QA-CHAOS-P1-P03-021`;
2. независимо перепроверить автоматический `ЕТО/ТО`;
3. не включать официальный рейтинг до появления подтверждённого плеча
   маршрута, решения по необъяснённому времени и анализа полноценной реальной
   водительской вахты;
4. отдельно выполнить production-like soak nginx/gunicorn перед массовым
   пилотом, если требуется доказать предел именно production-инфраструктуры.

Текущий результат остаётся локальным. Он не закоммичен, не отправлен в remote
и не развёрнут в production.
