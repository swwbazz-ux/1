# Демонстрационный сценарий MVP производственного контура

Дата создания: 17.06.2026

## Цель сценария

Показать первый рабочий цикл системы без лишней теории:

1. сотрудник входит в единое приложение по своему коду доступа;
2. система открывает интерфейс по роли;
3. водитель открывает смену;
4. горный мастер назначает самосвал под экскаватор;
5. водитель подтверждает назначение;
6. машинист экскаватора создает рейс;
7. водитель завершает рейс;
8. диспетчер видит процесс на пульте;
9. механик видит и сопровождает механический простой;
10. отчет по объемам, суточный отчет и витрина руководства собираются автоматически.

Главная мысль показа: данные больше не собираются вручную в конце смены, а появляются в системе там, где реально возникает событие.

## Участники демонстрации

### Администратор

Задача: показать, что система управляется централизованно.

Что можно показать:

- сотрудники;
- роли;
- доступы;
- техника;
- породы;
- точки разгрузки;
- правила кубатуры;
- шаблоны отчетов.

### Водитель самосвала

Задача: показать минимальное количество действий водителя.

Сценарий:

1. водитель входит по своему коду;
2. при первом входе выбирает смену, самосвал и место проживания;
3. открывает смену;
4. подтверждает назначение кнопкой `Принял`;
5. видит активный рейс;
6. после разгрузки нажимает `Выполнено`;
7. закрывает смену, указав топливо, пробег и моточасы на конец.

Что важно проговорить:

- водитель не считает объемы;
- водитель не выбирает сложные параметры отчета;
- система сама фиксирует время событий;
- если рейс остается активным на пересменке, смена все равно может быть закрыта.

### Горный мастер

Задача: показать расстановку техники.

Сценарий:

1. горный мастер входит по своему коду;
2. выбирает экскаватор;
3. выбирает самосвал;
4. создает назначение;
5. водитель видит это назначение у себя.

Что важно проговорить:

- горный мастер формирует рабочую связку `экскаватор - самосвал`;
- водитель не назначает себе технику сам после первичной регистрации;
- назначение становится рабочим после подтверждения водителем.

### Машинист экскаватора

Задача: показать создание рейса там, где начинается груз.

Сценарий:

1. машинист входит по своему коду;
2. видит принятые назначения самосвалов;
3. выбирает самосвал;
4. выбирает породу;
5. выбирает точку разгрузки;
6. отправляет самосвал в рейс.

Что важно проговорить:

- объем рейса считается автоматически;
- тоннаж считается автоматически, если в справочнике породы есть плотность;
- рейс попадает водителю как активный;
- рейс сразу становится видимым диспетчеру.

### Диспетчер

Задача: показать, что диспетчер видит процесс, а не собирает его вручную.

Сценарий:

1. диспетчер входит по своему коду;
2. открывается диспетчерский пульт;
3. видит активные незакрытые рейсы;
4. видит назначения, ожидающие подтверждения;
5. видит принятые назначения;
6. видит последние выполненные рейсы;
7. фильтрует пульт по самосвалу или экскаватору;
8. открывает отчет по объемам.

Что важно проговорить:

- диспетчер не переписывает данные из разных источников;
- пульт нужен для контроля смены в процессе;
- отчет нужен для итогов и выгрузки.

### Механик

Задача: показать, что механические простои не остаются текстом в отчете, а становятся отдельными событиями механической службы.

Сценарий:

1. механик входит по своему коду;
2. видит технику, где уже есть признак простоя;
3. выбирает причину механического простоя;
4. открывает событие простоя;
5. видит открытые простои, критичность и длительность;
6. закрывает простой после устранения проблемы;
7. видит недавно закрытые простои.

Что важно проговорить:

- экскаваторщик не обязан разбираться во всех видах ремонтов;
- механическая служба сама классифицирует механические причины;
- диспетчер и руководство видят открытые механические простои в своих интерфейсах;
- суточный отчет и Excel-выгрузка получают отдельный блок механических простоев.
- отдельный отчет по простоям позволяет отфильтровать события и выгрузить их в Excel.

### Руководство

Задача: показать управленческую ценность.

Что можно показать на текущем этапе:

- общий объем по выполненным рейсам;
- общий тоннаж;
- выполненные рейсы;
- переходящие рейсы;
- суточный план/факт/отклонение;
- распределение объемов по экскаваторам;
- распределение объемов по породам;
- открытые механические простои;
- быстрый переход к диспетчерскому пульту, отчету по объемам и суточному отчету.

## Главный демонстрационный цикл

### Шаг 1. Вход водителя

Код демо-доступа: `2000`

Ожидаемый результат:

- открывается интерфейс водителя;
- если первичная регистрация уже пройдена, водитель видит смену;
- если смена закрыта, можно открыть новую.

### Шаг 2. Вход горного мастера

Код демо-доступа: `4000`

Ожидаемый результат:

- открывается экран назначения самосвала под экскаватор;
- создается назначение;
- у водителя появляется уведомление о назначении.

### Шаг 3. Подтверждение водителем

Ожидаемый результат:

- водитель нажимает `Принял`;
- назначение становится принятым;
- его можно использовать для создания рейса.

### Шаг 4. Вход машиниста экскаватора

Код демо-доступа: `3000`

Ожидаемый результат:

- машинист создает рейс;
- выбирает породу и точку разгрузки;
- система автоматически считает объем и тоннаж.

### Шаг 5. Пульт диспетчера

Код демо-доступа: `5000`

Ожидаемый результат:

- диспетчер видит активный рейс;
- диспетчер видит открытые механические простои;
- может отфильтровать по самосвалу или экскаватору;
- после завершения рейса видит его в последних выполненных.

### Шаг 6. Экран механической службы

Код демо-доступа: `7000`

Ожидаемый результат:

- механик видит технику, где уже есть простой;
- механик открывает механический простой с причиной;
- механик видит длительность открытого простоя;
- после закрытия простой попадает в историю недавно закрытых.

### Шаг 7. Завершение рейса водителем

Ожидаемый результат:

- водитель нажимает `Выполнено`;
- рейс становится выполненным;
- рейс появляется в отчете.

### Шаг 8. Отчет по объемам

Ожидаемый результат:

- отчет показывает объемы;
- отчет показывает тоннаж;
- отчет показывает смену загрузки и смену разгрузки;
- переходящий рейс помечается отдельно;
- отчет можно выгрузить в Excel;
- если выбран шаблон отчета, выводятся только столбцы шаблона.

### Шаг 9. Суточный отчет заказчику

Ожидаемый результат:

- отчет показывает день, ночь и сутки;
- отчет показывает план, факт, отклонение, тоннаж и рейсы;
- отчет показывает механические простои за выбранную дату;
- Excel-выгрузка содержит производственные данные и механические простои.

### Шаг 10. Отчет по механическим простоям

Ожидаемый результат:

- отчет показывает открытые и закрытые механические простои;
- можно отфильтровать по датам, статусу, технике и причине;
- отчет показывает суммарную длительность;
- отчет выгружается в Excel.

### Шаг 11. Витрина руководства

Код демо-доступа: `6000`

Ожидаемый результат:

- руководство видит суточный срез;
- видит накопленную картину по объемам;
- видит открытые механические простои;
- может перейти в суточный отчет, отчет по объемам, диспетчерский пульт и экран механиков.

## Что нужно улучшить перед показом

1. Сделать визуальный стиль экранов единым и более презентабельным.
2. Подготовить стартовые демо-данные одной кнопкой или отдельной командой. Выполнено: создана команда `seed_demo_scenario`.
3. Сделать отдельную страницу руководства с крупными показателями.
4. Подготовить суточный отчет заказчику по структуре текущего Excel.
5. Проверить демонстрационный сценарий в браузере от начала до конца.

## Подготовка демо-данных

Для подготовки демонстрационной базы нужно выполнить команду:

```text
python manage.py seed_demo_scenario
```

Команда создает роли, демо-доступы, смены, назначение, активный рейс, выполненный рейс, шаблон отчета и пример открытого механического простоя.

После выполнения доступны коды:

- `1000` - администратор;
- `2000` - водитель;
- `3000` - машинист экскаватора;
- `4000` - горный мастер;
- `5000` - диспетчер.
- `6000` - руководство;
- `7000` - механик.

## Критерий готовности к первому показу

Первый показ можно считать готовым, если без ручного вмешательства в базу можно пройти цикл:

`водитель -> горный мастер -> водитель -> машинист экскаватора -> механик -> диспетчер -> отчет -> Excel -> руководство`.

Если этот цикл проходит стабильно, система уже демонстрирует главную ценность MVP.
## Обновление 17.06.2026

Перед браузерной проверкой демонстрационного сценария основные экраны MVP переведены на единую визуальную основу:

- единая шапка и структура страниц;
- единые карточки, формы, таблицы и кнопки;
- более читаемые рабочие блоки для водителя, машиниста экскаватора, горного мастера, диспетчера и отчета;
- исправлена запасная страница роли после единого входа.

Демонстрационный сценарий после визуальной переработки проверен в браузере по основным ролям:

- диспетчерский пульт;
- отчет по объемам;
- экран водителя;
- экран машиниста экскаватора;
- экран горного мастера.

Следующий шаг: доработать найденные неудобства демонстрационного цикла и добавить более убедительную витрину для руководства.

## Обновление 17.06.2026: код руководства и витрина

Для демонстрации управленческой ценности добавлена отдельная витрина руководства.

Демо-код доступа:

- `6000` - руководство.

После входа по коду `6000` открывается страница `reports/management/`.

На странице отображаются:

- выполненный объем;
- тоннаж;
- завершенные рейсы;
- активные рейсы;
- переходящие рейсы;
- объемы по экскаваторам;
- объемы по породам/грузам;
- последние завершенные рейсы;
- ссылки на отчет по объемам и диспетчерский пульт.

Витрина проверена в браузере.

## Обновление 18.06.2026: демонстрация механических простоев

Команда подготовки демо-базы `seed_demo_scenario` теперь дополнительно создает два события механической службы:

- открытый критический простой по технике, чтобы на экране механиков, диспетчера и руководства была видна текущая проблема;
- закрытый обычный простой с длительностью, чтобы отчет по механическим простоям показывал историю и сводки.

Это позволяет при демонстрации пройти цепочку:

`механик -> открытые простои -> закрытые простои -> отчет по механическим простоям -> Excel-выгрузка -> витрина руководства`.

Проверка:

- `manage.py check` - ошибок нет;
- `manage.py test users` - 36 тестов пройдено;
- локальная демо-база обновлена командой `seed_demo_scenario`;
- через демо-код механика `7000` проверено открытие отчета `/reports/downtimes/` и Excel-выгрузки `/reports/downtimes/export/`.
