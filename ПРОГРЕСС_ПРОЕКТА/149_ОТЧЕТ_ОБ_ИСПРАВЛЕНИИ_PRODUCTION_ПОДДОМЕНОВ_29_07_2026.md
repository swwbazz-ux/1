# Отчёт об исправлении production-поддоменов 29.07.2026

## 1. Итоговый статус

**ВЫПОЛНЕНО / PRODUCTION HTTPS И PWA ПРОВЕРЕНЫ.**

Отдельные рабочие адреса готовы к использованию:

- `https://timekeeper.driverform.ru/` — Табельщик;
- `https://site-manager.driverform.ru/` — Начальник участка.

Исправлен только разрешённый инфраструктурный контур:

- TLS-сертификат;
- `nginx server_name`;
- `DJANGO_ALLOWED_HOSTS`;
- `DJANGO_CSRF_TRUSTED_ORIGINS`.

Код приложения, рабочие данные, PostgreSQL, миграции, `private_media`,
статические файлы и остальные настройки не изменялись. Команды `migrate`
и `collectstatic` не выполнялись.

Production-код остался на commit:

`a0476ab74aed1f4e07b96aae69666767593e1392`

Активные маркеры `.deploy_commit` и `.deployed-commit` сохранили этот SHA.

## 2. Разрешённая граница

Владелец отдельно разрешил:

1. до изменений создать резервные копии сертификата, конфигурации nginx
   и `.env`;
2. подготовить проверяемый план отката;
3. сохранить все действующие домены сертификата;
4. добавить только:
   - `timekeeper.driverform.ru`;
   - `site-manager.driverform.ru`;
5. выполнить полную HTTPS/PWA-проверку двух поддоменов;
6. не изменять код, базу, миграции и остальные настройки.

Эта граница соблюдена.

## 3. Исходное состояние

До исправления подтверждено:

- оба новых имени уже резолвились в production IP `77.91.93.47`;
- HTTP возвращал `400`;
- обычная HTTPS-проверка завершалась ошибкой имени сертификата;
- при отключённой проверке TLS запрос также получал `400`;
- действующий сертификат содержал `12` SAN;
- старый SHA-256 fingerprint:
  `C9:9D:6C:73:11:F5:23:6B:47:10:2F:A0:0A:87:95:E4:66:A4:81:8A:A2:03:46:CB:C1:D3:29:6D:9F:D5:28:63`;
- оба имени отсутствовали в SAN, nginx и разрешённых Django hosts;
- прежние `12/12` доменов проходили HTTP, HTTPS и TLS-проверку.

Это подтверждает, что исправлялась предшествующая инфраструктурная
недонастройка, а не дефект кода PWA.

## 4. Резервная копия и откат

До первого изменения создан root-only комплект:

`/srv/accounting-mvp/backups/infra-20260728_173013-timekeeper-site-manager`

Каталог имеет режим `700`. Файлы с конфигурацией и секретным ключом
доступны только владельцу root и не выгружались с production-сервера.

### 4.1. Исходные файлы

| Файл | Владелец и режим | Размер | SHA-256 |
|---|---|---:|---|
| `app.env.before` | `deploy:deploy`, `600` | `1 381` байт | `1CC05153D435669E25474C40BD1BE69D3F0DCC8E210927A9857B70311EE700FB` |
| `nginx-accounting-mvp.before` | `root:root`, `600` | `4 263` байта | `BA18707B37C2D1D14BB803A737EAD73B39411A5F6FED9310EE01B5E8F783548C` |
| `nginx-config.before.tar.gz` | `root:root`, `600` | `2 655` байт | `48741B74D758BA45C11AF1CC89E3C0740D72B46463639214C910C49B102DA330` |
| `letsencrypt-driverform.before.tar.gz` | `root:root`, `600` | `5 782` байта | `8BA00D0FD52D4E8C0D916D9FC62E12CC9D856B54D7467242887D3271F5F2374B` |
| `before.txt` | `root:root`, `600` | `441` байт | `AFE67DD82C828CEC0C980F19D55AC7D021F4A3FCE0CD39560A3BE6F9741E2778` |

Архив Let’s Encrypt содержит точное исходное состояние:

- `/etc/letsencrypt/live/driverform.ru`;
- `/etc/letsencrypt/archive/driverform.ru`;
- `/etc/letsencrypt/renewal/driverform.ru.conf`.

В нём находится закрытый ключ. Его содержимое не выводилось и не
копировалось с production.

Манифест `SHA256SUMS` полностью перепроверен после завершения работ.

### 4.2. Подготовленный откат

Скрипт:

`/srv/accounting-mvp/backups/infra-20260728_173013-timekeeper-site-manager/rollback.sh`

Параметры:

- владелец `root:root`;
- режим `700`;
- SHA-256:
  `77AAD9B8E6E44D1328982860EB1EEB940B4D58BADD3F7B33E2F276F31337B9AD`.

Синтаксис скрипта проверен. Сам откат не выполнялся, потому что
послерелизная проверка прошла.

При вызове скрипт:

1. восстанавливает исходный `.env`;
2. восстанавливает исходную конфигурацию nginx;
3. восстанавливает точное прежнее состояние Let’s Encrypt;
4. проверяет `nginx -t`;
5. перезапускает `accounting-mvp`;
6. перезагружает nginx;
7. проверяет службы и возврат старого fingerprint.

## 5. Выполненные изменения

### 5.1. Nginx

В обе существующие строки `server_name` файла
`/etc/nginx/sites-available/accounting-mvp` добавлены только:

- `timekeeper.driverform.ru`;
- `site-manager.driverform.ru`.

Файл сохранил владельца `root:root` и режим `644`.

Автоматическая проверка доказала, что если удалить из итогового файла
два новых имени, он побайтово совпадает с резервной копией. Других
изменений nginx нет.

### 5.2. Django `.env`

В `/srv/accounting-mvp/.env` изменены только два ключа:

- `DJANGO_ALLOWED_HOSTS`;
- `DJANGO_CSRF_TRUSTED_ORIGINS`.

Добавлены только два host и два соответствующих HTTPS origin. Все
прежние значения сохранены.

Файл сохранил владельца `deploy:deploy` и режим `600`.

Автоматическое сравнение старого и нового `.env` подтвердило:

- множество изменённых ключей в точности равно двум разрешённым ключам;
- прежние значения являются подмножеством итоговых;
- разница содержит только два целевых host/origin;
- остальные ключи и значения не изменялись.

### 5.3. Сертификат

Существующий сертификат `driverform.ru` расширен штатным Certbot с
сохранением всех прежних имён и добавлением ровно двух новых.

Новый сертификат:

- тип ключа: ECDSA;
- generation: `3`;
- действует с `28.07.2026 16:37:24 UTC`;
- действует до `26.10.2026 16:37:23 UTC`;
- SHA-256 fingerprint:
  `19:F8:31:E5:C3:BE:C6:AC:F3:70:A6:BB:97:92:4E:29:8E:F4:51:56:F1:CE:DA:62:62:CB:8D:F2:D7:52:58:9F`.

Итоговый SAN содержит ровно `14` имён:

1. `driverform.ru`;
2. `www.driverform.ru`;
3. `demo.driverform.ru`;
4. `driver.driverform.ru`;
5. `excavator.driverform.ru`;
6. `mining-master.driverform.ru`;
7. `deputy.driverform.ru`;
8. `dispatcher.driverform.ru`;
9. `oup.driverform.ru`;
10. `mechanic.driverform.ru`;
11. `management.driverform.ru`;
12. `admin.driverform.ru`;
13. `timekeeper.driverform.ru`;
14. `site-manager.driverform.ru`.

Проверка множества SAN:

- утраченные имена: `0`;
- лишние имена: `0`;
- добавленные имена: ровно `2`;
- `openssl -checkhost` для обоих новых поддоменов: успешно.

`certbot.timer` остался `enabled/active`, а разовая
`certbot.service` после выпуска сертификата находится в штатном
состоянии `inactive`.

## 6. Активация

После успешных `nginx -t` и `manage.py check` выполнены только
необходимые действия:

1. перезапущен `accounting-mvp`, чтобы Django перечитал `.env`;
2. выполнен reload nginx.

PostgreSQL не перезапускался и не изменялся.

После активации:

- `accounting-mvp` — `active`;
- `nginx` — `active`;
- `postgresql` — `active`;
- `nginx -t` — успешно;
- `manage.py check` — без ошибок;
- четыре целевых Django-настройки — подтверждены;
- журнал ошибок `accounting-mvp` — `0`;
- журнал ошибок nginx — `0`;
- production-маркер — прежний commit `a0476ab…`.

Во время самой секунды перезапуска один служебный запрос к нецелевому
или незафиксированному в access log host попал до готовности Gunicorn и
получил `502`. Готовность была подтверждена второй попыткой. Для
`timekeeper` и `site-manager` ответов `5xx` не было.

После полной готовности отдельно выполнено `60` внешних запросов к
корням, manifest и service worker двух поддоменов:

- успешных: `60/60`;
- клиентских ошибок: `0`;
- новых ошибок `accounting-mvp`: `0`;
- новых ошибок nginx: `0`;
- новых ответов `5xx`: `0`.

## 7. Полная внешняя HTTPS-проверка

Проверка выполнялась с обычной TLS-валидацией, без `-k` и без
отключения проверки имени хоста.

Для всех `14/14` доменов подтверждено:

- DNS указывает на `77.91.93.47`;
- TLS handshake и hostname verification проходят;
- используется один новый сертификат с fingerprint `19:F8:31…`;
- HTTP `/` возвращает `301` на HTTPS того же host;
- HTTPS `/` возвращает `200`.

Таким образом:

- оба новых поддомена работают;
- прежние `12/12` доменов не потеряны и не повреждены;
- редирект не перебрасывает пользователя на чужой поддомен.

## 8. PWA Табельщика

Адрес:

`https://timekeeper.driverform.ru/`

Подтверждено:

- корень — `200`;
- manifest — `200`;
- service worker — `200`;
- контракт — `pwa-contract-v1`;
- shell — `timekeeper-shell-v8`;
- роль — `timekeeper`;
- `start_url` — `/timekeeper/`;
- scope — `/`;
- display — `standalone`;
- иконки `192×192`, `512×512` и maskable — `200`;
- фактические PNG-размеры совпадают с заявленными;
- защищённый `/timekeeper/` без сессии возвращает `302` на
  `https://timekeeper.driverform.ru/?next=%2Ftimekeeper%2F`.

## 9. PWA Начальника участка

Адрес:

`https://site-manager.driverform.ru/`

Подтверждено:

- корень — `200`;
- manifest — `200`;
- service worker — `200`;
- контракт — `pwa-contract-v1`;
- shell — `site-manager-shell-v8`;
- роль — `site_manager`;
- `start_url` — `/site-manager/extensions/`;
- scope — `/`;
- display — `standalone`;
- иконки `192×192`, `512×512` и maskable — `200`;
- фактические PNG-размеры совпадают с заявленными;
- защищённый маршрут без сессии возвращает `302` на
  `https://site-manager.driverform.ru/?next=%2Fsite-manager%2Fextensions%2F`.

## 10. Browser QA

Проверены публичные экраны входа без авторизованных действий и без
изменения production-данных.

### Табельщик

- desktop `1280×720` — заголовок и manifest правильные;
- mobile `390×844` — горизонтальный overflow отсутствует;
- основные элементы управления видимы;
- ошибок консоли нет;
- защищённый маршрут возвращает на вход того же поддомена.

### Начальник участка

- desktop `1280×720` — заголовок и manifest правильные;
- mobile `390×844` — горизонтальный overflow отсутствует;
- максимальная правая граница элементов находится внутри viewport;
- основные элементы управления видимы;
- ошибок консоли нет;
- защищённый маршрут возвращает на вход того же поддомена.

Визуально оба мобильных экрана пригодны для дальнейшей пользовательской
проверки на физическом устройстве.

## 11. Контроль неизменности запрещённых контуров

Не выполнялись и не изменялись:

- Git payload и код приложения;
- модели и формы;
- файлы миграций;
- команда `migrate`;
- рабочие записи PostgreSQL;
- резервное восстановление базы;
- `private_media`;
- `collectstatic`;
- systemd unit;
- HTTP/2 и отложенный `QA-TRAFFIC-P2-004`;
- остальные значения `.env`;
- остальные настройки nginx;
- остальные домены сертификата.

Текущий инфраструктурный пакет не переоткрывает результаты документа
148 и не меняет опубликованный трафиковый commit.

## 12. Итоговые контрольные файлы

На production сохранены:

- `before.txt`;
- `after.txt`;
- `SHA256SUMS`;
- `AFTER_SHA256SUMS`;
- `rollback.sh`.

Итоговые SHA-256:

| Файл или состояние | SHA-256 |
|---|---|
| `after.txt` | `A8FFC71304FA8D0E4E6FAA4310F6F444CDA77CF4FDFD2561CA4D96EAC9C3D97B` |
| `AFTER_SHA256SUMS` | `9264BC4FAEB44EA17617E0D1726F26ABD44EADFA2A9471263D491A7DDBC202D7` |
| итоговый nginx-файл | `3CAC5D7CF96661FF21C7938623753C832466E5FB9583D2BD1AA49B0843549A97` |
| итоговый `.env` | `5CDA111D607D7AECDAC3DD59EF6EFEE0B497C1D81A1E7C241BE7DEDB45943071` |
| итоговый `fullchain.pem` | `55F1BD03F093DC212E04345C46C598F2839F15785696E25A226F07B057371655` |
| `rollback.sh` | `77AAD9B8E6E44D1328982860EB1EEB940B4D58BADD3F7B33E2F276F31337B9AD` |

Оба манифеста хэшей перепроверены.

## 13. Итог

Инфраструктурный остаток документа 148 закрыт.

Табельщику можно выдавать:

`https://timekeeper.driverform.ru/`

Начальнику участка можно выдавать:

`https://site-manager.driverform.ru/`

В production опубликовано только разрешённое инфраструктурное
изменение. Локальный отчёт 149 и обновления четырёх журналов на момент
создания документа ещё не закоммичены и не отправлены в Git; для этого
потребуется отдельное разрешение владельца.
