# Карта backend Диспетчерского пульта — 25.09.2026

## Цель

Зафиксировать фактические серверные границы текущего production-derived пульта перед
дальнейшим разделением большого `trips/views.py`. Карта описывает существующее поведение;
она не вводит новые маршруты и не меняет правила смен, рейсов или расстановки техники.

## Фактический входной контур

- URL-конфигурация: `СИСТЕМА_MVP/backend/trips/urls.py`.
- PWA-оболочка: `СИСТЕМА_MVP/backend/trips/dispatcher_pwa.py`.
- Основной экран и операционные действия: `СИСТЕМА_MVP/backend/trips/views.py`.
- Заголовок и жизненный цикл обычной смены: `СИСТЕМА_MVP/backend/trips/dispatcher_header.py`.
- Каноническое описание ролевого приложения и shell-версия:
  `СИСТЕМА_MVP/backend/users/role_apps.py`.
- Клиентский экран: `templates/trips/dispatcher_control.html` и модули
  `static/js/dispatcher-*-v1.js`.

В `trips/urls.py` зарегистрировано 13 диспетчерских маршрутов: 2 PWA-маршрута и
11 операционных обработчиков.

## Группы маршрутов

| Граница | Маршруты | Текущий модуль | Ключевой контракт |
|---|---|---|---|
| PWA | `/dispatcher.webmanifest`, `/dispatcher-sw.js` | `trips/dispatcher_pwa.py` | Ролевая manifest-конфигурация, scope `/dispatcher/`, shell `v143`, push-контракт |
| Рабочий экран | `/dispatcher/control/` | `trips/views.py` + `trips/dispatcher_read_model.py` | Контроллер HTTP отдельно; общий read-model отдаёт данные полного HTML и operational fragment |
| Карточка техники | `/dispatcher/control/card/<category>/<equipment_id>/` | GET: `trips/dispatcher_read_model.py`; POST: `trips/dispatcher_equipment_commands.py`; фасад: `trips/views.py` | GET использует общий read-model, POST настроек сохраняет optimistic state version и production lock |
| Расстановка | `.../excavator/move/`, `.../truck/assign/` | фасад: `trips/views.py`; исполнение: `trips/dispatcher_topology_commands.py` | POST, активная смена, production lock, idempotency client action |
| Простои | `.../downtime/<event_id>/close/` | фасад: `trips/views.py`; исполнение: `trips/dispatcher_downtime_commands.py` | POST, блокировка production state, обновление operational state |
| Смена | `/dispatcher/shift/toggle/`, `.../shifts/<shift_id>/service-close/` | фасады: `trips/views.py`; исполнение: `trips/dispatcher_shift_commands.py` | Повторная идентификация общего ПК, закрытие своей/служебной смены |
| Назначения | `.../assignments/<assignment_id>/cancel/` | фасад: `trips/views.py`; исполнение: `trips/dispatcher_assignment_commands.py` | Только активная смена, плановый release, журнал действий |
| Рейсы | cancel, complete, manual-trip | фасад: `trips/views.py`; исполнение: `trips/dispatcher_trip_commands.py` | Только активная смена, блокировки, пересчёт состояния и аудит |

## Связность монолита

После выделения PWA в `trips/views.py` остаются 11 операционных диспетчерских
обработчиков и 67 функций, в имени которых присутствует `dispatcher`. Сам файл всё ещё
содержит около 8,9 тыс. строк и одновременно обслуживает Водителя и Экскаваторщика.

Критическая внешняя зависимость: `assignments/views.py` импортирует
`dispatcher_control_view` и повторно использует его как renderer экрана Горного мастера
через `access_override`, `enforce_dispatcher_access=False` и собственные header/context
overrides. Поэтому перенос рабочего экрана нельзя выполнять простым перемещением функции:
этот совместный контракт должен сохраняться отдельным тестом.

## Инварианты, которые нельзя потерять при разделении

1. Различать HTML redirect/message и JSON-ответы клиентских команд.
2. Проверять доступ роли и действующую смену до любой мутации.
3. Для расстановки, простоев и рейсов сохранять transaction/production lock.
4. Сохранять optimistic state version и idempotency client action.
5. После мутации обновлять operational state и журнал действий в прежнем порядке.
6. Не менять имена URL, параметры маршрутов и redirect query.
7. Не разрывать renderer-контракт Горного мастера.

## Выполненный первый этап

- PWA service worker и два PWA HTTP-обработчика перенесены в
  `trips/dispatcher_pwa.py`.
- Неиспользуемый локальный `DISPATCHER_MANIFEST` удалён из `trips/views.py`;
  канонический manifest по-прежнему формируется из `users/role_apps.py`.
- `trips/urls.py` импортирует PWA-маршруты из нового модуля.
- URL, содержимое ответов, scope и shell `dispatcher-desktop-shell-v143` не изменены.
- Новый runtime-файл добавлен в production allowlist; публикация не выполнялась.

## Безопасная последовательность следующих этапов

1. [x] Выделить общие dispatcher access/response guards с адресными тестами redirect и JSON.
2. [x] Отделить read-model рабочего экрана и карточки, сохранив renderer Горного мастера.
3. [x] Первой write-группой отделить расстановку техники.
4. [x] Отдельным commit выделить закрытие простоя.
5. [x] Отдельным commit выделить отмену активного назначения.
6. [x] Отдельным commit выделить отмену активного рейса.
7. [x] Отдельным commit выделить служебное завершение активного рейса.
8. [x] Отдельным commit выделить создание ручных рейсов.
9. [x] Отдельным commit выделить собственную смену Диспетчера.
10. [x] Отдельным commit выделить служебное закрытие чужой смены.
11. [x] Отдельным commit выделить POST настроек карточки техники, не затрагивая её GET/read-model.
12. Перед следующим переносом отдельно картировать оставшийся большой dashboard builder и
   отделять только чистые представления данных, не захватывая общий доменный контур Экскаваторщика.
13. После каждого шага запускать профильные Django-тесты, `manage.py check` и Browser QA
   на desktop/mobile; не совмещать перенос с изменением бизнес-логики.

## Выполненный второй этап

- В `trips/dispatcher_guards.py` вынесены восемь общих контрактов: фильтрованный
  URL пульта, безопасный action redirect, поиск доступа, блокирующая повторная
  проверка активной роли, JSON/redirect-ответ закрытой смены, JSON parser и
  единый ответ конфликта client action.
- `trips/views.py` продолжает экспортировать прежние имена. Для
  `lock_dispatcher_mutation_access` оставлен тонкий wrapper, который передаёт
  текущий `role_session_state`: patch-контракт конкурентных тестов и защита от
  старой вкладки сохранены.
- Отдельный boundary-тест подтверждает, что Горный мастер по-прежнему использует
  тот же `dispatcher_control_view` как renderer.
- Новый runtime-файл включён в production allowlist; публикация не выполнялась.

## Проверка второго этапа

- Адресные dispatcher/active-role тесты: `84/84`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний.
- Browser QA `1280×800` и `390×844`: экран открыт, защищённое начало
  тестовой смены прошло, horizontal overflow и ошибки/предупреждения консоли
  отсутствуют.

## Выполненный третий этап

- В `trips/dispatcher_read_model.py` вынесен общий read-only снимок рабочего пульта:
  фильтры, активные и завершённые рейсы, назначения, открытые смены, простои,
  техника, журнал действий и вызов существующего `build_dispatcher_dashboard_context`.
- `trips/views.py` сохраняет публичный `dispatcher_control_view` как совместимый HTTP-фасад:
  доступ, reconciliation, optimistic version, полный HTML, detail JSON и operational fragment
  остаются в прежнем порядке.
- GET карточки ограничивает read-model одной карточкой; POST настроек экскаватора не переносился
  и по-прежнему выполняет проверки роли/смены, transaction, production lock и bump состояния.
- `assignments/views.py` продолжает импортировать тот же `trips.views.dispatcher_control_view`
  для экрана Горного мастера с `access_override`, собственными header/context overrides и
  полным словарём `equipment_cards`.
- Новый runtime-файл включён в production allowlist. Публикация не выполнялась.

## Проверка третьего этапа

- Профильные Django-тесты рабочего экрана, карточки, guards, operational fragments и
  Горного мастера: `106/106`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA `1280×800` и `390×844`: полный пульт и карточка открываются, прямой GET
  карточки возвращает `dispatcher-equipment-detail-v1`, fragment —
  `operational-fragment-v1`; horizontal overflow и console error/warn отсутствуют.

## Выполненный четвертый этап

- Команды `excavator/move` и `truck/assign` вынесены в
  `trips/dispatcher_topology_commands.py`; URL и публичные view-имена остались в
  `trips.views`.
- Декораторы `require_POST` и `transaction.atomic` сохранены на публичных фасадах.
  Они передают прежний `lock_dispatcher_mutation_access`, поэтому patch-контракт
  `trips.views.role_session_state` и повторная проверка активной роли не изменились.
- Внутри команд сохранены production lock, optimistic `expected_*` состояния,
  идемпотентный `client_action_id`, порядок schedule/log/complete и прежние JSON-ответы.
- Парсеры optimistic state переэкспортируются из `trips.views`; другие мутации
  расстановки, смен, простоев и рейсов не переносились.
- Новый runtime-файл включён в production allowlist. Публикация не выполнялась.

## Проверка четвертого этапа

- Профильные Django-тесты: `85/85`, включая новый success/retry сценарий
  перемещения экскаватора в гараж и прежние active-role/realtime контракты.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД: `truck/assign` переназначил самосвал через
  production drag/drop-обработчики; `excavator/move` открыл штатный диалог
  «Расформировать комплекс?» и после подтверждения перенёс экскаватор в гараж.
- На `1280×800` и `390×844` пульт сохранил controls и карточки, horizontal overflow
  и пересечение ключевых controls отсутствуют; console error/warn пусты.

## Выполненный пятый этап

- Исполнение `downtime/<event_id>/close` вынесено в
  `trips/dispatcher_downtime_commands.py`; маршрут и публичный
  `dispatcher_close_downtime_view` сохранены в `trips.views`.
- На тонком фасаде сохранены `require_POST` и `transaction.atomic`. Через внедряемые
  швы он передаёт прежние active-role guard, response builder и event payload,
  поэтому URL, JSON-формат и patch-контракты не изменились.
- В команде сохранены проверка действующей смены, production lock, optimistic
  `state_version`, повторное идемпотентное закрытие, operational state и
  `source=dispatcher_override` с прежним actor.
- Новый runtime-файл включён в production allowlist. Публикация не выполнялась.

## Проверка пятого этапа

- Профильные Django-тесты и active-role регрессия: `27/27`; добавлены boundary-тесты
  публичного фасада и запрета мутации при неактивной роли.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД подтвердил штатный диалог «Завершить простой?»,
  успешное закрытие и возврат самосвала из «Ремонт ходовой» в «Свободен».
- На `1280×800` и `390×844` horizontal overflow и выход ключевых controls за viewport
  отсутствуют; console error/warn пусты.

## Выполненный шестой этап

- Исполнение `assignments/<assignment_id>/cancel` вынесено в
  `trips/dispatcher_assignment_commands.py`; маршрут и публичный
  `dispatcher_cancel_assignment_view` сохранены в `trips.views`.
- Транзакционная граница осталась на фасаде. Через внедряемые швы он передаёт прежние
  active-role guard и action logger, поэтому patch-контракт старой вкладки не изменился.
- Для ожидающего назначения сохранено немедленное состояние `cancelled`; для принятого —
  штатное планирование `release` через `schedule_haul_release`. Сохранены production lock,
  фильтрованный redirect, сообщения, причина, actor и ссылка журнала на фактическое решение.
- Доступ по-прежнему ограничен ролями `dispatcher` и `admin`; расширение на `manager`
  намеренно не выполнялось.
- Новый runtime-файл включён в production allowlist. Публикация не выполнялась.

## Проверка шестого этапа

- Профильные Django-тесты расстановки/realtime/guards и active-role: `39/39`;
  отдельно проверены обе ветки отмены, stale conflict, закрытая смена, GET без мутации,
  исходные redirect/messages и прежняя граница ролей.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД отправил фактическую HTML-форму: пульт показал
  сообщение об отмене, назначение стало `cancelled`, `ended_at` заполнен, журнал сохранил
  `cancel_assignment`, причину и диспетчера.
- На `1280×800` и `390×844` horizontal overflow и выход видимых controls за viewport
  отсутствуют; console error/warn пусты.

## Выполненный седьмой этап

- Исполнение `trips/<trip_id>/cancel` вынесено в
  `trips/dispatcher_trip_commands.py`; маршрут и публичный
  `dispatcher_cancel_trip_view` сохранены в `trips.views`.
- Транзакционная граница осталась на фасаде. Через внедряемые швы он передаёт прежние
  active-role guard, reconciliation экскаватора и action logger; patch-контракты и
  внешние импорты не изменились.
- Сохранён исходный порядок: проверка смены и обязательной причины, повторная проверка
  активной роли, production lock, затем trip lock. После `cancelled` по-прежнему
  закрывается использованный свободный ковш, пересчитывается ожидание самосвалов,
  пишутся журнал и событие `Trip:dispatcher_cancel_trip`.
- Доступ остался только для `dispatcher` и `admin`; URL, фильтрованный redirect,
  сообщения и realtime payload не менялись. Новый runtime-файл включён в production
  allowlist. Публикация не выполнялась.

## Проверка седьмого этапа

- Профильные Django/terminal/active-role тесты: `35/35`; отдельно закреплены тонкий
  фасад, обязательная причина, закрытая смена, неактивная роль, исходная ролевая
  граница, terminal idempotency, свободный ковш, аудит и operational payload.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД отправил фактическую HTML-форму с CSRF и
  обязательной причиной. Рейс исчез из активных, получил `cancelled` и `cancelled_at`,
  журнал сохранил причину и диспетчера, создано событие `trip_changed`.
- На `1280×800` и `390×844` horizontal overflow отсутствует; на mobile ни один из
  10 видимых controls не вышел за горизонтальную границу, console error/warn пусты.

## Выполненный восьмой этап

- Исполнение `trips/<trip_id>/complete` вынесено в уже подключённый
  `trips/dispatcher_trip_commands.py`; маршрут и публичный
  `dispatcher_complete_trip_view` сохранены в `trips.views`.
- Транзакционная граница осталась на фасаде. Он явно передаёт прежние active-role
  guard, `finalize_trip_unloaded` и action logger, поэтому общий доменный финализатор
  Водителя не копировался и не менялся.
- Сохранены два чтения водительской смены, стабильная блокировка обоих сотрудников,
  повторная проверка роли/смены, порядок production lock перед trip lock и защита от
  конкурирующего терминального действия.
- `finalize_trip_unloaded` по-прежнему записывает водителя и смену, серверное время
  разгрузки, закрывает ожидание разгрузки и свободный ковш и запускает reconciliation.
  Затем сохраняются исходные аудит, сообщения и `Trip:dispatcher_complete_trip`.
- Доступ остался только для `dispatcher` и `admin`; URL, фильтрованный redirect и
  realtime payload не менялись. Модуль уже находится в production allowlist;
  публикация не выполнялась.

## Проверка восьмого этапа

- Профильные Django/terminal/active-role тесты: `47/47`; отдельно закреплены тонкий
  фасад, обязательная причина, водительская смена, закрытая смена диспетчера,
  неактивная роль, исходная ролевая граница, terminal idempotency, свободный ковш,
  порядок production/trip lock, аудит и operational payload.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД отправил фактическую HTML-форму с CSRF,
  обязательной причиной и `maxlength=255`. Рейс исчез из активных, получил
  `completed`, `server_receipt`, водителя и unloading shift; журнал и событие
  `trip_changed` сохранили прежние данные.
- На `1280×800` и `390×844` horizontal overflow отсутствует; на mobile все 10
  видимых controls остались в горизонтальных границах, console error/warn пусты.

## Выполненный девятый этап

- Исполнение `trucks/<equipment_id>/manual-trip` вынесено в уже подключённый
  `trips/dispatcher_trip_commands.py`; маршрут и публичный
  `dispatcher_manual_trip_view` сохранены в `trips.views`.
- Транзакционная граница осталась на фасаде. Он передаёт прежние active-role guard,
  форматирование времени и action logger; парсер времени и лимит `10` рейсов
  переэкспортируются из `trips.views` для совместимости read-model и тестов.
- Сохранены граница ролей `dispatcher/admin`, обязательная причина, проверка открытой
  смены водителя, действующего назначения, точки, породы, времени и кубатуры, а также
  прежнее последовательное время группы рейсов.
- Рейсы по-прежнему сразу создаются в `completed`, получают водителя и unloading shift,
  возможную смену машиниста, плечо, забой и признак carryover. Для каждого рейса пишется
  аудит, после группы — одно событие `Trip:dispatcher_manual_trip` с полным списком ID.
- Уже записанный выполненный ручной рейс нельзя отменить через команду отмены активного
  рейса. Модуль уже находится в production allowlist; публикация не выполнялась.

## Проверка девятого этапа

- Профильные Django/terminal/active-role тесты: `64/64`; отдельно закреплены тонкий
  фасад, границы количества и времени, закрытая смена, неактивная роль, исходная ролевая
  граница, точный audit/realtime payload и запрет отмены выполненного ручного рейса.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД отправил фактическую HTML-форму с CSRF и штатным
  подтверждением. Созданы два завершённых рейса с последовательным временем, водителем,
  unloading shift, двумя записями аудита и одним `trip_changed` с ID обоих рейсов.
- На `1280×800` и `390×844` horizontal overflow отсутствует, поля и кнопки формы остаются
  в горизонтальных границах; console error/warn пусты. Временный QA-контур удалён.

## Выполненный десятый этап

- Исполнение `/dispatcher/shift/toggle/` вынесено в новый
  `trips/dispatcher_shift_commands.py`; URL и публичный
  `dispatcher_toggle_shift_view` сохранены в `trips.views`.
- Транзакционная граница осталась на тонком фасаде. Он явно передаёт прежние
  active-role guard и обработчик повторной авторизации общего ПК, поэтому patch-швы
  `trips.views.role_session_state` и shared re-auth сохранены.
- Сохранены исходная граница ролей `dispatcher/admin`, безопасный redirect с удалением
  fragment-параметров, отличия личного и общего устройства, закрытие смены другой роли
  только после подтверждения и прежние сообщения повторного действия.
- Доменное открытие и закрытие не дублировались: команда по-прежнему вызывает
  `open_dispatcher_shift` и `close_dispatcher_shift`, которые владеют employee/production
  locks, workplace code, временем и автором смены.
- Новый runtime-файл включён в production allowlist. Служебное закрытие чужой смены
  намеренно осталось в `trips.views`; публикация не выполнялась.

## Проверка десятого этапа

- Профильные Django/role-order/active-role тесты: `41/41`; отдельно закреплены URL и
  тонкий фасад, точные success/warning/error сообщения, повторное начало/завершение,
  общий ПК без/с неверными реквизитами и прежний запрет для `manager`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД подтвердил общий ПК: повторный вход с телефоном
  и PIN открыл ночную смену с правильным `opened_by`; защищённый десятисекундный диалог
  завершил её с правильным `closed_by`.
- На `1280×800` и `390×844` horizontal overflow отсутствует, кнопки и поля обоих
  диалогов остаются в горизонтальных границах; console error/warn пусты. Временный
  QA-контур удалён.

## Выполненный одиннадцатый этап

- Исполнение `/dispatcher/shifts/<shift_id>/service-close/` вынесено из
  `trips/views.py` в уже подключённый `trips/dispatcher_shift_commands.py`; URL и
  публичный `dispatcher_service_close_shift_view` сохранены.
- Транзакционная граница осталась на тонком фасаде. Он явно передаёт прежние
  active-role guard, парсер показаний, нормализатор вида закрытия, общий финализатор
  смены и action logger, поэтому существующие patch-швы и внешние имена сохранены.
- Не изменены стабильный порядок employee/equipment locks, запрет закрытия собственной
  смены, исключение для блокирующей смены предыдущего диспетчера, необязательные
  показания и их прежняя валидация для самосвала/экскаватора.
- Общий финализатор перенесён вместе с командой и по-прежнему закрывает смену и рабочие
  простои, отменяет принятия свободного ковша, переводит открытые рейсы в carryover и
  ставит паспорт водительской смены в очередь. Actor диспетчера и затронутый сотрудник
  остаются раздельными в смене и `DispatcherActionLog`.
- Runtime-модуль и `trips/views.py` уже находятся в production allowlist; манифест
  релиза не менялся. Публикация не выполнялась.

## Проверка одиннадцатого этапа

- Профильные Django/active-role/terminal/role-access тесты: `53` найдено, `51` пройден,
  `2` PostgreSQL-only теста штатно пропущены на SQLite. Добавлены boundary-тесты URL,
  тонкого фасада и точной передачи совместимых швов.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA на временной локальной БД выполнил реальную форму «по согласованию» с
  причиной и тремя показаниями. Подтверждены `closed_by`, вид/примечание закрытия,
  показания, actor аудита и завершение рабочего простоя.
- На `1280×800` и `390×844` horizontal overflow отсутствует, мобильная форма находится
  внутри карточки, console error/warn пусты. Временный QA-контур удалён.

## Проверка первого этапа

- Профильные Django/PWA-тесты: `81/81`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний.
- Manifest/service worker: HTTP `200`, ожидаемые content type, scope и shell `v143`.
- Browser QA `1280×800` и `390×844`: экран и диалог смены доступны,
  horizontal overflow и ошибки/предупреждения консоли отсутствуют.

## Статус публикации

Изменения локальные. Push, VERIFY, DEPLOY, collectstatic, миграции, база данных и
production не затрагивались.

## Выполненный двенадцатый этап

- POST-исполнение `/dispatcher/control/card/<category>/<equipment_id>/` вынесено из
  двухрежимного `dispatcher_equipment_detail_view` в новый модуль
  `trips/dispatcher_equipment_commands.py`. URL и публичный фасад сохранены.
- Общие проверки доступа, категории и существования техники остались перед развилкой
  GET/POST. GET по-прежнему использует `dispatcher_control_view` и общий read-model;
  renderer Горного мастера и его context overrides не затронуты.
- POST-фасад явно передаёт active-role, active-shift, JSON/parser, normalization,
  mutation-lock, response и settings seams. Благодаря этому сохранены прежние точки
  подмены `trips.views`, включая PostgreSQL-гонку повторной активации роли.
- Порядок мутации не менялся: валидация версии и справочников, повторная проверка
  роли/смены внутри transaction, production lock, optimistic version, equipment lock,
  сохранение размещения и точек, затем `equipment_changed`.
- Ответ остаётся `dispatcher-equipment-settings-v2`, ошибки —
  `dispatcher-equipment-detail-v1`, заголовки no-store/nosniff/Vary сохранены. Новый
  runtime-файл добавлен в production allowlist; публикация не выполнялась.

## Проверка двенадцатого этапа

- Профильные dispatcher command/GET-тесты: `62/62`; отдельный PostgreSQL-only
  active-role тест штатно пропущен на SQLite. Новые тесты закрепляют URL, тонкую
  POST-делегацию, точные compatibility seams, сохранение двух точек и stale-version.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA через фактический интерфейс сохранил горизонт `75`, блок `52`, породу и
  две точки с плечами `3,75` и `8,50`; повторное открытие карточки и локальная БД
  подтвердили round-trip и одно командное operational event.
- На `1280×800` и `390×844` карточка и кнопка сохранения находятся в viewport,
  horizontal overflow отсутствует, console error/warn пусты. Временный QA-контур
  после проверки удалён.
