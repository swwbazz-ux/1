# Карта backend Диспетчерского пульта — 25.09.2026

## Цель

Зафиксировать фактические серверные границы текущего production-derived пульта перед
дальнейшим разделением большого `trips/views.py`. Карта описывает существующее поведение;
она не вводит новые маршруты и не меняет правила смен, рейсов или расстановки техники.

## Фактический входной контур

- URL-конфигурация: `СИСТЕМА_MVP/backend/trips/urls.py`.
- PWA-оболочка: `СИСТЕМА_MVP/backend/trips/dispatcher_pwa.py`.
- Основной экран и операционные действия: `СИСТЕМА_MVP/backend/trips/views.py`.
- Заголовок и жизненный цикл обычной смены: `СИСТЕМА_MVP/backend/trips/dispatcher_header.py`.
- Каноническое описание ролевого приложения и shell-версия:
  `СИСТЕМА_MVP/backend/users/role_apps.py`.
- Клиентский экран: `templates/trips/dispatcher_control.html` и модули
  `static/js/dispatcher-*-v1.js`.

В `trips/urls.py` зарегистрировано 13 диспетчерских маршрутов: 2 PWA-маршрута и
11 операционных обработчиков.

## Группы маршрутов

| Граница | Маршруты | Текущий модуль | Ключевой контракт |
|---|---|---|---|
| PWA | `/dispatcher.webmanifest`, `/dispatcher-sw.js` | `trips/dispatcher_pwa.py` | Ролевая manifest-конфигурация, scope `/dispatcher/`, shell `v143`, push-контракт |
| Рабочий экран | `/dispatcher/control/` | `trips/views.py` + `trips/dispatcher_read_model.py` | Контроллер HTTP отдельно; общий read-model отдаёт данные полного HTML и operational fragment |
| Карточка техники | `/dispatcher/control/card/<category>/<equipment_id>/` | GET: `trips/dispatcher_read_model.py`; POST: `trips/views.py` | GET использует общий read-model, POST настроек сохраняет optimistic state version и production lock |
| Расстановка | `.../excavator/move/`, `.../truck/assign/` | `trips/views.py` | POST, активная смена, production lock, idempotency client action |
| Простои | `.../downtime/<event_id>/close/` | `trips/views.py` | POST, блокировка production state, обновление operational state |
| Смена | `/dispatcher/shift/toggle/`, `.../shifts/<shift_id>/service-close/` | `trips/views.py` | Повторная идентификация общего ПК, закрытие своей/служебной смены |
| Назначения | `.../assignments/<assignment_id>/cancel/` | `trips/views.py` | Только активная смена, плановый release, журнал действий |
| Рейсы | cancel, complete, manual-trip | `trips/views.py` | Только активная смена, блокировки, пересчёт состояния и аудит |

## Связность монолита

После выделения PWA в `trips/views.py` остаются 11 операционных диспетчерских
обработчиков и 67 функций, в имени которых присутствует `dispatcher`. Сам файл всё ещё
содержит около 8,9 тыс. строк и одновременно обслуживает Водителя и Экскаваторщика.

Критическая внешняя зависимость: `assignments/views.py` импортирует
`dispatcher_control_view` и повторно использует его как renderer экрана Горного мастера
через `access_override`, `enforce_dispatcher_access=False` и собственные header/context
overrides. Поэтому перенос рабочего экрана нельзя выполнять простым перемещением функции:
этот совместный контракт должен сохраняться отдельным тестом.

## Инварианты, которые нельзя потерять при разделении

1. Различать HTML redirect/message и JSON-ответы клиентских команд.
2. Проверять доступ роли и действующую смену до любой мутации.
3. Для расстановки, простоев и рейсов сохранять transaction/production lock.
4. Сохранять optimistic state version и idempotency client action.
5. После мутации обновлять operational state и журнал действий в прежнем порядке.
6. Не менять имена URL, параметры маршрутов и redirect query.
7. Не разрывать renderer-контракт Горного мастера.

## Выполненный первый этап

- PWA service worker и два PWA HTTP-обработчика перенесены в
  `trips/dispatcher_pwa.py`.
- Неиспользуемый локальный `DISPATCHER_MANIFEST` удалён из `trips/views.py`;
  канонический manifest по-прежнему формируется из `users/role_apps.py`.
- `trips/urls.py` импортирует PWA-маршруты из нового модуля.
- URL, содержимое ответов, scope и shell `dispatcher-desktop-shell-v143` не изменены.
- Новый runtime-файл добавлен в production allowlist; публикация не выполнялась.

## Безопасная последовательность следующих этапов

1. [x] Выделить общие dispatcher access/response guards с адресными тестами redirect и JSON.
2. [x] Отделить read-model рабочего экрана и карточки, сохранив renderer Горного мастера.
3. Отдельными commit разделить мутации: расстановка, смены, простои, рейсы.
4. После каждого шага запускать профильные Django-тесты, `manage.py check` и Browser QA
   на desktop/mobile; не совмещать перенос с изменением бизнес-логики.

## Выполненный второй этап

- В `trips/dispatcher_guards.py` вынесены восемь общих контрактов: фильтрованный
  URL пульта, безопасный action redirect, поиск доступа, блокирующая повторная
  проверка активной роли, JSON/redirect-ответ закрытой смены, JSON parser и
  единый ответ конфликта client action.
- `trips/views.py` продолжает экспортировать прежние имена. Для
  `lock_dispatcher_mutation_access` оставлен тонкий wrapper, который передаёт
  текущий `role_session_state`: patch-контракт конкурентных тестов и защита от
  старой вкладки сохранены.
- Отдельный boundary-тест подтверждает, что Горный мастер по-прежнему использует
  тот же `dispatcher_control_view` как renderer.
- Новый runtime-файл включён в production allowlist; публикация не выполнялась.

## Проверка второго этапа

- Адресные dispatcher/active-role тесты: `84/84`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний.
- Browser QA `1280×800` и `390×844`: экран открыт, защищённое начало
  тестовой смены прошло, horizontal overflow и ошибки/предупреждения консоли
  отсутствуют.

## Выполненный третий этап

- В `trips/dispatcher_read_model.py` вынесен общий read-only снимок рабочего пульта:
  фильтры, активные и завершённые рейсы, назначения, открытые смены, простои,
  техника, журнал действий и вызов существующего `build_dispatcher_dashboard_context`.
- `trips/views.py` сохраняет публичный `dispatcher_control_view` как совместимый HTTP-фасад:
  доступ, reconciliation, optimistic version, полный HTML, detail JSON и operational fragment
  остаются в прежнем порядке.
- GET карточки ограничивает read-model одной карточкой; POST настроек экскаватора не переносился
  и по-прежнему выполняет проверки роли/смены, transaction, production lock и bump состояния.
- `assignments/views.py` продолжает импортировать тот же `trips.views.dispatcher_control_view`
  для экрана Горного мастера с `access_override`, собственными header/context overrides и
  полным словарём `equipment_cards`.
- Новый runtime-файл включён в production allowlist. Публикация не выполнялась.

## Проверка третьего этапа

- Профильные Django-тесты рабочего экрана, карточки, guards, operational fragments и
  Горного мастера: `106/106`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний; release protocol — `17/17`.
- Browser QA `1280×800` и `390×844`: полный пульт и карточка открываются, прямой GET
  карточки возвращает `dispatcher-equipment-detail-v1`, fragment —
  `operational-fragment-v1`; horizontal overflow и console error/warn отсутствуют.

## Проверка первого этапа

- Профильные Django/PWA-тесты: `81/81`.
- Полный backend JS-набор: `754 passed`, `1` штатный skip из `755`.
- `manage.py check` — без замечаний.
- Manifest/service worker: HTTP `200`, ожидаемые content type, scope и shell `v143`.
- Browser QA `1280×800` и `390×844`: экран и диалог смены доступны,
  horizontal overflow и ошибки/предупреждения консоли отсутствуют.

## Статус публикации

Изменения локальные. Push, VERIFY, DEPLOY, collectstatic, миграции, база данных и
production не затрагивались.
