# Production performance-аудит Экскаваторщика, Горного мастера и Администратора

Дата: 24.08.2026 (Владивосток).

## Итог

Срочно проверены три готовых PWA. Экскаваторщик и Администратор уже укладывались
в принятые ограничения и не потребовали изменения рабочего интерфейса. Для
Горного мастера устранена лишняя загрузка цветных изображений техники.

Изменение опубликовано в production. Модели, миграции, рабочие данные, `.env`,
nginx и PostgreSQL не менялись.

## Изолированная среда

Активные действия выполнялись только на отдельной PostgreSQL QA-базе:

- база `copper_pwa_performance_qa_20260823`;
- адрес `127.0.0.1:55437`;
- рабочая SQLite и production-БД не использовались;
- после проверки QA-сервер и PostgreSQL остановлены, порты `8002` и `55437`
  освобождены.

## Результаты по приложениям

### Машинист экскаватора

- HTML: `273 913` Б raw / `43 015` Б gzip;
- суммарная холодная передача — около `309 КБ` gzip;
- ответ рабочего экрана — около `262 мс` в QA-контуре;
- realtime: в среднем `487` Б raw / `247` Б gzip на ответ;
- расчётная нагрузка: `69 120` запросов за 12 часов для восьми приложений;
- Browser QA `390×844`: горизонтального переполнения и ошибок консоли нет.

Оптимизация рабочего кода не потребовалась. Production shell сохранён на более
новой фактической версии `excavator-mobile-shell-v131`.

### Горный мастер

До оптимизации:

- HTML: `1 008 695` Б raw / `81 366` Б gzip;
- суммарная холодная передача — около `0,54 МБ` gzip.

После оптимизации:

- HTML: `1 007 269` Б raw / `81 137` Б gzip;
- холодные assets: `1 006 761` Б raw / `223 090` Б gzip;
- суммарная холодная передача: `304 227` Б gzip;
- сокращение холодной передачи — примерно `44%`;
- realtime: в среднем `476` Б raw / `247` Б gzip;
- расчётная нагрузка: `8 640` запросов за 12 часов;
- Browser QA `390×844`: горизонтального переполнения и ошибок консоли нет.

Цвет состояния техники продолжает передаваться существующими CSS-классами,
рамкой, фоном и текстом. Вместо загрузки набора одинаковых цветных PNG
используются два нейтральных изображения — Экскаватор и Самосвал. Service worker
также больше не загружает лишние цветные копии. Shell повышен с v121 до
`mining-master-mobile-shell-v122`; обновление через штатное окно PWA проверено.

### Системный администратор

- HTML: `79 111` Б raw / `14 928` Б gzip;
- холодные assets: `905 623` Б raw / `128 964` Б gzip;
- суммарная холодная передача: `143 892` Б gzip;
- realtime: в среднем `443` Б raw / `237` Б gzip;
- расчётная нагрузка: `2 880` запросов за 12 часов при интервале 15 секунд;
- Browser QA `1280×720` и `390×844`: горизонтального переполнения и ошибок
  консоли нет.

Оптимизация рабочего кода не потребовалась. Shell остался
`system-admin-shell-v19`.

## Автоматические проверки

- Горный мастер: `44/44 PASS`;
- серверные, ролевые, static- и audit-регрессии: `98/98 PASS`;
- финальный адресный набор: `32/32 PASS`;
- JavaScript operational fragment: `5/5 PASS`;
- JavaScript PWA contract: `32/32 PASS`;
- JavaScript realtime/auth: `16/16 PASS`;
- `manage.py check`: PASS;
- `makemigrations --check --dry-run`: `No changes detected`;
- `git diff --check`: PASS.

## Git и production

Кодовый commit:

`b34c6f2eb63c0b0933d66aae930c60ca8b189751`

Commit отправлен в `origin/codex/pwa-app-catalog-2026-08-23`; локальный и
удалённый SHA совпали.

На production адресно опубликованы три runtime-файла:

- `assignments/views.py`;
- `templates/trips/dispatcher_control.html`;
- `users/role_apps.py`.

`users/role_apps.py` собран как безопасный production-overlay: изменение
Горного мастера v121→v122 применено поверх фактической серверной версии, поэтому
более новый Excavator shell v131 не был понижен до локальной v127.

Резервная копия перед выпуском:

`/srv/accounting-mvp/backups/deploy-20260824T014242Z-mining-master-perf-before-b34c6f2`

После выпуска:

- `accounting-mvp`, nginx и PostgreSQL — active;
- `NRestarts=0` у приложения и nginx;
- ошибок приложения с момента выпуска — `0`;
- `nginx -t` — PASS;
- план миграций пуст, миграции не выполнялись;
- все три production origin отвечают `200` по HTTP/2;
- Горный мастер отдаёт v122, Экскаваторщик — v131, Администратор — v19.

## Где проверять

- `https://excavator.driverform.ru/`
- `https://mining-master.driverform.ru/`
- `https://admin.driverform.ru/`

У Горного мастера после первого открытия может один раз появиться штатное
предложение обновить приложение. После подтверждения должна использоваться
оболочка v122.

## Что отложено

В текущий срочный пакет не входят Заместитель начальника участка, ОУП и
Руководство. Их performance-аудит выполняется позже отдельными этапами.
