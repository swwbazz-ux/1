# Структура админки MVP

Дата создания: 19.06.2026

## Назначение

Админка нужна как управляющий контур системы.

На первом этапе администратор через админку:

- создает сотрудников;
- назначает должность/роль;
- выдает первичный доступ;
- контролирует активацию сотрудников;
- ведет основные справочники;
- видит конфликты регистрации и начала смены;
- управляет блокировкой, деактивацией, архивом и удалением;
- выгружает административные списки в Excel.

В дальнейшем часть функций по сотрудникам перейдет в отдел кадров, но у администратора системы должен сохраниться полный доступ.

## Принцип MVP

Админка MVP должна быть рабочей, но не перегруженной.

Главные правила:

- сначала делаем контроль доступа, сотрудников и справочников;
- не строим сложный HR-модуль до запуска производственного контура;
- все опасные действия должны сохраняться в журнале;
- удаление не должно ломать производственную историю;
- справочники на старте редактирует только администратор;
- рабочие пользователи не должны видеть лишние административные функции.

## Drag-and-drop как фирменная механика проекта

Для проекта принимается отдельное UX-направление: там, где это ускоряет работу и не повышает риск ошибки, использовать механику `перетащил объект в зону действия -> система изменила состояние`.

Смысл: администратор, диспетчер, горный мастер и другие ответственные сотрудники должны работать не только через таблицы и кнопки, но и через наглядные рабочие доски.

Первые сценарии для админки:

- перетащить карточку сотрудника в зону `Заблокировать доступ`;
- перетащить сотрудника в зону `В архив`;
- перетащить сотрудника в зону `Активные`, если доступ можно разблокировать;
- перетащить сотрудника в роль или группу, если позже появится управление ролями через доску;
- перетащить сотрудника в общежитие/секцию при развитии модуля заселения;
- перетащить справочную запись в `Архив`, если запись больше не используется.

Первые сценарии за пределами админки:

- горный мастер перетаскивает самосвалы под экскаваторы;
- машинист экскаватора перетаскивает самосвал на точку разгрузки для создания рейса;
- диспетчер перетаскивает проблемную технику в зону `ОФР`, `ТО`, `Ожидание решения` или под другой экскаватор;
- механик перетаскивает заявку между статусами `Новая`, `В работе`, `Ожидает запчасти`, `Готово`.

Правила безопасности:

- любое разрушительное действие через drag-and-drop требует красивого интерфейсного подтверждения, а не системного alert;
- блокировка доступа, архивирование, деактивация и удаление не выполняются молча;
- после перетаскивания показывать понятный результат: `Сотрудник заблокирован`, `Назначение изменено`, `Заявка переведена в работу`;
- каждое действие должно попадать в журнал: кто, когда, что перетащил, из какого состояния в какое;
- если действие невозможно, объект должен вернуться назад, а система должна объяснить причину;
- для мобильных экранов и клавиатуры должен быть fallback через обычные кнопки/меню, потому что drag-and-drop не всегда удобен на телефоне.

Принцип применения:

- drag-and-drop использовать как ускоритель частых действий;
- не заменять им полностью кнопки и формы;
- не использовать для действий, где требуется ввод причины, комментария или сложная проверка без промежуточного окна;
- зоны сброса должны быть крупными, подписанными и цветово различимыми;
- опасные зоны должны визуально отличаться от обычных рабочих зон.

## Основные экраны админки

### 1. Главная страница админки

Показывает быстрый обзор состояния системы:

- сотрудников всего;
- активных сотрудников;
- сотрудников, которые еще не активировали доступ;
- заблокированных сотрудников;
- деактивированных сотрудников;
- последние регистрации;
- последние входы;
- последние административные действия;
- конфликтные ситуации;
- быстрые действия.

Быстрые действия:

- `Создать сотрудника`;
- `Сотрудники`;
- `Доступы`;
- `Справочники`;
- `Конфликты`;
- `Журнал действий`;
- `Выгрузки`.

### 2. Сотрудники

Список сотрудников с фильтрами:

- по ФИО;
- по должности/роли;
- по статусу сотрудника;
- по статусу доступа;
- по дате приема;
- по дате увольнения;
- по вахте;
- по месту проживания.

В списке должны быть видны:

- ФИО;
- должность;
- телефон;
- статус сотрудника;
- статус доступа;
- дата последнего входа;
- место проживания, если заполнено;
- быстрый переход в карточку.

### 3. Карточка сотрудника

Карточка сотрудника содержит:

- ФИО;
- должность/роль;
- телефон;
- статус;
- комментарий;
- дату приема;
- дату увольнения;
- вахту;
- место проживания;
- паспортные/кадровые данные;
- фото сотрудника;
- статус доступа;
- дату последнего входа;
- историю действий по сотруднику.

Обязательные поля первого этапа:

- ФИО;
- должность;
- телефон;
- статус.

Необязательные поля, которые закладываются как фундамент:

- дата приема;
- дата увольнения;
- вахта;
- место проживания;
- паспортные/кадровые данные;
- фото сотрудника;
- комментарий.

### 4. Доступы и пинкоды

Экран управления доступами показывает:

- сотрудника;
- должность/роль;
- статус доступа;
- первичный пинкод, если он еще не активирован;
- дату выдачи первичного пинкода;
- дату активации;
- дату последнего входа;
- признак блокировки.

Действия администратора:

- сгенерировать первичный пинкод;
- сбросить постоянный пинкод;
- выдать новый первичный пинкод;
- заблокировать вход;
- разблокировать вход;
- деактивировать доступ;
- посмотреть историю доступа.

Формат доступа MVP:

- администратор создает сотрудника;
- система генерирует одноразовый первичный пинкод из 4-6 цифр;
- сотрудник входит по первичному пинкоду;
- сотрудник заполняет первичные данные;
- сотрудник создает постоянный пинкод;
- первичный пинкод после успешной активации больше не работает;
- после первичной регистрации сотрудник сразу попадает в свой рабочий интерфейс.

Отдельное подтверждение администратора после заполнения данных не требуется.

### 5. Справочники

Обязательные справочники первого этапа:

- сотрудники;
- должности/роли;
- техника;
- виды техники;
- породы;
- точки разгрузки;
- смены;
- вахты;
- общежития;
- блоки/секции общежитий;
- причины простоев;
- шаблоны отчетов.

Горизонты и блоки не являются справочниками админки MVP.

Причина: горизонт и блок являются оперативными данными смены. Их вводит машинист экскаватора в своем рабочем интерфейсе при начале смены или при изменении места работы экскаватора.

Права на справочники:

- администратор создает, редактирует, архивирует и удаляет справочные записи;
- диспетчер может смотреть справочники, но не редактировать;
- горный мастер, водитель, машинист экскаватора и механик используют справочники только в рабочих выпадающих списках;
- расширение прав на отдельные справочники переносится на будущий этап.

### 6. Конфликты

Экран конфликтов нужен для контроля ситуаций, которые система не должна пропускать автоматически.

Типы конфликтов первого этапа:

- попытка занять технику, которая уже занята в текущей смене;
- попытка начать смену без обязательных стартовых данных;
- переполнение секции общежития;
- попытка входа заблокированного сотрудника;
- ошибка первичной регистрации;
- попытка полного удаления сотрудника с производственной историей.

Для каждого конфликта фиксируется:

- дата и время;
- сотрудник;
- роль;
- тип конфликта;
- описание;
- экран или процесс;
- статус разбора;
- кто разобрал;
- комментарий администратора.

### 7. Журнал действий

Журнал действий фиксирует важные административные операции:

- создание сотрудника;
- изменение карточки сотрудника;
- назначение роли;
- генерация первичного пинкода;
- сброс пинкода;
- блокировка и разблокировка доступа;
- деактивация сотрудника;
- перевод в архив;
- попытка полного удаления;
- изменение справочников;
- исправление конфликтов.

Журнал должен отвечать на вопрос: кто, когда и что изменил.

### 8. Выгрузки

Администратор должен иметь Excel-выгрузки:

- список сотрудников;
- список доступов и статусов активации;
- список сотрудников, которые еще не вошли в систему;
- список заблокированных и деактивированных сотрудников;
- журнал действий администратора;
- список конфликтов регистрации и начала смены.

## Статусы сотрудника

Используются следующие статусы:

1. `Активен` - работает, может иметь доступы.
2. `Не активирован` - создан в системе, но еще не прошел первичную регистрацию.
3. `Деактивирован` - временно отключен, вход запрещен, сотрудник не уволен.
4. `В архиве` - скрыт из рабочих списков, но история сохранена.
5. `Уволен` - выведен из компании.
6. `Удален` - полное удаление, только если нет связанной истории или есть особое право администратора.

## Статусы доступа

Для доступа используются отдельные статусы:

1. `Не активирован` - первичный пинкод выдан, сотрудник еще не вошел.
2. `Активирован` - сотрудник прошел первичную регистрацию и создал постоянный пинкод.
3. `Заблокирован` - вход временно запрещен.
4. `Деактивирован` - доступ отключен при архиве, увольнении или решении администратора.

## Должностные роли первого этапа

В админке должны быть заложены роли:

- администратор;
- диспетчер;
- горный мастер;
- водитель самосвала;
- машинист экскаватора;
- механик;
- руководство;
- табельщик;
- ОУП;
- отдел кадров;
- охрана труда;
- специалист БДД;
- делопроизводитель.

Правило MVP: один сотрудник имеет одну должность/роль в системе.

Если позже потребуется совмещение ролей, это будет отдельное решение. На старте совмещение не включается, чтобы не усложнять доступы и ответственность.

## Первичная регистрация и начало смены

Первичная регистрация и начало смены - разные процессы.

При первичной регистрации сотрудник:

- подтверждает или заполняет личные данные;
- подтверждает телефон;
- создает постоянный пинкод;
- при необходимости создает логин и пароль;
- подтверждает свою должность/роль;
- заполняет только данные, которые не зависят от конкретной смены.

Сменные данные вводятся только при начале смены.

К сменным данным относятся:

- смена день/ночь;
- стартовые показатели техники;
- самосвал или экскаватор, если техника выбирается именно на смену;
- горизонт, блок и порода для машиниста экскаватора;
- подтверждение, что диспетчер, горный мастер или механик приступил к своей смене.

Без обязательных стартовых сменных данных смену начать нельзя.

## Модель данных админки MVP

### Сотрудник

Поля:

- ФИО;
- должность/роль;
- телефон;
- статус сотрудника;
- комментарий;
- дата приема;
- дата увольнения;
- вахта;
- место проживания;
- паспортные/кадровые данные;
- фото;
- дата создания;
- дата обновления.

Связи:

- сотрудник имеет одну роль;
- сотрудник имеет одну учетную запись доступа;
- сотрудник может иметь историю смен, рейсов, простоев, назначений и действий.

### Роль

Поля:

- название;
- описание;
- активность;
- доступный интерфейс;
- доступные виды техники.

Связи:

- роль назначается сотруднику;
- роль определяет, какой интерфейс открывается после входа.

### Доступ

Поля:

- сотрудник;
- первичный пинкод;
- постоянный пинкод;
- статус доступа;
- дата выдачи первичного пинкода;
- дата активации;
- дата последнего входа;
- дата блокировки;
- причина блокировки.

Правила:

- первичный пинкод одноразовый;
- постоянный пинкод создается сотрудником после первичной регистрации;
- доступ привязан к конкретному сотруднику и роли;
- после блокировки вход запрещен.

### Справочник

Для каждого справочника нужны общие поля:

- название;
- код или номер, если нужен;
- описание;
- активность;
- дата создания;
- дата обновления;
- кто изменил.

Для отдельных справочников добавляются специальные поля.

Техника:

- вид техники;
- номер;
- модель;
- VIN или идентификационный номер;
- грузоподъемность;
- объем кузова или ковша;
- собственная/подрядная;
- временная/постоянная;
- активность.

Породы:

- название;
- плотность;
- коэффициент разрыхления;
- комментарий.

Точки разгрузки:

- название;
- тип;
- активность.

Общежития:

- номер общежития;
- блок;
- секция;
- лимит для дневной смены;
- лимит для ночной смены;
- активность.

Причины простоев:

- название;
- вид техники;
- категория;
- критичность;
- активность.

### Конфликт

Поля:

- дата и время;
- сотрудник;
- роль;
- тип конфликта;
- процесс;
- описание;
- статус;
- кто разобрал;
- комментарий;
- дата разбора.

### Журнал действий

Поля:

- дата и время;
- пользователь;
- роль;
- действие;
- объект;
- старое значение;
- новое значение;
- комментарий;
- IP/устройство, если будет доступно.

## Проверки ядра

Админка и ядро должны проверять:

- у сотрудника не может быть больше одной активной роли в MVP;
- первичный пинкод нельзя использовать повторно после активации;
- заблокированный сотрудник не может войти;
- деактивированный сотрудник не может войти;
- сотрудника с производственной историей нельзя удалить полностью;
- справочная запись, которая уже использовалась в истории, не должна удаляться бесследно;
- неактивные справочные записи не показываются в рабочих выпадающих списках;
- технику нельзя назначить двум людям в одной смене, если это запрещено ролью и видом техники;
- секция общежития не должна превышать лимит 3 человека в день и 3 человека в ночь;
- смену нельзя начать без обязательных стартовых данных.

## Правила удаления и архива

Обычные действия:

- деактивация;
- архив;
- увольнение.

Полное удаление допустимо только если у сотрудника нет связанной истории:

- рейсов;
- смен;
- простоев;
- назначений;
- действий в журнале;
- других производственных данных.

Если история есть, система должна предлагать перевод в архив.

## Минимальный состав реализации

Для первого рабочего варианта админки достаточно:

1. Главная страница админки.
2. Список сотрудников.
3. Создание и редактирование карточки сотрудника.
4. Генерация первичного пинкода.
5. Статусы доступа.
6. Блокировка и разблокировка доступа.
7. Деактивация и архив сотрудника.
8. Справочники первой очереди.
9. Журнал действий.
10. Excel-выгрузка сотрудников и доступов.

## Что можно перенести после MVP

После запуска первого производственного контура можно отдельно развивать:

- полноценный кадровый контур;
- сложные паспортные и кадровые документы;
- фото и документы сотрудника;
- расширенные права на справочники по отделам;
- согласование изменений справочников;
- детальный аудит устройств;
- восстановление доступа через SMS или другой внешний канал;
- совмещение нескольких ролей у одного сотрудника;
- сложную матрицу прав по каждому экрану и действию.

## Порядок разработки админки

Практичный порядок реализации:

1. Проверить текущие модели сотрудников, ролей и кодов доступа.
2. Сравнить текущий код с требованиями этого документа.
3. Добавить недостающие поля моделей.
4. Сделать миграции.
5. Сделать главную страницу админки.
6. Сделать список сотрудников.
7. Сделать карточку сотрудника.
8. Сделать генерацию и сброс пинкодов.
9. Сделать блокировку, деактивацию и архив.
10. Сделать справочники MVP.
11. Сделать журнал действий.
12. Сделать Excel-выгрузки.
13. Покрыть основные сценарии тестами.
14. Проверить через браузер как администратор.

## Критерии готовности блока админки

Блок админки можно считать готовым для первого рабочего запуска, если:

- администратор может создать сотрудника;
- сотруднику можно выдать первичный пинкод;
- сотрудник может пройти первичную регистрацию;
- после регистрации сотрудник попадает в интерфейс своей роли;
- администратор видит статус активации;
- администратор может заблокировать и разблокировать доступ;
- администратор может деактивировать или архивировать сотрудника;
- система не дает удалить сотрудника с историей;
- справочники первой очереди доступны администратору;
- рабочие пользователи видят справочники только в своих выпадающих списках;
- ключевые административные действия попадают в журнал;
- сотрудники и доступы выгружаются в Excel.

## Фактическая реализация 19.06.2026

В код MVP добавлен первый рабочий каркас админки:

- маршрут `/system-admin/`;
- вход администратора по роли `admin` теперь открывает админку MVP;
- главная страница админки с показателями сотрудников, доступов, справочников, последних входов и конфликтов;
- список сотрудников `/system-admin/employees/`;
- создание сотрудника `/system-admin/employees/create/`;
- карточка сотрудника;
- генерация первичного пинкода;
- блокировка, разблокировка и деактивация доступа;
- деактивация и архив сотрудника;
- Excel-выгрузки сотрудников, доступов, журнала действий и конфликтов;
- модели `AdminActionLog` и `AdminConflict`;
- расширенные поля карточки сотрудника и доступа;
- регистрация новых моделей в стандартной Django-админке;
- тесты входа администратора, создания сотрудника, выдачи пинкода, блокировки доступа и Excel-выгрузки.

Проверка:

- `manage.py makemigrations users` - создана миграция `0003_employee_comment_employee_dismissed_at_and_more.py`;
- `manage.py migrate` - миграция применена;
- `manage.py check` - ошибок нет;
- `manage.py test users` - 43 теста пройдено.

Дополнительное изменение после реализации:

- смена и техника перенесены из первичной регистрации водителя в открытие смены;
- первичная регистрация водителя теперь хранит только место проживания;
- активные рейсы, назначения и завершение рейса привязываются к самосвалу открытой смены;
- создана миграция `0004_remove_driverprimaryregistration_shift_type_and_more.py`;
- проверки после переноса пройдены: `manage.py check`, `manage.py test users`, 43 теста.

Дополнительно закрыта защита полного удаления:

- в карточку сотрудника добавлено действие `Удалить полностью`;
- если у сотрудника нет производственной истории, карточка может быть удалена;
- если у сотрудника есть смены, рейсы, простои, назначения или диспетчерские действия, удаление блокируется;
- при блокировке удаления создается запись в административных конфликтах;
- администратор получает сообщение `Удаление запрещено`;
- для сотрудника с историей рабочее действие - перевод в архив;
- добавлены тесты удаления без истории и блокировки удаления с историей;
- проверки после изменения пройдены: `manage.py check`, `manage.py test users`, 45 тестов.

Дополнительно исправлена активация первичного пинкода:

- первичный пинкод больше не становится постоянным доступом автоматически;
- первый вход по первичному пинкоду открывает экран активации;
- сотрудник задает новый постоянный пинкод;
- после активации старый первичный пинкод не работает;
- демо-коды создаются уже активированными, чтобы не ломать демонстрационный сценарий;
- проверки после изменения пройдены: `manage.py check`, `manage.py test users`, 46 тестов.

Дополнительно добавлен реестр справочников админки:

- создан маршрут `/system-admin/references/`;
- на главной админки добавлена кнопка `Справочники`;
- справочники сгруппированы по блокам: сотрудники и доступы, техника, производственные справочники, проживание;
- для каждого справочника показано количество записей и ссылка на рабочий или технический экран;
- добавлен тест открытия реестра справочников;
- проверки после изменения пройдены: `manage.py check`, `manage.py test users`, 47 тестов.

## Дополнение 19.06.2026: экраны конфликтов и журнала действий

В пользовательскую админку MVP добавлены отдельные экраны контроля:

- `/system-admin/conflicts/` - журнал административных конфликтов с фильтром по статусу;
- `/system-admin/logs/` - журнал действий администратора с поиском по действию, объекту, сотруднику и комментарию.

На главной странице `/system-admin/` добавлены быстрые ссылки:

- `Конфликты`;
- `Журнал действий`;
- `Открыть конфликты`;
- `Открыть журнал действий`.

Смысл изменения:

- администратор теперь видит конфликтные ситуации и историю действий прямо в рабочем интерфейсе;
- Excel-выгрузки остаются для контроля, сверки и передачи данных;
- модели `AdminActionLog` и `AdminConflict` используются не только как техническое хранилище, но и как рабочие журналы.

Дополнительно реализовано управление статусом конфликта:

- `В работу`;
- `Решен`;
- `Отклонен`.

При смене статуса фиксируется администратор, время разбора и отдельная запись в журнале действий.

Проверка:

- `manage.py check` - ошибок нет;
- `manage.py test users` - 50 тестов пройдено.

## Дополнение 20.06.2026: правило UX/Figma-проверки админки

При подготовке концепта UX/UI админки в Figma выявлена отдельная категория визуальных ошибок: текст может не выходить за границы своего контейнера, но при этом накладываться на соседний текстовый блок или другой фрейм.

Для дальнейшей работы с админкой принято рабочее правило:

- после изменений интерфейса проверять не только переполнение текста внутри контейнера;
- отдельно проверять пересечения текстовых узлов между собой;
- отдельно проверять выход дочерних элементов за границы родительского контейнера;
- проблемные блоки проверять визуально через PNG-снимок, а не только по автоматическому числовому отчету.

По текущему Figma-концепту админки исправлено:

- блок `Что меняется по смыслу UX`;
- карточка `Drag-and-drop принцип`;
- модальное подтверждение DnD;
- журнал результата DnD;
- мобильный fallback-блок;
- кнопки шапки DnD-доски.

Итоговая проверка Figma-концепта:

- `layoutIssueCount = 0`;
- `collisionCount = 0`;
- проблемный блок дополнительно проверен визуально по PNG-снимку.

## Дополнение 20.06.2026: уточнен UX списка сотрудников и метрик админки

По визуальной проработке списка сотрудников принято уточнение:

- верхние карточки `Сотрудников всего`, `Активных`, `Ждут активации`, `Конфликты`, `Заблокировано` остаются отдельными навигационными метриками;
- эти карточки должны выглядеть и работать как активные кнопки: объем, hover/active-состояние, понятный курсор и переход к соответствующему списку или фильтру;
- внутри таблицы сотрудников не нужно создавать второй набор таких же метрик, чтобы не дублировать смысл и не занимать место;
- в таблице сотрудников отдельная колонка `Статус` не нужна: состояние сотрудника показывается через цвет аватара, строки, текста доступа и подписи под ФИО;
- в списке сотрудников нужны мини-фото или временные аватары с инициалами;
- в таблице допустима DnD-ручка для будущего перетаскивания карточки/строки сотрудника в рабочие зоны действий.

В Figma-концепте исправлено:

- дублирующие метрики внутри блока сотрудников удалены;
- список сотрудников оставлен с аватарами, цветовой индикацией статуса и DnD-ручками;
- существующие верхние карточки-метрики сделаны визуально активными;
- выполнена проверка Figma: `layoutIssueCount = 0`, `collisionCount = 0`, дублирующих metric-блоков внутри таблицы `0`;
- результат дополнительно проверен визуально по PNG-снимку.

## Дополнение 20.06.2026: фильтры списка сотрудников должны выглядеть как выпадающие списки

По визуальной проверке списка сотрудников уточнено, что фильтры `Все роли` и `Все доступы` не должны выглядеть как обычные кнопки, потому что это путает их с действиями.

Принято UX-правило для админки:

- фильтр должен выглядеть как поле выбора, а не как кнопка действия;
- внутри фильтра должна быть понятная подпись поля, например `Роль` или `Доступ`;
- выбранное значение должно отображаться отдельно, например `Все роли` или `Все доступы`;
- справа должна быть стрелка раскрытия, чтобы пользователь сразу понимал, что это выпадающий список;
- кнопка `Показать` остается отдельным действием применения фильтров, пока не будет принято решение об авто-применении.

В Figma-концепте исправлено:

- `Фильтр / Все роли` переделан в `Dropdown / Роль`;
- `Фильтр / Все доступы` переделан в `Dropdown / Доступ`;
- оба фильтра получили вид полей выбора: белый фон, граница, подпись, выбранное значение и стрелка вниз;
- выполнена проверка Figma: `layoutIssueCount = 0`, `collisionCount = 0`, дублирующих metric-блоков внутри таблицы `0`;
- результат дополнительно проверен визуально по PNG-снимку блока сотрудников.

## Дополнение 20.06.2026: очищены наложения и слабый контраст в Figma-концепте админки

По визуальной проверке выявлено, что в Figma-концепте были лишние плавающие текстовые слои, не вложенные в рабочие фреймы. Из-за этого текст `Сотрудники` попадал поверх темной левой навигации и визуально сливался с фоном.

Исправлено:

- удалены page-level текстовые слои `Сотрудники` и пояснение к таблице, которые лежали поверх основного desktop-макета;
- усилен контраст основного бирюзового акцента на кнопках и управляющих элементах;
- усилен контраст цветовых статусов в таблице сотрудников;
- проверены все крупные Figma-фреймы страницы: desktop-админка, mobile-админка, notes-блок, DnD-доска и mobile DnD fallback.

Итоговая проверка Figma:

- проверено крупных фреймов: `5`;
- плавающих текстов поверх фреймов: `0`;
- выходов дочерних элементов за границы: `0`;
- пересечений текста: `0`;
- слабого контраста текста: `0`.

## Дополнение 20.06.2026: первый перенос UX-концепта админки в рабочий Django-интерфейс

В рабочий экран `/system-admin/employees/` перенесен первый слой UX-концепта админки:

- список сотрудников больше не использует отдельную колонку статуса;
- статус сотрудника и доступа отображается через цвет строки, аватара и бейджей;
- в списке появились мини-фото сотрудников или временные аватары с первой буквой ФИО;
- добавлен фильтр по роли сотрудника;
- фильтры `Роль`, `Статус сотрудника`, `Статус доступа` оформлены как выпадающие списки, а не как обычные кнопки;
- рабочая область списка стала шире и лучше использует большой экран;
- добавлены DnD-зоны `Открыть карточку`, `В архив`, `Деактивировать`;
- DnD-действия `В архив` и `Деактивировать` используют существующие безопасные POST-маршруты и интерфейсное подтверждение;
- блокировка конкретного доступа пока оставлена в карточке сотрудника, потому что для нее обязательна причина блокировки.

Реальная бизнес-логика админки не переписана: сохранены существующие маршруты, CSRF-защита, запрет блокировки самого себя и журналирование действий.

Проверка после переноса:

- `manage.py check` - ошибок нет;
- `manage.py test users` - 61 тест пройден;
- `makemigrations --check --dry-run` - изменений миграций нет;
- `START_SERVER_MVP.bat` поднял сервер;
- `/system-admin/employees/` отвечает HTTP `200`;
- через Django test client подтверждено наличие новых элементов: `admin-employee-row`, `data-dnd-action`, `Все роли`, `Выгрузить в Excel`.

Ограничение проверки:

- автоматический визуальный скриншот через Node/Playwright не выполнен из-за ошибки доступа среды Codex `EPERM` к каталогу `C:\Users\swwba\AppData\Local\OpenAI\Codex`.
- итоговую визуальную проверку на экране нужно подтвердить в браузере вручную.

## Дополнение 20.06.2026: фиксированная рабочая область списка сотрудников

Для `/system-admin/employees/` принято уточнение UX:

- на desktop-ширине экран списка сотрудников должен помещаться в высоту окна браузера и не прокручивать всю страницу;
- верхняя зона фильтров и панель быстрых действий остаются фиксированными в рабочей области;
- прокручивается только список сотрудников внутри карточки;
- строки сотрудников визуально уплотнены, чтобы экран не выглядел как браузер с увеличенным масштабом;
- перетаскивание выполняется через отдельный drag-бейдж с фото/аватаром и ФИО сотрудника, а не через крупные кнопки внутри строки.

Проверено на временной HTML-странице с 36 строками сотрудников: внутренний скролл появляется в списке сотрудников, правая панель быстрых действий и верхние фильтры остаются на месте.

После проверки на реальном скриншоте пользователя фиксация рабочего экрана усилена: высота задается через `100dvh` на уровне `html/body`, верхняя шапка и панель фильтров дополнительно уплотнены, версия CSS обновлена до `20260620-ux7` для принудительного сброса кэша браузера.

## Дополнение 20.06.2026: рабочий шаблон Figma v3 применен к списку сотрудников

Экран `/system-admin/employees/` приведен к принятому варианту UX:

- верхняя шапка содержит название экрана, краткое назначение, текущего администратора и компактные кнопки `Назад` / `Админка`;
- блок `Поиск и управление` содержит поиск по ФИО, фильтры по роли, статусу сотрудника и статусу доступа;
- фильтры применяются автоматически, кнопка `Показать` удалена;
- для очистки используется общая кнопка `Сбросить`;
- список сотрудников использует плотные строки с аватаром, ФИО, ролью, статусом сотрудника, телефоном, статусом входа и проживанием;
- состояние сотрудника видно по цвету строки, аватара и статусного бейджа;
- справа находится панель DnD-действий, разделенная на `Доступ` и `Сотрудник`;
- опасные действия остаются через существующие POST-формы, CSRF и подтверждения.

Проверено:

- `manage.py check` - ошибок нет;
- `/system-admin/employees/` после входа отвечает HTTP 200;
- автоматическая браузерная проверка через Chrome: desktop 1920 px и mobile 390 px без горизонтального переполнения;
- контрольный скрин: `C:\Users\swwba\AppData\Local\Temp\admin_employees_ux56.png`.

Ограничение:

- в консоли Chrome осталась одна запись `404` без влияния на страницу; вероятная причина - служебный ресурс вроде favicon. Отдельно проверить при следующей чистке базового шаблона.
