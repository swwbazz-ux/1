# Паспорт Диспетчерского пульта — 26.09.2026

## Зачем нужен этот документ

Это единая точка входа для дальнейшей разработки пульта. Перед новой функцией или
правкой не нужно перечитывать весь `trips/views.py` и весь шаблон: сначала выбирается
раздел ниже, затем открываются только указанные владельцы кода и их тесты.

Главный порядок работы: сначала локально закончить карту, разделение и стабилизацию;
затем развивать интерфейс и добавлять функции; публикацию на production рассматривать
только отдельным последним этапом после полного локального release-аудита.

## Зафиксированный baseline и граница публикации

- Исходный production-подтверждённый baseline: `bbe6b8dc`; история его проверки и
  локальной стабилизации находится в документе `207`.
- Все последующие этапы разборки выполняются в изолированной ветке
  `codex/dispatcher-stabilization-20260925` малыми commit без изменения production.
- Push, `VERIFY`, `DEPLOY`, collectstatic, база данных и миграции — отдельные действия,
  которые не следуют автоматически из готовности локального кода.

## Карты, которые уже существуют

- `207_СТАБИЛИЗАЦИЯ_ТЕКУЩЕГО_ДИСПЕТЧЕРСКОГО_ПУЛЬТА_2026_09_25.md` — baseline,
  исправленный показ JSON и история разделения frontend/runtime.
- `208_АУДИТ_CSS_ДИСПЕТЧЕРСКОГО_ПУЛЬТА_2026_09_25.md` — шесть CSS-слоёв,
  удалённые точные дубли и сохранённые изменяющие overrides.
- `209_КАРТА_BACKEND_ДИСПЕТЧЕРСКОГО_ПУЛЬТА_2026_09_25.md` — URL, серверные
  инварианты, command/read-model модули и история backend-разделения.
- Этот документ `210` — верхнеуровневый навигатор и порядок дальнейшего развития.

## Быстрый выбор владельца кода

| Что меняем | Основной владелец | Связанные проверки |
|---|---|---|
| Полная доска и GET detail-карточки | `trips/dispatcher_read_model.py`, `trips/views.py` как фасад | `trips/test_dispatcher_read_model.py` |
| Чистые подписи, связь, строки смены, сортировка, сводка и приоритеты состояния техники | `trips/dispatcher_dashboard_projection.py` | `trips/test_dispatcher_dashboard_projection.py` |
| Перемещение экскаватора и назначение самосвала | `trips/dispatcher_topology_commands.py` | `trips/test_dispatcher_topology_commands.py` |
| Отмена назначения | `trips/dispatcher_assignment_commands.py` | `trips/test_dispatcher_assignment_commands.py` |
| Ручной, отменённый или служебно завершённый рейс | `trips/dispatcher_trip_commands.py` | `trips/test_dispatcher_trip_commands.py`, `trips/test_dispatcher_manual_trip.py` |
| Закрытие простоя | `trips/dispatcher_downtime_commands.py` | `trips/test_dispatcher_downtime_commands.py` |
| Своя смена и служебное закрытие чужой смены | `trips/dispatcher_shift_commands.py`, `trips/dispatcher_header.py` | `trips/test_dispatcher_shift_commands.py` |
| POST настроек detail-карточки | `trips/dispatcher_equipment_commands.py` | `trips/test_dispatcher_equipment_commands.py` |
| Доступ, active-role и формат ошибок | `trips/dispatcher_guards.py` | `trips/test_dispatcher_guards.py`, `trips/test_chaos_dispatcher_active_role.py` |
| PWA manifest/service worker | `trips/dispatcher_pwa.py` | PWA/role contract tests и Browser QA |
| HTML-компоновка рабочего экрана | `templates/trips/dispatcher_control.html` | dispatcher DOM/runtime tests и Browser QA |
| Карточка техники в браузере | `static/js/dispatcher-detail-v1.js`, `static/css/dispatcher-detail-v1.css` | `dispatcher-detail-*-runtime.test.js` |
| Доска, drag-and-drop и плитки | `static/js/dispatcher-board-v1.js`, `static/css/dispatcher-workspace-v1.css` | board/fragment/responsive runtime tests |
| Сеть, offline-команды и JSON-контракты | `static/js/dispatcher-transport-v1.js` | `dispatcher-transport-runtime.test.js` |
| Realtime fragment reconciliation | `static/js/dispatcher-realtime-v1.js` | `dispatcher-realtime-runtime.test.js` |
| Звуки и уведомления | `static/js/dispatcher-sounds-v1.js`, `core/dispatcher_push.py` | sounds/system-push runtime tests |
| Адаптивная компоновка | `static/css/dispatcher-adaptive-v1.css` | responsive/mobile runtime tests и Browser QA 390 px |
| Холст диспетчерских экранов | `static/js/dispatcher-canvas-v1.js`, `static/css/dispatcher-canvas-v1.css` | отдельная проверка подключения и упаковки |

## Фактические крупные узлы на 26.09.2026

- `trips/views.py` — `7338` строк. Dispatcher-код ещё присутствует в блоках
  формирования данных и в публичных view-фасадах, хотя write-команды уже вынесены.
- `templates/trips/dispatcher_control.html` — `7508` строк. Это следующий крупный
  объект разборки: HTML-секции и оставшийся inline JavaScript нужно сначала
  картировать, а затем выносить без одновременного редизайна.
- Frontend runtime уже разделён на `transport`, `detail`, `board`, `realtime`,
  `sounds`, `control` и `canvas`; самые крупные модули сейчас `detail` и `board`.
- CSS уже физически разделён на шесть слоёв, но в нём сохранены `69` изменяющих
  значение overrides. Их нельзя считать мусором и удалять массово.

## Межролевые границы

- `dispatcher_control_view` также используется как renderer Горного мастера. Нельзя
  менять его общий контекст, не проверив этот контракт.
- `dispatcher_shift_report_for_equipment`, plan payload/card helpers и часть расчётов
  используются экраном Экскаваторщика. Их нельзя переносить в Dispatcher-only модуль
  только из-за удобного имени.
- Общие realtime, connection, active-role и shift-контракты нельзя копировать внутрь
  пульта: у них должен сохраняться один источник истины.

## Уже найденный разрыв упаковки

Шаблон фактически загружает `dispatcher-canvas-v1.js` и
`dispatcher-canvas-v1.css`, но на 26.09.2026 оба файла отсутствуют в
`.github/deploy/production-files.txt` и в `CORE_ASSETS` service worker пульта.
Предыдущая документация утверждала обратное. Это release-риск: до будущей публикации
нужно отдельно решить, входят ли файлы в обязательную оболочку, исправить allowlist и
PWA-контракт с адресными тестами. На текущем этапе инфраструктурные файлы не изменялись.

## Как выполнять каждую следующую задачу

1. Найти строку задачи в таблице владельцев и открыть только эти файлы.
2. Проверить, нет ли межролевого вызова из Горного мастера или Экскаваторщика.
3. Сначала добавить или уточнить тест текущего поведения.
4. Сделать одну структурную или функциональную группу, не смешивая её с редизайном.
5. Запустить профильные Django/JavaScript-тесты, `manage.py check` и Browser QA.
6. Обновить этот паспорт, если появился новый файл, контракт или точка расширения.
7. Зафиксировать отдельный локальный commit. Публикацию не подразумевать.

## Текущий статус backend-карты

- Presentation-функции сотрудника, связи, строк смены, причины простоя, выбора
  запрошенной карточки и чистого порядка приоритетов состояния техники вынесены в
  `dispatcher_dashboard_projection.py`.
- В `build_dispatcher_dashboard_context` осталось три nested helpers. Они владеют
  расчётом плана/отчёта и ORM-выборкой рейсов и намеренно остаются у текущего владельца,
  потому что связаны с общим контуром и экраном Экскаваторщика.
- Справочник состояний, причины простоев, `OPEN_TRIP_STATUSES` и `AssignmentStatus`
  не продублированы. `trips/views.py` вычисляет готовые признаки и один раз строит карты
  состояния самосвалов и экскаваторов для всех представлений доски.
- Карта чистых вложенных helpers builder завершена. Следующая безопасная задача —
  разделение большого шаблона по уже построенной карте `211`.

## Карта большого шаблона

Построчная карта, DOM/data-контракты, внешний desktop runtime, inline runtime
Горного мастера и обязательные rebind-точки записаны в
`211_КАРТА_ШАБЛОНА_ДИСПЕТЧЕРСКОГО_ПУЛЬТА_2026_09_26.md`.

Ключевое ограничение: строки `1314–7499` находятся в mobile-ветке Горного мастера и
не должны переноситься в desktop-модули Диспетчера. Desktop runtime уже состоит из
шести внешних модулей; следующий безопасный этап — только HTML include без изменения
DOM, поведения и дизайна.

Новый include нельзя оставлять только в Git-дереве приложения: защищённая публикация
копирует manifest-scoped payload. Поэтому каждый include должен добавляться в
`.github/deploy/production-files.txt` и release-тест в том же локальном commit.
Локальная правка manifest ожидает отдельного разрешения; push, `VERIFY` и deploy этим
не разрешаются.

## Этапы до начала большой функциональной разработки

1. После разрешения на локальный production manifest разделить шаблон на именованные
   includes, начиная с одного безопасного HTML-блока без изменения DOM-контракта.
2. Вынести оставшийся inline JavaScript в
   существующие или новые узкие модули, сохраняя DOM и поведение.
3. Для каждого владельца закрепить минимальный профиль тестов и Browser QA-сценарий.
4. Провести отдельный аудит упаковки: template assets, service worker, release manifest,
   shell/cache version и полный production payload.
5. Только после этого переходить к новым функциям и заметной переработке интерфейса.

## Когда предварительная разборка считается законченной

- у каждого URL, действия, блока интерфейса и фонового процесса есть один записанный
  владелец кода;
- большой шаблон не содержит неописанных inline-контуров;
- Dispatcher-only код отделён от общих контрактов других ролей;
- для каждого модуля известны адресные тесты и браузерный сценарий;
- release manifest, PWA shell и реально загружаемые assets согласованы;
- локальная ветка чисто проходит полный профиль, а решение о production принимается
  отдельно после просмотра итоговой карты и release-пакета.
