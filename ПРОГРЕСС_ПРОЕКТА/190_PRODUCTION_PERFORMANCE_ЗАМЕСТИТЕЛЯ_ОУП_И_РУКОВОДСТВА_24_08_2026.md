# Production performance-аудит Заместителя, ОУП и Руководства

Дата: 24.08.2026 (Владивосток).

## Итог

Завершён performance-аудит последних трёх готовых PWA из каталога:

- Заместитель начальника горного участка;
- ОУП;
- Руководство.

Заместитель и Руководство уже укладываются в принятые ограничения, поэтому их
рабочая логика и интерфейс не менялись. Для ОУП добавлена отложенная загрузка
фотографий сотрудников: изображения вне видимой части длинного списка больше
не должны загружаться все сразу.

Изменение опубликовано в production. Модели, миграции, рабочие данные, `.env`,
nginx и PostgreSQL не менялись.

## Изолированная среда

Активные действия выполнялись только на отдельной PostgreSQL QA-базе:

- база `copper_pwa_performance_qa_20260823`;
- адрес `127.0.0.1:55437`;
- run ID `PWA-PERF-20260824-REMAINING-01` и повторный ОУП-run
  `PWA-PERF-20260824-REMAINING-02`;
- рабочая SQLite и production-БД не использовались;
- после проверки QA-сервер и PostgreSQL остановлены, порты `8002` и `55437`
  освобождены.

## Заместитель начальника горного участка

- HTML: `575 486` Б raw / `30 576` Б gzip;
- cold assets: `1 016 600` Б raw / `147 870` Б gzip;
- суммарная холодная передача: `178 446` Б gzip;
- ответ рабочего экрана в QA-контуре: около `614 мс`;
- постоянного realtime polling нет;
- DOM: `2 231` элемента на полном QA-срезе;
- Browser QA `1280×720` и `390×844`: горизонтального переполнения и ошибок
  консоли нет.

Оптимизация рабочего кода не потребовалась. Shell остался
`deputy-mining-manager-desktop-shell-v14`.

## ОУП

- HTML: `625 239` Б raw / `26 259` Б gzip;
- cold assets: `946 951` Б raw / `135 838` Б gzip;
- суммарная холодная передача без фотографий сотрудников: `162 097` Б gzip;
- ответ рабочего экрана в QA-контуре: около `148 мс`;
- постоянного realtime polling нет;
- DOM полного QA-списка: `4 250` элементов;
- Browser QA `1280×720` и `390×844`: горизонтального переполнения и ошибок
  консоли нет.

QA-состав не содержит реальных фотографий, поэтому их экономия не подменяется
выдуманным числом. Кодовый аудит production-шаблона подтвердил, что до правки
каждая фотография имела обычный `src` без lazy-loading. Теперь общий шаблон
строки сотрудника использует `loading="lazy"` и `decoding="async"`; фактическая
экономия будет зависеть от количества фотографий ниже видимой области.

Shell повышен с v21 до `oup-shell-v22`.

## Руководство

- HTML: `89 827` Б raw / `15 832` Б gzip;
- cold assets: `903 380` Б raw / `126 680` Б gzip;
- суммарная холодная передача: `142 512` Б gzip;
- ответ рабочего экрана в QA-контуре: около `281 мс`;
- realtime: один observer-клиент раз в 15 секунд;
- ответ realtime: в среднем `450,25` Б raw / `238,25` Б gzip;
- расчётная нагрузка: `2 880` запросов за 12 часов;
- Browser QA `1280×720` и `390×844`: горизонтального переполнения и ошибок
  консоли нет.

Оптимизация рабочего кода не потребовалась. Shell остался
`management-shell-v5`.

## Проверки

- ОУП и общая карточка сотрудника: `89/89 PASS`;
- QA-safety, Заместитель и Руководство: `45/45 PASS`;
- JavaScript PWA contract: `32/32 PASS`;
- JavaScript realtime/auth: `16/16 PASS`;
- `manage.py check`: PASS;
- `makemigrations --check --dry-run`: `No changes detected`;
- `git diff --check`: PASS.

## Git

Кодовый commit:

`6053239` — `perf(oup): defer employee photo loading`

Commit отправлен в `origin/codex/pwa-app-catalog-2026-08-23`.

## Production-выпуск

Production содержит более новые изменения общего шаблона строки сотрудника и
более новые версии нескольких PWA, которых нет в feature-ветке. Поэтому выпуск
выполнен точечным overlay поверх фактического production:

- изменён `templates/users/includes/employee_register_row.html`;
- изменён только ОУП shell v21→v22 в `users/role_apps.py`;
- сохранены Driver v116, Excavator v131, Mining Master v122, Dispatcher v45,
  Deputy v14, Management v5 и System Admin v20.

Успешная резервная копия:

`/srv/accounting-mvp/backups/deploy-20260824T042720Z-oup-performance-before-6053239`

Первая техническая попытка была автоматически откатана защитным скриптом на
финальном контрольном шаге. Резервная точка этой попытки сохранена отдельно:

`/srv/accounting-mvp/backups/deploy-20260824T042637Z-oup-performance-before-6053239`

Повторный запуск с полной трассировкой прошёл успешно. После выпуска:

- `accounting-mvp`, nginx и PostgreSQL — active;
- `NRestarts=0` у приложения и nginx;
- ошибок приложения с момента успешного выпуска — `0`;
- `nginx -t` — PASS;
- план миграций пуст, миграции не выполнялись;
- все три production origin отвечают `200` по HTTP/2;
- ОУП отдаёт v22, Заместитель — v14, Руководство — v5.

## Где проверять

- `https://deputy.driverform.ru/`
- `https://oup.driverform.ru/`
- `https://management.driverform.ru/`

У ОУП после первого открытия может один раз появиться штатное предложение
обновить приложение. После подтверждения должна использоваться оболочка v22.

## Статус полного каталога

Performance-аудит и публикация выполнены для всех восьми готовых PWA каталога:

- Горный диспетчер;
- Водитель самосвала;
- Машинист экскаватора;
- Горный мастер;
- Заместитель начальника горного участка;
- ОУП;
- Руководство;
- Системный администратор.
