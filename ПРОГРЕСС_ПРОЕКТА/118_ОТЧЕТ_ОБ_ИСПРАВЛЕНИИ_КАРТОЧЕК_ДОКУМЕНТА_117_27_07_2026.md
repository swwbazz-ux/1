# Отчёт об исправлении карточек документа 117

Дата: 27.07.2026  
Основание: `117_НЕЗАВИСИМАЯ_ПЕРЕПРОВЕРКА_POSTGRESQL_И_НОВЫЕ_КАРТОЧКИ_27_07_2026.md`  
Исправлялись только: `QA-PG-P2-003`, `QA-PG-P2-004`, `QA-CORE-P2-005`.  
Итоговый статус: **ГОТОВО К ПЕРЕПРОВЕРКЕ**.  
Статус `FIXED` не присваивается.

## 1. Границы и исходное состояние

- ветка: `codex/new-public-site-ux-prototype`;
- HEAD до и после работы:
  `e75922ce7d26c1b9cf4335d8c1af67064a40d440`;
- предсуществующее грязное дерево сохранено без общего отката;
- документы 113–117 не переписывались;
- production, рабочая SQLite, `private_media`, `.env`, модели и файлы
  миграций не изменялись;
- незавершённые роли и соседние карточки не перерабатывались;
- commit, push и deploy не выполнялись.

Для PostgreSQL-проверок использовался только остановленный до и после работы
переносимый PostgreSQL 16.14:

```text
C:\Users\swwba\AppData\Local\Temp\codex-pg16-qa-20260727-01
127.0.0.1:55432
```

## 2. QA-PG-P2-003

Файл:

```text
СИСТЕМА_MVP/backend/trips/test_chaos_p01_shift_downtime.py
```

Исправлена только тестовая фикстура:

- `create_driver()` теперь получает отдельный обязательный
  keyword-параметр `section_name`;
- применены короткие явные значения `QA-OLD` и `QA-NEW`;
- имя секции больше не строится из длинного `access_code`;
- в существующий `setUp` добавлена проверка точного набора созданных секций;
- количество тестов класса осталось прежним — `14`;
- `DormitorySection.max_length`, модель и миграции не менялись.

Результат:

```text
ChaosDriverShiftHandoffRegressionTests
SQLite:     14/14 OK
PostgreSQL: 14/14 OK
```

## 3. QA-PG-P2-004

Файл:

```text
СИСТЕМА_MVP/backend/trips/tests_achievements.py
```

В `AchievementPrizeApiTests`:

- удалено ручное повторное закрытие `image_response`;
- удалено ручное повторное закрытие `download_response`;
- `streaming_content` обоих `FileResponse` полностью потребляется через
  `b''.join(...)`;
- для обоих ответов проверяется фактическое содержимое `b'prize-image'`;
- сохранены последующие `achievement_shown`, `refresh_from_db()` и повторный
  `achievement_current`.

Production-обработчик призов не менялся.

Результат:

```text
AchievementPrizeApiTests
SQLite:     4/4 OK
PostgreSQL: 4/4 OK
```

## 4. QA-CORE-P2-005

Файлы:

```text
СИСТЕМА_MVP/backend/trips/views.py
СИСТЕМА_MVP/backend/trips/tests.py
```

В `excavator_truck_loaded_cancel_view` восстановлены пять корректных
пользовательских JSON-сообщений:

1. `Нет доступа к экрану Экскаваторщика.`
2. `Не передан client_action_id.`
3. `Некорректные параметры действия.`
4. `Незакрытый рейс для отмены не найден.`
5. `Точка разгрузки в действии не совпадает с рейсом.`

HTTP-статусы, условия ветвления, блокировки, идемпотентность и успешная отмена
не менялись.

В существующий интеграционный тест настоящего POST-обработчика добавлены
исполняемые проверки всех пяти error path:

- `403` без доступа;
- `400` без `client_action_id`;
- `400` с некорректными параметрами;
- `409`, если открытый рейс не найден;
- `409` при несовпадении точки разгрузки.

Для несовпадения точки дополнительно проверено, что рейс остаётся
`LOADED_WAITING_UNLOAD`, а `TripClientAction` отмены не создаётся. Исходный
успешный сценарий отмены в том же тесте сохранён.

## 5. PostgreSQL 16.14

PostgreSQL запускался только с явными адресом и портом:

```powershell
$pgRoot = 'C:\Users\swwba\AppData\Local\Temp\codex-pg16-qa-20260727-01'
& "$pgRoot\bin-dist\pgsql\bin\pg_ctl.exe" start `
  -D "$pgRoot\data" `
  -l "$pgRoot\postgres.log" `
  -o '-p 55432 -h 127.0.0.1' `
  -w
```

Готовность проверена отдельно:

```powershell
& "$pgRoot\bin-dist\pgsql\bin\pg_isready.exe" `
  -h 127.0.0.1 -p 55432 -d postgres -U qa_cluster_admin
```

Фактический ответ:

```text
127.0.0.1:55432 - accepting connections
```

Процесс-локальное окружение:

```powershell
$env:PYTHON_DOTENV_DISABLED = '1'
$env:DJANGO_DB_ENGINE = 'postgres'
$env:POSTGRES_DB = 'copper_resources_qa'
$env:POSTGRES_USER = 'copper_qa_runner'
$env:POSTGRES_PASSWORD = ''
$env:POSTGRES_HOST = '127.0.0.1'
$env:POSTGRES_PORT = '55432'
$env:POSTGRES_CONN_MAX_AGE = '0'
```

### 5.1. Два адресных класса

```text
Found 18 test(s)
Ran 18 tests in 1.841s
OK
```

### 5.2. Исходные concurrency-тесты

```text
Found 14 test(s)
Ran 14 tests in 28.163s
OK
```

### 5.3. Одиночные PostgreSQL POST-smoke

```text
Found 17 test(s)
Ran 17 tests in 0.991s
OK
```

### 5.4. Полный PostgreSQL-набор

```text
Found 704 test(s)
Ran 704 tests in 95.844s
OK
```

Пропущенных тестов в PostgreSQL-наборе нет.

## 6. SQLite и системные проверки

Полный штатный SQLite-набор выполнен в отдельном процессе без PostgreSQL
переменных:

```text
Found 704 test(s)
Ran 704 tests in 34.609s
OK (skipped=31)
```

Все `31` пропуска — допустимые PostgreSQL-only тесты: исходные `14`
concurrency и `17` POST-smoke.

Остальные проверки:

| Проверка | Фактический результат |
|---|---|
| `manage.py check` | `System check identified no issues (0 silenced).` |
| `manage.py makemigrations --check --dry-run` | `No changes detected` |
| `git diff --check` | exit code `0`; только информационные LF/CRLF warnings |

## 7. Browser QA Машиниста

Проверялся настоящий экран:

```text
http://127.0.0.1:8000/excavator/work/
PWA shell: excavator-mobile-shell-v122
Видимая версия: 117
```

Использовалась только изолированная PostgreSQL QA-база. Стандартный
`START_SERVER_MVP.bat` подготовил в ней существующую схему; рабочая SQLite
не участвовала. Изменений файлов миграций не было.

Проверка без принудительного reload:

| Размер | Результат |
|---|---|
| `1280×720` | фактический viewport `1280×720`; общий horizontal overflow отсутствует; очередь и диалог доступны |
| `390×844` | фактический viewport `390×844`; ширина `body/document` — `390`; общий horizontal overflow отсутствует |

На mobile диалог очереди полностью помещался в viewport:

```text
x=11.31, y=527.58, width=367.77, height=226.43
right=379.09, bottom=754.00
```

Ошибки browser console на обоих размерах:

```text
[]
```

Для безопасного error path был подготовлен stale-сценарий: рейс оставался
видимым в уже открытой очереди, а в изолированной QA-базе был переведён в
`COMPLETED` без изменения operational-state version. Долгое удержание,
открытие очереди и движение карточки вверх настоящим обработчиком выполнены.

Ограничение автоматизированного Browser QA: контроллер in-app Browser не
передал `pointerup` карточке после выхода указателя за её границу. Поэтому
завершающий POST жеста и визуальный показ сообщения
`Незакрытый рейс для отмены не найден.` в этой сессии не выдаются за
подтверждённый Browser PASS. Синтетический вызов функции, подмена DOM и
ручной `fetch` не использовались. Серверный JSON и его передача через
настоящий POST-обработчик покрыты исполняемой Django-регрессией; точный
ручной Browser-сценарий приведён в разделе 10.7.

## 8. Неизменность рабочих данных

Рабочая SQLite до и после:

| Параметр | До | После |
|---|---:|---:|
| размер | `2584576` | `2584576` |
| mtime UTC | `2026-07-23T17:11:26.0764680Z` | `2026-07-23T17:11:26.0764680Z` |
| SHA-256 | `A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222` | `A5CC387E15454B107FE5E04ADC6801E983873AD62EB79DE76E91283E02733222` |

`private_media` до и после:

| Параметр | До | После |
|---|---:|---:|
| файлов | `78` | `78` |
| байт | `3042` | `3042` |
| SHA-256 manifest | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` | `ABEDC5CD377D2FA49BE966E878CD34F1C0EAA972366DCC9E20062D05AFDF1B35` |

Документы 113–117 остались с контрольными SHA-256:

| Документ | SHA-256 |
|---|---|
| `113_ОТЧЕТ_О_ДОШЛИФОВКЕ_ГОТОВОГО_ПРОИЗВОДСТВЕННОГО_ЯДРА_27_07_2026.md` | `0E23913F947DD379F2D4D5175A511EBE877913C7C302BD6C58F913C6C7FA8F07` |
| `114_ПРИЕМОЧНЫЙ_BROWSER_SMOKE_И_АУДИТ_POSTGRESQL_27_07_2026.md` | `BD1985DBF13F85851437B8B1CF63C054178882FAFAC3D941CE88D3E1E36F00F0` |
| `115_РЕЗУЛЬТАТ_POSTGRESQL_CONCURRENCY_И_ЗАДАНИЕ_НА_ИСПРАВЛЕНИЕ_27_07_2026.md` | `7E65C42A23E366B61C81D57731D2DD3FDFE7826E76A6F681C4F36AA7887BA59E` |
| `116_ОТЧЕТ_ОБ_ИСПРАВЛЕНИИ_POSTGRESQL_CONCURRENCY_27_07_2026.md` | `81EC97E11B4E9C50E425C4ADE77DA90C7A644B6A8645823C9A21983DAB27B80B` |
| `117_НЕЗАВИСИМАЯ_ПЕРЕПРОВЕРКА_POSTGRESQL_И_НОВЫЕ_КАРТОЧКИ_27_07_2026.md` | `E572307802A3AF44E70832D44D4478F05EC77A39D3F9D78EF7DFBB1BE64701D1` |

После всех проверок:

```text
listener 8000 = 0
listener 55432 = 0
PostgreSQL processes = 0
```

Viewport override in-app Browser сброшен, тестовая вкладка закрыта.

## 9. Изменённые файлы и SHA-256

| Файл | Байт | SHA-256 |
|---|---:|---|
| `СИСТЕМА_MVP/backend/trips/test_chaos_p01_shift_downtime.py` | `70159` | `5B4774561FFD33F510EFBB368FCA23CECE5D4FEC3C45E48A8521E27B4EE261E7` |
| `СИСТЕМА_MVP/backend/trips/tests_achievements.py` | `9246` | `7B3A89B6746011E3AF39C1F8FDADDEDFBB824C241A26F2C8110E454CB5AE9F9D` |
| `СИСТЕМА_MVP/backend/trips/tests.py` | `151435` | `E7775A5F8827F11C37302D462B7D0A4E62587057B2A60DD71C62C375A26ED78C` |
| `СИСТЕМА_MVP/backend/trips/views.py` | `227219` | `4BBAB12F04CD0206E938DD89E81DE73312A1EEDE0966C74BC1D1A15F162BDE38` |

Этот документ является пятым файлом результата. Его собственный стабильный
SHA-256 рассчитывается после окончательной записи и передаётся в итоговом
сообщении.

Файлы уже содержали изменения предыдущих принятых этапов. Таблица фиксирует
полные текущие отпечатки файлов; в рамках документа 117 изменены только
описанные в разделах 2–4 участки.

## 10. Точная инструкция независимой перепроверки

### 10.1. Запустить переносимый PostgreSQL

```powershell
$pgRoot = 'C:\Users\swwba\AppData\Local\Temp\codex-pg16-qa-20260727-01'
& "$pgRoot\bin-dist\pgsql\bin\pg_ctl.exe" start `
  -D "$pgRoot\data" `
  -l "$pgRoot\postgres.log" `
  -o '-p 55432 -h 127.0.0.1' `
  -w

& "$pgRoot\bin-dist\pgsql\bin\pg_isready.exe" `
  -h 127.0.0.1 -p 55432 -d postgres -U qa_cluster_admin
```

Не продолжать, если ответ отличается от:

```text
127.0.0.1:55432 - accepting connections
```

### 10.2. Установить только process-local окружение

```powershell
$env:PYTHON_DOTENV_DISABLED = '1'
$env:DJANGO_DB_ENGINE = 'postgres'
$env:POSTGRES_DB = 'copper_resources_qa'
$env:POSTGRES_USER = 'copper_qa_runner'
$env:POSTGRES_PASSWORD = ''
$env:POSTGRES_HOST = '127.0.0.1'
$env:POSTGRES_PORT = '55432'
$env:POSTGRES_CONN_MAX_AGE = '0'

Set-Location 'C:\Users\swwba\Desktop\Проект учетная система\ПОЕКТ\СИСТЕМА_MVP\backend'
```

### 10.3. Выполнить адресные PostgreSQL-тесты

```powershell
..\.venv\Scripts\python.exe manage.py test `
  trips.test_chaos_p01_shift_downtime.ChaosDriverShiftHandoffRegressionTests `
  trips.tests_achievements.AchievementPrizeApiTests `
  --noinput --verbosity 1
```

Ожидается: `18/18 OK`.

### 10.4. Выполнить исходные concurrency и POST-smoke

```powershell
$labels = @(
  'trips.test_chaos_trip_terminal_idempotency.TripTerminalPostgreSQLConcurrencyTests',
  'trips.test_chaos_dispatcher_active_role.DispatcherActiveRolePostgreSQLConcurrencyTests',
  'shifts.test_chaos_p04_009_postgresql.ExcavatorConcurrentShiftStartPostgreSQLTests',
  'downtimes.test_chaos_p01_005_postgresql.DowntimePostgreSQLConcurrencyRegressionTests'
)

..\.venv\Scripts\python.exe manage.py test @labels --noinput --verbosity 1
..\.venv\Scripts\python.exe manage.py test `
  core.test_postgresql_ready_core_post_smoke.PostgreSQLReadyCorePostSmokeTests `
  --noinput --verbosity 1
```

Ожидается: `14/14 OK`, затем `17/17 OK`.

### 10.5. Выполнить полный PostgreSQL-набор

```powershell
..\.venv\Scripts\python.exe manage.py test --noinput --verbosity 1
```

Ожидается:

```text
Found 704 test(s)
Ran 704 tests
OK
```

### 10.6. Выполнить SQLite и системные проверки

В новом PowerShell-процессе:

```powershell
$env:PYTHON_DOTENV_DISABLED = '1'
Remove-Item Env:DJANGO_DB_ENGINE -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_DB -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_USER -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_PASSWORD -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_HOST -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_PORT -ErrorAction SilentlyContinue
Remove-Item Env:POSTGRES_CONN_MAX_AGE -ErrorAction SilentlyContinue

Set-Location 'C:\Users\swwba\Desktop\Проект учетная система\ПОЕКТ\СИСТЕМА_MVP\backend'
..\.venv\Scripts\python.exe manage.py test --noinput --verbosity 1
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run
Set-Location '..\..'
git diff --check
```

Ожидается: `704 OK (skipped=31)`, системных ошибок и новых миграций нет,
`git diff --check` возвращает `0`.

### 10.7. Выполнить ручной Browser QA безопасного 409

Использовать только изолированную PostgreSQL QA-базу и process-local окружение
раздела 10.2.

1. Из корня проекта запустить Django только через `START_SERVER_MVP.bat`.
2. Подготовить существующий QA-рейс перед первым viewport:

   ```powershell
   Set-Location 'C:\Users\swwba\Desktop\Проект учетная система\ПОЕКТ\СИСТЕМА_MVP\backend'
   ..\.venv\Scripts\python.exe manage.py shell -c "from trips.models import Trip, TripStatus; Trip.objects.filter(pk=1).update(status=TripStatus.LOADED_WAITING_UNLOAD, completed_at=None); print('trip 1 ready')"
   ```

3. Открыть `http://127.0.0.1:8000/` и войти как Машинист через
   выделенную локальную QA-учётную запись. Телефон и PIN получить вне
   репозитория; в отчёт и снимки экрана реквизиты не записывать.
4. Перейти на `/excavator/work/`, установить viewport `1280×720`, открыть
   точку `QA117 ККД` и оставить диалог очереди открытым.
5. Не перезагружая PWA, во втором PowerShell выполнить:

   ```powershell
   ..\.venv\Scripts\python.exe manage.py shell -c "from trips.models import Trip, TripStatus; Trip.objects.filter(pk=1).update(status=TripStatus.COMPLETED); print('trip 1 completed without state bump')"
   ```

6. Смахнуть карточку `QA117-T1` вверх штатным жестом.
7. Проверить HTTP `409` и точный текст в заметке диалога:
   `Незакрытый рейс для отмены не найден.`
8. Проверить, что нет общего horizontal overflow и ошибок console.
9. Повторить шаги 2, 4–8 без принудительного reload между открытием диалога
   и жестом для viewport `390×844`.
10. Сравнить количество действий до и после:

    ```powershell
    ..\.venv\Scripts\python.exe manage.py shell -c "from trips.models import TripClientAction; print(TripClientAction.objects.filter(action_type='truck_loaded_cancel', trip_id=1).count())"
    ```

    Безопасный `409` не должен создавать новое действие отмены.

### 10.8. Остановить оба сервера

```powershell
Set-Location 'C:\Users\swwba\Desktop\Проект учетная система\ПОЕКТ'
cmd /c "STOP_SERVER_MVP.bat < nul"

$pgRoot = 'C:\Users\swwba\AppData\Local\Temp\codex-pg16-qa-20260727-01'
& "$pgRoot\bin-dist\pgsql\bin\pg_ctl.exe" stop `
  -D "$pgRoot\data" `
  -m fast `
  -w
```

Проверить отсутствие listeners на `8000` и `55432`, а также отсутствие
процессов `postgres`.

## 11. Итог

Три разрешённые карточки реализованы. Два адресных класса, исходные
concurrency, POST-smoke и полные наборы PostgreSQL/SQLite зелёные. Рабочие
данные неизменны, оба сервера остановлены.

Визуальная часть Browser QA по размерам, overflow и console пройдена.
Визуальный показ исправленного безопасного сообщения остаётся обязательной
частью независимой ручной перепроверки по разделу 10.7 из-за ограничения
передачи `pointerup` автоматизированным in-app Browser.

Все изменения остались только локально. Commit, push, deploy и production
не выполнялись.
