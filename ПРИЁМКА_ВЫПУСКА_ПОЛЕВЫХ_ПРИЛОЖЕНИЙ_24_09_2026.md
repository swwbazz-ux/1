# Приёмка выпуска полевых приложений — 24.09.2026

Единый документ приёмки. Ведёт сессия-хозяин интеграционной ветки. Другие сессии
дописывают свои строки и кладут файлы-доказательства; хозяин держит целостность.

Ветка: `claude/field-integration-20260924`, вершина на момент составления `fb1cb83f`.
Бой: `af5a1f84`, стоит с 20.09.2026 и работает.

**Правило заполнения.** «Да» ставится только при наличии файла-доказательства,
который **открыли и посмотрели**. Имя файла доказательством не является, пересказ
чужого доклада — это «со слов», а не «да». Пустая клетка честнее галочки,
поставленной по памяти.

**Отпечаток обязателен.** Снимки перезаписываются под тем же именем, это уже
произошло дважды за час. Поэтому рядом с файлом пишутся время изменения и первые
12 знаков md5 той версии, которую смотрели. Без отпечатка ссылка на файл
недействительна: завтра под этим именем будет другая картинка.

**Настройки экрана фиксируются вместе со снимком.** Вёрстка Экскаваторщика была
отвергнута по снимку с телефона, у которого плотность стояла `560` при родных
`440`, а масштаб шрифта — `0.81`. То есть смотрели на искажённую картинку.
Настройки сбросили, пользователь посмотрел заново и решение подтвердил, но
ошибка могла уйти в любую сторону: так же легко принять плохую вёрстку за
хорошую. Поэтому перед съёмкой плотность и масштаб шрифта возвращаются к родным,
а их значения пишутся в доказательство рядом с отпечатком. Приёмка внешнего вида
на устройстве с нештатными настройками экрана недействительна.

## 1. Матрица приёмки

Расшифровка колонок: **код** — правка есть в ветке; **тесты** — есть автотест;
**телефон** — проверено на реальном Android; **чем доказано** — открываемый файл.

### Водитель

| Дефект | Код | Тесты | Телефон | Чем доказано |
|---|---|---|---|---|
| 1. Ложные «связь потеряна/восстановлена» | да, проверено | со слов 16/16 | **частично** | код: `7aa2dc67` в ветке `claude/driver-layout-20260924`; телефон: переходы сняты (`connection-watch.log` 19:01–19:20), **озвучка не воспроизведена** |
| 2. Ручное время на телефоне блокирует работу | да | да | **веб-часть да, системные часы нет** | `local-driver-clockskew.png` 03:30:18, md5 `31d2b4f261e3`; ограничение проверки см. раздел 3б |
| 3. Увеличенный системный шрифт ломает вёрстку | да | да | да | `local-driver-font1.png`, `local-driver-work-font2.png`, `local-driver-rabota-font2.png`, `local-driver-prostoi-font2.png` (03:28–03:32) |
| 4. Адаптивность экрана | да | да | частично | `landscape.png` (04:03), `fix-*.png` (03:38–03:50) |

### Машинист экскаватора

| Дефект | Код | Тесты | Телефон | Чем доказано |
|---|---|---|---|---|
| 1. Ложные «связь потеряна/восстановлена» | да | да | **частично** | 19,8 мин на устройстве, ни одного ложного, одна настоящая потеря отработана верно; md5 `1a468fb4d081`. **Озвучка не воспроизведена** — профиль собран без голосовых оповещений |
| 2. Ручное время на телефоне блокирует работу | да | да | да, веб-часть | `exc-clock-proof-on-device.txt` 08:13:18, md5 `05b66b39a84f`; погрузка 21:39:35 → `loaded_at` 22:09:37, `load_time_source=server_receipt`; переключение причины простоя принято, простой 14 закрыт и 15 начат одной секундой 22:12:02 |
| 3. Увеличенный системный шрифт ломает вёрстку | да | да | да | `exc-work-font2.png` 04:20:34, md5 `33de3ce4683c`; шапка — `exc-header-fixed.png` 04:30:05, md5 `6b8487c1eac2` |
| 4. Адаптивность экрана | да | да | **нет** | — |

### Обе роли

| Дефект | Код | Тесты | Телефон | Чем доказано |
|---|---|---|---|---|
| 5а. Экран врёт при часах, переведённых вперёд | да, Водитель | нет | да, Водитель | `clock-timer-fixed.png` 04:59:06, md5 `0b244b6c9b12`, коммит `fa4c4e36` в ветке `claude/driver-layout-20260924` |
| 5б. Телефонное время в базе, простои | **да, Водитель** | да | да, веб-часть | телефон 21:10:01 → `started_at` 21:40:03 серверное, длительность 7 с настоящая; завершение `ended_at` 21:40:41 серверное |
| 5б. Телефонное время в базе, рейсы | **нет, дефект жив** | нет | воспроизведён на стенде | смена 2 ч, часы −30 мин, погрузка → `loaded_at` ровно −30 мин, `created_at` серверный (экскаваторная сессия) |
| 5в. При часах назад нельзя сменить причину простоя | **да, Водитель** | да | да, веб-часть | телефон 21:10:08 → принято, `started_at` 21:40:10, предыдущий закрыт той же секундой без разрыва и нахлёста; раньше был отказ `downtime_switch_before_start` |

**5в — самый тяжёлый из всей группы часов, и он не про данные, а про работу.**
Проверил сам: `core/offline_sync.py:2163` —
`if open_event and normalized['occurred_at'] < open_event.started_at:` и сразу
конфликт `downtime_switch_before_start`.

При часах, переведённых назад, время нового события всегда оказывается раньше
начала уже открытого простоя, поэтому сервер отклоняет переключение. Водитель в
кабине не может сменить причину простоя — не «видит неверную цифру», а
**не может выполнить действие**. Остальные дефекты группы портят отображение или
запись; этот останавливает работу.

Тяжесть высокая. Лечится той же правкой, что 5б.

**5а закрыт, проверено мной.** Открыл снимок: таймер простоя показывает
`00:11:01` при часах телефона `4:59` — настоящая длительность, а не величина
сдвига. Прежний артефакт в три часа не воспроизводится.

**Противоречие по 5б разрешено, и разрешилось не в нашу пользу.** Было два
взаимоисключающих утверждения: разбор по коду говорил, что телефонное время
ложится в базу, а телефонная проверка со сдвигом ±3 часа показала в базе
серверное время. Проверил условие сам, в сведённом дереве:

`core/offline_sync.py:837` — `if occurred_at < shift.opened_at:` и только внутри
этой ветки `normalized['occurred_at'] = normalized['received_at']`.

То есть подмена на серверное время срабатывает **только когда событие оказалось
раньше начала смены**. Телефонная проверка сдвигала часы на три часа при недавно
открытой смене, поэтому событие проваливалось за её начало и попадало в это
исключение. Обычный сдвиг назад — например тридцать минут при смене, открытой
час назад — под условие не подпадает, и в базу идёт телефонное время:

- `core/offline_sync.py:2185` — `started_at=normalized['occurred_at']`;
- `core/offline_sync.py:529` — `trip.loaded_at = normalized['occurred_at']`.

Оба места проверил построчно. Значит отчёт «часы проверены, в базе серверное
время» верен только для исключения и не закрывает дефект.

**Сценарий приёмки, который обязателен:** смена открыта не меньше часа, часы
телефона переведены назад на тридцать минут, выполняются простой и ручная
погрузка, затем сверка `started_at` и `loaded_at` с временем получения. Для
рейсов этот сценарий уже отработан на стенде и дефект воспроизведён; для
простоев — нет.

### Тяжесть 5б ниже, чем казалось — проверено

Отчёты руководства сбитыми часами **не портятся**. Проверил сам, в сведённом
дереве:

- `reports/hourly_grid.py:102, 146, 148` — раскладка по часам идёт по
  `trip.created_at`;
- `reports/dispatcher_shift_forms.py:341` — `loaded_at = trip.created_at`;
  переменная названа `loaded_at`, но значение туда кладётся серверное.

`created_at` ставится сервером и телефоном не управляется. Значит почасовая
сетка и сменный отчёт Диспетчера показывают правду независимо от часов на
устройстве.

Искажение видно только там, где берётся именно `loaded_at`: почасовая справка
самого машиниста и окно видимости ручного рейса. Плюс `started_at` простоя.

**Тяжесть: средняя.** Это не порча отчётности предприятия, а расхождение в том,
что видит полевой сотрудник и что лежит в поле `loaded_at`. Решение «чинить до
выкладки» от этого не отменяется, но выкладка перестаёт быть невозможной:
это осознанное известное ограничение, а не тихая потеря данных руководства.

Отдельно, не путать с 5б: `created_at` — это момент, когда сервер принял
событие. Если телефон был офлайн, между погрузкой и приёмом пройдёт реальное
время, и отчёт по `created_at` сдвинется на величину офлайна. Это давнее
свойство контура, к сбитым часам отношения не имеет и в этот дефект не входит.

Решение пользователя по 5а и 5б — **чинить до выкладки**. Правка делается общим
модулем смещения «сервер минус телефон» по `server_received_at` из ответов
очереди отправки; серверная часть — флаг недостоверных часов в
`normalize_offline_event`. Обе роли правятся своими сессиями и отдаются сюда
после сведения. До приёмки на телефоне и сверки с базой клетки остаются пустыми:
«в работе» — это не «сделано».

Дефект 5 заведён по итогам разбора снимка `local-driver-clockskew.png`
(см. раздел 3) и затем разделён надвое, потому что это два разных по тяжести
дефекта.

**5а — косметика, но заметная.** При часах, переведённых вперёд, экран смешивает
серверное время расчёта с телефонным, поэтому таймер простоя показывает величину
сдвига. Водитель видит трёхчасовой обед. В базе при этом всё цело: сервер
подменяет заведомо будущее время на время получения.

**5б — дефект данных.** При часах, переведённых назад, сервер прошлое осознанно
не правит: такое время может описывать настоящее офлайн-действие. Поэтому
телефонное время ложится в базу как есть — в начало простоя и во время
погрузки. Величина ошибки равна сдвигу часов. Проверяется только сверкой с базой
после прогона на телефоне со сдвигом назад, по экрану это не видно.

Решение пользователя по обоим: **чинить до выкладки**. Оно совпало с
рекомендацией — испорченные данные смены нельзя отличить от настоящих задним
числом.

Телефон один (Xiaomi по USB), очередь общая, поэтому колонка «телефон»
заполняется не параллельно.

## 2. Что я проверил лично, а что принял со слов

**Проверил, открыв файлы.** `local-driver-clockskew.png` и `exc-work-font2.png` —
настоящие снимки телефона: строка состояния Android, кнопки навигации,
разрешение 1080×2400. Приставка `local` в именах означает локальный сервер, а не
браузер на компьютере. Оба засчитываю как телефонную проверку.

**Не проверял.** Остальные снимки из списка доказательств открыты не были;
они приняты по имени и времени файла. Проверка связи на телефоне не
подтверждена ни для одной роли — файла нет.

**Устаревшие сведения, которые ходили между сессиями.** Утверждение
«у экскаваторщика телефон не проверен ни по одному пункту» неверно: снимок по
шрифту сделан в 04:20. Матрицу нужно перечитывать перед каждым решением,
а не опираться на вчерашний пересказ.

## 2б. Открытый вопрос: связь дёргается каждые 20 секунд

Водительская сессия при съёмке на устройстве обнаружила, что состояние связи
идёт `ok → recovering → ok` **на каждом такте сверки, то есть каждые 20 секунд,
при ровном интернете**.

Озвучки это само по себе не даёт: голос привязан к состоянию «потеряна», а не
«восстанавливается». Но именно это объясняет, откуда при нулевых порогах брался
поток оповещений — порогов не было, и каждое дёрганье проходило насквозь.

Вопрос «так задумано или это дефект» пока открыт и отдан обеим полевым сессиям.
Важно не закрыть его молча: пороги гасят симптом, но если состояние дёргается
без причины, то под порогом остаётся неверная картина связи, а не исправленная.
Это кандидат в дефект 6, пока без номера.

## 2а. Отчёт «исправил все три» неверен для связи у Водителя

Это записано отдельно, чтобы не потерялось при следующей передаче работы.

Фильтр ложных оповещений о связи задаётся тремя атрибутами на теге подключения
модуля звуков. Проверил лично в сведённом дереве:

- `templates/trips/excavator_work.html:17` — атрибуты `data-connection-loss-stable-ms`,
  `data-connection-recovery-stable-ms`, `data-connection-alert-cooldown-ms` есть;
- `templates/users/driver_shift.html:36` — тот же модуль подключён с профилем
  `driver`, **ни одного из трёх атрибутов нет**.

Без атрибута `positiveDatasetNumber` возвращает `0`
(`mobile-operational-sounds-v1.js:47-50`), а при нуле обработчик не ставит
задержку, а оповещает сразу (`:529-533` для потери и `:542-547` для
восстановления). То есть у Водителя пауза не работает вовсе и любое мгновенное
моргание связи даёт голосовое оповещение.

Правка касалась только Экскаваторщика. Для Водителя дефект 1 остаётся открытым:
нужны атрибуты, подъём версии оболочки и проверка на телефоне.

## 3. Замечания, которые галочка в матрице не ловит

Они не отменяют «да» в клетке, но должны быть видны до выкладки.

1. **Таймер простоя показывает величину сдвига часов.** На
   `local-driver-clockskew.png` обед идёт `03:00:05` при часах телефона `3:30`.
   Приложение не заблокировано — это и проверялось, и это работает. Но водитель
   на смене видит трёхчасовой обед. Нужно понять, попадает ли эта величина в
   отчёт смены. Если да — это тихая порча данных, а не косметика.
   Статус: **не проверено**, нужен отдельный прогон.
2. **Наложение в шапке экскаваторщика при крупном шрифте — исправлено.**
   Разобрано в три захода, и разбор стоит запомнить как образец.
   На `exc-work-font2.png` 04:20:34 зелёный кружок связи налезал на текст
   `Окисле…`. Появился `exc-header-fixed.png`, и по имени его приняли за
   исправление — не открыв. Я открыл версию 04:24: наложение было на месте,
   отличалась только всплывшая плашка «Все действия отправлены». Файл затем
   перезаписали под тем же именем: на версии 04:30:05, md5 `6b8487c1eac2`,
   шапка действительно исправлена — `Бл. 52 О…` обрезано многоточием, кружок
   стоит отдельно справа, наложения нет.
   Отсюда правило об отпечатке в начале документа. Плашка «Все действия
   отправлены» — временное уведомление, не дефект.

## 3а. Ловушка в общей рабочей копии — не коммитить

В `C:/codex-tmp/field-integration-20260924` лежит 30 незакоммиченных позиций.
Три из них опасны для релиза:

- `mobile/capacitor-shell/android/app/build.gradle`
- `mobile/capacitor-shell/android/app/capacitor.build.gradle`
- `mobile/capacitor-shell/android/capacitor.settings.gradle`

Это временное послабление проверки адреса сервера для сборки под USB, помеченное
в самом коде как «NOT FOR COMMIT». Посмотрел содержимое: послабление узкое —
срабатывает только когда имя профиля оканчивается на `_local`, схема `http` или
`https` и хост `localhost` либо `127.0.0.1`. То есть даже при утечке в релиз
дыры наружу не открывает. Но в релизе ему всё равно не место.

Остальные позиции — снимки экрана и локальные профили `driver_local`,
`excavator_local`.

**Правило: в общей копии никаких `git add -A` и `git commit -a`, только
адресное добавление.** Моё сведение `09c551dc` делалось в отдельной чистой
копии, проверено — ни одного из этих файлов в нём нет.

## 3в. Починка часов подтверждена на устройстве — с двумя незакрытыми клетками

Сквозная проверка Водителя на живом телефоне, смена 286 минут, часы сдвинуты
назад на 30 минут, серверное время 21:40. Результат со слов водительской сессии,
сам я её не повторял.

Доказано, что починка работает:

- простой: телефон говорит 21:10:01, в базу легло `started_at` 21:40:03 —
  серверное, длительность 7 секунд настоящая;
- переключение причины простоя: принято, предыдущий закрыт той же секундой без
  разрыва и нахлёста. Раньше на этом месте был отказ
  `downtime_switch_before_start` — то есть дефект 5в на устройстве закрылся;
- завершение простоя: `ended_at` 21:40:41 серверное.

Две клетки остались незакрытыми, и **обе не по вине часов**:

- ручная погрузка `loaded_at` — сервер отказал по причине
  `manual_work_context_unavailable`: у размещения экскаватора зона «Неактивная
  смена». Это ограничение тестовых данных, ошибок времени в расписке нет;
- простой у Машиниста при сдвиге — отказ `downtime_owner_changed`, тоже
  доменный конфликт тестовых данных.

Обе переносятся на итоговый стенд. Важно не записать их в «часы не починены»:
причина другая.

## 3б. Чем именно проверяли часы — и чего эта проверка не покрывает

Оговорка важнее, чем кажется, поэтому вынесена отдельно.

Системные часы Android **никто не переставлял**. Проверка делалась подменой
объекта `Date` внутри страницы через отладочный канал WebView, и работает она
только на отладочной сборке.

**Что это покрывает.** Всю веб-часть: время событий в очереди отправки, экранные
таймеры, порядок действий. Для дефектов 5б и 5в этого достаточно — именно так
они и были пойманы, и пойманы верно.

**Чего не покрывает.** Нативную часть: фоновая служба связи и нативный пульс
читают системные часы напрямую, мимо подменённого `Date`.

**Отсюда вывод, который нельзя смягчать.** Сценарий, который реально случается в
карьере — сотрудник руками переставил время на телефоне — не проверен **ни у
одной роли**. Проверенное поведение и ожидаемое совпадают только в той мере, в
какой обе части читают одни и те же часы, а они читают разные.

Закрывается либо руками владельца на своём устройстве, либо полевой проверкой.
До этого клетка «часы — телефон» не может стоять как «да».

## 4. Набор публикации — проверено, чисто

Проект уже трижды обжигался на том, что правка не попадала в список публикации.
Сверил построчно `.github/deploy/production-files.txt` с фактическим диффом
`af5a1f84 → fb1cb83f`.

Изменено продуктовых файлов — 52, из них в списке публикации — 38.

Все файлы полевых ролей на месте, включая миграцию `trips/0016`, новое рабочее
место ручного режима водителя, перетаскивание и свайпы экскаваторщика,
`core/offline_sync.py`, `driver-offline-outbox-v2.js` и оба новых шаблона.

Не поедут 14 файлов, и это осознанно: инструменты QA, конфиг стенда QA,
`.env.example`, документация. Из видимого людям не поедут только новая стартовая
страница с каталогом приложений (`start-page-v1.*`, `app-catalog-v1.css`,
`og-start-v4.jpg`) и две мелкие правки экранов ОУП. Полевых ролей не касается.

## 5. Откат — проще, чем считалось

Ходило мнение, что из-за миграции `trips/0016` чистого отката больше нет.
Проверил: это не так.

Миграция — `AlterField` на `Trip.load_time_source`, меняются только `choices`,
добавляется значение `driver_device`. `max_length` остаётся `24`. Для
`CharField` перечень допустимых значений не хранится в базе, поэтому на
PostgreSQL это запись в журнале миграций, а не перестройка таблицы.

**Процедура отката:** выложить `af5a1f84` штатным workflow. Миграцию отменять не
нужно и не следует. Django не ругается на применённую миграцию, которой нет в
коде, — он проверяет только невыполненные. `makemigrations --check` в deploy
сверяет модели с файлами миграций, а не с базой, поэтому на `af5a1f84` пройдёт.

**Единственный след.** После выкладки рейсы, где время погрузки пришло с часов
водителя, получат `load_time_source='driver_device'`
(`core/offline_sync.py:1361`). В коде боя такого значения в перечне нет. Падения
не будет: Django возвращает сырое значение, если оно вне перечня. В отчётах в
этой графе вместо названия появится `driver_device`, пока не выложат снова.

Вывод: откат безопасен, цена — косметический след в графе источника времени
погрузки у части рейсов.

## 6. Путь выкладки — два варианта

Решение за пользователем. Ниже цена каждого.

### А. Через релизную ветку (рекомендую)

Влить интеграционную ветку в `codex/github-production-deploy-20260916` отдельным
PR, пройти обязательный `Required quality gate`, выложить с релизной ветки.

Цена: gate обязан быть зелёным, значит красные тесты придётся разобрать до
выкладки. Это дольше.

Что даёт: релизная ветка содержит выложенное. Это и есть защита от повтора
случая с кнопками водителя, когда выкладка мимо релизной ветки была снесена
следующей выкладкой.

### Б. Прямо с интеграционной ветки по SHA

Технически возможно: `production-deploy.yml` в интеграционной ветке есть и
отличается от релизного на 12 строк (только версии Actions), запуск идёт по
выбранной ревизии.

Цена: красные тесты выкладку **не остановят** — deploy гоняет лишь
`manage.py check` и `makemigrations --check`. Значит выложить можно быстро и
вслепую. И главное: релизная ветка не будет содержать выложенное, поэтому
следующая штатная выкладка с релизной ветки **снесёт этот выпуск**. Этот
сценарий в проекте уже происходил.

Вариант Б допустим только как осознанный временный шаг с немедленным
последующим слиянием в релизную ветку.

## 7. Эталонный замер — цифра больше, чем считалось

Один прогон полного набора на чистой копии вершины `fb1cb83f`, SQLite,
последовательно.

```
Ran 2891 tests in 1292.802s
FAILED (failures=59, errors=46, skipped=58)
```

**Красных 105, а не «около 44»**. Цифра 44 ходила между сессиями и устарела:
она снята до трёх водительских коммитов сверху и, судя по всему, по неполному
набору модулей.

Две оговорки, без которых этим числом нельзя размахивать.

1. Прогон шёл под нагрузкой: параллельно работали ещё два чужих набора тестов и
   сервер разработки. Часть падений может быть от нехватки процессора, а не от
   кода. Такие буду перепроверять поодиночке.
2. Подробный список падений в первом прогоне обрезан по моей вине — команда
   пропускала вывод через `tail`. Итог достоверен, поимённый список
   пересчитывается отдельным прогоном. Поэтому разбор «врёт тест или сломана
   программа» в этом документе пока не заполнен.

Что уже видно по сохранившемуся хвосту списка: падения сосредоточены в `trips`
(29 из 35 сохранившихся строк), остальное — `users` и `settlement`.

## 8. Что осталось до решения о выкладке

Состояние ветки на `4ba14654`: Django `2910` тестов, ноль падений;
JavaScript `733`, ноль падений. Утром было `105` красных и незакрытая
уязвимость.

Закрыто:

- [x] Уязвимость очереди разгрузки Экскаваторщика — сведена из релизной ветки,
      проверено на результате: опасных сборок ноль.
- [x] Ветка сведена с релизной, отставание на 33 коммита устранено.
- [x] Номера оболочек подняты: Водитель `v350`, Экскаваторщик `v258`, обе выше
      боевых `v299` и `v250` — сверено командой, не по памяти.
- [x] Все красные разобраны по корням. Дефектов приложения среди них нет.
- [x] Связь на телефоне — Водитель и Машинист, переходы состояния.
- [x] Часы — обе роли, веб-часть, с подтверждением по базе.
- [x] Шрифт — обе роли.
- [x] Адаптивность Водителя.

Осталось до решения о выкладке:

- [ ] **Озвучка связи — ни у одной роли.** Локальные профили собраны без
      голосовых оповещений, поэтому проверялись переходы состояния, а не звук.
      Жалоба людей была именно на звук. Считаю обязательным.
- [ ] **Системные часы Android — ни у одной роли.** См. раздел 3б: проверяли
      подменой `Date` в странице, нативный слой читает настоящие часы мимо неё.
      Карьерный сценарий не покрыт. Считаю обязательным.
- [ ] Адаптивность Экскаваторщика на телефоне — на усмотрение пользователя.
- [ ] Выбрать путь выкладки — решение пользователя, см. раздел 6.

## 9. Чего зелёный набор тестов не доказывает

Это главный вывод дня, и его нельзя смягчать при пересказе.

Из восьми падений, оставшихся после сведения, **дефектом приложения не
оказалось ни одно**: три от выноса кода в отдельные файлы, одно от неподнятого
человекочитаемого номера версии, одно плавающее, три с ошибками в самих тестах.

При этом все дефекты, которые сегодня действительно чинились, тесты **не
ловили**:

- уязвимость нашлась чтением кода при сведении;
- порча времени смены — разбором логики и проверкой на устройстве;
- неработающий фильтр оповещений связи у Водителя — сверкой двух шаблонов.

Отсюда: ноль красных означает, что тесты не спорят с кодом. Готовность к работе
в карьере этим не доказывается — она держится на телефонной приёмке, а в ней
остаются незакрытые клетки выше.
