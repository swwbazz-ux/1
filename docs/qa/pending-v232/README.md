# QA ожидающих назначений v232

Проверено 13.09.2026 на локальном Django-контуре с ненулевым серверным сроком и
искусственными QA-назначениями, без производственных рейсов.

- 390x844 и 800x360: исходящий перевод с длинной целью, входящий перевод с длинным
  источником, назначение из свободных и снятие в свободные; таймер и подпись находятся
  внутри карточки, не пересекают изображение, номер, статус Водителя и индикатор связи;
  горизонтальное переполнение равно нулю.
- После серверного применения исходящие карточки 61/64 исчезли, входящие 62/63 стали
  обычными.
- При достижении нуля без сети все четыре карточки показали «Ожидает подтверждения».
  За 12 секунд offline выполнена одна попытка fragment-refresh; после восстановления
  сети выполнен один fragment-refresh, экран автоматически применил серверное состояние.
- Ошибок JavaScript и console error не зафиксировано.

Скриншоты: `mobile-390x844.png`, `mobile-800x360.png`.

## Hotfix удержания при снятии в свободные

После исправления blanket-блокировки исходящих карточек отдельно проверен пассивный
самосвал с активным переходом «В свободные». На 390x844 карточка сохранила
`manual_available=1`, долгое удержание создало drag-preview, а отпускание вне точки
вернуло карточку без POST и без рейса. На 1280x800 карточка также видима; на обоих
viewport горизонтальное переполнение равно нулю, ошибок console/pageerror нет.
Серверный тест отдельно подтвердил успешную ручную отправку до срока и сохранение
ожидающего release до его собственного серверного дедлайна. Межэкскаваторная
исходящая карточка у прежнего машиниста осталась заблокированной.

Скриншот: `release-hold-hotfix-390x844.png`.

## Проверка на подключённом Android-телефоне

На Xiaomi 22011119UY с Android 13, APK Экскаваторщика 0.1.26
(`versionCode=42`) и WebView 153.0.8010.36 открытый production-экран автоматически
получил новый operational fragment после выкладки, без переоткрытия приложения и
без очистки данных или кэша.

На пассивной карточке №56 выполнены только безопасные действия: долгое удержание
создало поднятый drag-preview с ярким непрерывным ореолом и вызвало системный запрос
вибрации; отпускание без переноса вернуло карточку на место. От тестируемого телефона
не было POST отправки и рейс не создавался. Состояние «В свободные» на самом телефоне
повторно не создавалось: release №58 уже завершился по серверному сроку до выкладки.
Точная доступность release до дедлайна подтверждена локальным browser QA и серверными
регрессионными тестами.

Скриншоты: `device-hotfix-held-56.png`, `device-hotfix-after-cancel-56.png`.
